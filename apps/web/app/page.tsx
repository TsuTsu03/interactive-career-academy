import { CurriculumMap } from "@/components/curriculum-map";

/**
 * The curriculum map. Courses run in order, each one a project built across
 * many small steps, the way freeCodeCamp structures a certification.
 */
export default function Page() {
  return <CurriculumMap />;
}

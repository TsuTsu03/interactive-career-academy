import type { Course } from "@/lib/lesson-ir";

// Runs on the learner's own computer under PLAN.md decisions 43 and 45. The
// checker starts the learner's server and sends it real requests on
// 127.0.0.1; a pasted report is learner-reported practice only. The code and
// checks for every project are fixed in tools/api-basics-plan.mjs.
export const apiBasicsCourse: Course = {
  "id": "api-basics",
  "title": "Building APIs",
  "project": "Sari-Sari Store First Server",
  "projects": [
    {
      "id": "first-server-sari-sari",
      "title": "Sari-Sari Store First Server"
    },
    {
      "id": "json-routes-sari-sari",
      "title": "Sari-Sari Store JSON Routes"
    },
    {
      "id": "request-bodies-sari-sari",
      "title": "Sari-Sari Store Reading Request Bodies"
    },
    {
      "id": "one-item-sari-sari",
      "title": "Sari-Sari Store One Item at a Time"
    },
    {
      "id": "query-strings-sari-sari",
      "title": "Sari-Sari Store Filtering with Query Strings"
    },
    {
      "id": "middleware-sari-sari",
      "title": "Sari-Sari Store Middleware"
    },
    {
      "id": "sqlite-storage-sari-sari",
      "title": "Sari-Sari Store Storing Data in SQLite"
    },
    {
      "id": "sqlite-changes-sari-sari",
      "title": "Sari-Sari Store Updating and Deleting Rows"
    },
    {
      "id": "errors-sari-sari",
      "title": "Sari-Sari Store Errors and Status Codes"
    },
    {
      "id": "api-design-sari-sari",
      "title": "Sari-Sari Store Designing a Clean API"
    },
    {
      "id": "first-server-carinderia",
      "title": "Carinderia First Server"
    },
    {
      "id": "json-routes-carinderia",
      "title": "Carinderia JSON Routes"
    },
    {
      "id": "request-bodies-carinderia",
      "title": "Carinderia Reading Request Bodies"
    },
    {
      "id": "one-item-carinderia",
      "title": "Carinderia One Item at a Time"
    },
    {
      "id": "query-strings-carinderia",
      "title": "Carinderia Filtering with Query Strings"
    },
    {
      "id": "middleware-carinderia",
      "title": "Carinderia Middleware"
    },
    {
      "id": "sqlite-storage-carinderia",
      "title": "Carinderia Storing Data in SQLite"
    },
    {
      "id": "sqlite-changes-carinderia",
      "title": "Carinderia Updating and Deleting Rows"
    },
    {
      "id": "errors-carinderia",
      "title": "Carinderia Errors and Status Codes"
    },
    {
      "id": "api-design-carinderia",
      "title": "Carinderia Designing a Clean API"
    },
    {
      "id": "first-server-barangay",
      "title": "Barangay Office First Server"
    },
    {
      "id": "json-routes-barangay",
      "title": "Barangay Office JSON Routes"
    },
    {
      "id": "request-bodies-barangay",
      "title": "Barangay Office Reading Request Bodies"
    },
    {
      "id": "one-item-barangay",
      "title": "Barangay Office One Item at a Time"
    },
    {
      "id": "query-strings-barangay",
      "title": "Barangay Office Filtering with Query Strings"
    },
    {
      "id": "middleware-barangay",
      "title": "Barangay Office Middleware"
    },
    {
      "id": "sqlite-storage-barangay",
      "title": "Barangay Office Storing Data in SQLite"
    },
    {
      "id": "sqlite-changes-barangay",
      "title": "Barangay Office Updating and Deleting Rows"
    },
    {
      "id": "errors-barangay",
      "title": "Barangay Office Errors and Status Codes"
    },
    {
      "id": "api-design-barangay",
      "title": "Barangay Office Designing a Clean API"
    },
    {
      "id": "first-server-school-club",
      "title": "School Club First Server"
    },
    {
      "id": "json-routes-school-club",
      "title": "School Club JSON Routes"
    },
    {
      "id": "request-bodies-school-club",
      "title": "School Club Reading Request Bodies"
    },
    {
      "id": "one-item-school-club",
      "title": "School Club One Item at a Time"
    },
    {
      "id": "query-strings-school-club",
      "title": "School Club Filtering with Query Strings"
    },
    {
      "id": "middleware-school-club",
      "title": "School Club Middleware"
    },
    {
      "id": "sqlite-storage-school-club",
      "title": "School Club Storing Data in SQLite"
    },
    {
      "id": "sqlite-changes-school-club",
      "title": "School Club Updating and Deleting Rows"
    },
    {
      "id": "errors-school-club",
      "title": "School Club Errors and Status Codes"
    },
    {
      "id": "api-design-school-club",
      "title": "School Club Designing a Clean API"
    },
    {
      "id": "first-server-tricycle",
      "title": "Tricycle Terminal First Server"
    },
    {
      "id": "json-routes-tricycle",
      "title": "Tricycle Terminal JSON Routes"
    },
    {
      "id": "request-bodies-tricycle",
      "title": "Tricycle Terminal Reading Request Bodies"
    },
    {
      "id": "one-item-tricycle",
      "title": "Tricycle Terminal One Item at a Time"
    },
    {
      "id": "query-strings-tricycle",
      "title": "Tricycle Terminal Filtering with Query Strings"
    },
    {
      "id": "middleware-tricycle",
      "title": "Tricycle Terminal Middleware"
    },
    {
      "id": "sqlite-storage-tricycle",
      "title": "Tricycle Terminal Storing Data in SQLite"
    },
    {
      "id": "sqlite-changes-tricycle",
      "title": "Tricycle Terminal Updating and Deleting Rows"
    },
    {
      "id": "errors-tricycle",
      "title": "Tricycle Terminal Errors and Status Codes"
    },
    {
      "id": "api-design-tricycle",
      "title": "Tricycle Terminal Designing a Clean API"
    }
  ],
  "order": 15,
  "summary": "Build web APIs with Node built-ins: routes, JSON, request bodies, validation, filtering, middleware, SQLite storage, errors, and clean design. Needs a computer with Node.js 22.13 or newer. Checks run on your computer and are practice only.",
  "requires": [
    "node-basics"
  ],
  "requiresComputer": true,
  "kind": "local",
  "steps": []
};

// Validated local authoring batch: first-server-sari-sari.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-first-server-sari-sari-1",
    "index": 1,
    "task": "You will change the answer your server sends. The code below says the server answers every request with 'Sari-Sari Store API'.\n\nThis is the first answer your server gives. It tells users your server is ready.\n\nRun the checker to confirm your server answers GET / with 'Sari-Sari Store API'.\n\nIn server.js:\n```\n  res.end(\"Sari-Sari Store API\");\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "root",
        "label": "GET / answers Sari-Sari Store API",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/"
          }
        ],
        "status": 200,
        "bodyContains": "Sari-Sari Store API"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Your server must answer every request with the same message."
      },
      {
        "level": 2,
        "text": "Change the res.end line in server.js to say 'Sari-Sari Store API'.\n\nIn server.js:\n```\n  res.end(\"Sari-Sari Store API\");\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Sari-Sari Store API\");\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-server"
    ],
    "estimatedMinutes": 3,
    "projectId": "first-server-sari-sari"
  },
  {
    "id": "api-first-server-sari-sari-2",
    "index": 2,
    "task": "You will add a header to tell the client the answer is plain text. The code below sets the Content-Type header.\n\nThis header tells the browser or tool how to read your answer. Without it, the answer might not show right.\n\nRun the checker to confirm your server says its body is plain text.\n\nIn server.js:\n```\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "type",
        "label": "GET / says its body is plain text",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/"
          }
        ],
        "header": {
          "name": "content-type",
          "value": "text/plain"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Add a header before res.end to tell the client the answer format."
      },
      {
        "level": 2,
        "text": "Add the line above res.end in server.js to set Content-Type.\n\nIn server.js:\n```\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.end(\"Sari-Sari Store API\");\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-header"
    ],
    "estimatedMinutes": 4,
    "projectId": "first-server-sari-sari"
  },
  {
    "id": "api-first-server-sari-sari-3",
    "index": 3,
    "task": "You will add a custom header to name the place. The code below adds X-Place: sari-sari.\n\nThis header helps tools know where your server is located. It's like a tag on your answer.\n\nRun the checker to confirm your server sends X-Place: sari-sari.\n\nIn server.js:\n```\n  res.setHeader(\"X-Place\", \"sari-sari\");\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "custom",
        "label": "GET / sends X-Place: sari-sari",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/"
          }
        ],
        "header": {
          "name": "x-place",
          "value": "sari-sari"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Add a header to mark your server's location."
      },
      {
        "level": 2,
        "text": "Add the line above res.end in server.js to set X-Place.\n\nIn server.js:\n```\n  res.setHeader(\"X-Place\", \"sari-sari\");\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.setHeader(\"X-Place\", \"sari-sari\");\n  res.end(\"Sari-Sari Store API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "first-server-sari-sari"
  },
  {
    "id": "api-first-server-sari-sari-4",
    "index": 4,
    "task": "You will add a route for /health. The code below checks if the request is for /health.\n\nThis route lets tools check if your server is running. It answers with 'healthy'.\n\nRun the checker to confirm your server answers GET /health with 'healthy'.\n\nIn server.js:\n```\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "health",
        "label": "GET /health answers healthy",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/health"
          }
        ],
        "status": 200,
        "bodyContains": "healthy"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Add a condition to check if the request is for /health."
      },
      {
        "level": 2,
        "text": "Add the line at the top of the handler in server.js.\n\nIn server.js:\n```\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.setHeader(\"X-Place\", \"sari-sari\");\n  res.end(\"Sari-Sari Store API\");\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-route"
    ],
    "estimatedMinutes": 4,
    "projectId": "first-server-sari-sari"
  },
  {
    "id": "api-first-server-sari-sari-5",
    "index": 5,
    "task": "You will add a rule for any other path. The code below sets status 404 for any URL that is not / or /health.\n\nThis tells users the path they asked for does not exist. It helps your server stay organized.\n\nRun the checker to confirm your server answers 404 for /missing and still answers 200 for /.\n\nIn server.js:\n```\n  if (req.url !== \"/\") { res.statusCode = 404; res.end(\"Not found\"); return; }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "missing",
        "label": "GET /missing answers 404",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/missing"
          }
        ],
        "status": 404
      },
      {
        "id": "root",
        "label": "GET / still answers 200",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/"
          }
        ],
        "status": 200
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Add a rule to handle any path that is not / or /health."
      },
      {
        "level": 2,
        "text": "Add the line right after the /health line in server.js.\n\nIn server.js:\n```\n  if (req.url !== \"/\") { res.statusCode = 404; res.end(\"Not found\"); return; }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n  if (req.url !== \"/\") { res.statusCode = 404; res.end(\"Not found\"); return; }\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.setHeader(\"X-Place\", \"sari-sari\");\n  res.end(\"Sari-Sari Store API\");\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "real-response-status"
    ],
    "estimatedMinutes": 5,
    "projectId": "first-server-sari-sari"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: first-server-sari-sari.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-first-server-sari-sari-6",
    "index": 6,
    "task": "You will add a rule to stop any request that is not GET. This rule goes at the very top of the handler. The code below stops any method other than GET and sends status 405. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (req.method !== \"GET\") { res.statusCode = 405; res.end(\"Method not allowed\"); return; }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "post",
        "label": "POST / answers 405",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/",
            "body": "{}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 405
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Think of a request method as the action you want to do, like GET or POST."
      },
      {
        "level": 2,
        "text": "Add the code right after the first line of the handler, before any other checks.\n\nIn server.js:\n```\n  if (req.method !== \"GET\") { res.statusCode = 405; res.end(\"Method not allowed\"); return; }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.method !== \"GET\") { res.statusCode = 405; res.end(\"Method not allowed\"); return; }\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n  if (req.url !== \"/\") { res.statusCode = 404; res.end(\"Not found\"); return; }\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.setHeader(\"X-Place\", \"sari-sari\");\n  res.end(\"Sari-Sari Store API\");\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "real-request-method"
    ],
    "estimatedMinutes": 3,
    "projectId": "first-server-sari-sari"
  },
  {
    "id": "api-first-server-sari-sari-7",
    "index": 7,
    "task": "You will add a route to answer /info with a JSON object. This goes right after the /health line. The code below sends a JSON object with the store's name. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (req.url === \"/info\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify({ name: \"Sari-Sari Store\" })); return; }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "info",
        "label": "GET /info answers JSON with the name",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/info"
          }
        ],
        "bodyContains": "\"name\":\"Sari-Sari Store\"",
        "header": {
          "name": "content-type",
          "value": "application/json"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The JSON answer should have the key 'name' with the value 'Sari-Sari Store'."
      },
      {
        "level": 2,
        "text": "Add the code right after the /health route, before the 404 line.\n\nIn server.js:\n```\n  if (req.url === \"/info\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify({ name: \"Sari-Sari Store\" })); return; }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.method !== \"GET\") { res.statusCode = 405; res.end(\"Method not allowed\"); return; }\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n  if (req.url === \"/info\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify({ name: \"Sari-Sari Store\" })); return; }\n  if (req.url !== \"/\") { res.statusCode = 404; res.end(\"Not found\"); return; }\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.setHeader(\"X-Place\", \"sari-sari\");\n  res.end(\"Sari-Sari Store API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "first-server-sari-sari"
  },
  {
    "id": "api-first-server-sari-sari-8",
    "index": 8,
    "task": "You will add a counter to track how many requests come in. This counter starts at 0 and adds 1 for each request. Then you add a route for /visits to show the count. The code below adds the counter and the /visits route. Run the checker to confirm it works.\n\nIn server.js:\n```\nlet visits = 0;\n  visits += 1;\n  if (req.url === \"/visits\") { res.end(String(visits)); return; }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "visits",
        "label": "After two requests, GET /visits answers 3",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/"
          },
          {
            "method": "GET",
            "path": "/"
          },
          {
            "method": "GET",
            "path": "/visits"
          }
        ],
        "bodyContains": "3"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Start the counter at 0 and add 1 for each request before sending the answer."
      },
      {
        "level": 2,
        "text": "Add the counter above the server, and the /visits route before the 404 line.\n\nIn server.js:\n```\nlet visits = 0;\n  visits += 1;\n  if (req.url === \"/visits\") { res.end(String(visits)); return; }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nlet visits = 0;\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.method !== \"GET\") { res.statusCode = 405; res.end(\"Method not allowed\"); return; }\n  visits += 1;\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n  if (req.url === \"/info\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify({ name: \"Sari-Sari Store\" })); return; }\n  if (req.url === \"/visits\") { res.end(String(visits)); return; }\n  if (req.url !== \"/\") { res.statusCode = 404; res.end(\"Not found\"); return; }\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.setHeader(\"X-Place\", \"sari-sari\");\n  res.end(\"Sari-Sari Store API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 5,
    "projectId": "first-server-sari-sari"
  },
  {
    "id": "api-first-server-sari-sari-9",
    "index": 9,
    "task": "You will add a header to tell browsers not to store any answer. This goes at the very top of the handler. The code below sets the header 'Cache-Control' to 'no-store'. Run the checker to confirm it works.\n\nIn server.js:\n```\n  res.setHeader(\"Cache-Control\", \"no-store\");\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "cache",
        "label": "GET / sends Cache-Control: no-store",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/"
          }
        ],
        "header": {
          "name": "cache-control",
          "value": "no-store"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "This header tells browsers not to save the answer in memory or cache."
      },
      {
        "level": 2,
        "text": "Add the code right after the first line of the handler, before any other checks.\n\nIn server.js:\n```\n  res.setHeader(\"Cache-Control\", \"no-store\");\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nlet visits = 0;\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.setHeader(\"Cache-Control\", \"no-store\");\n  if (req.method !== \"GET\") { res.statusCode = 405; res.end(\"Method not allowed\"); return; }\n  visits += 1;\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n  if (req.url === \"/info\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify({ name: \"Sari-Sari Store\" })); return; }\n  if (req.url === \"/visits\") { res.end(String(visits)); return; }\n  if (req.url !== \"/\") { res.statusCode = 404; res.end(\"Not found\"); return; }\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.setHeader(\"X-Place\", \"sari-sari\");\n  res.end(\"Sari-Sari Store API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "first-server-sari-sari"
  },
  {
    "id": "api-first-server-sari-sari-10",
    "index": 10,
    "task": "You will add a route to answer /hello with a name from the query string. If no name is given, it uses 'friend'. This goes right before the 404 line. The code below uses URLSearchParams to read the name. Run the checker to confirm it works.\n\nIn server.js:\n```\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/hello\") { res.end(`Hello, ${url.searchParams.get(\"name\") ?? \"friend\"}`); return; }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "hello",
        "label": "GET /hello?name=Ana answers Hello, Ana",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/hello?name=Ana"
          }
        ],
        "bodyContains": "Hello, Ana"
      },
      {
        "id": "default",
        "label": "GET /hello answers Hello, friend",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/hello"
          }
        ],
        "bodyContains": "Hello, friend"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "URLSearchParams reads the values after the ? in the URL, like ?name=Ana."
      },
      {
        "level": 2,
        "text": "Add the two lines right before the 404 line, after the /visits route.\n\nIn server.js:\n```\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/hello\") { res.end(`Hello, ${url.searchParams.get(\"name\") ?? \"friend\"}`); return; }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nlet visits = 0;\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.setHeader(\"Cache-Control\", \"no-store\");\n  if (req.method !== \"GET\") { res.statusCode = 405; res.end(\"Method not allowed\"); return; }\n  visits += 1;\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n  if (req.url === \"/info\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify({ name: \"Sari-Sari Store\" })); return; }\n  if (req.url === \"/visits\") { res.end(String(visits)); return; }\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/hello\") { res.end(`Hello, ${url.searchParams.get(\"name\") ?? \"friend\"}`); return; }\n  if (req.url !== \"/\") { res.statusCode = 404; res.end(\"Not found\"); return; }\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.setHeader(\"X-Place\", \"sari-sari\");\n  res.end(\"Sari-Sari Store API\");\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "real-url-search-params"
    ],
    "estimatedMinutes": 6,
    "projectId": "first-server-sari-sari"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: json-routes-sari-sari.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-json-routes-sari-sari-1",
    "index": 11,
    "task": "You will send the whole list as JSON when someone asks for /items. The code below does that. Add it at the top of the handler. This helps the store show all products in one request. JSON is a text format for sending structured values such as objects and arrays.\n\nIn server.js:\n```\n  if (req.url === \"/items\") return send(res, 200, items);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "list",
        "label": "GET /items answers the list as JSON",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "bodyContains": "\"name\":\"Rice\"",
        "header": {
          "name": "content-type",
          "value": "application/json"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The send helper sends the list as JSON, so you don't need to format it yourself."
      },
      {
        "level": 2,
        "text": "Add this line right after the first if statement in server.js.\n\nIn server.js:\n```\n  if (req.url === \"/items\") return send(res, 200, items);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "real-json"
    ],
    "estimatedMinutes": 3,
    "projectId": "json-routes-sari-sari"
  },
  {
    "id": "api-json-routes-sari-sari-2",
    "index": 12,
    "task": "You will send the count of items when someone asks for /items/count. The code below does that. Add it after the /items line. This helps the store know how many products are available without listing them all.\n\nIn server.js:\n```\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "count",
        "label": "GET /items/count answers {\"count\":3}",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/count"
          }
        ],
        "bodyContains": "{\"count\":3}"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The code uses items.length to count how many items are in the list."
      },
      {
        "level": 2,
        "text": "Add this line right after the /items line in server.js.\n\nIn server.js:\n```\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "json-routes-sari-sari"
  },
  {
    "id": "api-json-routes-sari-sari-3",
    "index": 13,
    "task": "You will send only the first item when someone asks for /items/first. The code below does that. Add it after the /items/count line. This helps the store show the cheapest or most popular item quickly.\n\nIn server.js:\n```\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "first",
        "label": "GET /items/first answers Rice",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/first"
          }
        ],
        "bodyContains": "{\"id\":1,\"name\":\"Rice\",\"price\":50}"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The code uses items[0] to get the first item in the list."
      },
      {
        "level": 2,
        "text": "Add this line right after the /items/count line in server.js.\n\nIn server.js:\n```\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "json-routes-sari-sari"
  },
  {
    "id": "api-json-routes-sari-sari-4",
    "index": 14,
    "task": "You will send only the names of items when someone asks for /items/names. The code below does that. Add it after the /items/first line. This helps the store show only the product names, not the prices or other details.\n\nIn server.js:\n```\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "names",
        "label": "GET /items/names answers the three names",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/names"
          }
        ],
        "bodyContains": "[\"Rice\",\"Soap\",\"Egg\"]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The code uses map to get each item's name from the list."
      },
      {
        "level": 2,
        "text": "Add this line right after the /items/first line in server.js.\n\nIn server.js:\n```\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "json-routes-sari-sari"
  },
  {
    "id": "api-json-routes-sari-sari-5",
    "index": 15,
    "task": "You will send the total price of all items when someone asks for /items/total. The code below does that. Add it after the /items/names line. This helps the store calculate the total cost of all products quickly.\n\nIn server.js:\n```\n  if (req.url === \"/items/total\") return send(res, 200, { total: items.reduce((sum, item) => sum + item.price, 0) });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "total",
        "label": "GET /items/total answers 84",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/total"
          }
        ],
        "bodyContains": "{\"total\":84}"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The code uses reduce to add up all the prices in the list."
      },
      {
        "level": 2,
        "text": "Add this line right after the /items/names line in server.js.\n\nIn server.js:\n```\n  if (req.url === \"/items/total\") return send(res, 200, { total: items.reduce((sum, item) => sum + item.price, 0) });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n  if (req.url === \"/items/total\") return send(res, 200, { total: items.reduce((sum, item) => sum + item.price, 0) });\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "json-routes-sari-sari"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: json-routes-sari-sari.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-json-routes-sari-sari-6",
    "index": 16,
    "task": "You will change the last line of the handler. This line tells the server what to send when a path is not found. You will replace it with a 404 error in JSON format. The code below does that. Run the checker to confirm the server now answers 404 with a JSON error.\n\nIn server.js:\n```\n  send(res, 404, { error: \"Not found\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "missing",
        "label": "GET /nope answers 404 with a JSON error",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/nope"
          }
        ],
        "status": 404,
        "bodyContains": "{\"error\":\"Not found\"}"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The server must send a 404 status and a JSON error body when a path is unknown."
      },
      {
        "level": 2,
        "text": "Put the code at the end of the handler, right before the 404 line.\n\nIn server.js:\n```\n  send(res, 404, { error: \"Not found\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n  if (req.url === \"/items/total\") return send(res, 200, { total: items.reduce((sum, item) => sum + item.price, 0) });\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-json-error"
    ],
    "estimatedMinutes": 3,
    "projectId": "json-routes-sari-sari"
  },
  {
    "id": "api-json-routes-sari-sari-7",
    "index": 17,
    "task": "Add one line at the top of the handler. This line handles the root path. It sends back the API's name and its routes. The code below does that. Run the checker to confirm the server now lists the routes.\n\nIn server.js:\n```\n  if (req.url === \"/\") return send(res, 200, { name: \"Sari-Sari Store\", routes: [\"/items\"] });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "root",
        "label": "GET / lists the routes",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/"
          }
        ],
        "status": 200,
        "bodyContains": "\"routes\":[\"/items\"]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The server must return a 200 status and a JSON object with name and routes for the root path."
      },
      {
        "level": 2,
        "text": "Put the code at the top of the handler, right after the import lines.\n\nIn server.js:\n```\n  if (req.url === \"/\") return send(res, 200, { name: \"Sari-Sari Store\", routes: [\"/items\"] });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/\") return send(res, 200, { name: \"Sari-Sari Store\", routes: [\"/items\"] });\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n  if (req.url === \"/items/total\") return send(res, 200, { total: items.reduce((sum, item) => sum + item.price, 0) });\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "json-routes-sari-sari"
  },
  {
    "id": "api-json-routes-sari-sari-8",
    "index": 18,
    "task": "Add one line before the 404 line. This line handles the /items/cheapest path. It finds the item with the lowest price and sends it back. The code below does that. Run the checker to confirm the server now answers with the cheapest item.\n\nIn server.js:\n```\n  if (req.url === \"/items/cheapest\") return send(res, 200, [...items].sort((a, b) => a.price - b.price)[0]);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "cheapest",
        "label": "GET /items/cheapest answers Egg",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/cheapest"
          }
        ],
        "bodyContains": "\"name\":\"Egg\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The server must sort the items by price and pick the first one for /items/cheapest."
      },
      {
        "level": 2,
        "text": "Put the code before the 404 line, right after the /items path check.\n\nIn server.js:\n```\n  if (req.url === \"/items/cheapest\") return send(res, 200, [...items].sort((a, b) => a.price - b.price)[0]);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/\") return send(res, 200, { name: \"Sari-Sari Store\", routes: [\"/items\"] });\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n  if (req.url === \"/items/total\") return send(res, 200, { total: items.reduce((sum, item) => sum + item.price, 0) });\n  if (req.url === \"/items/cheapest\") return send(res, 200, [...items].sort((a, b) => a.price - b.price)[0]);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "json-routes-sari-sari"
  },
  {
    "id": "api-json-routes-sari-sari-9",
    "index": 19,
    "task": "Add one line before the 404 line. This line handles the /items/pretty path. It sends the items in indented JSON so it's easier to read. The code below does that. Run the checker to confirm the server now answers with indented JSON.\n\nIn server.js:\n```\n  if (req.url === \"/items/pretty\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify(items, null, 2)); return; }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "pretty",
        "label": "GET /items/pretty answers indented JSON",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/pretty"
          }
        ],
        "bodyContains": "[\n  {\n    \"id\": 1"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The server must set the Content-Type header to application/json and use JSON.stringify with 2 spaces for indentation."
      },
      {
        "level": 2,
        "text": "Put the code before the 404 line, right after the /items path check.\n\nIn server.js:\n```\n  if (req.url === \"/items/pretty\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify(items, null, 2)); return; }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/\") return send(res, 200, { name: \"Sari-Sari Store\", routes: [\"/items\"] });\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n  if (req.url === \"/items/total\") return send(res, 200, { total: items.reduce((sum, item) => sum + item.price, 0) });\n  if (req.url === \"/items/cheapest\") return send(res, 200, [...items].sort((a, b) => a.price - b.price)[0]);\n  if (req.url === \"/items/pretty\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify(items, null, 2)); return; }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "json-routes-sari-sari"
  },
  {
    "id": "api-json-routes-sari-sari-10",
    "index": 20,
    "task": "Add one line before the 404 line. This line handles the /items/expensive path. It filters items with price over 40 and sends them back. The code below does that. Run the checker to confirm the server now answers with expensive items.\n\nIn server.js:\n```\n  if (req.url === \"/items/expensive\") return send(res, 200, items.filter((item) => item.price > 40));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "expensive",
        "label": "GET /items/expensive answers Rice",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/expensive"
          }
        ],
        "bodyContains": "\"name\":\"Rice\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The server must use filter to keep only items with price greater than 40."
      },
      {
        "level": 2,
        "text": "Put the code before the 404 line, right after the /items path check.\n\nIn server.js:\n```\n  if (req.url === \"/items/expensive\") return send(res, 200, items.filter((item) => item.price > 40));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/\") return send(res, 200, { name: \"Sari-Sari Store\", routes: [\"/items\"] });\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n  if (req.url === \"/items/total\") return send(res, 200, { total: items.reduce((sum, item) => sum + item.price, 0) });\n  if (req.url === \"/items/cheapest\") return send(res, 200, [...items].sort((a, b) => a.price - b.price)[0]);\n  if (req.url === \"/items/pretty\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify(items, null, 2)); return; }\n  if (req.url === \"/items/expensive\") return send(res, 200, items.filter((item) => item.price > 40));\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "json-routes-sari-sari"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: request-bodies-sari-sari.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-request-bodies-sari-sari-1",
    "index": 21,
    "task": "You will read the text a client sends. This is called the request body. The code below reads it. Add this code above the server. Then add the /echo route at the top of the handler. This lets you test reading the body. Run the checker to confirm.\n\nIn server.js:\n```\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "echo",
        "label": "POST /echo with hello answers it back",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/echo",
            "body": "hello",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "bodyContains": "\"received\":\"hello\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Think of the request body as the message the client sends to your server."
      },
      {
        "level": 2,
        "text": "Add the code above the server and the /echo route at the top of the handler.\n\nIn server.js:\n```\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-request-body"
    ],
    "estimatedMinutes": 4,
    "projectId": "request-bodies-sari-sari"
  },
  {
    "id": "api-request-bodies-sari-sari-2",
    "index": 22,
    "task": "You will turn JSON text into an object. This is called parsing JSON. The code below does that. Add it after the /echo route. This lets you test parsing. Run the checker to confirm.\n\nIn server.js:\n```\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "json",
        "label": "POST /echo-json answers the object it received",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/echo-json",
            "body": "{\"name\":\"Ana\"}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "bodyContains": "\"name\":\"Ana\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use JSON.parse to turn the text into an object your code can use."
      },
      {
        "level": 2,
        "text": "Add the code after the /echo route.\n\nIn server.js:\n```\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-parse-json"
    ],
    "estimatedMinutes": 3,
    "projectId": "request-bodies-sari-sari"
  },
  {
    "id": "api-request-bodies-sari-sari-3",
    "index": 23,
    "task": "You will answer POST /items with status 201 and the new item. The code below does that. Replace the line inside createItem with these two lines. This lets you test creating an item. Run the checker to confirm.\n\nIn server.js:\n```\n  const data = JSON.parse(await readBody(req));\n  return send(res, 201, data);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "created",
        "label": "POST /items answers 201 with the item",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "{\"name\":\"Tea\",\"price\":12}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 201,
        "bodyContains": "\"name\":\"Tea\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use status 201 to say the item was created successfully."
      },
      {
        "level": 2,
        "text": "Replace the line inside createItem with the two lines.\n\nIn server.js:\n```\n  const data = JSON.parse(await readBody(req));\n  return send(res, 201, data);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const data = JSON.parse(await readBody(req));\n  return send(res, 201, data);\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-post"
    ],
    "estimatedMinutes": 4,
    "projectId": "request-bodies-sari-sari"
  },
  {
    "id": "api-request-bodies-sari-sari-4",
    "index": 24,
    "task": "You will store the new item so it shows up in the list. The code below does that. Add it inside createItem before the return. This lets you test storing. Run the checker to confirm.\n\nIn server.js:\n```\n  items.push(data);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "stored",
        "label": "After POST /items, GET /items includes Tea",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "{\"name\":\"Tea\",\"price\":12}",
            "headers": {
              "Content-Type": "application/json"
            }
          },
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "bodyContains": "\"name\":\"Tea\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Add the code inside createItem before the return."
      },
      {
        "level": 2,
        "text": "This line adds the new item to the list.\n\nIn server.js:\n```\n  items.push(data);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const data = JSON.parse(await readBody(req));\n  items.push(data);\n  return send(res, 201, data);\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "request-bodies-sari-sari"
  },
  {
    "id": "api-request-bodies-sari-sari-5",
    "index": 25,
    "task": "You will give each new item an id one higher than the last. The code below does that. Change createItem so each new item gets the next id. This lets you test assigning ids. Run the checker to confirm.\n\nIn server.js:\n```\n  const item = { id: items.length + 1, ...data };\n  items.push(item);\n  return send(res, 201, item);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "id",
        "label": "The new item answers with id 4",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "{\"name\":\"Tea\",\"price\":12}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "bodyContains": "\"id\":4"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use items.length + 1 to get the next id."
      },
      {
        "level": 2,
        "text": "Change createItem to include the new id.\n\nIn server.js:\n```\n  const item = { id: items.length + 1, ...data };\n  items.push(item);\n  return send(res, 201, item);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const data = JSON.parse(await readBody(req));\n  const item = { id: items.length + 1, ...data };\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 5,
    "projectId": "request-bodies-sari-sari"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: request-bodies-sari-sari.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-request-bodies-sari-sari-6",
    "index": 26,
    "task": "If the body is not JSON, the server crashes. You fix this by catching the error. The code below shows how to return 400 instead. This helps customers know their request is broken. Run the checker to confirm.\n\nIn server.js:\n```\n  let data;\n  try { data = JSON.parse(await readBody(req)); } catch { return send(res, 400, { error: \"Body must be JSON\" }); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "bad",
        "label": "POST /items with broken JSON answers 400",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "not json",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 400,
        "bodyContains": "Body must be JSON"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "When JSON.parse fails, the server should not crash. It should return 400."
      },
      {
        "level": 2,
        "text": "Put the two lines after the try block, right before the catch block.\n\nIn server.js:\n```\n  let data;\n  try { data = JSON.parse(await readBody(req)); } catch { return send(res, 400, { error: \"Body must be JSON\" }); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  let data;\n  try { data = JSON.parse(await readBody(req)); } catch { return send(res, 400, { error: \"Body must be JSON\" }); }\n  const item = { id: items.length + 1, ...data };\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-bad-request"
    ],
    "estimatedMinutes": 3,
    "projectId": "request-bodies-sari-sari"
  },
  {
    "id": "api-request-bodies-sari-sari-7",
    "index": 27,
    "task": "Validation checks if data is correct before storing it. If the name is missing or empty, return 422. This tells the user what is wrong. The code below shows how to check. Run the checker to confirm.\n\nIn server.js:\n```\n  if (typeof data.name !== \"string\" || !data.name.trim()) return send(res, 422, { error: \"name is required\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "noname",
        "label": "POST /items without a name answers 422",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "{\"price\":5}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 422,
        "bodyContains": "name is required"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Check if data.name is a string and not empty. If not, return 422."
      },
      {
        "level": 2,
        "text": "Put the check right after the try block, before the item creation.\n\nIn server.js:\n```\n  if (typeof data.name !== \"string\" || !data.name.trim()) return send(res, 422, { error: \"name is required\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  let data;\n  try { data = JSON.parse(await readBody(req)); } catch { return send(res, 400, { error: \"Body must be JSON\" }); }\n  if (typeof data.name !== \"string\" || !data.name.trim()) return send(res, 422, { error: \"name is required\" });\n  const item = { id: items.length + 1, ...data };\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-validation"
    ],
    "estimatedMinutes": 4,
    "projectId": "request-bodies-sari-sari"
  },
  {
    "id": "api-request-bodies-sari-sari-8",
    "index": 28,
    "task": "Price must be a number, and it must be 0 or more. If not, return 422. This stops bad data from being stored. The code below shows how to check. Run the checker to confirm.\n\nIn server.js:\n```\n  if (!Number.isFinite(data.price) || data.price < 0) return send(res, 422, { error: \"price must be a number 0 or more\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "badprice",
        "label": "POST /items with price five answers 422",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "{\"name\":\"Tea\",\"price\":\"five\"}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 422,
        "bodyContains": "price must be a number"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use Number.isFinite to check if price is a number. Then check if it's not less than 0."
      },
      {
        "level": 2,
        "text": "Put the check right after the name check, before creating the item.\n\nIn server.js:\n```\n  if (!Number.isFinite(data.price) || data.price < 0) return send(res, 422, { error: \"price must be a number 0 or more\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  let data;\n  try { data = JSON.parse(await readBody(req)); } catch { return send(res, 400, { error: \"Body must be JSON\" }); }\n  if (typeof data.name !== \"string\" || !data.name.trim()) return send(res, 422, { error: \"name is required\" });\n  if (!Number.isFinite(data.price) || data.price < 0) return send(res, 422, { error: \"price must be a number 0 or more\" });\n  const item = { id: items.length + 1, ...data };\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "request-bodies-sari-sari"
  },
  {
    "id": "api-request-bodies-sari-sari-9",
    "index": 29,
    "task": "Only keep the known fields: id, name, and price. Trim spaces from the name. This keeps data clean and safe. The code below shows how to build the item. Run the checker to confirm.\n\nIn server.js:\n```\n  const item = { id: items.length + 1, name: data.name.trim(), price: data.price };\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "trim",
        "label": "The new item is stored with a trimmed name and no extra fields",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "{\"name\":\"  Tea  \",\"price\":12,\"secret\":\"x\"}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "bodyContains": "{\"id\":4,\"name\":\"Tea\",\"price\":12}"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use data.name.trim() to remove spaces from the name."
      },
      {
        "level": 2,
        "text": "Replace the old item line with the new one, keeping only id, name, and price.\n\nIn server.js:\n```\n  const item = { id: items.length + 1, name: data.name.trim(), price: data.price };\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  let data;\n  try { data = JSON.parse(await readBody(req)); } catch { return send(res, 400, { error: \"Body must be JSON\" }); }\n  if (typeof data.name !== \"string\" || !data.name.trim()) return send(res, 422, { error: \"name is required\" });\n  if (!Number.isFinite(data.price) || data.price < 0) return send(res, 422, { error: \"price must be a number 0 or more\" });\n  const item = { id: items.length + 1, name: data.name.trim(), price: data.price };\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "request-bodies-sari-sari"
  },
  {
    "id": "api-request-bodies-sari-sari-10",
    "index": 30,
    "task": "After creating an item, send a Location header. This tells the client where to find the new item. The code below shows how to add it. Run the checker to confirm.\n\nIn server.js:\n```\n  res.setHeader(\"Location\", `/items/${item.id}`);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "location",
        "label": "POST /items sends Location: /items/4",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "{\"name\":\"Tea\",\"price\":12}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "header": {
          "name": "location",
          "value": "/items/4"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Add the header before the return line in createItem. Use the item's id."
      },
      {
        "level": 2,
        "text": "The header should be: Location: /items/{id}.\n\nIn server.js:\n```\n  res.setHeader(\"Location\", `/items/${item.id}`);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  let data;\n  try { data = JSON.parse(await readBody(req)); } catch { return send(res, 400, { error: \"Body must be JSON\" }); }\n  if (typeof data.name !== \"string\" || !data.name.trim()) return send(res, 422, { error: \"name is required\" });\n  if (!Number.isFinite(data.price) || data.price < 0) return send(res, 422, { error: \"price must be a number 0 or more\" });\n  const item = { id: items.length + 1, name: data.name.trim(), price: data.price };\n  items.push(item);\n  res.setHeader(\"Location\", `/items/${item.id}`);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-location-header"
    ],
    "estimatedMinutes": 3,
    "projectId": "request-bodies-sari-sari"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: one-item-sari-sari.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-one-item-sari-sari-1",
    "index": 31,
    "task": "You will read the item's id from the path. The path is /items/2. The 2 is the id. The code below picks that id. Add these two lines at the top of the handler. This lets you find the item by its id.\n\nIn server.js:\n```\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") return send(res, 200, items.find((item) => item.id === Number(match[1])));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "one",
        "label": "GET /items/2 answers Soap",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/2"
          }
        ],
        "bodyContains": "\"name\":\"Soap\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Look for the pattern in the URL: /items/number. The number is the id you need."
      },
      {
        "level": 2,
        "text": "Add the code at the top of the handler, right after the other lines.\n\nIn server.js:\n```\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") return send(res, 200, items.find((item) => item.id === Number(match[1])));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") return send(res, 200, items.find((item) => item.id === Number(match[1])));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-path-parameter"
    ],
    "estimatedMinutes": 3,
    "projectId": "one-item-sari-sari"
  },
  {
    "id": "api-one-item-sari-sari-2",
    "index": 32,
    "task": "You will check if the item exists. If it does not, answer 404. The code below checks for the item. Add this line and change the GET line. This stops errors when the item is missing.\n\nIn server.js:\n```\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "none",
        "label": "GET /items/99 answers 404",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/99"
          }
        ],
        "status": 404,
        "bodyContains": "Item not found"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "If the item is not found, send 404 with a message. The code below does that."
      },
      {
        "level": 2,
        "text": "Add the found line first, then change the GET line to use it.\n\nIn server.js:\n```\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-not-found"
    ],
    "estimatedMinutes": 4,
    "projectId": "one-item-sari-sari"
  },
  {
    "id": "api-one-item-sari-sari-3",
    "index": 33,
    "task": "You will delete an item. The code below removes it and answers 204. Add this line after the GET line. This tells the user the item is gone.\n\nIn server.js:\n```\n  if (found && req.method === \"DELETE\") { items.splice(items.indexOf(found), 1); res.statusCode = 204; return res.end(); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "deleted",
        "label": "DELETE /items/1 answers 204",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "DELETE",
            "path": "/items/1"
          }
        ],
        "status": 204
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "After checking if the item exists, add code to delete it. The code below does that."
      },
      {
        "level": 2,
        "text": "Add the DELETE code right after the GET code, in the same block.\n\nIn server.js:\n```\n  if (found && req.method === \"DELETE\") { items.splice(items.indexOf(found), 1); res.statusCode = 204; return res.end(); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"DELETE\") { items.splice(items.indexOf(found), 1); res.statusCode = 204; return res.end(); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-delete"
    ],
    "estimatedMinutes": 4,
    "projectId": "one-item-sari-sari"
  },
  {
    "id": "api-one-item-sari-sari-4",
    "index": 34,
    "task": "You will answer 404 when deleting an item that does not exist. Add this line after the DELETE line. This stops errors when the item is missing.\n\nIn server.js:\n```\n  if (match && req.method === \"DELETE\") return send(res, 404, { error: \"Item not found\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "gone",
        "label": "DELETE /items/99 answers 404 Item not found",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "DELETE",
            "path": "/items/99"
          }
        ],
        "status": 404,
        "bodyContains": "Item not found"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "If the item is not found, send 404 with a message. The code below does that."
      },
      {
        "level": 2,
        "text": "Add this line right after the DELETE code, before the next check.\n\nIn server.js:\n```\n  if (match && req.method === \"DELETE\") return send(res, 404, { error: \"Item not found\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"DELETE\") { items.splice(items.indexOf(found), 1); res.statusCode = 204; return res.end(); }\n  if (match && req.method === \"DELETE\") return send(res, 404, { error: \"Item not found\" });\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "one-item-sari-sari"
  },
  {
    "id": "api-one-item-sari-sari-5",
    "index": 35,
    "task": "You will update an item with PUT. The code below replaces the item's fields. Add this line after the DELETE lines. This lets you change the item's details.\n\nIn server.js:\n```\n  if (found && req.method === \"PUT\") { Object.assign(found, JSON.parse(await readBody(req))); return send(res, 200, found); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "put",
        "label": "PUT /items/1 with price 99 answers the new price",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "PUT",
            "path": "/items/1",
            "body": "{\"price\":99}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "bodyContains": "\"price\":99"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "PUT changes the item's fields. The code below reads the new fields and updates the item."
      },
      {
        "level": 2,
        "text": "Add this line after the DELETE lines, in the same block.\n\nIn server.js:\n```\n  if (found && req.method === \"PUT\") { Object.assign(found, JSON.parse(await readBody(req))); return send(res, 200, found); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"DELETE\") { items.splice(items.indexOf(found), 1); res.statusCode = 204; return res.end(); }\n  if (match && req.method === \"DELETE\") return send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"PUT\") { Object.assign(found, JSON.parse(await readBody(req))); return send(res, 200, found); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-put"
    ],
    "estimatedMinutes": 5,
    "projectId": "one-item-sari-sari"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: one-item-sari-sari.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-one-item-sari-sari-6",
    "index": 36,
    "task": "You will change the PUT line in server.js. The client may send a different id, but you must keep the item's original id. This keeps the item's identity safe. The code below shows the change. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (found && req.method === \"PUT\") { Object.assign(found, JSON.parse(await readBody(req)), { id: found.id }); return send(res, 200, found); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "keep",
        "label": "After PUT with id 50, GET /items/1 still works",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "PUT",
            "path": "/items/1",
            "body": "{\"id\":50,\"price\":1}",
            "headers": {
              "Content-Type": "application/json"
            }
          },
          {
            "method": "GET",
            "path": "/items/1"
          }
        ],
        "status": 200,
        "bodyContains": "\"id\":1"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The client may send a new id, but you must keep the old one. This keeps the item's identity safe."
      },
      {
        "level": 2,
        "text": "Change the PUT line in server.js. The code below shows the change.\n\nIn server.js:\n```\n  if (found && req.method === \"PUT\") { Object.assign(found, JSON.parse(await readBody(req)), { id: found.id }); return send(res, 200, found); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"DELETE\") { items.splice(items.indexOf(found), 1); res.statusCode = 204; return res.end(); }\n  if (match && req.method === \"DELETE\") return send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"PUT\") { Object.assign(found, JSON.parse(await readBody(req)), { id: found.id }); return send(res, 200, found); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "one-item-sari-sari"
  },
  {
    "id": "api-one-item-sari-sari-7",
    "index": 37,
    "task": "Add one line before the PUT line. This line checks if the body is not JSON. If it is not, send 415. This stops bad data from breaking the server. The code below shows the change. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (found && req.method === \"PUT\" && !req.headers[\"content-type\"]?.includes(\"application/json\")) return send(res, 415, { error: \"Send JSON\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "type",
        "label": "PUT with a text/plain body answers 415",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "PUT",
            "path": "/items/1",
            "body": "price=99",
            "headers": {
              "Content-Type": "text/plain"
            }
          }
        ],
        "status": 415
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "If the body is not JSON, send 415. This stops bad data from breaking the server."
      },
      {
        "level": 2,
        "text": "Add the line before the PUT line. The code below shows the change.\n\nIn server.js:\n```\n  if (found && req.method === \"PUT\" && !req.headers[\"content-type\"]?.includes(\"application/json\")) return send(res, 415, { error: \"Send JSON\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"DELETE\") { items.splice(items.indexOf(found), 1); res.statusCode = 204; return res.end(); }\n  if (match && req.method === \"DELETE\") return send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"PUT\" && !req.headers[\"content-type\"]?.includes(\"application/json\")) return send(res, 415, { error: \"Send JSON\" });\n  if (found && req.method === \"PUT\") { Object.assign(found, JSON.parse(await readBody(req)), { id: found.id }); return send(res, 200, found); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-unsupported-type"
    ],
    "estimatedMinutes": 3,
    "projectId": "one-item-sari-sari"
  },
  {
    "id": "api-one-item-sari-sari-8",
    "index": 38,
    "task": "Add one line before the 404 line. This line handles HEAD requests. HEAD asks for only the headers, not the body. The code below shows the change. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (req.url === \"/items\" && req.method === \"HEAD\") { res.setHeader(\"X-Total-Count\", String(items.length)); return res.end(); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "head",
        "label": "HEAD /items sends X-Total-Count: 3",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "HEAD",
            "path": "/items"
          }
        ],
        "header": {
          "name": "x-total-count",
          "value": "3"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "HEAD asks for only the headers, not the body. This line sends the X-Total-Count header."
      },
      {
        "level": 2,
        "text": "Add the line before the 404 line. The code below shows the change.\n\nIn server.js:\n```\n  if (req.url === \"/items\" && req.method === \"HEAD\") { res.setHeader(\"X-Total-Count\", String(items.length)); return res.end(); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"DELETE\") { items.splice(items.indexOf(found), 1); res.statusCode = 204; return res.end(); }\n  if (match && req.method === \"DELETE\") return send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"PUT\" && !req.headers[\"content-type\"]?.includes(\"application/json\")) return send(res, 415, { error: \"Send JSON\" });\n  if (found && req.method === \"PUT\") { Object.assign(found, JSON.parse(await readBody(req)), { id: found.id }); return send(res, 200, found); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"HEAD\") { res.setHeader(\"X-Total-Count\", String(items.length)); return res.end(); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-head"
    ],
    "estimatedMinutes": 3,
    "projectId": "one-item-sari-sari"
  },
  {
    "id": "api-one-item-sari-sari-9",
    "index": 39,
    "task": "Add one line after the PUT line. This line handles OPTIONS requests. OPTIONS asks which methods a path supports. The code below shows the change. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (match && req.method === \"OPTIONS\") { res.setHeader(\"Allow\", \"GET, PUT, DELETE\"); res.statusCode = 204; return res.end(); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "options",
        "label": "OPTIONS /items/1 lists GET, PUT, DELETE",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "OPTIONS",
            "path": "/items/1"
          }
        ],
        "header": {
          "name": "allow",
          "value": "GET, PUT, DELETE"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "OPTIONS asks which methods a path supports. This line sends the Allow header with GET, PUT, DELETE."
      },
      {
        "level": 2,
        "text": "Add the line after the PUT line. The code below shows the change.\n\nIn server.js:\n```\n  if (match && req.method === \"OPTIONS\") { res.setHeader(\"Allow\", \"GET, PUT, DELETE\"); res.statusCode = 204; return res.end(); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"DELETE\") { items.splice(items.indexOf(found), 1); res.statusCode = 204; return res.end(); }\n  if (match && req.method === \"DELETE\") return send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"PUT\" && !req.headers[\"content-type\"]?.includes(\"application/json\")) return send(res, 415, { error: \"Send JSON\" });\n  if (found && req.method === \"PUT\") { Object.assign(found, JSON.parse(await readBody(req)), { id: found.id }); return send(res, 200, found); }\n  if (match && req.method === \"OPTIONS\") { res.setHeader(\"Allow\", \"GET, PUT, DELETE\"); res.statusCode = 204; return res.end(); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"HEAD\") { res.setHeader(\"X-Total-Count\", String(items.length)); return res.end(); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-options"
    ],
    "estimatedMinutes": 3,
    "projectId": "one-item-sari-sari"
  },
  {
    "id": "api-one-item-sari-sari-10",
    "index": 40,
    "task": "Add one line after the OPTIONS line. This line handles any other method on one item. It sends 405 for those methods. The code below shows the change. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (match) return send(res, 405, { error: \"Method not allowed\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "post",
        "label": "POST /items/1 answers 405",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items/1",
            "body": "{}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 405
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Any method not listed (like POST) should get 405. This line handles that."
      },
      {
        "level": 2,
        "text": "Add the line after the OPTIONS line. The code below shows the change.\n\nIn server.js:\n```\n  if (match) return send(res, 405, { error: \"Method not allowed\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"DELETE\") { items.splice(items.indexOf(found), 1); res.statusCode = 204; return res.end(); }\n  if (match && req.method === \"DELETE\") return send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"PUT\" && !req.headers[\"content-type\"]?.includes(\"application/json\")) return send(res, 415, { error: \"Send JSON\" });\n  if (found && req.method === \"PUT\") { Object.assign(found, JSON.parse(await readBody(req)), { id: found.id }); return send(res, 200, found); }\n  if (match && req.method === \"OPTIONS\") { res.setHeader(\"Allow\", \"GET, PUT, DELETE\"); res.statusCode = 204; return res.end(); }\n  if (match) return send(res, 405, { error: \"Method not allowed\" });\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"HEAD\") { res.setHeader(\"X-Total-Count\", String(items.length)); return res.end(); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-method-not-allowed"
    ],
    "estimatedMinutes": 3,
    "projectId": "one-item-sari-sari"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: query-strings-sari-sari.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-query-strings-sari-sari-1",
    "index": 41,
    "task": "You will add two lines to server.js. The first line gets the items. The second line checks if the client sent ?max. If yes, it filters the list to keep only items that cost at most that number. This helps the client find items they can afford. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.pathname === \"/items\") return send(res, 200, list);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Soap\", price: 25 }, { id: 2, name: \"Egg\", price: 9 }, { id: 3, name: \"Rice\", price: 50 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "max",
        "label": "GET /items?max=25 answers only the items up to 25",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items?max=25"
          }
        ],
        "bodyContains": "[{\"id\":1,\"name\":\"Soap\",\"price\":25},{\"id\":2,\"name\":\"Egg\",\"price\":9}]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Think of filtering like picking only the items that fit your budget."
      },
      {
        "level": 2,
        "text": "Add the code after the url line, right before the /items line.\n\nIn server.js:\n```\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.pathname === \"/items\") return send(res, 200, list);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Soap\", price: 25 }, { id: 2, name: \"Egg\", price: 9 }, { id: 3, name: \"Rice\", price: 50 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.pathname === \"/items\") return send(res, 200, list);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-filter"
    ],
    "estimatedMinutes": 4,
    "projectId": "query-strings-sari-sari"
  },
  {
    "id": "api-query-strings-sari-sari-2",
    "index": 42,
    "task": "You will add one line to server.js. This line checks if the client sent ?min. If yes, it filters the list to keep only items that cost at least that number. This helps the client find items they want to buy. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Soap\", price: 25 }, { id: 2, name: \"Egg\", price: 9 }, { id: 3, name: \"Rice\", price: 50 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "min",
        "label": "GET /items?min=25 answers only the items from 25",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items?min=25"
          }
        ],
        "bodyContains": "[{\"id\":1,\"name\":\"Soap\",\"price\":25},{\"id\":3,\"name\":\"Rice\",\"price\":50}]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Think of filtering like picking only the items that cost more than or equal to the minimum."
      },
      {
        "level": 2,
        "text": "Add the code after the max line, right before the q line.\n\nIn server.js:\n```\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Soap\", price: 25 }, { id: 2, name: \"Egg\", price: 9 }, { id: 3, name: \"Rice\", price: 50 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n  if (url.pathname === \"/items\") return send(res, 200, list);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "query-strings-sari-sari"
  },
  {
    "id": "api-query-strings-sari-sari-3",
    "index": 43,
    "task": "You will add one line to server.js. This line checks if the client sent ?q. If yes, it filters the list to keep only items whose name contains what the client typed, ignoring upper and lower case. This helps the client find items by name. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (url.searchParams.has(\"q\")) list = list.filter((item) => item.name.toLowerCase().includes(url.searchParams.get(\"q\").toLowerCase()));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Soap\", price: 25 }, { id: 2, name: \"Egg\", price: 9 }, { id: 3, name: \"Rice\", price: 50 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "q",
        "label": "GET /items?q=gg finds Egg",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items?q=gg"
          }
        ],
        "bodyContains": "[{\"id\":2,\"name\":\"Egg\",\"price\":9}]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Think of searching like looking for a word in a list, no matter if it's uppercase or lowercase."
      },
      {
        "level": 2,
        "text": "Add the code after the min line, right before the sort line.\n\nIn server.js:\n```\n  if (url.searchParams.has(\"q\")) list = list.filter((item) => item.name.toLowerCase().includes(url.searchParams.get(\"q\").toLowerCase()));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Soap\", price: 25 }, { id: 2, name: \"Egg\", price: 9 }, { id: 3, name: \"Rice\", price: 50 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n  if (url.searchParams.has(\"q\")) list = list.filter((item) => item.name.toLowerCase().includes(url.searchParams.get(\"q\").toLowerCase()));\n  if (url.pathname === \"/items\") return send(res, 200, list);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-search"
    ],
    "estimatedMinutes": 4,
    "projectId": "query-strings-sari-sari"
  },
  {
    "id": "api-query-strings-sari-sari-4",
    "index": 44,
    "task": "You will add one line to server.js. This line checks if the client sent ?sort=price. If yes, it sorts the list from cheapest to most expensive. This helps the client see the best deals first. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (url.searchParams.get(\"sort\") === \"price\") list = [...list].sort((a, b) => a.price - b.price);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Soap\", price: 25 }, { id: 2, name: \"Egg\", price: 9 }, { id: 3, name: \"Rice\", price: 50 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "asc",
        "label": "GET /items?sort=price answers cheapest first",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items?sort=price"
          }
        ],
        "bodyContains": "[{\"id\":2,\"name\":\"Egg\",\"price\":9},{\"id\":1,\"name\":\"Soap\",\"price\":25},{\"id\":3,\"name\":\"Rice\",\"price\":50}]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Think of sorting like arranging items from lowest to highest price."
      },
      {
        "level": 2,
        "text": "Add the code after the q line, right before the -price line.\n\nIn server.js:\n```\n  if (url.searchParams.get(\"sort\") === \"price\") list = [...list].sort((a, b) => a.price - b.price);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Soap\", price: 25 }, { id: 2, name: \"Egg\", price: 9 }, { id: 3, name: \"Rice\", price: 50 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n  if (url.searchParams.has(\"q\")) list = list.filter((item) => item.name.toLowerCase().includes(url.searchParams.get(\"q\").toLowerCase()));\n  if (url.searchParams.get(\"sort\") === \"price\") list = [...list].sort((a, b) => a.price - b.price);\n  if (url.pathname === \"/items\") return send(res, 200, list);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-sort"
    ],
    "estimatedMinutes": 4,
    "projectId": "query-strings-sari-sari"
  },
  {
    "id": "api-query-strings-sari-sari-5",
    "index": 45,
    "task": "You will add one line to server.js. This line checks if the client sent ?sort=-price. If yes, it sorts the list from most expensive to cheapest. This helps the client see the most costly items first. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (url.searchParams.get(\"sort\") === \"-price\") list = [...list].sort((a, b) => b.price - a.price);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Soap\", price: 25 }, { id: 2, name: \"Egg\", price: 9 }, { id: 3, name: \"Rice\", price: 50 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "desc",
        "label": "GET /items?sort=-price answers most expensive first",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items?sort=-price"
          }
        ],
        "bodyContains": "[{\"id\":3,\"name\":\"Rice\",\"price\":50},{\"id\":1,\"name\":\"Soap\",\"price\":25},{\"id\":2,\"name\":\"Egg\",\"price\":9}]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Think of sorting like arranging items from highest to lowest price."
      },
      {
        "level": 2,
        "text": "Add the code after the sort line, right before the end of the filter block.\n\nIn server.js:\n```\n  if (url.searchParams.get(\"sort\") === \"-price\") list = [...list].sort((a, b) => b.price - a.price);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Soap\", price: 25 }, { id: 2, name: \"Egg\", price: 9 }, { id: 3, name: \"Rice\", price: 50 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n  if (url.searchParams.has(\"q\")) list = list.filter((item) => item.name.toLowerCase().includes(url.searchParams.get(\"q\").toLowerCase()));\n  if (url.searchParams.get(\"sort\") === \"price\") list = [...list].sort((a, b) => a.price - b.price);\n  if (url.searchParams.get(\"sort\") === \"-price\") list = [...list].sort((a, b) => b.price - a.price);\n  if (url.pathname === \"/items\") return send(res, 200, list);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "query-strings-sari-sari"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: query-strings-sari-sari.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-query-strings-sari-sari-6",
    "index": 46,
    "task": "You add a new line after the second sort line. This line checks if the client asks for a limit. If yes, it cuts the list to only the first few items. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (url.searchParams.has(\"limit\")) list = list.slice(0, Number(url.searchParams.get(\"limit\")));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Soap\", price: 25 }, { id: 2, name: \"Egg\", price: 9 }, { id: 3, name: \"Rice\", price: 50 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "limit",
        "label": "GET /items?sort=price&limit=1 answers only the cheapest item",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items?sort=price&limit=1"
          }
        ],
        "bodyContains": "[{\"id\":2,\"name\":\"Egg\",\"price\":9}]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Think of limit as a cap on how many items to show."
      },
      {
        "level": 2,
        "text": "Add this line right after the second sort line, in server.js.\n\nIn server.js:\n```\n  if (url.searchParams.has(\"limit\")) list = list.slice(0, Number(url.searchParams.get(\"limit\")));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Soap\", price: 25 }, { id: 2, name: \"Egg\", price: 9 }, { id: 3, name: \"Rice\", price: 50 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n  if (url.searchParams.has(\"q\")) list = list.filter((item) => item.name.toLowerCase().includes(url.searchParams.get(\"q\").toLowerCase()));\n  if (url.searchParams.get(\"sort\") === \"price\") list = [...list].sort((a, b) => a.price - b.price);\n  if (url.searchParams.get(\"sort\") === \"-price\") list = [...list].sort((a, b) => b.price - a.price);\n  if (url.searchParams.has(\"limit\")) list = list.slice(0, Number(url.searchParams.get(\"limit\")));\n  if (url.pathname === \"/items\") return send(res, 200, list);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-limit"
    ],
    "estimatedMinutes": 3,
    "projectId": "query-strings-sari-sari"
  },
  {
    "id": "api-query-strings-sari-sari-7",
    "index": 47,
    "task": "You add a new line after the limit line. This line checks if the client asks for only the name field. If yes, it changes the list to only names. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (url.searchParams.get(\"fields\") === \"name\") list = list.map((item) => ({ name: item.name }));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Soap\", price: 25 }, { id: 2, name: \"Egg\", price: 9 }, { id: 3, name: \"Rice\", price: 50 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "fields",
        "label": "GET /items?fields=name answers names only",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items?fields=name"
          }
        ],
        "bodyContains": "[{\"name\":\"Soap\"},{\"name\":\"Egg\"},{\"name\":\"Rice\"}]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Field selection means you send only what the client asks for, not extra data."
      },
      {
        "level": 2,
        "text": "Add this line right after the limit line, in server.js.\n\nIn server.js:\n```\n  if (url.searchParams.get(\"fields\") === \"name\") list = list.map((item) => ({ name: item.name }));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Soap\", price: 25 }, { id: 2, name: \"Egg\", price: 9 }, { id: 3, name: \"Rice\", price: 50 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n  if (url.searchParams.has(\"q\")) list = list.filter((item) => item.name.toLowerCase().includes(url.searchParams.get(\"q\").toLowerCase()));\n  if (url.searchParams.get(\"sort\") === \"price\") list = [...list].sort((a, b) => a.price - b.price);\n  if (url.searchParams.get(\"sort\") === \"-price\") list = [...list].sort((a, b) => b.price - a.price);\n  if (url.searchParams.has(\"limit\")) list = list.slice(0, Number(url.searchParams.get(\"limit\")));\n  if (url.searchParams.get(\"fields\") === \"name\") list = list.map((item) => ({ name: item.name }));\n  if (url.pathname === \"/items\") return send(res, 200, list);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-fields"
    ],
    "estimatedMinutes": 3,
    "projectId": "query-strings-sari-sari"
  },
  {
    "id": "api-query-strings-sari-sari-8",
    "index": 48,
    "task": "You add a new line right after the url line. This line checks if the client asks for a max that is not a number. If yes, it sends a 400 error. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (url.searchParams.has(\"max\") && Number.isNaN(Number(url.searchParams.get(\"max\")))) return send(res, 400, { error: \"max must be a number\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Soap\", price: 25 }, { id: 2, name: \"Egg\", price: 9 }, { id: 3, name: \"Rice\", price: 50 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "bad",
        "label": "GET /items?max=abc answers 400",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items?max=abc"
          }
        ],
        "status": 400
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "If max is not a number, the server must say 400, not try to use it."
      },
      {
        "level": 2,
        "text": "Add this line right after the url line, in server.js.\n\nIn server.js:\n```\n  if (url.searchParams.has(\"max\") && Number.isNaN(Number(url.searchParams.get(\"max\")))) return send(res, 400, { error: \"max must be a number\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Soap\", price: 25 }, { id: 2, name: \"Egg\", price: 9 }, { id: 3, name: \"Rice\", price: 50 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.searchParams.has(\"max\") && Number.isNaN(Number(url.searchParams.get(\"max\")))) return send(res, 400, { error: \"max must be a number\" });\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n  if (url.searchParams.has(\"q\")) list = list.filter((item) => item.name.toLowerCase().includes(url.searchParams.get(\"q\").toLowerCase()));\n  if (url.searchParams.get(\"sort\") === \"price\") list = [...list].sort((a, b) => a.price - b.price);\n  if (url.searchParams.get(\"sort\") === \"-price\") list = [...list].sort((a, b) => b.price - a.price);\n  if (url.searchParams.has(\"limit\")) list = list.slice(0, Number(url.searchParams.get(\"limit\")));\n  if (url.searchParams.get(\"fields\") === \"name\") list = list.map((item) => ({ name: item.name }));\n  if (url.pathname === \"/items\") return send(res, 200, list);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "query-strings-sari-sari"
  },
  {
    "id": "api-query-strings-sari-sari-9",
    "index": 49,
    "task": "You change the /items line. This line sends a header called X-Total-Count that says how many items matched. Then it sends the list. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (url.pathname === \"/items\") { res.setHeader(\"X-Total-Count\", String(list.length)); return send(res, 200, list); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Soap\", price: 25 }, { id: 2, name: \"Egg\", price: 9 }, { id: 3, name: \"Rice\", price: 50 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "count",
        "label": "GET /items?max=25 sends X-Total-Count: 2",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items?max=25"
          }
        ],
        "header": {
          "name": "x-total-count",
          "value": "2"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "X-Total-Count is a header that tells the client how many items matched, not in the body."
      },
      {
        "level": 2,
        "text": "Change the /items line to send the header and list, in server.js.\n\nIn server.js:\n```\n  if (url.pathname === \"/items\") { res.setHeader(\"X-Total-Count\", String(list.length)); return send(res, 200, list); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Soap\", price: 25 }, { id: 2, name: \"Egg\", price: 9 }, { id: 3, name: \"Rice\", price: 50 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.searchParams.has(\"max\") && Number.isNaN(Number(url.searchParams.get(\"max\")))) return send(res, 400, { error: \"max must be a number\" });\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n  if (url.searchParams.has(\"q\")) list = list.filter((item) => item.name.toLowerCase().includes(url.searchParams.get(\"q\").toLowerCase()));\n  if (url.searchParams.get(\"sort\") === \"price\") list = [...list].sort((a, b) => a.price - b.price);\n  if (url.searchParams.get(\"sort\") === \"-price\") list = [...list].sort((a, b) => b.price - a.price);\n  if (url.searchParams.has(\"limit\")) list = list.slice(0, Number(url.searchParams.get(\"limit\")));\n  if (url.searchParams.get(\"fields\") === \"name\") list = list.map((item) => ({ name: item.name }));\n  if (url.pathname === \"/items\") { res.setHeader(\"X-Total-Count\", String(list.length)); return send(res, 200, list); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-count-header"
    ],
    "estimatedMinutes": 4,
    "projectId": "query-strings-sari-sari"
  },
  {
    "id": "api-query-strings-sari-sari-10",
    "index": 50,
    "task": "You add a new line after the 400 line. This line answers /items/options with a list of all supported filters and sort options. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (url.pathname === \"/items/options\") return send(res, 200, { filters: [\"max\", \"min\", \"q\"], sort: [\"price\", \"-price\"], other: [\"limit\", \"fields\"] });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Soap\", price: 25 }, { id: 2, name: \"Egg\", price: 9 }, { id: 3, name: \"Rice\", price: 50 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "options",
        "label": "GET /items/options lists the filters",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/options"
          }
        ],
        "bodyContains": "\"filters\":[\"max\",\"min\",\"q\"]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The /items/options endpoint lists all the filters and sorts the client can use."
      },
      {
        "level": 2,
        "text": "Add this line after the 400 line, in server.js.\n\nIn server.js:\n```\n  if (url.pathname === \"/items/options\") return send(res, 200, { filters: [\"max\", \"min\", \"q\"], sort: [\"price\", \"-price\"], other: [\"limit\", \"fields\"] });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Soap\", price: 25 }, { id: 2, name: \"Egg\", price: 9 }, { id: 3, name: \"Rice\", price: 50 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.searchParams.has(\"max\") && Number.isNaN(Number(url.searchParams.get(\"max\")))) return send(res, 400, { error: \"max must be a number\" });\n  if (url.pathname === \"/items/options\") return send(res, 200, { filters: [\"max\", \"min\", \"q\"], sort: [\"price\", \"-price\"], other: [\"limit\", \"fields\"] });\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n  if (url.searchParams.has(\"q\")) list = list.filter((item) => item.name.toLowerCase().includes(url.searchParams.get(\"q\").toLowerCase()));\n  if (url.searchParams.get(\"sort\") === \"price\") list = [...list].sort((a, b) => a.price - b.price);\n  if (url.searchParams.get(\"sort\") === \"-price\") list = [...list].sort((a, b) => b.price - a.price);\n  if (url.searchParams.has(\"limit\")) list = list.slice(0, Number(url.searchParams.get(\"limit\")));\n  if (url.searchParams.get(\"fields\") === \"name\") list = list.map((item) => ({ name: item.name }));\n  if (url.pathname === \"/items\") { res.setHeader(\"X-Total-Count\", String(list.length)); return send(res, 200, list); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "query-strings-sari-sari"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: middleware-sari-sari.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-middleware-sari-sari-1",
    "index": 51,
    "task": "You will add a counter and a function to add request IDs. This helps every answer have a unique number. The code below goes above the server. You call this function at the top of the handler. This way, every request gets a numbered header.\n\nIn server.js:\n```\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\n  addRequestId(req, res);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "id",
        "label": "The first answer sends X-Request-Id: 1",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "header": {
          "name": "x-request-id",
          "value": "1"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Think of the counter as a small machine that counts up each time you use it."
      },
      {
        "level": 2,
        "text": "Put the code above the server, right after the empty line.\n\nIn server.js:\n```\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\n  addRequestId(req, res);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  addRequestId(req, res);\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-middleware"
    ],
    "estimatedMinutes": 3,
    "projectId": "middleware-sari-sari"
  },
  {
    "id": "api-middleware-sari-sari-2",
    "index": 52,
    "task": "You will add another function to set a place header. This tells the browser where the answer comes from. The code below goes right after the first function. You call it after the first one. This way, every answer gets the store's name.\n\nIn server.js:\n```\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"sari-sari\"); }\n  addPlace(req, res);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "place",
        "label": "Answers send X-Place: sari-sari",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "header": {
          "name": "x-place",
          "value": "sari-sari"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Imagine this function as a sign that says 'sari-sari' on every answer."
      },
      {
        "level": 2,
        "text": "Put the code after the first function, right before the first call.\n\nIn server.js:\n```\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"sari-sari\"); }\n  addPlace(req, res);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"sari-sari\"); }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  addRequestId(req, res);\n  addPlace(req, res);\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 2,
    "projectId": "middleware-sari-sari"
  },
  {
    "id": "api-middleware-sari-sari-3",
    "index": 53,
    "task": "You will make a list of all the functions you added. This lets you run them all in order. The code below replaces the two calls with one loop. This way, you can add or change steps without changing the handler.\n\nIn server.js:\n```\nconst middleware = [addRequestId, addPlace];\n  for (const step of middleware) step(req, res);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "list",
        "label": "server.js keeps its middleware in one list",
        "kind": "local-file-contains",
        "path": "server.js",
        "value": "const middleware = [addRequestId, addPlace];"
      },
      {
        "id": "still",
        "label": "Answers still send X-Place",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "header": {
          "name": "x-place",
          "value": "sari-sari"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Think of the list as a recipe where you follow each step in order."
      },
      {
        "level": 2,
        "text": "Put the code right after the functions, before the handler.\n\nIn server.js:\n```\nconst middleware = [addRequestId, addPlace];\n  for (const step of middleware) step(req, res);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"sari-sari\"); }\nconst middleware = [addRequestId, addPlace];\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  for (const step of middleware) step(req, res);\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-middleware-list"
    ],
    "estimatedMinutes": 4,
    "projectId": "middleware-sari-sari"
  },
  {
    "id": "api-middleware-sari-sari-4",
    "index": 54,
    "task": "You will add a new function that sets a powered-by header. This tells the browser what tools you used. The code below adds this function to the list. You do not change the handler. This way, every answer gets the tool name.\n\nIn server.js:\n```\nfunction addPoweredBy(req, res) { res.setHeader(\"X-Powered-By\", \"Node built-ins\"); }\nconst middleware = [addRequestId, addPlace, addPoweredBy];\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "powered",
        "label": "Answers send X-Powered-By: Node built-ins",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "header": {
          "name": "x-powered-by",
          "value": "Node built-ins"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Think of this function as a credit card for your tools."
      },
      {
        "level": 2,
        "text": "Add the code to the list, right after addPlace.\n\nIn server.js:\n```\nfunction addPoweredBy(req, res) { res.setHeader(\"X-Powered-By\", \"Node built-ins\"); }\nconst middleware = [addRequestId, addPlace, addPoweredBy];\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"sari-sari\"); }\nfunction addPoweredBy(req, res) { res.setHeader(\"X-Powered-By\", \"Node built-ins\"); }\nconst middleware = [addRequestId, addPlace, addPoweredBy];\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  for (const step of middleware) step(req, res);\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "middleware-sari-sari"
  },
  {
    "id": "api-middleware-sari-sari-5",
    "index": 55,
    "task": "You will add a function to let other websites call this API. This uses CORS headers. The code below adds this function to the list. You do not change the handler. This way, every answer lets other sites use it.\n\nIn server.js:\n```\nfunction allowBrowsers(req, res) { res.setHeader(\"Access-Control-Allow-Origin\", \"*\"); }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers];\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "cors",
        "label": "Answers send Access-Control-Allow-Origin: *",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "header": {
          "name": "access-control-allow-origin",
          "value": "*"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Think of CORS as a door that opens for other websites."
      },
      {
        "level": 2,
        "text": "Add the code to the list, right after addPoweredBy.\n\nIn server.js:\n```\nfunction allowBrowsers(req, res) { res.setHeader(\"Access-Control-Allow-Origin\", \"*\"); }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers];\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"sari-sari\"); }\nfunction addPoweredBy(req, res) { res.setHeader(\"X-Powered-By\", \"Node built-ins\"); }\nfunction allowBrowsers(req, res) { res.setHeader(\"Access-Control-Allow-Origin\", \"*\"); }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers];\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  for (const step of middleware) step(req, res);\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-cors"
    ],
    "estimatedMinutes": 4,
    "projectId": "middleware-sari-sari"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: middleware-sari-sari.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-middleware-sari-sari-6",
    "index": 56,
    "task": "You add a function called requireKey. This function checks if the request has the right API key. If not, it sends a 401 error. You add this function to the middleware list. Then, when the loop runs, it stops early if requireKey returns true. The code below shows what to add. Run the checker to confirm it works.\n\nIn server.js:\n```\nfunction requireKey(req, res) { if (req.headers[\"x-api-key\"] !== \"secret123\") { send(res, 401, { error: \"Missing or wrong API key\" }); return true; } }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers, requireKey];\n  for (const step of middleware) if (step(req, res)) return;\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "nokey",
        "label": "GET /items without a key answers 401",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "status": 401
      },
      {
        "id": "key",
        "label": "GET /items with the key answers 200",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items",
            "headers": {
              "x-api-key": "secret123"
            }
          }
        ],
        "status": 200
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The key check must happen before any other middleware, so put it in the list after allowBrowsers."
      },
      {
        "level": 2,
        "text": "Add the requireKey function right before the middleware list, then add it to the list.\n\nIn server.js:\n```\nfunction requireKey(req, res) { if (req.headers[\"x-api-key\"] !== \"secret123\") { send(res, 401, { error: \"Missing or wrong API key\" }); return true; } }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers, requireKey];\n  for (const step of middleware) if (step(req, res)) return;\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"sari-sari\"); }\nfunction addPoweredBy(req, res) { res.setHeader(\"X-Powered-By\", \"Node built-ins\"); }\nfunction allowBrowsers(req, res) { res.setHeader(\"Access-Control-Allow-Origin\", \"*\"); }\nfunction requireKey(req, res) { if (req.headers[\"x-api-key\"] !== \"secret123\") { send(res, 401, { error: \"Missing or wrong API key\" }); return true; } }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers, requireKey];\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  for (const step of middleware) if (step(req, res)) return;\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-api-key"
    ],
    "estimatedMinutes": 4,
    "projectId": "middleware-sari-sari"
  },
  {
    "id": "api-middleware-sari-sari-7",
    "index": 57,
    "task": "You add a line at the top of the handler. This line checks if the request is for /health. If so, it sends a 200 OK response and stops the loop. This lets monitors check the server without a key. The code below shows what to add. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (req.url === \"/health\") return send(res, 200, { ok: true });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "health",
        "label": "GET /health answers without a key",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/health"
          }
        ],
        "status": 200,
        "bodyContains": "\"ok\":true"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Put this check right after the start of the handler, before the loop runs."
      },
      {
        "level": 2,
        "text": "The /health check must happen before the key check, so put it before requireKey.\n\nIn server.js:\n```\n  if (req.url === \"/health\") return send(res, 200, { ok: true });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"sari-sari\"); }\nfunction addPoweredBy(req, res) { res.setHeader(\"X-Powered-By\", \"Node built-ins\"); }\nfunction allowBrowsers(req, res) { res.setHeader(\"Access-Control-Allow-Origin\", \"*\"); }\nfunction requireKey(req, res) { if (req.headers[\"x-api-key\"] !== \"secret123\") { send(res, 401, { error: \"Missing or wrong API key\" }); return true; } }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers, requireKey];\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/health\") return send(res, 200, { ok: true });\n  for (const step of middleware) if (step(req, res)) return;\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "middleware-sari-sari"
  },
  {
    "id": "api-middleware-sari-sari-8",
    "index": 58,
    "task": "You change the allowBrowsers function. Now, if the request method is OPTIONS, you send a 204 status and stop the loop. This lets browsers ask permission before making real requests. The code below shows what to change. Run the checker to confirm it works.\n\nIn server.js:\n```\nfunction allowBrowsers(req, res) { res.setHeader(\"Access-Control-Allow-Origin\", \"*\"); if (req.method === \"OPTIONS\") { res.statusCode = 204; res.end(); return true; } }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "preflight",
        "label": "OPTIONS /items answers 204 without a key",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "OPTIONS",
            "path": "/items"
          }
        ],
        "status": 204
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The preflight check must happen before the key check, so put it before requireKey."
      },
      {
        "level": 2,
        "text": "The OPTIONS check must come right after setting the Access-Control-Allow-Origin header.\n\nIn server.js:\n```\nfunction allowBrowsers(req, res) { res.setHeader(\"Access-Control-Allow-Origin\", \"*\"); if (req.method === \"OPTIONS\") { res.statusCode = 204; res.end(); return true; } }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"sari-sari\"); }\nfunction addPoweredBy(req, res) { res.setHeader(\"X-Powered-By\", \"Node built-ins\"); }\nfunction allowBrowsers(req, res) { res.setHeader(\"Access-Control-Allow-Origin\", \"*\"); if (req.method === \"OPTIONS\") { res.statusCode = 204; res.end(); return true; } }\nfunction requireKey(req, res) { if (req.headers[\"x-api-key\"] !== \"secret123\") { send(res, 401, { error: \"Missing or wrong API key\" }); return true; } }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers, requireKey];\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/health\") return send(res, 200, { ok: true });\n  for (const step of middleware) if (step(req, res)) return;\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-preflight"
    ],
    "estimatedMinutes": 4,
    "projectId": "middleware-sari-sari"
  },
  {
    "id": "api-middleware-sari-sari-9",
    "index": 59,
    "task": "You add a line after the loop. This line checks if the request is for /stats. If so, it sends a 200 OK response with the number of requests served. The code below shows what to add. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (req.url === \"/stats\") return send(res, 200, { served: nextId - 1 });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "stats",
        "label": "After two requests, GET /stats answers served 3",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items",
            "headers": {
              "x-api-key": "secret123"
            }
          },
          {
            "method": "GET",
            "path": "/items",
            "headers": {
              "x-api-key": "secret123"
            }
          },
          {
            "method": "GET",
            "path": "/stats",
            "headers": {
              "x-api-key": "secret123"
            }
          }
        ],
        "bodyContains": "\"served\":3"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Put this check right after the loop, so it runs after all middleware."
      },
      {
        "level": 2,
        "text": "The /stats check must happen after the key check, so put it after requireKey.\n\nIn server.js:\n```\n  if (req.url === \"/stats\") return send(res, 200, { served: nextId - 1 });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"sari-sari\"); }\nfunction addPoweredBy(req, res) { res.setHeader(\"X-Powered-By\", \"Node built-ins\"); }\nfunction allowBrowsers(req, res) { res.setHeader(\"Access-Control-Allow-Origin\", \"*\"); if (req.method === \"OPTIONS\") { res.statusCode = 204; res.end(); return true; } }\nfunction requireKey(req, res) { if (req.headers[\"x-api-key\"] !== \"secret123\") { send(res, 401, { error: \"Missing or wrong API key\" }); return true; } }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers, requireKey];\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/health\") return send(res, 200, { ok: true });\n  for (const step of middleware) if (step(req, res)) return;\n  if (req.url === \"/stats\") return send(res, 200, { served: nextId - 1 });\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "middleware-sari-sari"
  },
  {
    "id": "api-middleware-sari-sari-10",
    "index": 60,
    "task": "You add a function called secureHeaders. This function adds a security header to every response. You add this function to the middleware list. The code below shows what to add. Run the checker to confirm it works.\n\nIn server.js:\n```\nfunction secureHeaders(req, res) { res.setHeader(\"X-Content-Type-Options\", \"nosniff\"); }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers, secureHeaders, requireKey];\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "nosniff",
        "label": "Answers send X-Content-Type-Options: nosniff",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items",
            "headers": {
              "x-api-key": "secret123"
            }
          }
        ],
        "header": {
          "name": "x-content-type-options",
          "value": "nosniff"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The secureHeaders function must run after all other middleware, so put it last in the list."
      },
      {
        "level": 2,
        "text": "Add the secureHeaders function right before the middleware list, then add it to the list.\n\nIn server.js:\n```\nfunction secureHeaders(req, res) { res.setHeader(\"X-Content-Type-Options\", \"nosniff\"); }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers, secureHeaders, requireKey];\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"sari-sari\"); }\nfunction addPoweredBy(req, res) { res.setHeader(\"X-Powered-By\", \"Node built-ins\"); }\nfunction allowBrowsers(req, res) { res.setHeader(\"Access-Control-Allow-Origin\", \"*\"); if (req.method === \"OPTIONS\") { res.statusCode = 204; res.end(); return true; } }\nfunction requireKey(req, res) { if (req.headers[\"x-api-key\"] !== \"secret123\") { send(res, 401, { error: \"Missing or wrong API key\" }); return true; } }\nfunction secureHeaders(req, res) { res.setHeader(\"X-Content-Type-Options\", \"nosniff\"); }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers, secureHeaders, requireKey];\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/health\") return send(res, 200, { ok: true });\n  for (const step of middleware) if (step(req, res)) return;\n  if (req.url === \"/stats\") return send(res, 200, { served: nextId - 1 });\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-security-header"
    ],
    "estimatedMinutes": 4,
    "projectId": "middleware-sari-sari"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: sqlite-storage-sari-sari.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-sqlite-storage-sari-sari-1",
    "index": 61,
    "task": "You will open a database in memory. This is fast and temporary. You will make a table called items. This holds the store's products. You will answer a request to /items. The code below does this. Run the checker to test it.\n\nIn server.js:\n```\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "empty",
        "label": "GET /items answers an empty list from the database",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "status": 200,
        "bodyContains": "[]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Think of the database as a notebook you open and close. It holds the store's items."
      },
      {
        "level": 2,
        "text": "Add the code above createItem and before the 404 line.\n\nIn server.js:\n```\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-sqlite"
    ],
    "estimatedMinutes": 4,
    "projectId": "sqlite-storage-sari-sari"
  },
  {
    "id": "api-sqlite-storage-sari-sari-2",
    "index": 62,
    "task": "You will add three items to the database. Rice, Soap, and Egg. You will use a prepared insert. This is safe and fast. You will run the code below after the CREATE TABLE line. Run the checker to test it.\n\nIn server.js:\n```\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Rice\",50],[\"Soap\",25],[\"Egg\",9]]) insert.run(name, price);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "rows",
        "label": "GET /items includes Rice from the database",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "bodyContains": "\"name\":\"Rice\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use the prepared insert to add items without writing SQL each time."
      },
      {
        "level": 2,
        "text": "Add the code after the CREATE TABLE line.\n\nIn server.js:\n```\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Rice\",50],[\"Soap\",25],[\"Egg\",9]]) insert.run(name, price);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Rice\",50],[\"Soap\",25],[\"Egg\",9]]) insert.run(name, price);\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-insert-rows"
    ],
    "estimatedMinutes": 4,
    "projectId": "sqlite-storage-sari-sari"
  },
  {
    "id": "api-sqlite-storage-sari-sari-3",
    "index": 63,
    "task": "You will change the SQL to sort items by price. Cheapest first. You will use ORDER BY. This helps the customer see the cheapest items first. You will change the SQL in the GET /items line. Run the checker to test it.\n\nIn server.js:\n```\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items ORDER BY price\").all());\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "ordered",
        "label": "GET /items lists the items cheapest first, starting with Egg",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "bodyContains": "[{\"id\":3,\"name\":\"Egg\",\"price\":9},{\"id\":2,\"name\":\"Soap\",\"price\":25},{\"id\":1,\"name\":\"Rice\",\"price\":50}]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "ORDER BY tells the database to sort the rows before sending them."
      },
      {
        "level": 2,
        "text": "Change the SQL in the GET /items line.\n\nIn server.js:\n```\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items ORDER BY price\").all());\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Rice\",50],[\"Soap\",25],[\"Egg\",9]]) insert.run(name, price);\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items ORDER BY price\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-order-by"
    ],
    "estimatedMinutes": 3,
    "projectId": "sqlite-storage-sari-sari"
  },
  {
    "id": "api-sqlite-storage-sari-sari-4",
    "index": 64,
    "task": "You will add a route to count the items. You will use COUNT(*). This tells how many rows there are. You will add the code below after the GET /items line. Run the checker to test it.\n\nIn server.js:\n```\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "count",
        "label": "GET /items/count answers {\"count\":3}",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/count"
          }
        ],
        "bodyContains": "{\"count\":3}"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "COUNT(*) asks the database how many rows there are."
      },
      {
        "level": 2,
        "text": "Add the code after the GET /items line.\n\nIn server.js:\n```\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Rice\",50],[\"Soap\",25],[\"Egg\",9]]) insert.run(name, price);\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items ORDER BY price\").all());\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-count"
    ],
    "estimatedMinutes": 3,
    "projectId": "sqlite-storage-sari-sari"
  },
  {
    "id": "api-sqlite-storage-sari-sari-5",
    "index": 65,
    "task": "You will answer one item by its id. You will use a SQL parameter. This is safe. You will add the code below before the 404 line. Run the checker to test it.\n\nIn server.js:\n```\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match) return send(res, 200, db.prepare(\"SELECT * FROM items WHERE id = ?\").get(Number(match[1])));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "one",
        "label": "GET /items/2 answers Soap",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/2"
          }
        ],
        "bodyContains": "\"name\":\"Soap\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "A SQL parameter is a ? that you fill with a value separately. This keeps the value as data, not code."
      },
      {
        "level": 2,
        "text": "Add the code before the 404 line.\n\nIn server.js:\n```\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match) return send(res, 200, db.prepare(\"SELECT * FROM items WHERE id = ?\").get(Number(match[1])));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Rice\",50],[\"Soap\",25],[\"Egg\",9]]) insert.run(name, price);\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items ORDER BY price\").all());\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match) return send(res, 200, db.prepare(\"SELECT * FROM items WHERE id = ?\").get(Number(match[1])));\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-sql-parameter"
    ],
    "estimatedMinutes": 4,
    "projectId": "sqlite-storage-sari-sari"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: sqlite-storage-sari-sari.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-sqlite-storage-sari-sari-6",
    "index": 66,
    "task": "You change one line in server.js. That line checks if an item exists by ID. If no row matches, it now answers 404. This helps the store app know when a customer asks for something not in stock. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\n  if (match) { const row = db.prepare(\"SELECT * FROM items WHERE id = ?\").get(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "none",
        "label": "GET /items/99 answers 404",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/99"
          }
        ],
        "status": 404
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "If no row matches, send 404. Don't send 200."
      },
      {
        "level": 2,
        "text": "Put the code inside the if (match) block, right after the SELECT line.\n\nIn server.js:\n```\n  if (match) { const row = db.prepare(\"SELECT * FROM items WHERE id = ?\").get(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Rice\",50],[\"Soap\",25],[\"Egg\",9]]) insert.run(name, price);\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items ORDER BY price\").all());\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match) { const row = db.prepare(\"SELECT * FROM items WHERE id = ?\").get(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "sqlite-storage-sari-sari"
  },
  {
    "id": "api-sqlite-storage-sari-sari-7",
    "index": 67,
    "task": "You replace one line with three lines. The first reads the item data from the request. The second inserts it into the database. The third sends back the new row's ID. This lets the store app know what ID to use for the new item. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\n  const data = JSON.parse(await readBody(req));\n  const result = insert.run(data.name, data.price);\n  return send(res, 201, { id: Number(result.lastInsertRowid), ...data });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "insert",
        "label": "POST /items answers 201 with id 4",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "{\"name\":\"Tea\",\"price\":12}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 201,
        "bodyContains": "\"id\":4"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use JSON.parse to read the body. Then insert the data. Then send the ID."
      },
      {
        "level": 2,
        "text": "Put the code inside the createItem function, right after the if (req.method === 'POST') line.\n\nIn server.js:\n```\n  const data = JSON.parse(await readBody(req));\n  const result = insert.run(data.name, data.price);\n  return send(res, 201, { id: Number(result.lastInsertRowid), ...data });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Rice\",50],[\"Soap\",25],[\"Egg\",9]]) insert.run(name, price);\nasync function createItem(req, res) {\n  const data = JSON.parse(await readBody(req));\n  const result = insert.run(data.name, data.price);\n  return send(res, 201, { id: Number(result.lastInsertRowid), ...data });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items ORDER BY price\").all());\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match) { const row = db.prepare(\"SELECT * FROM items WHERE id = ?\").get(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-last-insert-id"
    ],
    "estimatedMinutes": 4,
    "projectId": "sqlite-storage-sari-sari"
  },
  {
    "id": "api-sqlite-storage-sari-sari-8",
    "index": 68,
    "task": "You add one route after the count route. This route answers with the total price of all items. This helps the store owner know how much money is in stock. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\n  if (req.url === \"/items/total\") return send(res, 200, db.prepare(\"SELECT SUM(price) AS total FROM items\").get());\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "sum",
        "label": "GET /items/total answers the sum of prices",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/total"
          }
        ],
        "bodyContains": "{\"total\":84}"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use SUM to add up the prices. Send the result as JSON."
      },
      {
        "level": 2,
        "text": "Put the code after the count route, right before the next route.\n\nIn server.js:\n```\n  if (req.url === \"/items/total\") return send(res, 200, db.prepare(\"SELECT SUM(price) AS total FROM items\").get());\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Rice\",50],[\"Soap\",25],[\"Egg\",9]]) insert.run(name, price);\nasync function createItem(req, res) {\n  const data = JSON.parse(await readBody(req));\n  const result = insert.run(data.name, data.price);\n  return send(res, 201, { id: Number(result.lastInsertRowid), ...data });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items ORDER BY price\").all());\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n  if (req.url === \"/items/total\") return send(res, 200, db.prepare(\"SELECT SUM(price) AS total FROM items\").get());\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match) { const row = db.prepare(\"SELECT * FROM items WHERE id = ?\").get(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-sum"
    ],
    "estimatedMinutes": 3,
    "projectId": "sqlite-storage-sari-sari"
  },
  {
    "id": "api-sqlite-storage-sari-sari-9",
    "index": 69,
    "task": "You add two lines after the total route. The first gets the search text from the URL. The second uses LIKE to find items matching that text. This lets the store app search for items by name. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items/search\") return send(res, 200, db.prepare(\"SELECT * FROM items WHERE name LIKE ?\").all(`%${url.searchParams.get(\"q\") ?? \"\"}%`));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "like",
        "label": "GET /items/search?q=gg finds Egg",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/search?q=gg"
          }
        ],
        "bodyContains": "\"name\":\"Egg\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use URL to get the search parameter. Use LIKE with % to match any text."
      },
      {
        "level": 2,
        "text": "Put the code after the total route, right before the next route.\n\nIn server.js:\n```\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items/search\") return send(res, 200, db.prepare(\"SELECT * FROM items WHERE name LIKE ?\").all(`%${url.searchParams.get(\"q\") ?? \"\"}%`));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Rice\",50],[\"Soap\",25],[\"Egg\",9]]) insert.run(name, price);\nasync function createItem(req, res) {\n  const data = JSON.parse(await readBody(req));\n  const result = insert.run(data.name, data.price);\n  return send(res, 201, { id: Number(result.lastInsertRowid), ...data });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items ORDER BY price\").all());\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n  if (req.url === \"/items/total\") return send(res, 200, db.prepare(\"SELECT SUM(price) AS total FROM items\").get());\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items/search\") return send(res, 200, db.prepare(\"SELECT * FROM items WHERE name LIKE ?\").all(`%${url.searchParams.get(\"q\") ?? \"\"}%`));\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match) { const row = db.prepare(\"SELECT * FROM items WHERE id = ?\").get(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-like-search"
    ],
    "estimatedMinutes": 4,
    "projectId": "sqlite-storage-sari-sari"
  },
  {
    "id": "api-sqlite-storage-sari-sari-10",
    "index": 70,
    "task": "You change three lines. The first opens a database file. The second creates the table if it doesn't exist. The third adds sample rows if the table is empty. This lets the store app keep data even after restarting. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\nconst db = new DatabaseSync(process.env.DB_FILE ?? \":memory:\");\ndb.exec(\"CREATE TABLE IF NOT EXISTS items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nif (db.prepare(\"SELECT COUNT(*) AS count FROM items\").get().count === 0) for (const [name, price] of [[\"Rice\",50],[\"Soap\",25],[\"Egg\",9]]) insert.run(name, price);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "file",
        "label": "With DB_FILE=shop.db the server answers from a file",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "env": {
          "DB_FILE": "shop.db"
        },
        "status": 200,
        "bodyContains": "\"name\":\"Rice\""
      },
      {
        "id": "saved",
        "label": "shop.db exists on disk",
        "kind": "local-file-exists",
        "path": "shop.db"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use process.env.DB_FILE to get the file name. Use CREATE TABLE IF NOT EXISTS to avoid errors."
      },
      {
        "level": 2,
        "text": "Put the code at the top of server.js, right after the const db = line.\n\nIn server.js:\n```\nconst db = new DatabaseSync(process.env.DB_FILE ?? \":memory:\");\ndb.exec(\"CREATE TABLE IF NOT EXISTS items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nif (db.prepare(\"SELECT COUNT(*) AS count FROM items\").get().count === 0) for (const [name, price] of [[\"Rice\",50],[\"Soap\",25],[\"Egg\",9]]) insert.run(name, price);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(process.env.DB_FILE ?? \":memory:\");\ndb.exec(\"CREATE TABLE IF NOT EXISTS items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nif (db.prepare(\"SELECT COUNT(*) AS count FROM items\").get().count === 0) for (const [name, price] of [[\"Rice\",50],[\"Soap\",25],[\"Egg\",9]]) insert.run(name, price);\nasync function createItem(req, res) {\n  const data = JSON.parse(await readBody(req));\n  const result = insert.run(data.name, data.price);\n  return send(res, 201, { id: Number(result.lastInsertRowid), ...data });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items ORDER BY price\").all());\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n  if (req.url === \"/items/total\") return send(res, 200, db.prepare(\"SELECT SUM(price) AS total FROM items\").get());\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items/search\") return send(res, 200, db.prepare(\"SELECT * FROM items WHERE name LIKE ?\").all(`%${url.searchParams.get(\"q\") ?? \"\"}%`));\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match) { const row = db.prepare(\"SELECT * FROM items WHERE id = ?\").get(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-database-file"
    ],
    "estimatedMinutes": 5,
    "projectId": "sqlite-storage-sari-sari"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: sqlite-changes-sari-sari.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-sqlite-changes-sari-sari-1",
    "index": 71,
    "task": "You will delete a row from the store's database. Go to server.js. Replace the line inside deleteItem with three lines. The code below deletes the row, sets the response to 204 No Content, and ends it. This tells the client the row was deleted without sending extra data. Run the checker to test it. The checker will send a DELETE request to /items/1 and confirm it answers 204.\n\nIn server.js:\n```\n  db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  res.statusCode = 204;\n  return res.end();\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Rice\",50],[\"Soap\",25],[\"Egg\",9]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "deleted",
        "label": "DELETE /items/1 answers 204",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "DELETE",
            "path": "/items/1"
          }
        ],
        "status": 204
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Think of 204 as 'gone, no message needed'."
      },
      {
        "level": 2,
        "text": "Put the three lines inside the deleteItem function, right after the db.prepare line.\n\nIn server.js:\n```\n  db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  res.statusCode = 204;\n  return res.end();\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Rice\",50],[\"Soap\",25],[\"Egg\",9]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  res.statusCode = 204;\n  return res.end();\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-delete-row"
    ],
    "estimatedMinutes": 3,
    "projectId": "sqlite-changes-sari-sari"
  },
  {
    "id": "api-sqlite-changes-sari-sari-2",
    "index": 72,
    "task": "You will check if the row was found before deleting it. Go to server.js. Change the DELETE line to store the result. Then add a check after it. If result.changes is 0, it means no row matched, so return 404 with an error message. This stops the server from deleting nothing. Run the checker to test it. The checker will send DELETE to /items/99 and confirm it answers 404.\n\nIn server.js:\n```\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Rice\",50],[\"Soap\",25],[\"Egg\",9]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "none",
        "label": "DELETE /items/99 answers 404",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "DELETE",
            "path": "/items/99"
          }
        ],
        "status": 404
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "If result.changes is 0, nothing was deleted, that's an error."
      },
      {
        "level": 2,
        "text": "Put the check right after the DELETE line, inside deleteItem.\n\nIn server.js:\n```\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Rice\",50],[\"Soap\",25],[\"Egg\",9]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  res.statusCode = 204;\n  return res.end();\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-changes"
    ],
    "estimatedMinutes": 4,
    "projectId": "sqlite-changes-sari-sari"
  },
  {
    "id": "api-sqlite-changes-sari-sari-3",
    "index": 73,
    "task": "You will update a row's price. Go to server.js. Replace the line inside updateItem with three lines. The code below parses the request body, updates the price using UPDATE, and sends back the updated row. This lets the client know the price changed. Run the checker to test it. The checker will send PUT to /items/1 with price 99 and confirm it answers with the new price.\n\nIn server.js:\n```\n  const data = JSON.parse(await readBody(req));\n  db.prepare(\"UPDATE items SET price = ? WHERE id = ?\").run(data.price, id);\n  return send(res, 200, one(id));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Rice\",50],[\"Soap\",25],[\"Egg\",9]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "updated",
        "label": "PUT /items/1 with price 99 answers the new price",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "PUT",
            "path": "/items/1",
            "body": "{\"price\":99}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "bodyContains": "\"price\":99"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "UPDATE changes the price only if the row exists and matches the ID."
      },
      {
        "level": 2,
        "text": "Put the three lines inside the updateItem function, right after the JSON.parse line.\n\nIn server.js:\n```\n  const data = JSON.parse(await readBody(req));\n  db.prepare(\"UPDATE items SET price = ? WHERE id = ?\").run(data.price, id);\n  return send(res, 200, one(id));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Rice\",50],[\"Soap\",25],[\"Egg\",9]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  res.statusCode = 204;\n  return res.end();\n}\nasync function updateItem(req, res, id) {\n  const data = JSON.parse(await readBody(req));\n  db.prepare(\"UPDATE items SET price = ? WHERE id = ?\").run(data.price, id);\n  return send(res, 200, one(id));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-update-row"
    ],
    "estimatedMinutes": 4,
    "projectId": "sqlite-changes-sari-sari"
  },
  {
    "id": "api-sqlite-changes-sari-sari-4",
    "index": 74,
    "task": "You will check if the row was found before updating it. Go to server.js. Change the UPDATE line to store the result. Then add a check after it. If no rows were updated, return 404. This stops the server from updating nothing. Run the checker to test it. The checker will send PUT to /items/99 and confirm it answers 404.\n\nIn server.js:\n```\n  const result = db.prepare(\"UPDATE items SET price = ? WHERE id = ?\").run(data.price, id);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Rice\",50],[\"Soap\",25],[\"Egg\",9]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "none",
        "label": "PUT /items/99 answers 404",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "PUT",
            "path": "/items/99",
            "body": "{\"price\":1}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 404
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "If no rows were updated, it means the ID was wrong or the row doesn't exist."
      },
      {
        "level": 2,
        "text": "Put the check right after the UPDATE line, inside updateItem.\n\nIn server.js:\n```\n  const result = db.prepare(\"UPDATE items SET price = ? WHERE id = ?\").run(data.price, id);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Rice\",50],[\"Soap\",25],[\"Egg\",9]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  res.statusCode = 204;\n  return res.end();\n}\nasync function updateItem(req, res, id) {\n  const data = JSON.parse(await readBody(req));\n  const result = db.prepare(\"UPDATE items SET price = ? WHERE id = ?\").run(data.price, id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  return send(res, 200, one(id));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "sqlite-changes-sari-sari"
  },
  {
    "id": "api-sqlite-changes-sari-sari-5",
    "index": 75,
    "task": "You will check if the price is a whole number 0 or more. Go to server.js. Add one line after the JSON.parse line. The code below checks if data.price is not a whole number or less than 0. If so, return 422 with an error message. This stops the server from accepting bad prices. Run the checker to test it. The checker will send PUT to /items/1 with price -5 and confirm it answers 422.\n\nIn server.js:\n```\n  if (!Number.isInteger(data.price) || data.price < 0) return send(res, 422, { error: \"price must be a whole number 0 or more\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Rice\",50],[\"Soap\",25],[\"Egg\",9]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "bad",
        "label": "PUT /items/1 with price -5 answers 422",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "PUT",
            "path": "/items/1",
            "body": "{\"price\":-5}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 422
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "422 means 'invalid input', the price must be a whole number 0 or more."
      },
      {
        "level": 2,
        "text": "Put the check right after JSON.parse, inside updateItem.\n\nIn server.js:\n```\n  if (!Number.isInteger(data.price) || data.price < 0) return send(res, 422, { error: \"price must be a whole number 0 or more\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Rice\",50],[\"Soap\",25],[\"Egg\",9]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  res.statusCode = 204;\n  return res.end();\n}\nasync function updateItem(req, res, id) {\n  const data = JSON.parse(await readBody(req));\n  if (!Number.isInteger(data.price) || data.price < 0) return send(res, 422, { error: \"price must be a whole number 0 or more\" });\n  const result = db.prepare(\"UPDATE items SET price = ? WHERE id = ?\").run(data.price, id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  return send(res, 200, one(id));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "sqlite-changes-sari-sari"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: sqlite-changes-sari-sari.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-sqlite-changes-sari-sari-6",
    "index": 76,
    "task": "You will change the price check and the UPDATE line. The code below lets PUT change the name, the price, or both. It keeps whatever is not sent. This is important because customers may only want to change one thing, not both. The code below uses COALESCE. COALESCE(a, b) uses a unless it is missing, then falls back to b. It keeps old values when a field is not sent.\n\nIn server.js:\n```\n  if (data.price !== undefined && (!Number.isInteger(data.price) || data.price < 0)) return send(res, 422, { error: \"price must be a whole number 0 or more\" });\n  const result = db.prepare(\"UPDATE items SET name = COALESCE(?, name), price = COALESCE(?, price) WHERE id = ?\").run(data.name ?? null, data.price ?? null, id);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Rice\",50],[\"Soap\",25],[\"Egg\",9]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "name",
        "label": "PUT /items/1 with only a name keeps the price",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "PUT",
            "path": "/items/1",
            "body": "{\"name\":\"Tea\"}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "bodyContains": "\"name\":\"Tea\",\"price\":50"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Think of COALESCE like a safety net: if you don't send a value, it doesn't change the old one."
      },
      {
        "level": 2,
        "text": "Put the code in the PUT /items/1 section, right after the price check.\n\nIn server.js:\n```\n  if (data.price !== undefined && (!Number.isInteger(data.price) || data.price < 0)) return send(res, 422, { error: \"price must be a whole number 0 or more\" });\n  const result = db.prepare(\"UPDATE items SET name = COALESCE(?, name), price = COALESCE(?, price) WHERE id = ?\").run(data.name ?? null, data.price ?? null, id);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Rice\",50],[\"Soap\",25],[\"Egg\",9]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  res.statusCode = 204;\n  return res.end();\n}\nasync function updateItem(req, res, id) {\n  const data = JSON.parse(await readBody(req));\n  if (data.price !== undefined && (!Number.isInteger(data.price) || data.price < 0)) return send(res, 422, { error: \"price must be a whole number 0 or more\" });\n  const result = db.prepare(\"UPDATE items SET name = COALESCE(?, name), price = COALESCE(?, price) WHERE id = ?\").run(data.name ?? null, data.price ?? null, id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  return send(res, 200, one(id));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-coalesce"
    ],
    "estimatedMinutes": 5,
    "projectId": "sqlite-changes-sari-sari"
  },
  {
    "id": "api-sqlite-changes-sari-sari-7",
    "index": 77,
    "task": "You will add one route at the top of the handler. The code below lets you raise every price by 1 inside a transaction. A transaction groups changes so they all happen together or not at all. This is important because if one price fails, no price changes at all. The code below uses BEGIN and COMMIT to start and finish the transaction.\n\nIn server.js:\n```\n  if (req.url === \"/items/raise\" && req.method === \"POST\") { db.exec(\"BEGIN\"); db.prepare(\"UPDATE items SET price = price + 1\").run(); db.exec(\"COMMIT\"); return send(res, 200, db.prepare(\"SELECT * FROM items\").all()); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Rice\",50],[\"Soap\",25],[\"Egg\",9]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "raise",
        "label": "POST /items/raise answers the raised prices",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items/raise",
            "body": "{}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "bodyContains": "\"price\":51"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Think of a transaction like a group of actions that must all succeed or none at all."
      },
      {
        "level": 2,
        "text": "Put the code right after the PUT route, before the DELETE route.\n\nIn server.js:\n```\n  if (req.url === \"/items/raise\" && req.method === \"POST\") { db.exec(\"BEGIN\"); db.prepare(\"UPDATE items SET price = price + 1\").run(); db.exec(\"COMMIT\"); return send(res, 200, db.prepare(\"SELECT * FROM items\").all()); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Rice\",50],[\"Soap\",25],[\"Egg\",9]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  res.statusCode = 204;\n  return res.end();\n}\nasync function updateItem(req, res, id) {\n  const data = JSON.parse(await readBody(req));\n  if (data.price !== undefined && (!Number.isInteger(data.price) || data.price < 0)) return send(res, 422, { error: \"price must be a whole number 0 or more\" });\n  const result = db.prepare(\"UPDATE items SET name = COALESCE(?, name), price = COALESCE(?, price) WHERE id = ?\").run(data.name ?? null, data.price ?? null, id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  return send(res, 200, one(id));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items/raise\" && req.method === \"POST\") { db.exec(\"BEGIN\"); db.prepare(\"UPDATE items SET price = price + 1\").run(); db.exec(\"COMMIT\"); return send(res, 200, db.prepare(\"SELECT * FROM items\").all()); }\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-transaction"
    ],
    "estimatedMinutes": 6,
    "projectId": "sqlite-changes-sari-sari"
  },
  {
    "id": "api-sqlite-changes-sari-sari-8",
    "index": 78,
    "task": "You will add one route after the raise route. The code below lets you count the rows so a delete can be seen. This is important because you need to know how many items are left after deleting. The code below uses COUNT(*) to count all rows.\n\nIn server.js:\n```\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Rice\",50],[\"Soap\",25],[\"Egg\",9]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "count",
        "label": "After DELETE /items/1, GET /items/count answers 2",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "DELETE",
            "path": "/items/1"
          },
          {
            "method": "GET",
            "path": "/items/count"
          }
        ],
        "bodyContains": "{\"count\":2}"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Think of COUNT(*) as a counter that tells you how many rows are left after deleting."
      },
      {
        "level": 2,
        "text": "Put the code after the POST /items/raise route, before the DELETE route.\n\nIn server.js:\n```\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Rice\",50],[\"Soap\",25],[\"Egg\",9]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  res.statusCode = 204;\n  return res.end();\n}\nasync function updateItem(req, res, id) {\n  const data = JSON.parse(await readBody(req));\n  if (data.price !== undefined && (!Number.isInteger(data.price) || data.price < 0)) return send(res, 422, { error: \"price must be a whole number 0 or more\" });\n  const result = db.prepare(\"UPDATE items SET name = COALESCE(?, name), price = COALESCE(?, price) WHERE id = ?\").run(data.name ?? null, data.price ?? null, id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  return send(res, 200, one(id));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items/raise\" && req.method === \"POST\") { db.exec(\"BEGIN\"); db.prepare(\"UPDATE items SET price = price + 1\").run(); db.exec(\"COMMIT\"); return send(res, 200, db.prepare(\"SELECT * FROM items\").all()); }\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "sqlite-changes-sari-sari"
  },
  {
    "id": "api-sqlite-changes-sari-sari-9",
    "index": 79,
    "task": "You will change the GET /items line. The code below sends the number of rows in an X-Total-Count header. This is important because it tells the client how many items there are. The code below uses res.setHeader to add the header.\n\nIn server.js:\n```\n  if (req.url === \"/items\") { const rows = db.prepare(\"SELECT * FROM items\").all(); res.setHeader(\"X-Total-Count\", String(rows.length)); return send(res, 200, rows); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Rice\",50],[\"Soap\",25],[\"Egg\",9]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "header",
        "label": "GET /items sends X-Total-Count: 3",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "header": {
          "name": "x-total-count",
          "value": "3"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Think of X-Total-Count as a label that tells the client how many rows are there."
      },
      {
        "level": 2,
        "text": "Put the code right after the GET /items route, before the send(res, 200, rows); line.\n\nIn server.js:\n```\n  if (req.url === \"/items\") { const rows = db.prepare(\"SELECT * FROM items\").all(); res.setHeader(\"X-Total-Count\", String(rows.length)); return send(res, 200, rows); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Rice\",50],[\"Soap\",25],[\"Egg\",9]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  res.statusCode = 204;\n  return res.end();\n}\nasync function updateItem(req, res, id) {\n  const data = JSON.parse(await readBody(req));\n  if (data.price !== undefined && (!Number.isInteger(data.price) || data.price < 0)) return send(res, 422, { error: \"price must be a whole number 0 or more\" });\n  const result = db.prepare(\"UPDATE items SET name = COALESCE(?, name), price = COALESCE(?, price) WHERE id = ?\").run(data.name ?? null, data.price ?? null, id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  return send(res, 200, one(id));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items/raise\" && req.method === \"POST\") { db.exec(\"BEGIN\"); db.prepare(\"UPDATE items SET price = price + 1\").run(); db.exec(\"COMMIT\"); return send(res, 200, db.prepare(\"SELECT * FROM items\").all()); }\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") { const rows = db.prepare(\"SELECT * FROM items\").all(); res.setHeader(\"X-Total-Count\", String(rows.length)); return send(res, 200, rows); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 5,
    "projectId": "sqlite-changes-sari-sari"
  },
  {
    "id": "api-sqlite-changes-sari-sari-10",
    "index": 80,
    "task": "You will add two lines after the count route. The code below lets you search by exact name with a ? parameter. This is important because typed text can never change the SQL. The code below uses URL to get the search parameter and ? to prevent SQL injection. SQL injection happens when typed text becomes part of the SQL itself. ? parameters prevent it.\n\nIn server.js:\n```\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items/search\") return send(res, 200, db.prepare(\"SELECT * FROM items WHERE name = ?\").all(url.searchParams.get(\"name\") ?? \"\"));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Rice\",50],[\"Soap\",25],[\"Egg\",9]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "found",
        "label": "GET /items/search?name=Soap finds it",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/search?name=Soap"
          }
        ],
        "bodyContains": "\"name\":\"Soap\""
      },
      {
        "id": "safe",
        "label": "A search for ' OR '1'='1 finds nothing",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/search?name=%27%20OR%20%271%27%3D%271"
          }
        ],
        "status": 200,
        "bodyContains": "[]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Think of ? as a safe box that keeps typed text out of the SQL."
      },
      {
        "level": 2,
        "text": "Put the code after the GET /items/count route, before the GET /items route.\n\nIn server.js:\n```\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items/search\") return send(res, 200, db.prepare(\"SELECT * FROM items WHERE name = ?\").all(url.searchParams.get(\"name\") ?? \"\"));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Rice\",50],[\"Soap\",25],[\"Egg\",9]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  res.statusCode = 204;\n  return res.end();\n}\nasync function updateItem(req, res, id) {\n  const data = JSON.parse(await readBody(req));\n  if (data.price !== undefined && (!Number.isInteger(data.price) || data.price < 0)) return send(res, 422, { error: \"price must be a whole number 0 or more\" });\n  const result = db.prepare(\"UPDATE items SET name = COALESCE(?, name), price = COALESCE(?, price) WHERE id = ?\").run(data.name ?? null, data.price ?? null, id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  return send(res, 200, one(id));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items/raise\" && req.method === \"POST\") { db.exec(\"BEGIN\"); db.prepare(\"UPDATE items SET price = price + 1\").run(); db.exec(\"COMMIT\"); return send(res, 200, db.prepare(\"SELECT * FROM items\").all()); }\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items/search\") return send(res, 200, db.prepare(\"SELECT * FROM items WHERE name = ?\").all(url.searchParams.get(\"name\") ?? \"\"));\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") { const rows = db.prepare(\"SELECT * FROM items\").all(); res.setHeader(\"X-Total-Count\", String(rows.length)); return send(res, 200, rows); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-sql-injection"
    ],
    "estimatedMinutes": 7,
    "projectId": "sqlite-changes-sari-sari"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: errors-sari-sari.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-errors-sari-sari-1",
    "index": 81,
    "task": "Add a /boom route at the top of handle. This route will throw an error to test the server's error handling. Change the handle call at the bottom to catch any error and send a 500 response. This keeps the server running even when something breaks. The code below shows what to add. Run the checker to confirm the server answers 500 for /boom and keeps running after an error.\n\nIn server.js:\n```\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  handle(req, res).catch(() => send(res, 500, { error: \"Something went wrong\" }));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res);\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "500",
        "label": "GET /boom answers 500",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/boom"
          }
        ],
        "status": 500,
        "bodyContains": "Something went wrong"
      },
      {
        "id": "alive",
        "label": "The server keeps answering after an error",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/boom"
          },
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "status": 200
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Throw an error in the /boom route to test how the server handles it."
      },
      {
        "level": 2,
        "text": "Add the code at the top of handle and change the bottom catch to send 500.\n\nIn server.js:\n```\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  handle(req, res).catch(() => send(res, 500, { error: \"Something went wrong\" }));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res).catch(() => send(res, 500, { error: \"Something went wrong\" }));\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-server-error"
    ],
    "estimatedMinutes": 3,
    "projectId": "errors-sari-sari"
  },
  {
    "id": "api-errors-sari-sari-2",
    "index": 82,
    "task": "Add a counter variable above handle to track how many errors happen. Change the catch to include this counter in the error answer. This helps you find which error matches a user's report. The code below shows what to add. Run the checker to confirm the first error answers id 1.\n\nIn server.js:\n```\nlet errorCount = 0;\n  handle(req, res).catch(() => send(res, 500, { error: \"Something went wrong\", id: ++errorCount }));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res);\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "id",
        "label": "The first error answers id 1",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/boom"
          }
        ],
        "bodyContains": "\"id\":1"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Track errors with a counter so you can match user reports to server logs."
      },
      {
        "level": 2,
        "text": "Add the counter above handle and update the catch to include it.\n\nIn server.js:\n```\nlet errorCount = 0;\n  handle(req, res).catch(() => send(res, 500, { error: \"Something went wrong\", id: ++errorCount }));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet errorCount = 0;\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res).catch(() => send(res, 500, { error: \"Something went wrong\", id: ++errorCount }));\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-error-id"
    ],
    "estimatedMinutes": 3,
    "projectId": "errors-sari-sari"
  },
  {
    "id": "api-errors-sari-sari-3",
    "index": 83,
    "task": "Add an HttpError class to let code throw errors with their own status codes. Add a /teapot route that throws this error. Change the catch to use the error's status if it exists, or fall back to 500. This lets deep code say which answer to send. The code below shows what to add. Run the checker to confirm /teapot answers 418 and unexpected errors still hide their details.\n\nIn server.js:\n```\nclass HttpError extends Error { constructor(status, message) { super(message); this.status = status; } }\n  if (req.url === \"/teapot\") throw new HttpError(418, \"I am a teapot\");\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", id: ++errorCount }));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res);\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "teapot",
        "label": "GET /teapot answers 418",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/teapot"
          }
        ],
        "status": 418,
        "bodyContains": "I am a teapot"
      },
      {
        "id": "hidden",
        "label": "Unexpected errors still hide their details",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/boom"
          }
        ],
        "status": 500,
        "bodyContains": "Something went wrong"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Create a custom error class to carry its own status code for better control."
      },
      {
        "level": 2,
        "text": "Add the class, route, and updated catch at the top and bottom of handle.\n\nIn server.js:\n```\nclass HttpError extends Error { constructor(status, message) { super(message); this.status = status; } }\n  if (req.url === \"/teapot\") throw new HttpError(418, \"I am a teapot\");\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", id: ++errorCount }));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet errorCount = 0;\nclass HttpError extends Error { constructor(status, message) { super(message); this.status = status; } }\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  if (req.url === \"/teapot\") throw new HttpError(418, \"I am a teapot\");\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", id: ++errorCount }));\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-http-error"
    ],
    "estimatedMinutes": 4,
    "projectId": "errors-sari-sari"
  },
  {
    "id": "api-errors-sari-sari-4",
    "index": 84,
    "task": "Add a readJson helper function to read and parse the request body. Use it in createItem to check if the body is valid JSON. If not, throw an HttpError with status 400. This tells users their JSON is broken. The code below shows what to add. Run the checker to confirm POST /items with broken JSON answers 400.\n\nIn server.js:\n```\nasync function readJson(req) { const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\n  const item = await readJson(req);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res);\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "400",
        "label": "POST /items with broken JSON answers 400",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "not json",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 400
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use a helper to read and check the body, then throw an error if it's not valid JSON."
      },
      {
        "level": 2,
        "text": "Add the helper and use it in createItem before the item assignment.\n\nIn server.js:\n```\nasync function readJson(req) { const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\n  const item = await readJson(req);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet errorCount = 0;\nclass HttpError extends Error { constructor(status, message) { super(message); this.status = status; } }\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function readJson(req) { const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\nasync function createItem(req, res) {\n  const item = await readJson(req);\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  if (req.url === \"/teapot\") throw new HttpError(418, \"I am a teapot\");\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", id: ++errorCount }));\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "errors-sari-sari"
  },
  {
    "id": "api-errors-sari-sari-5",
    "index": 85,
    "task": "Add a route before GET /items to check if the method is not GET or POST. If not, set the Allow header to \"GET, POST\" and throw an HttpError with status 405. This tells users which methods are allowed. The code below shows what to add. Run the checker to confirm DELETE /items answers 405 with Allow: GET, POST.\n\nIn server.js:\n```\n  if (req.url === \"/items\" && ![\"GET\", \"POST\"].includes(req.method)) { res.setHeader(\"Allow\", \"GET, POST\"); throw new HttpError(405, \"Method not allowed\"); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res);\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "405",
        "label": "DELETE /items answers 405 with Allow: GET, POST",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "DELETE",
            "path": "/items"
          }
        ],
        "status": 405,
        "header": {
          "name": "allow",
          "value": "GET, POST"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Check the method before the GET route and set the Allow header if it's not allowed."
      },
      {
        "level": 2,
        "text": "Add the route check right before the GET /items line.\n\nIn server.js:\n```\n  if (req.url === \"/items\" && ![\"GET\", \"POST\"].includes(req.method)) { res.setHeader(\"Allow\", \"GET, POST\"); throw new HttpError(405, \"Method not allowed\"); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet errorCount = 0;\nclass HttpError extends Error { constructor(status, message) { super(message); this.status = status; } }\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function readJson(req) { const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\nasync function createItem(req, res) {\n  const item = await readJson(req);\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  if (req.url === \"/teapot\") throw new HttpError(418, \"I am a teapot\");\n  if (req.url === \"/items\" && ![\"GET\", \"POST\"].includes(req.method)) { res.setHeader(\"Allow\", \"GET, POST\"); throw new HttpError(405, \"Method not allowed\"); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", id: ++errorCount }));\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "errors-sari-sari"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: errors-sari-sari.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-errors-sari-sari-6",
    "index": 86,
    "task": "You will change the readBody helper. This helper reads the body of a request. It stops if the body is too big. You will make it stop at 1,000 characters. This protects the server from huge uploads. The code below does this. Run the checker to test it.\n\nIn server.js:\n```\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) { text += chunk; if (text.length > 1000) throw new HttpError(413, \"Body too large\"); } return text; }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res);\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "413",
        "label": "A 2,000-character body answers 413",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "{\"name\":\"\",\"price\":1}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 413
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Think of a body as a message. If it's too long, the server can't handle it."
      },
      {
        "level": 2,
        "text": "Put the code in the readBody function in server.js.\n\nIn server.js:\n```\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) { text += chunk; if (text.length > 1000) throw new HttpError(413, \"Body too large\"); } return text; }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet errorCount = 0;\nclass HttpError extends Error { constructor(status, message) { super(message); this.status = status; } }\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) { text += chunk; if (text.length > 1000) throw new HttpError(413, \"Body too large\"); } return text; }\nasync function readJson(req) { const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\nasync function createItem(req, res) {\n  const item = await readJson(req);\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  if (req.url === \"/teapot\") throw new HttpError(418, \"I am a teapot\");\n  if (req.url === \"/items\" && ![\"GET\", \"POST\"].includes(req.method)) { res.setHeader(\"Allow\", \"GET, POST\"); throw new HttpError(405, \"Method not allowed\"); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", id: ++errorCount }));\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-payload-limit"
    ],
    "estimatedMinutes": 4,
    "projectId": "errors-sari-sari"
  },
  {
    "id": "api-errors-sari-sari-7",
    "index": 87,
    "task": "You will change the readJson helper. This helper checks if the body is marked as JSON. If not, it throws 415. This tells the client to send JSON. The code below does this. Run the checker to test it.\n\nIn server.js:\n```\nasync function readJson(req) { if (!req.headers[\"content-type\"]?.includes(\"application/json\")) throw new HttpError(415, \"Send JSON\"); const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res);\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "415",
        "label": "POST /items with text/plain answers 415",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "name=Tea",
            "headers": {
              "Content-Type": "text/plain"
            }
          }
        ],
        "status": 415
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The server needs to know the body is JSON before it can read it."
      },
      {
        "level": 2,
        "text": "Put the code in the readJson function in server.js.\n\nIn server.js:\n```\nasync function readJson(req) { if (!req.headers[\"content-type\"]?.includes(\"application/json\")) throw new HttpError(415, \"Send JSON\"); const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet errorCount = 0;\nclass HttpError extends Error { constructor(status, message) { super(message); this.status = status; } }\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) { text += chunk; if (text.length > 1000) throw new HttpError(413, \"Body too large\"); } return text; }\nasync function readJson(req) { if (!req.headers[\"content-type\"]?.includes(\"application/json\")) throw new HttpError(415, \"Send JSON\"); const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\nasync function createItem(req, res) {\n  const item = await readJson(req);\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  if (req.url === \"/teapot\") throw new HttpError(418, \"I am a teapot\");\n  if (req.url === \"/items\" && ![\"GET\", \"POST\"].includes(req.method)) { res.setHeader(\"Allow\", \"GET, POST\"); throw new HttpError(405, \"Method not allowed\"); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", id: ++errorCount }));\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "errors-sari-sari"
  },
  {
    "id": "api-errors-sari-sari-8",
    "index": 88,
    "task": "You will add one line in createItem. This line checks if an item with that name already exists. If it does, it throws 409. This stops duplicates. The code below does this. Run the checker to test it.\n\nIn server.js:\n```\n  if (items.some((existing) => existing.name === item.name)) throw new HttpError(409, \"Name already exists\");\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res);\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "409",
        "label": "POST /items with Rice again answers 409",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "{\"name\":\"Rice\",\"price\":1}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 409
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Check the list of items before adding a new one. If it's there, don't add it."
      },
      {
        "level": 2,
        "text": "Put the code right after you read the item in createItem.\n\nIn server.js:\n```\n  if (items.some((existing) => existing.name === item.name)) throw new HttpError(409, \"Name already exists\");\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet errorCount = 0;\nclass HttpError extends Error { constructor(status, message) { super(message); this.status = status; } }\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) { text += chunk; if (text.length > 1000) throw new HttpError(413, \"Body too large\"); } return text; }\nasync function readJson(req) { if (!req.headers[\"content-type\"]?.includes(\"application/json\")) throw new HttpError(415, \"Send JSON\"); const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\nasync function createItem(req, res) {\n  const item = await readJson(req);\n  if (items.some((existing) => existing.name === item.name)) throw new HttpError(409, \"Name already exists\");\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  if (req.url === \"/teapot\") throw new HttpError(418, \"I am a teapot\");\n  if (req.url === \"/items\" && ![\"GET\", \"POST\"].includes(req.method)) { res.setHeader(\"Allow\", \"GET, POST\"); throw new HttpError(405, \"Method not allowed\"); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", id: ++errorCount }));\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-conflict"
    ],
    "estimatedMinutes": 3,
    "projectId": "errors-sari-sari"
  },
  {
    "id": "api-errors-sari-sari-9",
    "index": 89,
    "task": "You will change the catch at the bottom. This part handles errors. You will make it send the status code inside the error body. This helps the client know what went wrong. The code below does this. Run the checker to test it.\n\nIn server.js:\n```\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", status: error.status ?? 500, id: ++errorCount }));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res);\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "status",
        "label": "The /teapot error body includes status 418",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/teapot"
          }
        ],
        "bodyContains": "\"status\":418"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The error body should include the status code so the client knows what to do next."
      },
      {
        "level": 2,
        "text": "Put the code in the catch block at the bottom of server.js.\n\nIn server.js:\n```\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", status: error.status ?? 500, id: ++errorCount }));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet errorCount = 0;\nclass HttpError extends Error { constructor(status, message) { super(message); this.status = status; } }\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) { text += chunk; if (text.length > 1000) throw new HttpError(413, \"Body too large\"); } return text; }\nasync function readJson(req) { if (!req.headers[\"content-type\"]?.includes(\"application/json\")) throw new HttpError(415, \"Send JSON\"); const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\nasync function createItem(req, res) {\n  const item = await readJson(req);\n  if (items.some((existing) => existing.name === item.name)) throw new HttpError(409, \"Name already exists\");\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  if (req.url === \"/teapot\") throw new HttpError(418, \"I am a teapot\");\n  if (req.url === \"/items\" && ![\"GET\", \"POST\"].includes(req.method)) { res.setHeader(\"Allow\", \"GET, POST\"); throw new HttpError(405, \"Method not allowed\"); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", status: error.status ?? 500, id: ++errorCount }));\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "errors-sari-sari"
  },
  {
    "id": "api-errors-sari-sari-10",
    "index": 90,
    "task": "You will replace the last line of handle. This line throws a 404 error. It will include the method and path. This tells the client the route doesn't exist. The code below does this. Run the checker to test it.\n\nIn server.js:\n```\n  throw new HttpError(404, `No route for ${req.method} ${req.url}`);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res);\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "route",
        "label": "GET /nope answers No route for GET /nope",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/nope"
          }
        ],
        "status": 404,
        "bodyContains": "No route for GET /nope"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The error message should say what method and path were used. This helps the client fix the request."
      },
      {
        "level": 2,
        "text": "Put the code in the handle function, replacing the last line.\n\nIn server.js:\n```\n  throw new HttpError(404, `No route for ${req.method} ${req.url}`);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Rice\", price: 50 }, { id: 2, name: \"Soap\", price: 25 }, { id: 3, name: \"Egg\", price: 9 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet errorCount = 0;\nclass HttpError extends Error { constructor(status, message) { super(message); this.status = status; } }\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) { text += chunk; if (text.length > 1000) throw new HttpError(413, \"Body too large\"); } return text; }\nasync function readJson(req) { if (!req.headers[\"content-type\"]?.includes(\"application/json\")) throw new HttpError(415, \"Send JSON\"); const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\nasync function createItem(req, res) {\n  const item = await readJson(req);\n  if (items.some((existing) => existing.name === item.name)) throw new HttpError(409, \"Name already exists\");\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  if (req.url === \"/teapot\") throw new HttpError(418, \"I am a teapot\");\n  if (req.url === \"/items\" && ![\"GET\", \"POST\"].includes(req.method)) { res.setHeader(\"Allow\", \"GET, POST\"); throw new HttpError(405, \"Method not allowed\"); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  throw new HttpError(404, `No route for ${req.method} ${req.url}`);\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", status: error.status ?? 500, id: ++errorCount }));\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "errors-sari-sari"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: api-design-sari-sari.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-api-design-sari-sari-1",
    "index": 91,
    "task": "Add this route after the url line. It lets the API change later without breaking old clients. The code below does that. Run the checker to test it.\n\nIn server.js:\n```\n  if (url.pathname === \"/v1/items\") return send(res, 200, items);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst names = [\"Rice\",\"Soap\",\"Egg\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "v1",
        "label": "GET /v1/items answers the list",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/v1/items"
          }
        ],
        "status": 200,
        "bodyContains": "\"name\":\"Rice 1\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Put the route after the url line. It checks if the path is /v1/items."
      },
      {
        "level": 2,
        "text": "Add it to server.js where the url line is.\n\nIn server.js:\n```\n  if (url.pathname === \"/v1/items\") return send(res, 200, items);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst names = [\"Rice\",\"Soap\",\"Egg\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/v1/items\") return send(res, 200, items);\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-versioning"
    ],
    "estimatedMinutes": 3,
    "projectId": "api-design-sari-sari"
  },
  {
    "id": "api-api-design-sari-sari-2",
    "index": 92,
    "task": "Add a page line to get the page number. Change the /v1/items route to send five items per page. The code below does that. Run the checker to test it.\n\nIn server.js:\n```\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  if (url.pathname === \"/v1/items\") return send(res, 200, items.slice((page - 1) * 5, page * 5));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst names = [\"Rice\",\"Soap\",\"Egg\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "page2",
        "label": "GET /v1/items?page=2 answers items 6 to 10",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/v1/items?page=2"
          }
        ],
        "bodyContains": "[{\"id\":6,\"name\":\"Egg 6\",\"price\":30},{\"id\":7,\"name\":\"Rice 7\",\"price\":35},{\"id\":8,\"name\":\"Soap 8\",\"price\":40},{\"id\":9,\"name\":\"Egg 9\",\"price\":45},{\"id\":10,\"name\":\"Rice 10\",\"price\":50}]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use url.searchParams to get the page number. Set it to 1 if not given."
      },
      {
        "level": 2,
        "text": "Add this to server.js after the url line.\n\nIn server.js:\n```\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  if (url.pathname === \"/v1/items\") return send(res, 200, items.slice((page - 1) * 5, page * 5));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst names = [\"Rice\",\"Soap\",\"Egg\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  if (url.pathname === \"/v1/items\") return send(res, 200, items.slice((page - 1) * 5, page * 5));\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-pagination"
    ],
    "estimatedMinutes": 4,
    "projectId": "api-design-sari-sari"
  },
  {
    "id": "api-api-design-sari-sari-3",
    "index": 93,
    "task": "Add a perPage line to get the number of items per page. Change the route to use it. The code below does that. Run the checker to test it.\n\nIn server.js:\n```\n  const perPage = Number(url.searchParams.get(\"perPage\") ?? 5);\n  if (url.pathname === \"/v1/items\") return send(res, 200, items.slice((page - 1) * perPage, page * perPage));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst names = [\"Rice\",\"Soap\",\"Egg\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "per",
        "label": "GET /v1/items?perPage=3 answers three items",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/v1/items?perPage=3"
          }
        ],
        "bodyContains": "[{\"id\":1,\"name\":\"Rice 1\",\"price\":5},{\"id\":2,\"name\":\"Soap 2\",\"price\":10},{\"id\":3,\"name\":\"Egg 3\",\"price\":15}]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use url.searchParams to get perPage. Set it to 5 if not given."
      },
      {
        "level": 2,
        "text": "Add this to server.js after the page line.\n\nIn server.js:\n```\n  const perPage = Number(url.searchParams.get(\"perPage\") ?? 5);\n  if (url.pathname === \"/v1/items\") return send(res, 200, items.slice((page - 1) * perPage, page * perPage));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst names = [\"Rice\",\"Soap\",\"Egg\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  const perPage = Number(url.searchParams.get(\"perPage\") ?? 5);\n  if (url.pathname === \"/v1/items\") return send(res, 200, items.slice((page - 1) * perPage, page * perPage));\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "api-design-sari-sari"
  },
  {
    "id": "api-api-design-sari-sari-4",
    "index": 94,
    "task": "Add a data line to store the sliced items. Change the route to send an object with data, page, and perPage. The code below does that. Run the checker to test it.\n\nIn server.js:\n```\n  const data = items.slice((page - 1) * perPage, page * perPage);\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst names = [\"Rice\",\"Soap\",\"Egg\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "envelope",
        "label": "The answer says page 2 and perPage 5",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/v1/items?page=2"
          }
        ],
        "bodyContains": "\"page\":2,\"perPage\":5"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Store the sliced items in a data variable. Wrap it in an object with page and perPage."
      },
      {
        "level": 2,
        "text": "Add this to server.js after the perPage line.\n\nIn server.js:\n```\n  const data = items.slice((page - 1) * perPage, page * perPage);\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst names = [\"Rice\",\"Soap\",\"Egg\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  const perPage = Number(url.searchParams.get(\"perPage\") ?? 5);\n  const data = items.slice((page - 1) * perPage, page * perPage);\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage });\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-envelope"
    ],
    "estimatedMinutes": 4,
    "projectId": "api-design-sari-sari"
  },
  {
    "id": "api-api-design-sari-sari-5",
    "index": 95,
    "task": "Change the /v1/items route to add total: items.length. The code below does that. Run the checker to test it.\n\nIn server.js:\n```\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst names = [\"Rice\",\"Soap\",\"Egg\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "total",
        "label": "The answer says total 12",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/v1/items"
          }
        ],
        "bodyContains": "\"total\":12"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Add total: items.length to the object. It shows how many items there are."
      },
      {
        "level": 2,
        "text": "Change the route in server.js to include total.\n\nIn server.js:\n```\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst names = [\"Rice\",\"Soap\",\"Egg\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  const perPage = Number(url.searchParams.get(\"perPage\") ?? 5);\n  const data = items.slice((page - 1) * perPage, page * perPage);\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length });\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "api-design-sari-sari"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: api-design-sari-sari.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-api-design-sari-sari-6",
    "index": 96,
    "task": "Add a next line to the answer. This line tells the client where to go for the next page. If there is no next page, the value is null. The code below shows how to make this work. Run the checker to confirm the next link works on page 1 and that the last page has no next link.\n\nIn server.js:\n```\n  const next = page * perPage < items.length ? `/v1/items?page=${page + 1}&perPage=${perPage}` : null;\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length, next });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst names = [\"Rice\",\"Soap\",\"Egg\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "next",
        "label": "Page 1 links to page 2",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/v1/items"
          }
        ],
        "bodyContains": "\"next\":\"/v1/items?page=2&perPage=5\""
      },
      {
        "id": "last",
        "label": "The last page has no next link",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/v1/items?page=3"
          }
        ],
        "bodyContains": "\"next\":null"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The next link should be the URL for the next page, or null if there is no next page."
      },
      {
        "level": 2,
        "text": "Add the code after the line that already has the data and page info.\n\nIn server.js:\n```\n  const next = page * perPage < items.length ? `/v1/items?page=${page + 1}&perPage=${perPage}` : null;\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length, next });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst names = [\"Rice\",\"Soap\",\"Egg\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  const perPage = Number(url.searchParams.get(\"perPage\") ?? 5);\n  const data = items.slice((page - 1) * perPage, page * perPage);\n  const next = page * perPage < items.length ? `/v1/items?page=${page + 1}&perPage=${perPage}` : null;\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length, next });\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-next-link"
    ],
    "estimatedMinutes": 4,
    "projectId": "api-design-sari-sari"
  },
  {
    "id": "api-api-design-sari-sari-7",
    "index": 97,
    "task": "Add a previous line to the answer. This line tells the client where to go for the previous page. If there is no previous page, the value is null. The code below shows how to make this work. Run the checker to confirm the previous link works on page 2.\n\nIn server.js:\n```\n  const previous = page > 1 ? `/v1/items?page=${page - 1}&perPage=${perPage}` : null;\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length, next, previous });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst names = [\"Rice\",\"Soap\",\"Egg\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "previous",
        "label": "Page 2 links back to page 1",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/v1/items?page=2"
          }
        ],
        "bodyContains": "\"previous\":\"/v1/items?page=1&perPage=5\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The previous link should be the URL for the previous page, or null if there is no previous page."
      },
      {
        "level": 2,
        "text": "Add the code after the line that already has the next and page info.\n\nIn server.js:\n```\n  const previous = page > 1 ? `/v1/items?page=${page - 1}&perPage=${perPage}` : null;\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length, next, previous });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst names = [\"Rice\",\"Soap\",\"Egg\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  const perPage = Number(url.searchParams.get(\"perPage\") ?? 5);\n  const data = items.slice((page - 1) * perPage, page * perPage);\n  const next = page * perPage < items.length ? `/v1/items?page=${page + 1}&perPage=${perPage}` : null;\n  const previous = page > 1 ? `/v1/items?page=${page - 1}&perPage=${perPage}` : null;\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length, next, previous });\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "api-design-sari-sari"
  },
  {
    "id": "api-api-design-sari-sari-8",
    "index": 98,
    "task": "Add one line after the page line. This line checks if the page number is not a whole number or less than 1. If so, send a 400 error with a message. The code below shows how to make this work. Run the checker to confirm that asking for page 0 returns a 400 error.\n\nIn server.js:\n```\n  if (!Number.isInteger(page) || page < 1) return send(res, 400, { error: \"page must be a whole number 1 or more\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst names = [\"Rice\",\"Soap\",\"Egg\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "bad",
        "label": "GET /v1/items?page=0 answers 400",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/v1/items?page=0"
          }
        ],
        "status": 400
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "If the page number is not a whole number or less than 1, send a 400 error with a message."
      },
      {
        "level": 2,
        "text": "Add the code after the line that checks if the page is a whole number.\n\nIn server.js:\n```\n  if (!Number.isInteger(page) || page < 1) return send(res, 400, { error: \"page must be a whole number 1 or more\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst names = [\"Rice\",\"Soap\",\"Egg\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  if (!Number.isInteger(page) || page < 1) return send(res, 400, { error: \"page must be a whole number 1 or more\" });\n  const perPage = Number(url.searchParams.get(\"perPage\") ?? 5);\n  const data = items.slice((page - 1) * perPage, page * perPage);\n  const next = page * perPage < items.length ? `/v1/items?page=${page + 1}&perPage=${perPage}` : null;\n  const previous = page > 1 ? `/v1/items?page=${page - 1}&perPage=${perPage}` : null;\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length, next, previous });\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "api-design-sari-sari"
  },
  {
    "id": "api-api-design-sari-sari-9",
    "index": 99,
    "task": "Change the perPage line to limit the number of items per page to 10. This is called a limit cap. The code below shows how to make this work. Run the checker to confirm that asking for 100 items per page returns a limit cap of 10.\n\nIn server.js:\n```\n  const perPage = Math.min(Number(url.searchParams.get(\"perPage\") ?? 5), 10);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst names = [\"Rice\",\"Soap\",\"Egg\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "cap",
        "label": "GET /v1/items?perPage=100 answers perPage 10",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/v1/items?perPage=100"
          }
        ],
        "bodyContains": "\"perPage\":10,\"total\":12"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Set the perPage value to the smaller of what the client asks or 10."
      },
      {
        "level": 2,
        "text": "Replace the current perPage line with the code below.\n\nIn server.js:\n```\n  const perPage = Math.min(Number(url.searchParams.get(\"perPage\") ?? 5), 10);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst names = [\"Rice\",\"Soap\",\"Egg\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  if (!Number.isInteger(page) || page < 1) return send(res, 400, { error: \"page must be a whole number 1 or more\" });\n  const perPage = Math.min(Number(url.searchParams.get(\"perPage\") ?? 5), 10);\n  const data = items.slice((page - 1) * perPage, page * perPage);\n  const next = page * perPage < items.length ? `/v1/items?page=${page + 1}&perPage=${perPage}` : null;\n  const previous = page > 1 ? `/v1/items?page=${page - 1}&perPage=${perPage}` : null;\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length, next, previous });\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-limit-cap"
    ],
    "estimatedMinutes": 3,
    "projectId": "api-design-sari-sari"
  },
  {
    "id": "api-api-design-sari-sari-10",
    "index": 100,
    "task": "Change the old /items route to mark it as deprecated. Add a header called \"Deprecation\" with the value \"true\". The code below shows how to make this work. Run the checker to confirm that asking for /items sends the Deprecation header.\n\nIn server.js:\n```\n  if (url.pathname === \"/items\") { res.setHeader(\"Deprecation\", \"true\"); return send(res, 200, items); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Sari-Sari Store API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst names = [\"Rice\",\"Soap\",\"Egg\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "deprecated",
        "label": "GET /items sends Deprecation: true",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "header": {
          "name": "deprecation",
          "value": "true"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Add the header \"Deprecation: true\" to the response for the old route."
      },
      {
        "level": 2,
        "text": "Replace the old route code with the code below.\n\nIn server.js:\n```\n  if (url.pathname === \"/items\") { res.setHeader(\"Deprecation\", \"true\"); return send(res, 200, items); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst names = [\"Rice\",\"Soap\",\"Egg\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  if (!Number.isInteger(page) || page < 1) return send(res, 400, { error: \"page must be a whole number 1 or more\" });\n  const perPage = Math.min(Number(url.searchParams.get(\"perPage\") ?? 5), 10);\n  const data = items.slice((page - 1) * perPage, page * perPage);\n  const next = page * perPage < items.length ? `/v1/items?page=${page + 1}&perPage=${perPage}` : null;\n  const previous = page > 1 ? `/v1/items?page=${page - 1}&perPage=${perPage}` : null;\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length, next, previous });\n  if (url.pathname === \"/items\") { res.setHeader(\"Deprecation\", \"true\"); return send(res, 200, items); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "conceptIds": [
      "api-deprecation"
    ],
    "estimatedMinutes": 3,
    "projectId": "api-design-sari-sari"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: first-server-carinderia.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-first-server-carinderia-1",
    "index": 101,
    "task": "You will change server.js to answer every request. The code below says the server replies with 'Carinderia API'. This is the first message your server sends. When you run the server, it will say this for any request. Run the checker to test it.\n\nIn server.js:\n```\n  res.end(\"Carinderia API\");\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "root",
        "label": "GET / answers Carinderia API",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/"
          }
        ],
        "status": 200,
        "bodyContains": "Carinderia API"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The server must reply with the same message for every request."
      },
      {
        "level": 2,
        "text": "Change the res.end line in server.js to say 'Carinderia API'.\n\nIn server.js:\n```\n  res.end(\"Carinderia API\");\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Carinderia API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "first-server-carinderia"
  },
  {
    "id": "api-first-server-carinderia-2",
    "index": 102,
    "task": "You will add a header to tell the browser the answer is plain text. The code below sets the Content-Type header. This helps the browser know how to show the answer. Run the checker to test it.\n\nIn server.js:\n```\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "type",
        "label": "GET / says its body is plain text",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/"
          }
        ],
        "header": {
          "name": "content-type",
          "value": "text/plain"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Add the header before the res.end line so the browser knows the answer format."
      },
      {
        "level": 2,
        "text": "Place the new line above res.end in server.js.\n\nIn server.js:\n```\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.end(\"Carinderia API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "first-server-carinderia"
  },
  {
    "id": "api-first-server-carinderia-3",
    "index": 103,
    "task": "You will add a custom header called X-Place. The code below sets it to 'carinderia'. This helps other systems know where the server is. Run the checker to test it.\n\nIn server.js:\n```\n  res.setHeader(\"X-Place\", \"carinderia\");\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "custom",
        "label": "GET / sends X-Place: carinderia",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/"
          }
        ],
        "header": {
          "name": "x-place",
          "value": "carinderia"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Add the header before the res.end line so it goes with the answer."
      },
      {
        "level": 2,
        "text": "Place the new line above res.end in server.js.\n\nIn server.js:\n```\n  res.setHeader(\"X-Place\", \"carinderia\");\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.setHeader(\"X-Place\", \"carinderia\");\n  res.end(\"Carinderia API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "first-server-carinderia"
  },
  {
    "id": "api-first-server-carinderia-4",
    "index": 104,
    "task": "You will add a new path for /health. The code below checks if the request is for /health. If so, it replies with 'healthy'. This helps a monitor know the server is working. Run the checker to test it.\n\nIn server.js:\n```\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "health",
        "label": "GET /health answers healthy",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/health"
          }
        ],
        "status": 200,
        "bodyContains": "healthy"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Check the URL before sending the answer. If it's /health, reply with 'healthy'."
      },
      {
        "level": 2,
        "text": "Place the new code at the top of the handler in server.js.\n\nIn server.js:\n```\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.setHeader(\"X-Place\", \"carinderia\");\n  res.end(\"Carinderia API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "first-server-carinderia"
  },
  {
    "id": "api-first-server-carinderia-5",
    "index": 105,
    "task": "You will add a rule for any other path. The code below checks if the URL is not /. If so, it sends status 404 and the message 'Not found'. This tells users the path does not exist. Run the checker to test it.\n\nIn server.js:\n```\n  if (req.url !== \"/\") { res.statusCode = 404; res.end(\"Not found\"); return; }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "missing",
        "label": "GET /missing answers 404",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/missing"
          }
        ],
        "status": 404
      },
      {
        "id": "root",
        "label": "GET / still answers 200",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/"
          }
        ],
        "status": 200
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Add the check after the /health code so it runs after checking for /health."
      },
      {
        "level": 2,
        "text": "Place the new code right after the /health line in server.js.\n\nIn server.js:\n```\n  if (req.url !== \"/\") { res.statusCode = 404; res.end(\"Not found\"); return; }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n  if (req.url !== \"/\") { res.statusCode = 404; res.end(\"Not found\"); return; }\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.setHeader(\"X-Place\", \"carinderia\");\n  res.end(\"Carinderia API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "first-server-carinderia"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: first-server-carinderia.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-first-server-carinderia-6",
    "index": 106,
    "task": "You must stop any request that is not GET. Add this line at the very top of the handler. This line stops bad requests and sends a 405 error. The checker will test this.\n\nIn server.js:\n```\n  if (req.method !== \"GET\") { res.statusCode = 405; res.end(\"Method not allowed\"); return; }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "post",
        "label": "POST / answers 405",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/",
            "body": "{}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 405
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Think of 405 as 'not allowed'. You must check the method before doing anything else."
      },
      {
        "level": 2,
        "text": "Add the line at the very top of the handler, right after the imports.\n\nIn server.js:\n```\n  if (req.method !== \"GET\") { res.statusCode = 405; res.end(\"Method not allowed\"); return; }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.method !== \"GET\") { res.statusCode = 405; res.end(\"Method not allowed\"); return; }\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n  if (req.url !== \"/\") { res.statusCode = 404; res.end(\"Not found\"); return; }\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.setHeader(\"X-Place\", \"carinderia\");\n  res.end(\"Carinderia API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "first-server-carinderia"
  },
  {
    "id": "api-first-server-carinderia-7",
    "index": 107,
    "task": "You must answer /info with a JSON object. Add this line right after the /health line. This tells the browser the name of the carinderia. The checker will test this.\n\nIn server.js:\n```\n  if (req.url === \"/info\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify({ name: \"Carinderia\" })); return; }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "info",
        "label": "GET /info answers JSON with the name",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/info"
          }
        ],
        "bodyContains": "\"name\":\"Carinderia\"",
        "header": {
          "name": "content-type",
          "value": "application/json"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "You must send a JSON object with the name. Use the code below to make it."
      },
      {
        "level": 2,
        "text": "Add the line right after the /health line, before the 404 line.\n\nIn server.js:\n```\n  if (req.url === \"/info\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify({ name: \"Carinderia\" })); return; }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.method !== \"GET\") { res.statusCode = 405; res.end(\"Method not allowed\"); return; }\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n  if (req.url === \"/info\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify({ name: \"Carinderia\" })); return; }\n  if (req.url !== \"/\") { res.statusCode = 404; res.end(\"Not found\"); return; }\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.setHeader(\"X-Place\", \"carinderia\");\n  res.end(\"Carinderia API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "first-server-carinderia"
  },
  {
    "id": "api-first-server-carinderia-8",
    "index": 108,
    "task": "You must count how many requests come in. Add a counter above the server. Then, add a /visits route before the 404 line. This route will show the count. The checker will test this.\n\nIn server.js:\n```\nlet visits = 0;\n  visits += 1;\n  if (req.url === \"/visits\") { res.end(String(visits)); return; }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "visits",
        "label": "After two requests, GET /visits answers 3",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/"
          },
          {
            "method": "GET",
            "path": "/"
          },
          {
            "method": "GET",
            "path": "/visits"
          }
        ],
        "bodyContains": "3"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Start with a counter set to zero. Add one to it every time a request comes."
      },
      {
        "level": 2,
        "text": "Add the counter above the server, then add the /visits route before the 404 line.\n\nIn server.js:\n```\nlet visits = 0;\n  visits += 1;\n  if (req.url === \"/visits\") { res.end(String(visits)); return; }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nlet visits = 0;\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.method !== \"GET\") { res.statusCode = 405; res.end(\"Method not allowed\"); return; }\n  visits += 1;\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n  if (req.url === \"/info\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify({ name: \"Carinderia\" })); return; }\n  if (req.url === \"/visits\") { res.end(String(visits)); return; }\n  if (req.url !== \"/\") { res.statusCode = 404; res.end(\"Not found\"); return; }\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.setHeader(\"X-Place\", \"carinderia\");\n  res.end(\"Carinderia API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 5,
    "projectId": "first-server-carinderia"
  },
  {
    "id": "api-first-server-carinderia-9",
    "index": 109,
    "task": "You must tell browsers not to store any answer. Add this line at the very top of the handler. This line stops browsers from caching your answers. The checker will test this.\n\nIn server.js:\n```\n  res.setHeader(\"Cache-Control\", \"no-store\");\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "cache",
        "label": "GET / sends Cache-Control: no-store",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/"
          }
        ],
        "header": {
          "name": "cache-control",
          "value": "no-store"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Cache-Control is a header that tells browsers not to save the answer."
      },
      {
        "level": 2,
        "text": "Add the line at the very top of the handler, right after the imports.\n\nIn server.js:\n```\n  res.setHeader(\"Cache-Control\", \"no-store\");\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nlet visits = 0;\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.setHeader(\"Cache-Control\", \"no-store\");\n  if (req.method !== \"GET\") { res.statusCode = 405; res.end(\"Method not allowed\"); return; }\n  visits += 1;\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n  if (req.url === \"/info\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify({ name: \"Carinderia\" })); return; }\n  if (req.url === \"/visits\") { res.end(String(visits)); return; }\n  if (req.url !== \"/\") { res.statusCode = 404; res.end(\"Not found\"); return; }\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.setHeader(\"X-Place\", \"carinderia\");\n  res.end(\"Carinderia API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "first-server-carinderia"
  },
  {
    "id": "api-first-server-carinderia-10",
    "index": 110,
    "task": "You must read a name from the query string at /hello. Use 'friend' as the default. Add these two lines right before the 404 line. This tells the browser to greet the person. The checker will test this.\n\nIn server.js:\n```\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/hello\") { res.end(`Hello, ${url.searchParams.get(\"name\") ?? \"friend\"}`); return; }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "hello",
        "label": "GET /hello?name=Ana answers Hello, Ana",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/hello?name=Ana"
          }
        ],
        "bodyContains": "Hello, Ana"
      },
      {
        "id": "default",
        "label": "GET /hello answers Hello, friend",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/hello"
          }
        ],
        "bodyContains": "Hello, friend"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use the URL class to get the query string. Then, get the name or use 'friend' if none is given."
      },
      {
        "level": 2,
        "text": "Add the two lines right before the 404 line, after the /visits route.\n\nIn server.js:\n```\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/hello\") { res.end(`Hello, ${url.searchParams.get(\"name\") ?? \"friend\"}`); return; }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nlet visits = 0;\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.setHeader(\"Cache-Control\", \"no-store\");\n  if (req.method !== \"GET\") { res.statusCode = 405; res.end(\"Method not allowed\"); return; }\n  visits += 1;\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n  if (req.url === \"/info\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify({ name: \"Carinderia\" })); return; }\n  if (req.url === \"/visits\") { res.end(String(visits)); return; }\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/hello\") { res.end(`Hello, ${url.searchParams.get(\"name\") ?? \"friend\"}`); return; }\n  if (req.url !== \"/\") { res.statusCode = 404; res.end(\"Not found\"); return; }\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.setHeader(\"X-Place\", \"carinderia\");\n  res.end(\"Carinderia API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 6,
    "projectId": "first-server-carinderia"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: json-routes-carinderia.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-json-routes-carinderia-1",
    "index": 111,
    "task": "You will answer requests for /items. The code below sends the whole list as JSON. Add it at the top of the handler. This makes the server return the menu when someone asks for /items. Run the checker to confirm.\n\nIn server.js:\n```\n  if (req.url === \"/items\") return send(res, 200, items);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "list",
        "label": "GET /items answers the list as JSON",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "bodyContains": "\"name\":\"Adobo\"",
        "header": {
          "name": "content-type",
          "value": "application/json"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The code sends the whole list as JSON. Put it right after the if statement for /items."
      },
      {
        "level": 2,
        "text": "Add it at the top of the handler, right after the first if line.\n\nIn server.js:\n```\n  if (req.url === \"/items\") return send(res, 200, items);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "json-routes-carinderia"
  },
  {
    "id": "api-json-routes-carinderia-2",
    "index": 112,
    "task": "You will answer requests for /items/count. The code below sends how many items there are. Add it after the /items line. This helps the server count the menu items. Run the checker to confirm.\n\nIn server.js:\n```\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "count",
        "label": "GET /items/count answers {\"count\":3}",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/count"
          }
        ],
        "bodyContains": "{\"count\":3}"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The code counts the items and sends that number. Put it after the /items line."
      },
      {
        "level": 2,
        "text": "Add it after the /items line, right before the /items/first line.\n\nIn server.js:\n```\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "json-routes-carinderia"
  },
  {
    "id": "api-json-routes-carinderia-3",
    "index": 113,
    "task": "You will answer requests for /items/first. The code below sends only the first item. Add it after the /items/count line. This lets the server show the first dish. Run the checker to confirm.\n\nIn server.js:\n```\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "first",
        "label": "GET /items/first answers Adobo",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/first"
          }
        ],
        "bodyContains": "{\"id\":1,\"name\":\"Adobo\",\"price\":80}"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The code gets the first item from the list and sends it. Put it after the /items/count line."
      },
      {
        "level": 2,
        "text": "Add it after the /items/count line, right before the /items/names line.\n\nIn server.js:\n```\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "json-routes-carinderia"
  },
  {
    "id": "api-json-routes-carinderia-4",
    "index": 114,
    "task": "You will answer requests for /items/names. The code below sends only the names using map. Add it after the /items/first line. This lets the server show only the names. Run the checker to confirm.\n\nIn server.js:\n```\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "names",
        "label": "GET /items/names answers the three names",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/names"
          }
        ],
        "bodyContains": "[\"Adobo\",\"Pancit\",\"Lumpia\"]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The code uses map to get only the names. Put it after the /items/first line."
      },
      {
        "level": 2,
        "text": "Add it after the /items/first line, right before the /items/total line.\n\nIn server.js:\n```\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "json-routes-carinderia"
  },
  {
    "id": "api-json-routes-carinderia-5",
    "index": 115,
    "task": "You will answer requests for /items/total. The code below sends the sum of all prices using reduce. Add it after the /items/names line. This lets the server show the total price. Run the checker to confirm.\n\nIn server.js:\n```\n  if (req.url === \"/items/total\") return send(res, 200, { total: items.reduce((sum, item) => sum + item.price, 0) });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "total",
        "label": "GET /items/total answers 155",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/total"
          }
        ],
        "bodyContains": "{\"total\":155}"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The code adds all prices using reduce. Put it after the /items/names line."
      },
      {
        "level": 2,
        "text": "Add it after the /items/names line, right before the closing brace of the handler.\n\nIn server.js:\n```\n  if (req.url === \"/items/total\") return send(res, 200, { total: items.reduce((sum, item) => sum + item.price, 0) });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n  if (req.url === \"/items/total\") return send(res, 200, { total: items.reduce((sum, item) => sum + item.price, 0) });\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "json-routes-carinderia"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: json-routes-carinderia.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-json-routes-carinderia-6",
    "index": 116,
    "task": "Your server must say 404 when someone asks for a path that does not exist. Change the last line of the handler to send a JSON error. This helps users know when a request fails. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  send(res, 404, { error: \"Not found\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "missing",
        "label": "GET /nope answers 404 with a JSON error",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/nope"
          }
        ],
        "status": 404,
        "bodyContains": "{\"error\":\"Not found\"}"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "If the path is not known, send a 404 status and a JSON message."
      },
      {
        "level": 2,
        "text": "Put the new line at the end of the handler, right before the 404 line.\n\nIn server.js:\n```\n  send(res, 404, { error: \"Not found\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n  if (req.url === \"/items/total\") return send(res, 200, { total: items.reduce((sum, item) => sum + item.price, 0) });\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "json-routes-carinderia"
  },
  {
    "id": "api-json-routes-carinderia-7",
    "index": 117,
    "task": "When someone asks for the root path, your server must reply with the API's name and its routes. Add a line at the top of the handler to handle this case. This helps users discover what your API offers. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (req.url === \"/\") return send(res, 200, { name: \"Carinderia\", routes: [\"/items\"] });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "root",
        "label": "GET / lists the routes",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/"
          }
        ],
        "status": 200,
        "bodyContains": "\"routes\":[\"/items\"]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Check if the URL is exactly \"/\", then send a JSON object with the name and routes."
      },
      {
        "level": 2,
        "text": "Put this line at the top of the handler, right after the `if (req.method === 'GET')` line.\n\nIn server.js:\n```\n  if (req.url === \"/\") return send(res, 200, { name: \"Carinderia\", routes: [\"/items\"] });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/\") return send(res, 200, { name: \"Carinderia\", routes: [\"/items\"] });\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n  if (req.url === \"/items/total\") return send(res, 200, { total: items.reduce((sum, item) => sum + item.price, 0) });\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "json-routes-carinderia"
  },
  {
    "id": "api-json-routes-carinderia-8",
    "index": 118,
    "task": "When someone asks for /items/cheapest, your server must reply with the item that costs the least. Add a line before the 404 line to handle this. This helps users find the cheapest dish. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (req.url === \"/items/cheapest\") return send(res, 200, [...items].sort((a, b) => a.price - b.price)[0]);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "cheapest",
        "label": "GET /items/cheapest answers Lumpia",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/cheapest"
          }
        ],
        "bodyContains": "\"name\":\"Lumpia\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Sort the items by price and pick the first one. Use the `sort` and `filter` methods."
      },
      {
        "level": 2,
        "text": "Put this line before the 404 line, after the `if (req.url === \"/\")` line.\n\nIn server.js:\n```\n  if (req.url === \"/items/cheapest\") return send(res, 200, [...items].sort((a, b) => a.price - b.price)[0]);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/\") return send(res, 200, { name: \"Carinderia\", routes: [\"/items\"] });\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n  if (req.url === \"/items/total\") return send(res, 200, { total: items.reduce((sum, item) => sum + item.price, 0) });\n  if (req.url === \"/items/cheapest\") return send(res, 200, [...items].sort((a, b) => a.price - b.price)[0]);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 5,
    "projectId": "json-routes-carinderia"
  },
  {
    "id": "api-json-routes-carinderia-9",
    "index": 119,
    "task": "When someone asks for /items/pretty, your server must reply with indented JSON. Add a line before the 404 line to handle this. This helps people read the data more easily. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (req.url === \"/items/pretty\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify(items, null, 2)); return; }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "pretty",
        "label": "GET /items/pretty answers indented JSON",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/pretty"
          }
        ],
        "bodyContains": "[\n  {\n    \"id\": 1"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Set the Content-Type header to application/json, then use JSON.stringify with 2 spaces for indentation."
      },
      {
        "level": 2,
        "text": "Put this line before the 404 line, after the /items/cheapest line.\n\nIn server.js:\n```\n  if (req.url === \"/items/pretty\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify(items, null, 2)); return; }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/\") return send(res, 200, { name: \"Carinderia\", routes: [\"/items\"] });\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n  if (req.url === \"/items/total\") return send(res, 200, { total: items.reduce((sum, item) => sum + item.price, 0) });\n  if (req.url === \"/items/cheapest\") return send(res, 200, [...items].sort((a, b) => a.price - b.price)[0]);\n  if (req.url === \"/items/pretty\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify(items, null, 2)); return; }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 5,
    "projectId": "json-routes-carinderia"
  },
  {
    "id": "api-json-routes-carinderia-10",
    "index": 120,
    "task": "When someone asks for /items/expensive, your server must reply with items priced over 40. Add a line before the 404 line to handle this. This helps users find expensive dishes. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (req.url === \"/items/expensive\") return send(res, 200, items.filter((item) => item.price > 40));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "expensive",
        "label": "GET /items/expensive answers Adobo and Pancit",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/expensive"
          }
        ],
        "bodyContains": "\"name\":\"Adobo\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use the `filter` method to keep only items with price greater than 40."
      },
      {
        "level": 2,
        "text": "Put this line before the 404 line, after the /items/pretty line.\n\nIn server.js:\n```\n  if (req.url === \"/items/expensive\") return send(res, 200, items.filter((item) => item.price > 40));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/\") return send(res, 200, { name: \"Carinderia\", routes: [\"/items\"] });\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n  if (req.url === \"/items/total\") return send(res, 200, { total: items.reduce((sum, item) => sum + item.price, 0) });\n  if (req.url === \"/items/cheapest\") return send(res, 200, [...items].sort((a, b) => a.price - b.price)[0]);\n  if (req.url === \"/items/pretty\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify(items, null, 2)); return; }\n  if (req.url === \"/items/expensive\") return send(res, 200, items.filter((item) => item.price > 40));\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 5,
    "projectId": "json-routes-carinderia"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: request-bodies-carinderia.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-request-bodies-carinderia-1",
    "index": 121,
    "task": "You will read what a client sends. The code below does that. Add it above the server. Then add a route for /echo. This lets you test reading the body. Run the checker to see if it works.\n\nIn server.js:\n```\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "echo",
        "label": "POST /echo with hello answers it back",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/echo",
            "body": "hello",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "bodyContains": "\"received\":\"hello\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The code reads chunks of data one by one until it's all done."
      },
      {
        "level": 2,
        "text": "Put the code above the server and the /echo route at the top of the handler.\n\nIn server.js:\n```\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "request-bodies-carinderia"
  },
  {
    "id": "api-request-bodies-carinderia-2",
    "index": 122,
    "task": "You will turn text into an object. Add one line after the /echo route. This line uses JSON.parse to turn text into an object. Run the checker to see if it works.\n\nIn server.js:\n```\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "json",
        "label": "POST /echo-json answers the object it received",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/echo-json",
            "body": "{\"name\":\"Ana\"}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "bodyContains": "\"name\":\"Ana\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "JSON.parse turns a string into an object. You need to wait for the body to finish before parsing."
      },
      {
        "level": 2,
        "text": "Add the line after the /echo route. The page will add the code after your hint.\n\nIn server.js:\n```\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "request-bodies-carinderia"
  },
  {
    "id": "api-request-bodies-carinderia-3",
    "index": 123,
    "task": "You will answer POST /items with status 201 and the new item. Replace the line inside createItem with two lines. The first line parses the body. The second sends the item. Run the checker to see if it works.\n\nIn server.js:\n```\n  const data = JSON.parse(await readBody(req));\n  return send(res, 201, data);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "created",
        "label": "POST /items answers 201 with the item",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "{\"name\":\"Tea\",\"price\":12}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 201,
        "bodyContains": "\"name\":\"Tea\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "You need to parse the body first, then send the item with status 201."
      },
      {
        "level": 2,
        "text": "Replace the line inside createItem. The page will add the code after your hint.\n\nIn server.js:\n```\n  const data = JSON.parse(await readBody(req));\n  return send(res, 201, data);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const data = JSON.parse(await readBody(req));\n  return send(res, 201, data);\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "request-bodies-carinderia"
  },
  {
    "id": "api-request-bodies-carinderia-4",
    "index": 124,
    "task": "You will store the new item so it shows up in the list. Add one line inside createItem before the return. This line adds the item to the list. Run the checker to see if it works.\n\nIn server.js:\n```\n  items.push(data);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "stored",
        "label": "After POST /items, GET /items includes Tea",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "{\"name\":\"Tea\",\"price\":12}",
            "headers": {
              "Content-Type": "application/json"
            }
          },
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "bodyContains": "\"name\":\"Tea\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Add the item to the list before you send it back. This way, it will appear in the list."
      },
      {
        "level": 2,
        "text": "Add the line inside createItem. The page will add the code after your hint.\n\nIn server.js:\n```\n  items.push(data);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const data = JSON.parse(await readBody(req));\n  items.push(data);\n  return send(res, 201, data);\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "request-bodies-carinderia"
  },
  {
    "id": "api-request-bodies-carinderia-5",
    "index": 125,
    "task": "You will give each new item an id one higher than the last. Change createItem so each new item gets the next id. Add the id to the item, then push it. Run the checker to see if it works.\n\nIn server.js:\n```\n  const item = { id: items.length + 1, ...data };\n  items.push(item);\n  return send(res, 201, item);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "id",
        "label": "The new item answers with id 4",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "{\"name\":\"Tea\",\"price\":12}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "bodyContains": "\"id\":4"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The last item's id is the length of the list. Add one to get the next id."
      },
      {
        "level": 2,
        "text": "Change createItem. The page will add the code after your hint.\n\nIn server.js:\n```\n  const item = { id: items.length + 1, ...data };\n  items.push(item);\n  return send(res, 201, item);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const data = JSON.parse(await readBody(req));\n  const item = { id: items.length + 1, ...data };\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 5,
    "projectId": "request-bodies-carinderia"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: request-bodies-carinderia.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-request-bodies-carinderia-6",
    "index": 126,
    "task": "If the body is not JSON, the server crashes. You fix this by catching the error. This stops the server from breaking. The code below does that. Run the checker to test it.\n\nIn server.js:\n```\n  let data;\n  try { data = JSON.parse(await readBody(req)); } catch { return send(res, 400, { error: \"Body must be JSON\" }); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "bad",
        "label": "POST /items with broken JSON answers 400",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "not json",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 400,
        "bodyContains": "Body must be JSON"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "When JSON.parse fails, you send a 400 error instead of crashing."
      },
      {
        "level": 2,
        "text": "Put the code in server.js, right after the try block.\n\nIn server.js:\n```\n  let data;\n  try { data = JSON.parse(await readBody(req)); } catch { return send(res, 400, { error: \"Body must be JSON\" }); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  let data;\n  try { data = JSON.parse(await readBody(req)); } catch { return send(res, 400, { error: \"Body must be JSON\" }); }\n  const item = { id: items.length + 1, ...data };\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "request-bodies-carinderia"
  },
  {
    "id": "api-request-bodies-carinderia-7",
    "index": 127,
    "task": "If the name is missing or empty, you send a 422 error. This tells the user what's wrong. The code below does that. Run the checker to test it.\n\nIn server.js:\n```\n  if (typeof data.name !== \"string\" || !data.name.trim()) return send(res, 422, { error: \"name is required\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "noname",
        "label": "POST /items without a name answers 422",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "{\"price\":5}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 422,
        "bodyContains": "name is required"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Check if the name is a string and not empty after trimming."
      },
      {
        "level": 2,
        "text": "Put the code in server.js, right after the try block.\n\nIn server.js:\n```\n  if (typeof data.name !== \"string\" || !data.name.trim()) return send(res, 422, { error: \"name is required\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  let data;\n  try { data = JSON.parse(await readBody(req)); } catch { return send(res, 400, { error: \"Body must be JSON\" }); }\n  if (typeof data.name !== \"string\" || !data.name.trim()) return send(res, 422, { error: \"name is required\" });\n  const item = { id: items.length + 1, ...data };\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "request-bodies-carinderia"
  },
  {
    "id": "api-request-bodies-carinderia-8",
    "index": 128,
    "task": "If the price is not a number or less than zero, you send a 422 error. This stops bad prices from being saved. The code below does that. Run the checker to test it.\n\nIn server.js:\n```\n  if (!Number.isFinite(data.price) || data.price < 0) return send(res, 422, { error: \"price must be a number 0 or more\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "badprice",
        "label": "POST /items with price five answers 422",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "{\"name\":\"Tea\",\"price\":\"five\"}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 422,
        "bodyContains": "price must be a number"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Check if the price is a finite number and not negative."
      },
      {
        "level": 2,
        "text": "Put the code in server.js, right after the name check.\n\nIn server.js:\n```\n  if (!Number.isFinite(data.price) || data.price < 0) return send(res, 422, { error: \"price must be a number 0 or more\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  let data;\n  try { data = JSON.parse(await readBody(req)); } catch { return send(res, 400, { error: \"Body must be JSON\" }); }\n  if (typeof data.name !== \"string\" || !data.name.trim()) return send(res, 422, { error: \"name is required\" });\n  if (!Number.isFinite(data.price) || data.price < 0) return send(res, 422, { error: \"price must be a number 0 or more\" });\n  const item = { id: items.length + 1, ...data };\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "request-bodies-carinderia"
  },
  {
    "id": "api-request-bodies-carinderia-9",
    "index": 129,
    "task": "You only keep the name and price fields. You trim spaces from the name. This keeps the data clean. The code below does that. Run the checker to test it.\n\nIn server.js:\n```\n  const item = { id: items.length + 1, name: data.name.trim(), price: data.price };\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "trim",
        "label": "The new item is stored with a trimmed name and no extra fields",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "{\"name\":\"  Tea  \",\"price\":12,\"secret\":\"x\"}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "bodyContains": "{\"id\":4,\"name\":\"Tea\",\"price\":12}"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Only include name and price in the item object. Trim the name."
      },
      {
        "level": 2,
        "text": "Put the code in server.js, right after the data check.\n\nIn server.js:\n```\n  const item = { id: items.length + 1, name: data.name.trim(), price: data.price };\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  let data;\n  try { data = JSON.parse(await readBody(req)); } catch { return send(res, 400, { error: \"Body must be JSON\" }); }\n  if (typeof data.name !== \"string\" || !data.name.trim()) return send(res, 422, { error: \"name is required\" });\n  if (!Number.isFinite(data.price) || data.price < 0) return send(res, 422, { error: \"price must be a number 0 or more\" });\n  const item = { id: items.length + 1, name: data.name.trim(), price: data.price };\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "request-bodies-carinderia"
  },
  {
    "id": "api-request-bodies-carinderia-10",
    "index": 130,
    "task": "You add a Location header to tell where the new item lives. This helps clients find it. The code below does that. Run the checker to test it.\n\nIn server.js:\n```\n  res.setHeader(\"Location\", `/items/${item.id}`);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "location",
        "label": "POST /items sends Location: /items/4",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "{\"name\":\"Tea\",\"price\":12}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "header": {
          "name": "location",
          "value": "/items/4"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Set the Location header to the new item's URL before returning."
      },
      {
        "level": 2,
        "text": "Put the code in server.js, right before the return in createItem.\n\nIn server.js:\n```\n  res.setHeader(\"Location\", `/items/${item.id}`);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  let data;\n  try { data = JSON.parse(await readBody(req)); } catch { return send(res, 400, { error: \"Body must be JSON\" }); }\n  if (typeof data.name !== \"string\" || !data.name.trim()) return send(res, 422, { error: \"name is required\" });\n  if (!Number.isFinite(data.price) || data.price < 0) return send(res, 422, { error: \"price must be a number 0 or more\" });\n  const item = { id: items.length + 1, name: data.name.trim(), price: data.price };\n  items.push(item);\n  res.setHeader(\"Location\", `/items/${item.id}`);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "request-bodies-carinderia"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: one-item-carinderia.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-one-item-carinderia-1",
    "index": 131,
    "task": "You will read the item ID from the path. The code below finds the ID in /items/2. Add these two lines at the top of the handler. This lets you get one item by its ID.\n\nIn server.js:\n```\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") return send(res, 200, items.find((item) => item.id === Number(match[1])));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "one",
        "label": "GET /items/2 answers Pancit",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/2"
          }
        ],
        "bodyContains": "\"name\":\"Pancit\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Look for the pattern /items/ followed by a number in the URL."
      },
      {
        "level": 2,
        "text": "Add the code at the top of the handler, right after the imports.\n\nIn server.js:\n```\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") return send(res, 200, items.find((item) => item.id === Number(match[1])));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") return send(res, 200, items.find((item) => item.id === Number(match[1])));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "one-item-carinderia"
  },
  {
    "id": "api-one-item-carinderia-2",
    "index": 132,
    "task": "Now, check if the item exists. If not, send a 404 error. Add the found line and change the GET line. This stops the server from sending a wrong item.\n\nIn server.js:\n```\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "none",
        "label": "GET /items/99 answers 404",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/99"
          }
        ],
        "status": 404,
        "bodyContains": "Item not found"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "If the item is not found, send a 404 with an error message."
      },
      {
        "level": 2,
        "text": "Add the found line before the GET line, then update the GET line to check found.\n\nIn server.js:\n```\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "one-item-carinderia"
  },
  {
    "id": "api-one-item-carinderia-3",
    "index": 133,
    "task": "Add code to delete an item. When you delete, send 204 No Content. Add this line after the GET line. This tells the server to remove the item.\n\nIn server.js:\n```\n  if (found && req.method === \"DELETE\") { items.splice(items.indexOf(found), 1); res.statusCode = 204; return res.end(); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "deleted",
        "label": "DELETE /items/1 answers 204",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "DELETE",
            "path": "/items/1"
          }
        ],
        "status": 204
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Delete an item by removing it from the items array using splice."
      },
      {
        "level": 2,
        "text": "Add the code after the GET line, right before the DELETE line.\n\nIn server.js:\n```\n  if (found && req.method === \"DELETE\") { items.splice(items.indexOf(found), 1); res.statusCode = 204; return res.end(); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"DELETE\") { items.splice(items.indexOf(found), 1); res.statusCode = 204; return res.end(); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "one-item-carinderia"
  },
  {
    "id": "api-one-item-carinderia-4",
    "index": 134,
    "task": "Add code to send 404 when deleting a non-existent item. Add this line after the DELETE line. This stops the server from deleting something that doesn't exist.\n\nIn server.js:\n```\n  if (match && req.method === \"DELETE\") return send(res, 404, { error: \"Item not found\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "gone",
        "label": "DELETE /items/99 answers 404 Item not found",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "DELETE",
            "path": "/items/99"
          }
        ],
        "status": 404,
        "bodyContains": "Item not found"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "If the item is not found, send a 404 with an error message."
      },
      {
        "level": 2,
        "text": "Add this line after the DELETE line, right before the PUT line.\n\nIn server.js:\n```\n  if (match && req.method === \"DELETE\") return send(res, 404, { error: \"Item not found\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"DELETE\") { items.splice(items.indexOf(found), 1); res.statusCode = 204; return res.end(); }\n  if (match && req.method === \"DELETE\") return send(res, 404, { error: \"Item not found\" });\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "one-item-carinderia"
  },
  {
    "id": "api-one-item-carinderia-5",
    "index": 135,
    "task": "Add code to update an item with PUT. When you update, send back the new item. Add this line after the DELETE lines. This lets the server change the item's fields.\n\nIn server.js:\n```\n  if (found && req.method === \"PUT\") { Object.assign(found, JSON.parse(await readBody(req))); return send(res, 200, found); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "put",
        "label": "PUT /items/1 with price 99 answers the new price",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "PUT",
            "path": "/items/1",
            "body": "{\"price\":99}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "bodyContains": "\"price\":99"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use Object.assign to replace the item's fields with the new data."
      },
      {
        "level": 2,
        "text": "Add this line after the DELETE lines, right before the end of the handler.\n\nIn server.js:\n```\n  if (found && req.method === \"PUT\") { Object.assign(found, JSON.parse(await readBody(req))); return send(res, 200, found); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"DELETE\") { items.splice(items.indexOf(found), 1); res.statusCode = 204; return res.end(); }\n  if (match && req.method === \"DELETE\") return send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"PUT\") { Object.assign(found, JSON.parse(await readBody(req))); return send(res, 200, found); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "one-item-carinderia"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: one-item-carinderia.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-one-item-carinderia-6",
    "index": 136,
    "task": "The client might send a PUT request with a different id. Keep the original id. Change the PUT line. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\n  if (found && req.method === \"PUT\") { Object.assign(found, JSON.parse(await readBody(req)), { id: found.id }); return send(res, 200, found); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "keep",
        "label": "After PUT with id 50, GET /items/1 still works",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "PUT",
            "path": "/items/1",
            "body": "{\"id\":50,\"price\":1}",
            "headers": {
              "Content-Type": "application/json"
            }
          },
          {
            "method": "GET",
            "path": "/items/1"
          }
        ],
        "status": 200,
        "bodyContains": "\"id\":1"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The client might send a different id. Keep the original id."
      },
      {
        "level": 2,
        "text": "Change the PUT line in server.js.\n\nIn server.js:\n```\n  if (found && req.method === \"PUT\") { Object.assign(found, JSON.parse(await readBody(req)), { id: found.id }); return send(res, 200, found); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"DELETE\") { items.splice(items.indexOf(found), 1); res.statusCode = 204; return res.end(); }\n  if (match && req.method === \"DELETE\") return send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"PUT\") { Object.assign(found, JSON.parse(await readBody(req)), { id: found.id }); return send(res, 200, found); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "one-item-carinderia"
  },
  {
    "id": "api-one-item-carinderia-7",
    "index": 137,
    "task": "If the client sends a PUT body that is not JSON, answer 415. Add one line before the PUT line. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\n  if (found && req.method === \"PUT\" && !req.headers[\"content-type\"]?.includes(\"application/json\")) return send(res, 415, { error: \"Send JSON\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "type",
        "label": "PUT with a text/plain body answers 415",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "PUT",
            "path": "/items/1",
            "body": "price=99",
            "headers": {
              "Content-Type": "text/plain"
            }
          }
        ],
        "status": 415
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "If the body is not JSON, answer 415. Check the content-type header."
      },
      {
        "level": 2,
        "text": "Add one line before the PUT line in server.js.\n\nIn server.js:\n```\n  if (found && req.method === \"PUT\" && !req.headers[\"content-type\"]?.includes(\"application/json\")) return send(res, 415, { error: \"Send JSON\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"DELETE\") { items.splice(items.indexOf(found), 1); res.statusCode = 204; return res.end(); }\n  if (match && req.method === \"DELETE\") return send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"PUT\" && !req.headers[\"content-type\"]?.includes(\"application/json\")) return send(res, 415, { error: \"Send JSON\" });\n  if (found && req.method === \"PUT\") { Object.assign(found, JSON.parse(await readBody(req)), { id: found.id }); return send(res, 200, found); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "one-item-carinderia"
  },
  {
    "id": "api-one-item-carinderia-8",
    "index": 138,
    "task": "For HEAD /items, send only the X-Total-Count header. No body. Add one line before the 404 line. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\n  if (req.url === \"/items\" && req.method === \"HEAD\") { res.setHeader(\"X-Total-Count\", String(items.length)); return res.end(); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "head",
        "label": "HEAD /items sends X-Total-Count: 3",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "HEAD",
            "path": "/items"
          }
        ],
        "header": {
          "name": "x-total-count",
          "value": "3"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "HEAD requests need only headers. Send X-Total-Count."
      },
      {
        "level": 2,
        "text": "Add one line before the 404 line in server.js.\n\nIn server.js:\n```\n  if (req.url === \"/items\" && req.method === \"HEAD\") { res.setHeader(\"X-Total-Count\", String(items.length)); return res.end(); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"DELETE\") { items.splice(items.indexOf(found), 1); res.statusCode = 204; return res.end(); }\n  if (match && req.method === \"DELETE\") return send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"PUT\" && !req.headers[\"content-type\"]?.includes(\"application/json\")) return send(res, 415, { error: \"Send JSON\" });\n  if (found && req.method === \"PUT\") { Object.assign(found, JSON.parse(await readBody(req)), { id: found.id }); return send(res, 200, found); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"HEAD\") { res.setHeader(\"X-Total-Count\", String(items.length)); return res.end(); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "one-item-carinderia"
  },
  {
    "id": "api-one-item-carinderia-9",
    "index": 139,
    "task": "For OPTIONS /items, send an Allow header with GET, PUT, DELETE. Add one line after the PUT line. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\n  if (match && req.method === \"OPTIONS\") { res.setHeader(\"Allow\", \"GET, PUT, DELETE\"); res.statusCode = 204; return res.end(); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "options",
        "label": "OPTIONS /items/1 lists GET, PUT, DELETE",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "OPTIONS",
            "path": "/items/1"
          }
        ],
        "header": {
          "name": "allow",
          "value": "GET, PUT, DELETE"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "OPTIONS requests need an Allow header. List the allowed methods."
      },
      {
        "level": 2,
        "text": "Add one line after the PUT line in server.js.\n\nIn server.js:\n```\n  if (match && req.method === \"OPTIONS\") { res.setHeader(\"Allow\", \"GET, PUT, DELETE\"); res.statusCode = 204; return res.end(); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"DELETE\") { items.splice(items.indexOf(found), 1); res.statusCode = 204; return res.end(); }\n  if (match && req.method === \"DELETE\") return send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"PUT\" && !req.headers[\"content-type\"]?.includes(\"application/json\")) return send(res, 415, { error: \"Send JSON\" });\n  if (found && req.method === \"PUT\") { Object.assign(found, JSON.parse(await readBody(req)), { id: found.id }); return send(res, 200, found); }\n  if (match && req.method === \"OPTIONS\") { res.setHeader(\"Allow\", \"GET, PUT, DELETE\"); res.statusCode = 204; return res.end(); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"HEAD\") { res.setHeader(\"X-Total-Count\", String(items.length)); return res.end(); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "one-item-carinderia"
  },
  {
    "id": "api-one-item-carinderia-10",
    "index": 140,
    "task": "For any other method on one item, answer 405. Add one line after the OPTIONS line. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\n  if (match) return send(res, 405, { error: \"Method not allowed\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "post",
        "label": "POST /items/1 answers 405",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items/1",
            "body": "{}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 405
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "If the method is not GET, PUT, or DELETE, answer 405."
      },
      {
        "level": 2,
        "text": "Add one line after the OPTIONS line in server.js.\n\nIn server.js:\n```\n  if (match) return send(res, 405, { error: \"Method not allowed\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"DELETE\") { items.splice(items.indexOf(found), 1); res.statusCode = 204; return res.end(); }\n  if (match && req.method === \"DELETE\") return send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"PUT\" && !req.headers[\"content-type\"]?.includes(\"application/json\")) return send(res, 415, { error: \"Send JSON\" });\n  if (found && req.method === \"PUT\") { Object.assign(found, JSON.parse(await readBody(req)), { id: found.id }); return send(res, 200, found); }\n  if (match && req.method === \"OPTIONS\") { res.setHeader(\"Allow\", \"GET, PUT, DELETE\"); res.statusCode = 204; return res.end(); }\n  if (match) return send(res, 405, { error: \"Method not allowed\" });\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"HEAD\") { res.setHeader(\"X-Total-Count\", String(items.length)); return res.end(); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "one-item-carinderia"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: query-strings-carinderia.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-query-strings-carinderia-1",
    "index": 141,
    "task": "You will add code to filter items by price. This code goes after the url line in server.js. It checks if the user asked for a maximum price using ?max=60. If so, it keeps only the items that cost at most that amount. This helps the carinderia show only affordable dishes. The code below does this. Run the checker to test it.\n\nIn server.js:\n```\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.pathname === \"/items\") return send(res, 200, list);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Pancit\", price: 60 }, { id: 2, name: \"Lumpia\", price: 15 }, { id: 3, name: \"Adobo\", price: 80 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "max",
        "label": "GET /items?max=60 answers only the items up to 60",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items?max=60"
          }
        ],
        "bodyContains": "[{\"id\":1,\"name\":\"Pancit\",\"price\":60},{\"id\":2,\"name\":\"Lumpia\",\"price\":15}]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Think of ?max as a limit on how much you want to spend."
      },
      {
        "level": 2,
        "text": "Add this code after the line that sets 'url'.\n\nIn server.js:\n```\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.pathname === \"/items\") return send(res, 200, list);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Pancit\", price: 60 }, { id: 2, name: \"Lumpia\", price: 15 }, { id: 3, name: \"Adobo\", price: 80 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.pathname === \"/items\") return send(res, 200, list);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "query-strings-carinderia"
  },
  {
    "id": "api-query-strings-carinderia-2",
    "index": 142,
    "task": "Now add code to filter items by minimum price. This code goes after the max code. It checks if the user asked for a minimum price using ?min=60. If so, it keeps only the items that cost at least that amount. This helps the carinderia show only expensive dishes. The code below does this. Run the checker to test it.\n\nIn server.js:\n```\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Pancit\", price: 60 }, { id: 2, name: \"Lumpia\", price: 15 }, { id: 3, name: \"Adobo\", price: 80 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "min",
        "label": "GET /items?min=60 answers only the items from 60",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items?min=60"
          }
        ],
        "bodyContains": "[{\"id\":1,\"name\":\"Pancit\",\"price\":60},{\"id\":3,\"name\":\"Adobo\",\"price\":80}]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Think of ?min as the lowest price you will accept."
      },
      {
        "level": 2,
        "text": "Add this code after the line that filters by max.\n\nIn server.js:\n```\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Pancit\", price: 60 }, { id: 2, name: \"Lumpia\", price: 15 }, { id: 3, name: \"Adobo\", price: 80 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n  if (url.pathname === \"/items\") return send(res, 200, list);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "query-strings-carinderia"
  },
  {
    "id": "api-query-strings-carinderia-3",
    "index": 143,
    "task": "Next, add code to search for items by name. This code goes after the min code. It checks if the user asked for a search using ?q=ump. If so, it keeps only the items whose name includes that word, ignoring upper or lower case. This helps the carinderia find dishes by name. The code below does this. Run the checker to test it.\n\nIn server.js:\n```\n  if (url.searchParams.has(\"q\")) list = list.filter((item) => item.name.toLowerCase().includes(url.searchParams.get(\"q\").toLowerCase()));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Pancit\", price: 60 }, { id: 2, name: \"Lumpia\", price: 15 }, { id: 3, name: \"Adobo\", price: 80 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "q",
        "label": "GET /items?q=ump finds Lumpia",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items?q=ump"
          }
        ],
        "bodyContains": "[{\"id\":2,\"name\":\"Lumpia\",\"price\":15}]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Think of ?q as a word you want to find in the dish names."
      },
      {
        "level": 2,
        "text": "Add this code after the line that filters by min.\n\nIn server.js:\n```\n  if (url.searchParams.has(\"q\")) list = list.filter((item) => item.name.toLowerCase().includes(url.searchParams.get(\"q\").toLowerCase()));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Pancit\", price: 60 }, { id: 2, name: \"Lumpia\", price: 15 }, { id: 3, name: \"Adobo\", price: 80 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n  if (url.searchParams.has(\"q\")) list = list.filter((item) => item.name.toLowerCase().includes(url.searchParams.get(\"q\").toLowerCase()));\n  if (url.pathname === \"/items\") return send(res, 200, list);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "query-strings-carinderia"
  },
  {
    "id": "api-query-strings-carinderia-4",
    "index": 144,
    "task": "Now add code to sort items by price from cheapest to most expensive. This code goes after the q code. It checks if the user asked to sort by price using ?sort=price. If so, it rearranges the list so the cheapest item comes first. This helps the carinderia show dishes in order. The code below does this. Run the checker to test it.\n\nIn server.js:\n```\n  if (url.searchParams.get(\"sort\") === \"price\") list = [...list].sort((a, b) => a.price - b.price);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Pancit\", price: 60 }, { id: 2, name: \"Lumpia\", price: 15 }, { id: 3, name: \"Adobo\", price: 80 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "asc",
        "label": "GET /items?sort=price answers cheapest first",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items?sort=price"
          }
        ],
        "bodyContains": "[{\"id\":2,\"name\":\"Lumpia\",\"price\":15},{\"id\":1,\"name\":\"Pancit\",\"price\":60},{\"id\":3,\"name\":\"Adobo\",\"price\":80}]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Think of ?sort=price as asking for cheapest first."
      },
      {
        "level": 2,
        "text": "Add this code after the line that searches by q.\n\nIn server.js:\n```\n  if (url.searchParams.get(\"sort\") === \"price\") list = [...list].sort((a, b) => a.price - b.price);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Pancit\", price: 60 }, { id: 2, name: \"Lumpia\", price: 15 }, { id: 3, name: \"Adobo\", price: 80 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n  if (url.searchParams.has(\"q\")) list = list.filter((item) => item.name.toLowerCase().includes(url.searchParams.get(\"q\").toLowerCase()));\n  if (url.searchParams.get(\"sort\") === \"price\") list = [...list].sort((a, b) => a.price - b.price);\n  if (url.pathname === \"/items\") return send(res, 200, list);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "query-strings-carinderia"
  },
  {
    "id": "api-query-strings-carinderia-5",
    "index": 145,
    "task": "Finally, add code to sort items by price from most expensive to cheapest. This code goes after the sort code. It checks if the user asked to sort by price using ?sort=-price. If so, it rearranges the list so the most expensive item comes first. This helps the carinderia show dishes in reverse order. The code below does this. Run the checker to test it.\n\nIn server.js:\n```\n  if (url.searchParams.get(\"sort\") === \"-price\") list = [...list].sort((a, b) => b.price - a.price);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Pancit\", price: 60 }, { id: 2, name: \"Lumpia\", price: 15 }, { id: 3, name: \"Adobo\", price: 80 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "desc",
        "label": "GET /items?sort=-price answers most expensive first",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items?sort=-price"
          }
        ],
        "bodyContains": "[{\"id\":3,\"name\":\"Adobo\",\"price\":80},{\"id\":1,\"name\":\"Pancit\",\"price\":60},{\"id\":2,\"name\":\"Lumpia\",\"price\":15}]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Think of ?sort=-price as asking for most expensive first."
      },
      {
        "level": 2,
        "text": "Add this code after the line that sorts by price.\n\nIn server.js:\n```\n  if (url.searchParams.get(\"sort\") === \"-price\") list = [...list].sort((a, b) => b.price - a.price);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Pancit\", price: 60 }, { id: 2, name: \"Lumpia\", price: 15 }, { id: 3, name: \"Adobo\", price: 80 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n  if (url.searchParams.has(\"q\")) list = list.filter((item) => item.name.toLowerCase().includes(url.searchParams.get(\"q\").toLowerCase()));\n  if (url.searchParams.get(\"sort\") === \"price\") list = [...list].sort((a, b) => a.price - b.price);\n  if (url.searchParams.get(\"sort\") === \"-price\") list = [...list].sort((a, b) => b.price - a.price);\n  if (url.pathname === \"/items\") return send(res, 200, list);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "query-strings-carinderia"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: query-strings-carinderia.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-query-strings-carinderia-6",
    "index": 146,
    "task": "You will add a line to limit the list. This line goes after the second sort line. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (url.searchParams.has(\"limit\")) list = list.slice(0, Number(url.searchParams.get(\"limit\")));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Pancit\", price: 60 }, { id: 2, name: \"Lumpia\", price: 15 }, { id: 3, name: \"Adobo\", price: 80 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "limit",
        "label": "GET /items?sort=price&limit=1 answers only the cheapest item",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items?sort=price&limit=1"
          }
        ],
        "bodyContains": "[{\"id\":2,\"name\":\"Lumpia\",\"price\":15}]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "You need to check if the limit is in the URL. Then cut the list to that size."
      },
      {
        "level": 2,
        "text": "Add the code after the second sort line in server.js.\n\nIn server.js:\n```\n  if (url.searchParams.has(\"limit\")) list = list.slice(0, Number(url.searchParams.get(\"limit\")));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Pancit\", price: 60 }, { id: 2, name: \"Lumpia\", price: 15 }, { id: 3, name: \"Adobo\", price: 80 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n  if (url.searchParams.has(\"q\")) list = list.filter((item) => item.name.toLowerCase().includes(url.searchParams.get(\"q\").toLowerCase()));\n  if (url.searchParams.get(\"sort\") === \"price\") list = [...list].sort((a, b) => a.price - b.price);\n  if (url.searchParams.get(\"sort\") === \"-price\") list = [...list].sort((a, b) => b.price - a.price);\n  if (url.searchParams.has(\"limit\")) list = list.slice(0, Number(url.searchParams.get(\"limit\")));\n  if (url.pathname === \"/items\") return send(res, 200, list);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "query-strings-carinderia"
  },
  {
    "id": "api-query-strings-carinderia-7",
    "index": 147,
    "task": "You will add a line to show only names. This line goes after the limit line. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (url.searchParams.get(\"fields\") === \"name\") list = list.map((item) => ({ name: item.name }));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Pancit\", price: 60 }, { id: 2, name: \"Lumpia\", price: 15 }, { id: 3, name: \"Adobo\", price: 80 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "fields",
        "label": "GET /items?fields=name answers names only",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items?fields=name"
          }
        ],
        "bodyContains": "[{\"name\":\"Pancit\"},{\"name\":\"Lumpia\"},{\"name\":\"Adobo\"}]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "You need to check if the fields are set to name. Then change each item to only have its name."
      },
      {
        "level": 2,
        "text": "Add the code after the limit line in server.js.\n\nIn server.js:\n```\n  if (url.searchParams.get(\"fields\") === \"name\") list = list.map((item) => ({ name: item.name }));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Pancit\", price: 60 }, { id: 2, name: \"Lumpia\", price: 15 }, { id: 3, name: \"Adobo\", price: 80 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n  if (url.searchParams.has(\"q\")) list = list.filter((item) => item.name.toLowerCase().includes(url.searchParams.get(\"q\").toLowerCase()));\n  if (url.searchParams.get(\"sort\") === \"price\") list = [...list].sort((a, b) => a.price - b.price);\n  if (url.searchParams.get(\"sort\") === \"-price\") list = [...list].sort((a, b) => b.price - a.price);\n  if (url.searchParams.has(\"limit\")) list = list.slice(0, Number(url.searchParams.get(\"limit\")));\n  if (url.searchParams.get(\"fields\") === \"name\") list = list.map((item) => ({ name: item.name }));\n  if (url.pathname === \"/items\") return send(res, 200, list);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "query-strings-carinderia"
  },
  {
    "id": "api-query-strings-carinderia-8",
    "index": 148,
    "task": "You will add a line to send 400 if max is not a number. This line goes right after the url line. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (url.searchParams.has(\"max\") && Number.isNaN(Number(url.searchParams.get(\"max\")))) return send(res, 400, { error: \"max must be a number\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Pancit\", price: 60 }, { id: 2, name: \"Lumpia\", price: 15 }, { id: 3, name: \"Adobo\", price: 80 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "bad",
        "label": "GET /items?max=abc answers 400",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items?max=abc"
          }
        ],
        "status": 400
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "You need to check if max is in the URL and if it is not a number. Then send 400 with an error message."
      },
      {
        "level": 2,
        "text": "Add the code right after the url line in server.js.\n\nIn server.js:\n```\n  if (url.searchParams.has(\"max\") && Number.isNaN(Number(url.searchParams.get(\"max\")))) return send(res, 400, { error: \"max must be a number\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Pancit\", price: 60 }, { id: 2, name: \"Lumpia\", price: 15 }, { id: 3, name: \"Adobo\", price: 80 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.searchParams.has(\"max\") && Number.isNaN(Number(url.searchParams.get(\"max\")))) return send(res, 400, { error: \"max must be a number\" });\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n  if (url.searchParams.has(\"q\")) list = list.filter((item) => item.name.toLowerCase().includes(url.searchParams.get(\"q\").toLowerCase()));\n  if (url.searchParams.get(\"sort\") === \"price\") list = [...list].sort((a, b) => a.price - b.price);\n  if (url.searchParams.get(\"sort\") === \"-price\") list = [...list].sort((a, b) => b.price - a.price);\n  if (url.searchParams.has(\"limit\")) list = list.slice(0, Number(url.searchParams.get(\"limit\")));\n  if (url.searchParams.get(\"fields\") === \"name\") list = list.map((item) => ({ name: item.name }));\n  if (url.pathname === \"/items\") return send(res, 200, list);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "query-strings-carinderia"
  },
  {
    "id": "api-query-strings-carinderia-9",
    "index": 149,
    "task": "You will change the /items line to send a header. This line replaces the old /items line. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (url.pathname === \"/items\") { res.setHeader(\"X-Total-Count\", String(list.length)); return send(res, 200, list); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Pancit\", price: 60 }, { id: 2, name: \"Lumpia\", price: 15 }, { id: 3, name: \"Adobo\", price: 80 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "count",
        "label": "GET /items?max=60 sends X-Total-Count: 2",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items?max=60"
          }
        ],
        "header": {
          "name": "x-total-count",
          "value": "2"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "You need to send the total number of items in the X-Total-Count header before sending the list."
      },
      {
        "level": 2,
        "text": "Replace the /items line in server.js with the new code.\n\nIn server.js:\n```\n  if (url.pathname === \"/items\") { res.setHeader(\"X-Total-Count\", String(list.length)); return send(res, 200, list); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Pancit\", price: 60 }, { id: 2, name: \"Lumpia\", price: 15 }, { id: 3, name: \"Adobo\", price: 80 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.searchParams.has(\"max\") && Number.isNaN(Number(url.searchParams.get(\"max\")))) return send(res, 400, { error: \"max must be a number\" });\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n  if (url.searchParams.has(\"q\")) list = list.filter((item) => item.name.toLowerCase().includes(url.searchParams.get(\"q\").toLowerCase()));\n  if (url.searchParams.get(\"sort\") === \"price\") list = [...list].sort((a, b) => a.price - b.price);\n  if (url.searchParams.get(\"sort\") === \"-price\") list = [...list].sort((a, b) => b.price - a.price);\n  if (url.searchParams.has(\"limit\")) list = list.slice(0, Number(url.searchParams.get(\"limit\")));\n  if (url.searchParams.get(\"fields\") === \"name\") list = list.map((item) => ({ name: item.name }));\n  if (url.pathname === \"/items\") { res.setHeader(\"X-Total-Count\", String(list.length)); return send(res, 200, list); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "query-strings-carinderia"
  },
  {
    "id": "api-query-strings-carinderia-10",
    "index": 150,
    "task": "You will add a line to show the supported options. This line goes after the 400 line. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (url.pathname === \"/items/options\") return send(res, 200, { filters: [\"max\", \"min\", \"q\"], sort: [\"price\", \"-price\"], other: [\"limit\", \"fields\"] });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Pancit\", price: 60 }, { id: 2, name: \"Lumpia\", price: 15 }, { id: 3, name: \"Adobo\", price: 80 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "options",
        "label": "GET /items/options lists the filters",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/options"
          }
        ],
        "bodyContains": "\"filters\":[\"max\",\"min\",\"q\"]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "You need to check if the path is /items/options. Then send a JSON object with the filters, sort, and other options."
      },
      {
        "level": 2,
        "text": "Add the code after the 400 line in server.js.\n\nIn server.js:\n```\n  if (url.pathname === \"/items/options\") return send(res, 200, { filters: [\"max\", \"min\", \"q\"], sort: [\"price\", \"-price\"], other: [\"limit\", \"fields\"] });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Pancit\", price: 60 }, { id: 2, name: \"Lumpia\", price: 15 }, { id: 3, name: \"Adobo\", price: 80 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.searchParams.has(\"max\") && Number.isNaN(Number(url.searchParams.get(\"max\")))) return send(res, 400, { error: \"max must be a number\" });\n  if (url.pathname === \"/items/options\") return send(res, 200, { filters: [\"max\", \"min\", \"q\"], sort: [\"price\", \"-price\"], other: [\"limit\", \"fields\"] });\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n  if (url.searchParams.has(\"q\")) list = list.filter((item) => item.name.toLowerCase().includes(url.searchParams.get(\"q\").toLowerCase()));\n  if (url.searchParams.get(\"sort\") === \"price\") list = [...list].sort((a, b) => a.price - b.price);\n  if (url.searchParams.get(\"sort\") === \"-price\") list = [...list].sort((a, b) => b.price - a.price);\n  if (url.searchParams.has(\"limit\")) list = list.slice(0, Number(url.searchParams.get(\"limit\")));\n  if (url.searchParams.get(\"fields\") === \"name\") list = list.map((item) => ({ name: item.name }));\n  if (url.pathname === \"/items\") { res.setHeader(\"X-Total-Count\", String(list.length)); return send(res, 200, list); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "query-strings-carinderia"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: middleware-carinderia.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-middleware-carinderia-1",
    "index": 151,
    "task": "You will add a counter to track each request. This counter starts at 1. You will also add a function called addRequestId. This function adds a header to every answer. The header is named X-Request-Id. The header's value is the counter number. You will call this function right after the handler starts. The code below shows what to add. Run the checker to confirm the first answer sends X-Request-Id: 1.\n\nIn server.js:\n```\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\n  addRequestId(req, res);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "id",
        "label": "The first answer sends X-Request-Id: 1",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "header": {
          "name": "x-request-id",
          "value": "1"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Think of the counter like a ticket number for each customer. It starts at 1 and goes up."
      },
      {
        "level": 2,
        "text": "Add the counter and function above the server code. The checker will test this.\n\nIn server.js:\n```\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\n  addRequestId(req, res);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  addRequestId(req, res);\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "middleware-carinderia"
  },
  {
    "id": "api-middleware-carinderia-2",
    "index": 152,
    "task": "You will add another function called addPlace. This function adds a header named X-Place. The value is always 'carinderia'. You will call this function after addRequestId. The code below shows what to add. Run the checker to confirm answers now send X-Place: carinderia.\n\nIn server.js:\n```\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"carinderia\"); }\n  addPlace(req, res);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "place",
        "label": "Answers send X-Place: carinderia",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "header": {
          "name": "x-place",
          "value": "carinderia"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The X-Place header tells where the food comes from, like a small eatery."
      },
      {
        "level": 2,
        "text": "Add the function and call it after addRequestId. The checker will test this.\n\nIn server.js:\n```\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"carinderia\"); }\n  addPlace(req, res);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"carinderia\"); }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  addRequestId(req, res);\n  addPlace(req, res);\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "middleware-carinderia"
  },
  {
    "id": "api-middleware-carinderia-3",
    "index": 153,
    "task": "You will make a list of all middleware functions. This list includes addRequestId and addPlace. You will replace the two separate calls with a loop. The loop runs each function in the list. The code below shows what to add. Run the checker to confirm the server keeps its middleware in one list and answers still send X-Place.\n\nIn server.js:\n```\nconst middleware = [addRequestId, addPlace];\n  for (const step of middleware) step(req, res);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "list",
        "label": "server.js keeps its middleware in one list",
        "kind": "local-file-contains",
        "path": "server.js",
        "value": "const middleware = [addRequestId, addPlace];"
      },
      {
        "id": "still",
        "label": "Answers still send X-Place",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "header": {
          "name": "x-place",
          "value": "carinderia"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "A list is like a menu, you pick each item one by one."
      },
      {
        "level": 2,
        "text": "Replace the two calls with a for loop. The checker will test this.\n\nIn server.js:\n```\nconst middleware = [addRequestId, addPlace];\n  for (const step of middleware) step(req, res);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"carinderia\"); }\nconst middleware = [addRequestId, addPlace];\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  for (const step of middleware) step(req, res);\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "middleware-carinderia"
  },
  {
    "id": "api-middleware-carinderia-4",
    "index": 154,
    "task": "You will add a new function called addPoweredBy. This function adds a header named X-Powered-By. The value is 'Node built-ins'. You will add this function to the middleware list. The code below shows what to add. Run the checker to confirm answers now send X-Powered-By: Node built-ins.\n\nIn server.js:\n```\nfunction addPoweredBy(req, res) { res.setHeader(\"X-Powered-By\", \"Node built-ins\"); }\nconst middleware = [addRequestId, addPlace, addPoweredBy];\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "powered",
        "label": "Answers send X-Powered-By: Node built-ins",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "header": {
          "name": "x-powered-by",
          "value": "Node built-ins"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "This header tells users what tools built the server, like a kitchen tool list."
      },
      {
        "level": 2,
        "text": "Add the function to the list. The checker will test this.\n\nIn server.js:\n```\nfunction addPoweredBy(req, res) { res.setHeader(\"X-Powered-By\", \"Node built-ins\"); }\nconst middleware = [addRequestId, addPlace, addPoweredBy];\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"carinderia\"); }\nfunction addPoweredBy(req, res) { res.setHeader(\"X-Powered-By\", \"Node built-ins\"); }\nconst middleware = [addRequestId, addPlace, addPoweredBy];\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  for (const step of middleware) step(req, res);\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "middleware-carinderia"
  },
  {
    "id": "api-middleware-carinderia-5",
    "index": 155,
    "task": "You will add a function called allowBrowsers. This function adds a header named Access-Control-Allow-Origin. The value is '*'. This lets web pages from other sites call your API. You will add this function to the middleware list. The code below shows what to add. Run the checker to confirm answers now send Access-Control-Allow-Origin: *.\n\nIn server.js:\n```\nfunction allowBrowsers(req, res) { res.setHeader(\"Access-Control-Allow-Origin\", \"*\"); }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers];\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "cors",
        "label": "Answers send Access-Control-Allow-Origin: *",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "header": {
          "name": "access-control-allow-origin",
          "value": "*"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "This header lets other websites use your server, like sharing a recipe."
      },
      {
        "level": 2,
        "text": "Add the function to the list. The checker will test this.\n\nIn server.js:\n```\nfunction allowBrowsers(req, res) { res.setHeader(\"Access-Control-Allow-Origin\", \"*\"); }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers];\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"carinderia\"); }\nfunction addPoweredBy(req, res) { res.setHeader(\"X-Powered-By\", \"Node built-ins\"); }\nfunction allowBrowsers(req, res) { res.setHeader(\"Access-Control-Allow-Origin\", \"*\"); }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers];\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  for (const step of middleware) step(req, res);\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "middleware-carinderia"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: middleware-carinderia.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-middleware-carinderia-6",
    "index": 156,
    "task": "You add a function called requireKey. It checks if the request has the right API key. If not, it sends a 401 error. You add this function to the middleware list. Then, when the loop runs, it stops early if the key is wrong. The code below does this. Run the checker to confirm the server now refuses requests without the right key.\n\nIn server.js:\n```\nfunction requireKey(req, res) { if (req.headers[\"x-api-key\"] !== \"secret123\") { send(res, 401, { error: \"Missing or wrong API key\" }); return true; } }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers, requireKey];\n  for (const step of middleware) if (step(req, res)) return;\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "nokey",
        "label": "GET /items without a key answers 401",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "status": 401
      },
      {
        "id": "key",
        "label": "GET /items with the key answers 200",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items",
            "headers": {
              "x-api-key": "secret123"
            }
          }
        ],
        "status": 200
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The key must be in the header named X-Api-Key. If it's missing or wrong, the server must reject the request."
      },
      {
        "level": 2,
        "text": "Add the requireKey function to the middleware list right after addPoweredBy.\n\nIn server.js:\n```\nfunction requireKey(req, res) { if (req.headers[\"x-api-key\"] !== \"secret123\") { send(res, 401, { error: \"Missing or wrong API key\" }); return true; } }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers, requireKey];\n  for (const step of middleware) if (step(req, res)) return;\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"carinderia\"); }\nfunction addPoweredBy(req, res) { res.setHeader(\"X-Powered-By\", \"Node built-ins\"); }\nfunction allowBrowsers(req, res) { res.setHeader(\"Access-Control-Allow-Origin\", \"*\"); }\nfunction requireKey(req, res) { if (req.headers[\"x-api-key\"] !== \"secret123\") { send(res, 401, { error: \"Missing or wrong API key\" }); return true; } }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers, requireKey];\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  for (const step of middleware) if (step(req, res)) return;\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "middleware-carinderia"
  },
  {
    "id": "api-middleware-carinderia-7",
    "index": 157,
    "task": "You add a line at the top of the handler. This line checks if the URL is /health. If so, it sends a 200 OK response and stops. This lets monitors check the server without needing a key. The code below does this. Run the checker to confirm /health still works without a key.\n\nIn server.js:\n```\n  if (req.url === \"/health\") return send(res, 200, { ok: true });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "health",
        "label": "GET /health answers without a key",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/health"
          }
        ],
        "status": 200,
        "bodyContains": "\"ok\":true"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The /health endpoint must always answer 200, even if the key is missing. This is for system checks."
      },
      {
        "level": 2,
        "text": "Put this line right after the middleware loop starts, before the loop runs.\n\nIn server.js:\n```\n  if (req.url === \"/health\") return send(res, 200, { ok: true });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"carinderia\"); }\nfunction addPoweredBy(req, res) { res.setHeader(\"X-Powered-By\", \"Node built-ins\"); }\nfunction allowBrowsers(req, res) { res.setHeader(\"Access-Control-Allow-Origin\", \"*\"); }\nfunction requireKey(req, res) { if (req.headers[\"x-api-key\"] !== \"secret123\") { send(res, 401, { error: \"Missing or wrong API key\" }); return true; } }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers, requireKey];\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/health\") return send(res, 200, { ok: true });\n  for (const step of middleware) if (step(req, res)) return;\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "middleware-carinderia"
  },
  {
    "id": "api-middleware-carinderia-8",
    "index": 158,
    "task": "You change the allowBrowsers function. Now, if the request method is OPTIONS, it sends a 204 status and stops. This lets browsers ask for permissions before sending real requests. The code below does this. Run the checker to confirm OPTIONS requests now answer 204.\n\nIn server.js:\n```\nfunction allowBrowsers(req, res) { res.setHeader(\"Access-Control-Allow-Origin\", \"*\"); if (req.method === \"OPTIONS\") { res.statusCode = 204; res.end(); return true; } }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "preflight",
        "label": "OPTIONS /items answers 204 without a key",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "OPTIONS",
            "path": "/items"
          }
        ],
        "status": 204
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "OPTIONS requests are browser preflight checks. They must answer 204 before the key check."
      },
      {
        "level": 2,
        "text": "Change the allowBrowsers function to return true when the method is OPTIONS.\n\nIn server.js:\n```\nfunction allowBrowsers(req, res) { res.setHeader(\"Access-Control-Allow-Origin\", \"*\"); if (req.method === \"OPTIONS\") { res.statusCode = 204; res.end(); return true; } }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"carinderia\"); }\nfunction addPoweredBy(req, res) { res.setHeader(\"X-Powered-By\", \"Node built-ins\"); }\nfunction allowBrowsers(req, res) { res.setHeader(\"Access-Control-Allow-Origin\", \"*\"); if (req.method === \"OPTIONS\") { res.statusCode = 204; res.end(); return true; } }\nfunction requireKey(req, res) { if (req.headers[\"x-api-key\"] !== \"secret123\") { send(res, 401, { error: \"Missing or wrong API key\" }); return true; } }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers, requireKey];\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/health\") return send(res, 200, { ok: true });\n  for (const step of middleware) if (step(req, res)) return;\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "middleware-carinderia"
  },
  {
    "id": "api-middleware-carinderia-9",
    "index": 159,
    "task": "You add a line after the loop. This line checks if the URL is /stats. If so, it sends back how many requests the server has answered. The code below does this. Run the checker to confirm /stats answers with the correct count after two requests.\n\nIn server.js:\n```\n  if (req.url === \"/stats\") return send(res, 200, { served: nextId - 1 });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "stats",
        "label": "After two requests, GET /stats answers served 3",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items",
            "headers": {
              "x-api-key": "secret123"
            }
          },
          {
            "method": "GET",
            "path": "/items",
            "headers": {
              "x-api-key": "secret123"
            }
          },
          {
            "method": "GET",
            "path": "/stats",
            "headers": {
              "x-api-key": "secret123"
            }
          }
        ],
        "bodyContains": "\"served\":3"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The /stats endpoint must show how many requests have been served. Use nextId - 1 to get the count."
      },
      {
        "level": 2,
        "text": "Add this line right after the middleware loop ends, before any other code.\n\nIn server.js:\n```\n  if (req.url === \"/stats\") return send(res, 200, { served: nextId - 1 });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"carinderia\"); }\nfunction addPoweredBy(req, res) { res.setHeader(\"X-Powered-By\", \"Node built-ins\"); }\nfunction allowBrowsers(req, res) { res.setHeader(\"Access-Control-Allow-Origin\", \"*\"); if (req.method === \"OPTIONS\") { res.statusCode = 204; res.end(); return true; } }\nfunction requireKey(req, res) { if (req.headers[\"x-api-key\"] !== \"secret123\") { send(res, 401, { error: \"Missing or wrong API key\" }); return true; } }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers, requireKey];\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/health\") return send(res, 200, { ok: true });\n  for (const step of middleware) if (step(req, res)) return;\n  if (req.url === \"/stats\") return send(res, 200, { served: nextId - 1 });\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "middleware-carinderia"
  },
  {
    "id": "api-middleware-carinderia-10",
    "index": 160,
    "task": "You add a new function called secureHeaders. It adds a security header to every response. You add this function to the middleware list. The code below does this. Run the checker to confirm every answer now includes X-Content-Type-Options: nosniff.\n\nIn server.js:\n```\nfunction secureHeaders(req, res) { res.setHeader(\"X-Content-Type-Options\", \"nosniff\"); }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers, secureHeaders, requireKey];\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "nosniff",
        "label": "Answers send X-Content-Type-Options: nosniff",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items",
            "headers": {
              "x-api-key": "secret123"
            }
          }
        ],
        "header": {
          "name": "x-content-type-options",
          "value": "nosniff"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The secureHeaders function must run for every request. Add it to the middleware list before requireKey."
      },
      {
        "level": 2,
        "text": "The header X-Content-Type-Options: nosniff stops browsers from guessing wrong content types.\n\nIn server.js:\n```\nfunction secureHeaders(req, res) { res.setHeader(\"X-Content-Type-Options\", \"nosniff\"); }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers, secureHeaders, requireKey];\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"carinderia\"); }\nfunction addPoweredBy(req, res) { res.setHeader(\"X-Powered-By\", \"Node built-ins\"); }\nfunction allowBrowsers(req, res) { res.setHeader(\"Access-Control-Allow-Origin\", \"*\"); if (req.method === \"OPTIONS\") { res.statusCode = 204; res.end(); return true; } }\nfunction requireKey(req, res) { if (req.headers[\"x-api-key\"] !== \"secret123\") { send(res, 401, { error: \"Missing or wrong API key\" }); return true; } }\nfunction secureHeaders(req, res) { res.setHeader(\"X-Content-Type-Options\", \"nosniff\"); }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers, secureHeaders, requireKey];\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/health\") return send(res, 200, { ok: true });\n  for (const step of middleware) if (step(req, res)) return;\n  if (req.url === \"/stats\") return send(res, 200, { served: nextId - 1 });\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 5,
    "projectId": "middleware-carinderia"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: sqlite-storage-carinderia.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-sqlite-storage-carinderia-1",
    "index": 161,
    "task": "Open an in-memory SQLite database. Make a table called items. Answer GET /items with the data from that table. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "empty",
        "label": "GET /items answers an empty list from the database",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "status": 200,
        "bodyContains": "[]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "You need to create a database and a table before you can answer requests."
      },
      {
        "level": 2,
        "text": "Add the code above createItem and before the 404 line.\n\nIn server.js:\n```\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "sqlite-storage-carinderia"
  },
  {
    "id": "api-sqlite-storage-carinderia-2",
    "index": 162,
    "task": "Prepare a statement to add items to the database. Add three items: Adobo, Pancit, and Lumpia. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Adobo\",80],[\"Pancit\",60],[\"Lumpia\",15]]) insert.run(name, price);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "rows",
        "label": "GET /items includes Adobo from the database",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "bodyContains": "\"name\":\"Adobo\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use a loop to add the items one by one. The loop uses an array of name-price pairs."
      },
      {
        "level": 2,
        "text": "Add the code after the CREATE TABLE line.\n\nIn server.js:\n```\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Adobo\",80],[\"Pancit\",60],[\"Lumpia\",15]]) insert.run(name, price);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Adobo\",80],[\"Pancit\",60],[\"Lumpia\",15]]) insert.run(name, price);\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "sqlite-storage-carinderia"
  },
  {
    "id": "api-sqlite-storage-carinderia-3",
    "index": 163,
    "task": "Change the SQL in the GET /items route to sort items by price. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items ORDER BY price\").all());\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "ordered",
        "label": "GET /items lists the items cheapest first, starting with Lumpia",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "bodyContains": "[{\"id\":3,\"name\":\"Lumpia\",\"price\":15},{\"id\":2,\"name\":\"Pancit\",\"price\":60},{\"id\":1,\"name\":\"Adobo\",\"price\":80}]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Add ORDER BY price to the SELECT statement to sort cheapest first."
      },
      {
        "level": 2,
        "text": "Change the SQL in the GET /items line.\n\nIn server.js:\n```\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items ORDER BY price\").all());\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Adobo\",80],[\"Pancit\",60],[\"Lumpia\",15]]) insert.run(name, price);\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items ORDER BY price\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "sqlite-storage-carinderia"
  },
  {
    "id": "api-sqlite-storage-carinderia-4",
    "index": 164,
    "task": "Add a route to count the items in the database. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "count",
        "label": "GET /items/count answers {\"count\":3}",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/count"
          }
        ],
        "bodyContains": "{\"count\":3}"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use COUNT(*) to count all rows. Send the count as a JSON object."
      },
      {
        "level": 2,
        "text": "Add the code after the GET /items line.\n\nIn server.js:\n```\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Adobo\",80],[\"Pancit\",60],[\"Lumpia\",15]]) insert.run(name, price);\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items ORDER BY price\").all());\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "sqlite-storage-carinderia"
  },
  {
    "id": "api-sqlite-storage-carinderia-5",
    "index": 165,
    "task": "Answer one item by its id. Use a ? parameter to get the id from the URL. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match) return send(res, 200, db.prepare(\"SELECT * FROM items WHERE id = ?\").get(Number(match[1])));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "one",
        "label": "GET /items/2 answers Pancit",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/2"
          }
        ],
        "bodyContains": "\"name\":\"Pancit\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use a regex to get the id from the URL. Then use it in the SQL WHERE clause."
      },
      {
        "level": 2,
        "text": "Add the code before the 404 line.\n\nIn server.js:\n```\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match) return send(res, 200, db.prepare(\"SELECT * FROM items WHERE id = ?\").get(Number(match[1])));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Adobo\",80],[\"Pancit\",60],[\"Lumpia\",15]]) insert.run(name, price);\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items ORDER BY price\").all());\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match) return send(res, 200, db.prepare(\"SELECT * FROM items WHERE id = ?\").get(Number(match[1])));\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "sqlite-storage-carinderia"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: sqlite-storage-carinderia.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-sqlite-storage-carinderia-6",
    "index": 166,
    "task": "When the database has no item with that id, the server must answer 404. Change the one-row line in server.js. The code below does that. Run the checker to confirm.\n\nIn server.js:\n```\n  if (match) { const row = db.prepare(\"SELECT * FROM items WHERE id = ?\").get(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "none",
        "label": "GET /items/99 answers 404",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/99"
          }
        ],
        "status": 404
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "If the row is not found, return a 404 error with a message."
      },
      {
        "level": 2,
        "text": "Put the new code in server.js, replacing the current line.\n\nIn server.js:\n```\n  if (match) { const row = db.prepare(\"SELECT * FROM items WHERE id = ?\").get(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Adobo\",80],[\"Pancit\",60],[\"Lumpia\",15]]) insert.run(name, price);\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items ORDER BY price\").all());\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match) { const row = db.prepare(\"SELECT * FROM items WHERE id = ?\").get(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "sqlite-storage-carinderia"
  },
  {
    "id": "api-sqlite-storage-carinderia-7",
    "index": 167,
    "task": "When you POST an item, the server must save it and answer with the id the database gave it. Replace the line inside createItem with three lines. The code below does that. Run the checker to confirm.\n\nIn server.js:\n```\n  const data = JSON.parse(await readBody(req));\n  const result = insert.run(data.name, data.price);\n  return send(res, 201, { id: Number(result.lastInsertRowid), ...data });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "insert",
        "label": "POST /items answers 201 with id 4",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "{\"name\":\"Tea\",\"price\":12}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 201,
        "bodyContains": "\"id\":4"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Parse the JSON body, insert the data, then return the new id."
      },
      {
        "level": 2,
        "text": "Put the new code in server.js, replacing the current line inside createItem.\n\nIn server.js:\n```\n  const data = JSON.parse(await readBody(req));\n  const result = insert.run(data.name, data.price);\n  return send(res, 201, { id: Number(result.lastInsertRowid), ...data });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Adobo\",80],[\"Pancit\",60],[\"Lumpia\",15]]) insert.run(name, price);\nasync function createItem(req, res) {\n  const data = JSON.parse(await readBody(req));\n  const result = insert.run(data.name, data.price);\n  return send(res, 201, { id: Number(result.lastInsertRowid), ...data });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items ORDER BY price\").all());\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match) { const row = db.prepare(\"SELECT * FROM items WHERE id = ?\").get(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "sqlite-storage-carinderia"
  },
  {
    "id": "api-sqlite-storage-carinderia-8",
    "index": 168,
    "task": "Add a route to get the total price of all items. Add one route after the count route. The code below does that. Run the checker to confirm.\n\nIn server.js:\n```\n  if (req.url === \"/items/total\") return send(res, 200, db.prepare(\"SELECT SUM(price) AS total FROM items\").get());\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "sum",
        "label": "GET /items/total answers the sum of prices",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/total"
          }
        ],
        "bodyContains": "{\"total\":155}"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use SUM to add all prices in the database."
      },
      {
        "level": 2,
        "text": "Put the new route after the count route in server.js.\n\nIn server.js:\n```\n  if (req.url === \"/items/total\") return send(res, 200, db.prepare(\"SELECT SUM(price) AS total FROM items\").get());\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Adobo\",80],[\"Pancit\",60],[\"Lumpia\",15]]) insert.run(name, price);\nasync function createItem(req, res) {\n  const data = JSON.parse(await readBody(req));\n  const result = insert.run(data.name, data.price);\n  return send(res, 201, { id: Number(result.lastInsertRowid), ...data });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items ORDER BY price\").all());\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n  if (req.url === \"/items/total\") return send(res, 200, db.prepare(\"SELECT SUM(price) AS total FROM items\").get());\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match) { const row = db.prepare(\"SELECT * FROM items WHERE id = ?\").get(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "sqlite-storage-carinderia"
  },
  {
    "id": "api-sqlite-storage-carinderia-9",
    "index": 169,
    "task": "Add a search route that finds items by name. Add two lines after the total route. The code below does that. Run the checker to confirm.\n\nIn server.js:\n```\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items/search\") return send(res, 200, db.prepare(\"SELECT * FROM items WHERE name LIKE ?\").all(`%${url.searchParams.get(\"q\") ?? \"\"}%`));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "like",
        "label": "GET /items/search?q=ump finds Lumpia",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/search?q=ump"
          }
        ],
        "bodyContains": "\"name\":\"Lumpia\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use LIKE to match names with the search text."
      },
      {
        "level": 2,
        "text": "Put the new lines after the total route in server.js.\n\nIn server.js:\n```\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items/search\") return send(res, 200, db.prepare(\"SELECT * FROM items WHERE name LIKE ?\").all(`%${url.searchParams.get(\"q\") ?? \"\"}%`));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Adobo\",80],[\"Pancit\",60],[\"Lumpia\",15]]) insert.run(name, price);\nasync function createItem(req, res) {\n  const data = JSON.parse(await readBody(req));\n  const result = insert.run(data.name, data.price);\n  return send(res, 201, { id: Number(result.lastInsertRowid), ...data });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items ORDER BY price\").all());\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n  if (req.url === \"/items/total\") return send(res, 200, db.prepare(\"SELECT SUM(price) AS total FROM items\").get());\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items/search\") return send(res, 200, db.prepare(\"SELECT * FROM items WHERE name LIKE ?\").all(`%${url.searchParams.get(\"q\") ?? \"\"}%`));\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match) { const row = db.prepare(\"SELECT * FROM items WHERE id = ?\").get(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "sqlite-storage-carinderia"
  },
  {
    "id": "api-sqlite-storage-carinderia-10",
    "index": 170,
    "task": "The server must keep data in a file named by DB_FILE. Create the table and rows only once. Change the open, CREATE TABLE, and starting-rows lines. The code below does that. Run the checker to confirm.\n\nIn server.js:\n```\nconst db = new DatabaseSync(process.env.DB_FILE ?? \":memory:\");\ndb.exec(\"CREATE TABLE IF NOT EXISTS items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nif (db.prepare(\"SELECT COUNT(*) AS count FROM items\").get().count === 0) for (const [name, price] of [[\"Adobo\",80],[\"Pancit\",60],[\"Lumpia\",15]]) insert.run(name, price);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "file",
        "label": "With DB_FILE=shop.db the server answers from a file",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "env": {
          "DB_FILE": "shop.db"
        },
        "status": 200,
        "bodyContains": "\"name\":\"Adobo\""
      },
      {
        "id": "saved",
        "label": "shop.db exists on disk",
        "kind": "local-file-exists",
        "path": "shop.db"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use process.env.DB_FILE to get the file name. Create the table if it doesn't exist."
      },
      {
        "level": 2,
        "text": "Put the new code in server.js, replacing the current lines for opening, creating, and inserting data.\n\nIn server.js:\n```\nconst db = new DatabaseSync(process.env.DB_FILE ?? \":memory:\");\ndb.exec(\"CREATE TABLE IF NOT EXISTS items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nif (db.prepare(\"SELECT COUNT(*) AS count FROM items\").get().count === 0) for (const [name, price] of [[\"Adobo\",80],[\"Pancit\",60],[\"Lumpia\",15]]) insert.run(name, price);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(process.env.DB_FILE ?? \":memory:\");\ndb.exec(\"CREATE TABLE IF NOT EXISTS items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nif (db.prepare(\"SELECT COUNT(*) AS count FROM items\").get().count === 0) for (const [name, price] of [[\"Adobo\",80],[\"Pancit\",60],[\"Lumpia\",15]]) insert.run(name, price);\nasync function createItem(req, res) {\n  const data = JSON.parse(await readBody(req));\n  const result = insert.run(data.name, data.price);\n  return send(res, 201, { id: Number(result.lastInsertRowid), ...data });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items ORDER BY price\").all());\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n  if (req.url === \"/items/total\") return send(res, 200, db.prepare(\"SELECT SUM(price) AS total FROM items\").get());\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items/search\") return send(res, 200, db.prepare(\"SELECT * FROM items WHERE name LIKE ?\").all(`%${url.searchParams.get(\"q\") ?? \"\"}%`));\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match) { const row = db.prepare(\"SELECT * FROM items WHERE id = ?\").get(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 5,
    "projectId": "sqlite-storage-carinderia"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: sqlite-changes-carinderia.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-sqlite-changes-carinderia-1",
    "index": 171,
    "task": "You will delete a row in the carinderia's menu. Go to server.js. Replace the line inside deleteItem with the three lines below. This tells the server to delete the row and send back 204 No Content. The checker will test if it works.\n\nIn server.js:\n```\n  db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  res.statusCode = 204;\n  return res.end();\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Adobo\",80],[\"Pancit\",60],[\"Lumpia\",15]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "deleted",
        "label": "DELETE /items/1 answers 204",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "DELETE",
            "path": "/items/1"
          }
        ],
        "status": 204
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Think of 204 as 'gone, no reply needed'."
      },
      {
        "level": 2,
        "text": "Put the three lines inside the deleteItem function, right after the db.prepare line.\n\nIn server.js:\n```\n  db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  res.statusCode = 204;\n  return res.end();\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Adobo\",80],[\"Pancit\",60],[\"Lumpia\",15]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  res.statusCode = 204;\n  return res.end();\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "sqlite-changes-carinderia"
  },
  {
    "id": "api-sqlite-changes-carinderia-2",
    "index": 172,
    "task": "Now, if no row is found to delete, the server should answer 404. Go to server.js. Change the DELETE line to store the result. Then add a check after it. If the result shows no changes, send 404. The checker will test this.\n\nIn server.js:\n```\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Adobo\",80],[\"Pancit\",60],[\"Lumpia\",15]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "none",
        "label": "DELETE /items/99 answers 404",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "DELETE",
            "path": "/items/99"
          }
        ],
        "status": 404
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "If the row doesn't exist, the server should say 'not found'."
      },
      {
        "level": 2,
        "text": "Put the new code after the DELETE line, inside the deleteItem function.\n\nIn server.js:\n```\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Adobo\",80],[\"Pancit\",60],[\"Lumpia\",15]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  res.statusCode = 204;\n  return res.end();\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "sqlite-changes-carinderia"
  },
  {
    "id": "api-sqlite-changes-carinderia-3",
    "index": 173,
    "task": "You will now change a row's price. Go to server.js. Replace the line inside updateItem with the three lines below. This updates the price and sends back the new row. The checker will test if it works.\n\nIn server.js:\n```\n  const data = JSON.parse(await readBody(req));\n  db.prepare(\"UPDATE items SET price = ? WHERE id = ?\").run(data.price, id);\n  return send(res, 200, one(id));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Adobo\",80],[\"Pancit\",60],[\"Lumpia\",15]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "updated",
        "label": "PUT /items/1 with price 99 answers the new price",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "PUT",
            "path": "/items/1",
            "body": "{\"price\":99}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "bodyContains": "\"price\":99"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The server should send back the updated row after changing the price."
      },
      {
        "level": 2,
        "text": "Put the three lines inside the updateItem function, right after the JSON.parse line.\n\nIn server.js:\n```\n  const data = JSON.parse(await readBody(req));\n  db.prepare(\"UPDATE items SET price = ? WHERE id = ?\").run(data.price, id);\n  return send(res, 200, one(id));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Adobo\",80],[\"Pancit\",60],[\"Lumpia\",15]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  res.statusCode = 204;\n  return res.end();\n}\nasync function updateItem(req, res, id) {\n  const data = JSON.parse(await readBody(req));\n  db.prepare(\"UPDATE items SET price = ? WHERE id = ?\").run(data.price, id);\n  return send(res, 200, one(id));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "sqlite-changes-carinderia"
  },
  {
    "id": "api-sqlite-changes-carinderia-4",
    "index": 174,
    "task": "If no row is found to update, the server should answer 404. Go to server.js. Change the UPDATE line to store the result. Then add a check after it. If the result shows no changes, send 404. The checker will test this.\n\nIn server.js:\n```\n  const result = db.prepare(\"UPDATE items SET price = ? WHERE id = ?\").run(data.price, id);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Adobo\",80],[\"Pancit\",60],[\"Lumpia\",15]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "none",
        "label": "PUT /items/99 answers 404",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "PUT",
            "path": "/items/99",
            "body": "{\"price\":1}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 404
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "If the row doesn't exist, the server should say 'not found'."
      },
      {
        "level": 2,
        "text": "Put the new code after the UPDATE line, inside the updateItem function.\n\nIn server.js:\n```\n  const result = db.prepare(\"UPDATE items SET price = ? WHERE id = ?\").run(data.price, id);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Adobo\",80],[\"Pancit\",60],[\"Lumpia\",15]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  res.statusCode = 204;\n  return res.end();\n}\nasync function updateItem(req, res, id) {\n  const data = JSON.parse(await readBody(req));\n  const result = db.prepare(\"UPDATE items SET price = ? WHERE id = ?\").run(data.price, id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  return send(res, 200, one(id));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "sqlite-changes-carinderia"
  },
  {
    "id": "api-sqlite-changes-carinderia-5",
    "index": 175,
    "task": "If the price is not a whole number or less than zero, the server should answer 422. Go to server.js. Add one line after the JSON.parse line. This checks if the price is valid. If not, send 422. The checker will test this.\n\nIn server.js:\n```\n  if (!Number.isInteger(data.price) || data.price < 0) return send(res, 422, { error: \"price must be a whole number 0 or more\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Adobo\",80],[\"Pancit\",60],[\"Lumpia\",15]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "bad",
        "label": "PUT /items/1 with price -5 answers 422",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "PUT",
            "path": "/items/1",
            "body": "{\"price\":-5}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 422
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "422 means 'invalid input'. Check if the price is a whole number and not negative."
      },
      {
        "level": 2,
        "text": "Put the new line right after the JSON.parse line, inside the updateItem function.\n\nIn server.js:\n```\n  if (!Number.isInteger(data.price) || data.price < 0) return send(res, 422, { error: \"price must be a whole number 0 or more\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Adobo\",80],[\"Pancit\",60],[\"Lumpia\",15]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  res.statusCode = 204;\n  return res.end();\n}\nasync function updateItem(req, res, id) {\n  const data = JSON.parse(await readBody(req));\n  if (!Number.isInteger(data.price) || data.price < 0) return send(res, 422, { error: \"price must be a whole number 0 or more\" });\n  const result = db.prepare(\"UPDATE items SET price = ? WHERE id = ?\").run(data.price, id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  return send(res, 200, one(id));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "sqlite-changes-carinderia"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: sqlite-changes-carinderia.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-sqlite-changes-carinderia-6",
    "index": 176,
    "task": "You change the price check to let 0 be allowed. This lets customers set free items. Then you change the UPDATE line to keep the old name or price if the user doesn't send it. This way, only the sent fields change. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (data.price !== undefined && (!Number.isInteger(data.price) || data.price < 0)) return send(res, 422, { error: \"price must be a whole number 0 or more\" });\n  const result = db.prepare(\"UPDATE items SET name = COALESCE(?, name), price = COALESCE(?, price) WHERE id = ?\").run(data.name ?? null, data.price ?? null, id);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Adobo\",80],[\"Pancit\",60],[\"Lumpia\",15]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "name",
        "label": "PUT /items/1 with only a name keeps the price",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "PUT",
            "path": "/items/1",
            "body": "{\"name\":\"Tea\"}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "bodyContains": "\"name\":\"Tea\",\"price\":80"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Make sure the price check allows 0 and only checks for whole numbers."
      },
      {
        "level": 2,
        "text": "Put the new UPDATE line where the old one was, and use COALESCE to keep old values.\n\nIn server.js:\n```\n  if (data.price !== undefined && (!Number.isInteger(data.price) || data.price < 0)) return send(res, 422, { error: \"price must be a whole number 0 or more\" });\n  const result = db.prepare(\"UPDATE items SET name = COALESCE(?, name), price = COALESCE(?, price) WHERE id = ?\").run(data.name ?? null, data.price ?? null, id);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Adobo\",80],[\"Pancit\",60],[\"Lumpia\",15]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  res.statusCode = 204;\n  return res.end();\n}\nasync function updateItem(req, res, id) {\n  const data = JSON.parse(await readBody(req));\n  if (data.price !== undefined && (!Number.isInteger(data.price) || data.price < 0)) return send(res, 422, { error: \"price must be a whole number 0 or more\" });\n  const result = db.prepare(\"UPDATE items SET name = COALESCE(?, name), price = COALESCE(?, price) WHERE id = ?\").run(data.name ?? null, data.price ?? null, id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  return send(res, 200, one(id));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "sqlite-changes-carinderia"
  },
  {
    "id": "api-sqlite-changes-carinderia-7",
    "index": 177,
    "task": "You add a new route for raising prices. This route runs a transaction to update every item. It then sends back all the updated items. The code below adds this route. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (req.url === \"/items/raise\" && req.method === \"POST\") { db.exec(\"BEGIN\"); db.prepare(\"UPDATE items SET price = price + 1\").run(); db.exec(\"COMMIT\"); return send(res, 200, db.prepare(\"SELECT * FROM items\").all()); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Adobo\",80],[\"Pancit\",60],[\"Lumpia\",15]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "raise",
        "label": "POST /items/raise answers the raised prices",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items/raise",
            "body": "{}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "bodyContains": "\"price\":81"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use db.exec to start and finish the transaction. Run the UPDATE inside it."
      },
      {
        "level": 2,
        "text": "Put this route right after the PUT route, at the top of the handler.\n\nIn server.js:\n```\n  if (req.url === \"/items/raise\" && req.method === \"POST\") { db.exec(\"BEGIN\"); db.prepare(\"UPDATE items SET price = price + 1\").run(); db.exec(\"COMMIT\"); return send(res, 200, db.prepare(\"SELECT * FROM items\").all()); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Adobo\",80],[\"Pancit\",60],[\"Lumpia\",15]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  res.statusCode = 204;\n  return res.end();\n}\nasync function updateItem(req, res, id) {\n  const data = JSON.parse(await readBody(req));\n  if (data.price !== undefined && (!Number.isInteger(data.price) || data.price < 0)) return send(res, 422, { error: \"price must be a whole number 0 or more\" });\n  const result = db.prepare(\"UPDATE items SET name = COALESCE(?, name), price = COALESCE(?, price) WHERE id = ?\").run(data.name ?? null, data.price ?? null, id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  return send(res, 200, one(id));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items/raise\" && req.method === \"POST\") { db.exec(\"BEGIN\"); db.prepare(\"UPDATE items SET price = price + 1\").run(); db.exec(\"COMMIT\"); return send(res, 200, db.prepare(\"SELECT * FROM items\").all()); }\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 5,
    "projectId": "sqlite-changes-carinderia"
  },
  {
    "id": "api-sqlite-changes-carinderia-8",
    "index": 178,
    "task": "You add a route to count the rows. This helps you know how many items are left after deleting. The code below adds this route. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Adobo\",80],[\"Pancit\",60],[\"Lumpia\",15]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "count",
        "label": "After DELETE /items/1, GET /items/count answers 2",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "DELETE",
            "path": "/items/1"
          },
          {
            "method": "GET",
            "path": "/items/count"
          }
        ],
        "bodyContains": "{\"count\":2}"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use db.prepare to count rows with COUNT(*)."
      },
      {
        "level": 2,
        "text": "Put this route after the raise route, before the delete route.\n\nIn server.js:\n```\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Adobo\",80],[\"Pancit\",60],[\"Lumpia\",15]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  res.statusCode = 204;\n  return res.end();\n}\nasync function updateItem(req, res, id) {\n  const data = JSON.parse(await readBody(req));\n  if (data.price !== undefined && (!Number.isInteger(data.price) || data.price < 0)) return send(res, 422, { error: \"price must be a whole number 0 or more\" });\n  const result = db.prepare(\"UPDATE items SET name = COALESCE(?, name), price = COALESCE(?, price) WHERE id = ?\").run(data.name ?? null, data.price ?? null, id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  return send(res, 200, one(id));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items/raise\" && req.method === \"POST\") { db.exec(\"BEGIN\"); db.prepare(\"UPDATE items SET price = price + 1\").run(); db.exec(\"COMMIT\"); return send(res, 200, db.prepare(\"SELECT * FROM items\").all()); }\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "sqlite-changes-carinderia"
  },
  {
    "id": "api-sqlite-changes-carinderia-9",
    "index": 179,
    "task": "You change the GET /items route to send a header with the total number of rows. This helps clients know how many items there are. The code below adds this header. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (req.url === \"/items\") { const rows = db.prepare(\"SELECT * FROM items\").all(); res.setHeader(\"X-Total-Count\", String(rows.length)); return send(res, 200, rows); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Adobo\",80],[\"Pancit\",60],[\"Lumpia\",15]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "header",
        "label": "GET /items sends X-Total-Count: 3",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "header": {
          "name": "x-total-count",
          "value": "3"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Set the header with res.setHeader before sending the rows."
      },
      {
        "level": 2,
        "text": "Put this change right after the count route, in the GET /items block.\n\nIn server.js:\n```\n  if (req.url === \"/items\") { const rows = db.prepare(\"SELECT * FROM items\").all(); res.setHeader(\"X-Total-Count\", String(rows.length)); return send(res, 200, rows); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Adobo\",80],[\"Pancit\",60],[\"Lumpia\",15]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  res.statusCode = 204;\n  return res.end();\n}\nasync function updateItem(req, res, id) {\n  const data = JSON.parse(await readBody(req));\n  if (data.price !== undefined && (!Number.isInteger(data.price) || data.price < 0)) return send(res, 422, { error: \"price must be a whole number 0 or more\" });\n  const result = db.prepare(\"UPDATE items SET name = COALESCE(?, name), price = COALESCE(?, price) WHERE id = ?\").run(data.name ?? null, data.price ?? null, id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  return send(res, 200, one(id));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items/raise\" && req.method === \"POST\") { db.exec(\"BEGIN\"); db.prepare(\"UPDATE items SET price = price + 1\").run(); db.exec(\"COMMIT\"); return send(res, 200, db.prepare(\"SELECT * FROM items\").all()); }\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") { const rows = db.prepare(\"SELECT * FROM items\").all(); res.setHeader(\"X-Total-Count\", String(rows.length)); return send(res, 200, rows); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "sqlite-changes-carinderia"
  },
  {
    "id": "api-sqlite-changes-carinderia-10",
    "index": 180,
    "task": "You add a search route that looks for exact names. This stops bad text from changing the SQL. The code below adds this route. Run the checker to confirm it works.\n\nIn server.js:\n```\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items/search\") return send(res, 200, db.prepare(\"SELECT * FROM items WHERE name = ?\").all(url.searchParams.get(\"name\") ?? \"\"));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Adobo\",80],[\"Pancit\",60],[\"Lumpia\",15]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "found",
        "label": "GET /items/search?name=Pancit finds it",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/search?name=Pancit"
          }
        ],
        "bodyContains": "\"name\":\"Pancit\""
      },
      {
        "id": "safe",
        "label": "A search for ' OR '1'='1 finds nothing",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/search?name=%27%20OR%20%271%27%3D%271"
          }
        ],
        "status": 200,
        "bodyContains": "[]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use URL to get the search parameter. Use it in the WHERE clause."
      },
      {
        "level": 2,
        "text": "Put this route after the count route, before the delete route.\n\nIn server.js:\n```\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items/search\") return send(res, 200, db.prepare(\"SELECT * FROM items WHERE name = ?\").all(url.searchParams.get(\"name\") ?? \"\"));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Adobo\",80],[\"Pancit\",60],[\"Lumpia\",15]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  res.statusCode = 204;\n  return res.end();\n}\nasync function updateItem(req, res, id) {\n  const data = JSON.parse(await readBody(req));\n  if (data.price !== undefined && (!Number.isInteger(data.price) || data.price < 0)) return send(res, 422, { error: \"price must be a whole number 0 or more\" });\n  const result = db.prepare(\"UPDATE items SET name = COALESCE(?, name), price = COALESCE(?, price) WHERE id = ?\").run(data.name ?? null, data.price ?? null, id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  return send(res, 200, one(id));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items/raise\" && req.method === \"POST\") { db.exec(\"BEGIN\"); db.prepare(\"UPDATE items SET price = price + 1\").run(); db.exec(\"COMMIT\"); return send(res, 200, db.prepare(\"SELECT * FROM items\").all()); }\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items/search\") return send(res, 200, db.prepare(\"SELECT * FROM items WHERE name = ?\").all(url.searchParams.get(\"name\") ?? \"\"));\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") { const rows = db.prepare(\"SELECT * FROM items\").all(); res.setHeader(\"X-Total-Count\", String(rows.length)); return send(res, 200, rows); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 5,
    "projectId": "sqlite-changes-carinderia"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: errors-carinderia.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-errors-carinderia-1",
    "index": 181,
    "task": "Add a /boom route. This route will crash the server if not caught. Change the handle call to catch any error. This way, the server does not crash. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  handle(req, res).catch(() => send(res, 500, { error: \"Something went wrong\" }));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res);\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "500",
        "label": "GET /boom answers 500",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/boom"
          }
        ],
        "status": 500,
        "bodyContains": "Something went wrong"
      },
      {
        "id": "alive",
        "label": "The server keeps answering after an error",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/boom"
          },
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "status": 200
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Make sure the error is thrown before the handle call."
      },
      {
        "level": 2,
        "text": "Put the new route at the top of the handle function.\n\nIn server.js:\n```\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  handle(req, res).catch(() => send(res, 500, { error: \"Something went wrong\" }));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res).catch(() => send(res, 500, { error: \"Something went wrong\" }));\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "errors-carinderia"
  },
  {
    "id": "api-errors-carinderia-2",
    "index": 182,
    "task": "Add a counter to track errors. Change the catch to include the counter. This helps you know how many errors happened. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\nlet errorCount = 0;\n  handle(req, res).catch(() => send(res, 500, { error: \"Something went wrong\", id: ++errorCount }));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res);\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "id",
        "label": "The first error answers id 1",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/boom"
          }
        ],
        "bodyContains": "\"id\":1"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The counter starts at zero and increases with each error."
      },
      {
        "level": 2,
        "text": "Put the counter above the handle function.\n\nIn server.js:\n```\nlet errorCount = 0;\n  handle(req, res).catch(() => send(res, 500, { error: \"Something went wrong\", id: ++errorCount }));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet errorCount = 0;\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res).catch(() => send(res, 500, { error: \"Something went wrong\", id: ++errorCount }));\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "errors-carinderia"
  },
  {
    "id": "api-errors-carinderia-3",
    "index": 183,
    "task": "Add an HttpError class to make errors carry their own status code. Add a /teapot route that throws this error. Change the catch to use the error's status. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\nclass HttpError extends Error { constructor(status, message) { super(message); this.status = status; } }\n  if (req.url === \"/teapot\") throw new HttpError(418, \"I am a teapot\");\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", id: ++errorCount }));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res);\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "teapot",
        "label": "GET /teapot answers 418",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/teapot"
          }
        ],
        "status": 418,
        "bodyContains": "I am a teapot"
      },
      {
        "id": "hidden",
        "label": "Unexpected errors still hide their details",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/boom"
          }
        ],
        "status": 500,
        "bodyContains": "Something went wrong"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The HttpError class takes a status and message."
      },
      {
        "level": 2,
        "text": "Put the new route before the handle call.\n\nIn server.js:\n```\nclass HttpError extends Error { constructor(status, message) { super(message); this.status = status; } }\n  if (req.url === \"/teapot\") throw new HttpError(418, \"I am a teapot\");\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", id: ++errorCount }));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet errorCount = 0;\nclass HttpError extends Error { constructor(status, message) { super(message); this.status = status; } }\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  if (req.url === \"/teapot\") throw new HttpError(418, \"I am a teapot\");\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", id: ++errorCount }));\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "errors-carinderia"
  },
  {
    "id": "api-errors-carinderia-4",
    "index": 184,
    "task": "Add a readJson helper to check if the body is valid JSON. If not, throw an HttpError with status 400. Use this in createItem. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\nasync function readJson(req) { const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\n  const item = await readJson(req);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res);\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "400",
        "label": "POST /items with broken JSON answers 400",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "not json",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 400
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The helper reads the body and tries to parse it as JSON."
      },
      {
        "level": 2,
        "text": "Put the helper before the createItem function.\n\nIn server.js:\n```\nasync function readJson(req) { const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\n  const item = await readJson(req);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet errorCount = 0;\nclass HttpError extends Error { constructor(status, message) { super(message); this.status = status; } }\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function readJson(req) { const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\nasync function createItem(req, res) {\n  const item = await readJson(req);\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  if (req.url === \"/teapot\") throw new HttpError(418, \"I am a teapot\");\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", id: ++errorCount }));\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "errors-carinderia"
  },
  {
    "id": "api-errors-carinderia-5",
    "index": 185,
    "task": "Add a route to check if the method is allowed. If not, throw an HttpError with status 405 and an Allow header. Put this before the GET /items line. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\n  if (req.url === \"/items\" && ![\"GET\", \"POST\"].includes(req.method)) { res.setHeader(\"Allow\", \"GET, POST\"); throw new HttpError(405, \"Method not allowed\"); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res);\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "405",
        "label": "DELETE /items answers 405 with Allow: GET, POST",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "DELETE",
            "path": "/items"
          }
        ],
        "status": 405,
        "header": {
          "name": "allow",
          "value": "GET, POST"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The Allow header tells what methods are allowed."
      },
      {
        "level": 2,
        "text": "Put the new route before the GET /items line.\n\nIn server.js:\n```\n  if (req.url === \"/items\" && ![\"GET\", \"POST\"].includes(req.method)) { res.setHeader(\"Allow\", \"GET, POST\"); throw new HttpError(405, \"Method not allowed\"); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet errorCount = 0;\nclass HttpError extends Error { constructor(status, message) { super(message); this.status = status; } }\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function readJson(req) { const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\nasync function createItem(req, res) {\n  const item = await readJson(req);\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  if (req.url === \"/teapot\") throw new HttpError(418, \"I am a teapot\");\n  if (req.url === \"/items\" && ![\"GET\", \"POST\"].includes(req.method)) { res.setHeader(\"Allow\", \"GET, POST\"); throw new HttpError(405, \"Method not allowed\"); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", id: ++errorCount }));\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "errors-carinderia"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: errors-carinderia.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-errors-carinderia-6",
    "index": 186,
    "task": "You will change the readBody helper. This helper reads the body of a request. It stops if the body is too big. You will make it stop at 1,000 characters. That way, it throws a 413 error if the body is bigger. The code below does that. Run the checker to test it.\n\nIn server.js:\n```\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) { text += chunk; if (text.length > 1000) throw new HttpError(413, \"Body too large\"); } return text; }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res);\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "413",
        "label": "A 2,000-character body answers 413",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "{\"name\":\"\",\"price\":1}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 413
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "If the body is too big, throw an error with status 413."
      },
      {
        "level": 2,
        "text": "Put the code inside the readBody function in server.js.\n\nIn server.js:\n```\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) { text += chunk; if (text.length > 1000) throw new HttpError(413, \"Body too large\"); } return text; }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet errorCount = 0;\nclass HttpError extends Error { constructor(status, message) { super(message); this.status = status; } }\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) { text += chunk; if (text.length > 1000) throw new HttpError(413, \"Body too large\"); } return text; }\nasync function readJson(req) { const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\nasync function createItem(req, res) {\n  const item = await readJson(req);\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  if (req.url === \"/teapot\") throw new HttpError(418, \"I am a teapot\");\n  if (req.url === \"/items\" && ![\"GET\", \"POST\"].includes(req.method)) { res.setHeader(\"Allow\", \"GET, POST\"); throw new HttpError(405, \"Method not allowed\"); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", id: ++errorCount }));\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "errors-carinderia"
  },
  {
    "id": "api-errors-carinderia-7",
    "index": 187,
    "task": "You will change the readJson helper. This helper checks if the body is JSON. If not, it throws a 415 error. The code below checks the content-type header. Run the checker to test it.\n\nIn server.js:\n```\nasync function readJson(req) { if (!req.headers[\"content-type\"]?.includes(\"application/json\")) throw new HttpError(415, \"Send JSON\"); const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res);\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "415",
        "label": "POST /items with text/plain answers 415",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "name=Tea",
            "headers": {
              "Content-Type": "text/plain"
            }
          }
        ],
        "status": 415
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "If the content-type is not application/json, throw a 415 error."
      },
      {
        "level": 2,
        "text": "Put the code inside the readJson function in server.js.\n\nIn server.js:\n```\nasync function readJson(req) { if (!req.headers[\"content-type\"]?.includes(\"application/json\")) throw new HttpError(415, \"Send JSON\"); const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet errorCount = 0;\nclass HttpError extends Error { constructor(status, message) { super(message); this.status = status; } }\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) { text += chunk; if (text.length > 1000) throw new HttpError(413, \"Body too large\"); } return text; }\nasync function readJson(req) { if (!req.headers[\"content-type\"]?.includes(\"application/json\")) throw new HttpError(415, \"Send JSON\"); const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\nasync function createItem(req, res) {\n  const item = await readJson(req);\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  if (req.url === \"/teapot\") throw new HttpError(418, \"I am a teapot\");\n  if (req.url === \"/items\" && ![\"GET\", \"POST\"].includes(req.method)) { res.setHeader(\"Allow\", \"GET, POST\"); throw new HttpError(405, \"Method not allowed\"); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", id: ++errorCount }));\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "errors-carinderia"
  },
  {
    "id": "api-errors-carinderia-8",
    "index": 188,
    "task": "You will add a check for duplicate names. This check happens after reading the item. If a name already exists, throw a 409 error. The code below does that. Run the checker to test it.\n\nIn server.js:\n```\n  if (items.some((existing) => existing.name === item.name)) throw new HttpError(409, \"Name already exists\");\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res);\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "409",
        "label": "POST /items with Adobo again answers 409",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "{\"name\":\"Adobo\",\"price\":1}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 409
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Check if any item has the same name. If yes, throw a 409 error."
      },
      {
        "level": 2,
        "text": "Put the code inside the createItem function after reading the item.\n\nIn server.js:\n```\n  if (items.some((existing) => existing.name === item.name)) throw new HttpError(409, \"Name already exists\");\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet errorCount = 0;\nclass HttpError extends Error { constructor(status, message) { super(message); this.status = status; } }\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) { text += chunk; if (text.length > 1000) throw new HttpError(413, \"Body too large\"); } return text; }\nasync function readJson(req) { if (!req.headers[\"content-type\"]?.includes(\"application/json\")) throw new HttpError(415, \"Send JSON\"); const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\nasync function createItem(req, res) {\n  const item = await readJson(req);\n  if (items.some((existing) => existing.name === item.name)) throw new HttpError(409, \"Name already exists\");\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  if (req.url === \"/teapot\") throw new HttpError(418, \"I am a teapot\");\n  if (req.url === \"/items\" && ![\"GET\", \"POST\"].includes(req.method)) { res.setHeader(\"Allow\", \"GET, POST\"); throw new HttpError(405, \"Method not allowed\"); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", id: ++errorCount }));\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "errors-carinderia"
  },
  {
    "id": "api-errors-carinderia-9",
    "index": 189,
    "task": "You will change the catch block at the bottom. This block handles errors. It now includes the status code in the error body. The code below does that. Run the checker to test it.\n\nIn server.js:\n```\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", status: error.status ?? 500, id: ++errorCount }));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res);\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "status",
        "label": "The /teapot error body includes status 418",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/teapot"
          }
        ],
        "bodyContains": "\"status\":418"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Add the status code to the error body so it shows in the response."
      },
      {
        "level": 2,
        "text": "Put the code inside the catch block at the bottom of server.js.\n\nIn server.js:\n```\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", status: error.status ?? 500, id: ++errorCount }));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet errorCount = 0;\nclass HttpError extends Error { constructor(status, message) { super(message); this.status = status; } }\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) { text += chunk; if (text.length > 1000) throw new HttpError(413, \"Body too large\"); } return text; }\nasync function readJson(req) { if (!req.headers[\"content-type\"]?.includes(\"application/json\")) throw new HttpError(415, \"Send JSON\"); const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\nasync function createItem(req, res) {\n  const item = await readJson(req);\n  if (items.some((existing) => existing.name === item.name)) throw new HttpError(409, \"Name already exists\");\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  if (req.url === \"/teapot\") throw new HttpError(418, \"I am a teapot\");\n  if (req.url === \"/items\" && ![\"GET\", \"POST\"].includes(req.method)) { res.setHeader(\"Allow\", \"GET, POST\"); throw new HttpError(405, \"Method not allowed\"); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", status: error.status ?? 500, id: ++errorCount }));\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "errors-carinderia"
  },
  {
    "id": "api-errors-carinderia-10",
    "index": 190,
    "task": "You will replace the last line of handle. This line now throws a 404 error with a message that says what method and path were used. The code below does that. Run the checker to test it.\n\nIn server.js:\n```\n  throw new HttpError(404, `No route for ${req.method} ${req.url}`);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res);\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "route",
        "label": "GET /nope answers No route for GET /nope",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/nope"
          }
        ],
        "status": 404,
        "bodyContains": "No route for GET /nope"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Throw a 404 error with a message that includes the method and path."
      },
      {
        "level": 2,
        "text": "Put the code inside the handle function, replacing the last line.\n\nIn server.js:\n```\n  throw new HttpError(404, `No route for ${req.method} ${req.url}`);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Adobo\", price: 80 }, { id: 2, name: \"Pancit\", price: 60 }, { id: 3, name: \"Lumpia\", price: 15 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet errorCount = 0;\nclass HttpError extends Error { constructor(status, message) { super(message); this.status = status; } }\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) { text += chunk; if (text.length > 1000) throw new HttpError(413, \"Body too large\"); } return text; }\nasync function readJson(req) { if (!req.headers[\"content-type\"]?.includes(\"application/json\")) throw new HttpError(415, \"Send JSON\"); const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\nasync function createItem(req, res) {\n  const item = await readJson(req);\n  if (items.some((existing) => existing.name === item.name)) throw new HttpError(409, \"Name already exists\");\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  if (req.url === \"/teapot\") throw new HttpError(418, \"I am a teapot\");\n  if (req.url === \"/items\" && ![\"GET\", \"POST\"].includes(req.method)) { res.setHeader(\"Allow\", \"GET, POST\"); throw new HttpError(405, \"Method not allowed\"); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  throw new HttpError(404, `No route for ${req.method} ${req.url}`);\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", status: error.status ?? 500, id: ++errorCount }));\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "errors-carinderia"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: api-design-carinderia.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-api-design-carinderia-1",
    "index": 191,
    "task": "You add a route for the API. This route answers requests to /v1/items. It sends the list of items. This lets clients get the menu. The code below does this. Run the checker to test it.\n\nIn server.js:\n```\n  if (url.pathname === \"/v1/items\") return send(res, 200, items);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst names = [\"Adobo\",\"Pancit\",\"Lumpia\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "v1",
        "label": "GET /v1/items answers the list",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/v1/items"
          }
        ],
        "status": 200,
        "bodyContains": "\"name\":\"Adobo 1\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The route checks the URL path. If it matches /v1/items, it sends the items."
      },
      {
        "level": 2,
        "text": "Add this code after the url line in server.js.\n\nIn server.js:\n```\n  if (url.pathname === \"/v1/items\") return send(res, 200, items);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst names = [\"Adobo\",\"Pancit\",\"Lumpia\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/v1/items\") return send(res, 200, items);\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "api-design-carinderia"
  },
  {
    "id": "api-api-design-carinderia-2",
    "index": 192,
    "task": "You add a page number. This lets clients choose which page to see. You change the route to slice the items. This shows five items per page. The code below does this. Run the checker to test it.\n\nIn server.js:\n```\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  if (url.pathname === \"/v1/items\") return send(res, 200, items.slice((page - 1) * 5, page * 5));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst names = [\"Adobo\",\"Pancit\",\"Lumpia\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "page2",
        "label": "GET /v1/items?page=2 answers items 6 to 10",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/v1/items?page=2"
          }
        ],
        "bodyContains": "[{\"id\":6,\"name\":\"Lumpia 6\",\"price\":30},{\"id\":7,\"name\":\"Adobo 7\",\"price\":35},{\"id\":8,\"name\":\"Pancit 8\",\"price\":40},{\"id\":9,\"name\":\"Lumpia 9\",\"price\":45},{\"id\":10,\"name\":\"Adobo 10\",\"price\":50}]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The page number comes from the URL's ?page query. Use it to pick items."
      },
      {
        "level": 2,
        "text": "Add this code after the url line in server.js. Then change the route.\n\nIn server.js:\n```\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  if (url.pathname === \"/v1/items\") return send(res, 200, items.slice((page - 1) * 5, page * 5));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst names = [\"Adobo\",\"Pancit\",\"Lumpia\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  if (url.pathname === \"/v1/items\") return send(res, 200, items.slice((page - 1) * 5, page * 5));\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "api-design-carinderia"
  },
  {
    "id": "api-api-design-carinderia-3",
    "index": 193,
    "task": "You add a perPage number. This lets clients choose how many items to show. You change the route to use it. The code below does this. Run the checker to test it.\n\nIn server.js:\n```\n  const perPage = Number(url.searchParams.get(\"perPage\") ?? 5);\n  if (url.pathname === \"/v1/items\") return send(res, 200, items.slice((page - 1) * perPage, page * perPage));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst names = [\"Adobo\",\"Pancit\",\"Lumpia\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "per",
        "label": "GET /v1/items?perPage=3 answers three items",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/v1/items?perPage=3"
          }
        ],
        "bodyContains": "[{\"id\":1,\"name\":\"Adobo 1\",\"price\":5},{\"id\":2,\"name\":\"Pancit 2\",\"price\":10},{\"id\":3,\"name\":\"Lumpia 3\",\"price\":15}]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The perPage number comes from ?perPage. Use it to slice the items."
      },
      {
        "level": 2,
        "text": "Add this code after the url line in server.js. Then change the route.\n\nIn server.js:\n```\n  const perPage = Number(url.searchParams.get(\"perPage\") ?? 5);\n  if (url.pathname === \"/v1/items\") return send(res, 200, items.slice((page - 1) * perPage, page * perPage));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst names = [\"Adobo\",\"Pancit\",\"Lumpia\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  const perPage = Number(url.searchParams.get(\"perPage\") ?? 5);\n  if (url.pathname === \"/v1/items\") return send(res, 200, items.slice((page - 1) * perPage, page * perPage));\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "api-design-carinderia"
  },
  {
    "id": "api-api-design-carinderia-4",
    "index": 194,
    "task": "You add a data object. This wraps the sliced items. You also add page and perPage. The route now sends this object. The code below does this. Run the checker to test it.\n\nIn server.js:\n```\n  const data = items.slice((page - 1) * perPage, page * perPage);\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst names = [\"Adobo\",\"Pancit\",\"Lumpia\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "envelope",
        "label": "The answer says page 2 and perPage 5",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/v1/items?page=2"
          }
        ],
        "bodyContains": "\"page\":2,\"perPage\":5"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Wrap the sliced items in an object with data, page, and perPage."
      },
      {
        "level": 2,
        "text": "Add this code after the url line in server.js. Then change the route.\n\nIn server.js:\n```\n  const data = items.slice((page - 1) * perPage, page * perPage);\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst names = [\"Adobo\",\"Pancit\",\"Lumpia\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  const perPage = Number(url.searchParams.get(\"perPage\") ?? 5);\n  const data = items.slice((page - 1) * perPage, page * perPage);\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage });\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "api-design-carinderia"
  },
  {
    "id": "api-api-design-carinderia-5",
    "index": 195,
    "task": "You add a total number. This shows how many items are in the menu. You change the route to include it. The code below does this. Run the checker to test it.\n\nIn server.js:\n```\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst names = [\"Adobo\",\"Pancit\",\"Lumpia\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "total",
        "label": "The answer says total 12",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/v1/items"
          }
        ],
        "bodyContains": "\"total\":12"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Add total: items.length to the answer object. This tells clients how many items there are."
      },
      {
        "level": 2,
        "text": "Change the route to send this object.\n\nIn server.js:\n```\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst names = [\"Adobo\",\"Pancit\",\"Lumpia\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  const perPage = Number(url.searchParams.get(\"perPage\") ?? 5);\n  const data = items.slice((page - 1) * perPage, page * perPage);\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length });\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "api-design-carinderia"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: api-design-carinderia.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-api-design-carinderia-6",
    "index": 196,
    "task": "Add a line for next. This line tells the browser where to go if the user clicks next. The code below does this. Run the checker to test it. The checker will test if page 1 links to page 2 and if the last page has no next link.\n\nIn server.js:\n```\n  const next = page * perPage < items.length ? `/v1/items?page=${page + 1}&perPage=${perPage}` : null;\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length, next });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst names = [\"Adobo\",\"Pancit\",\"Lumpia\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "next",
        "label": "Page 1 links to page 2",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/v1/items"
          }
        ],
        "bodyContains": "\"next\":\"/v1/items?page=2&perPage=5\""
      },
      {
        "id": "last",
        "label": "The last page has no next link",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/v1/items?page=3"
          }
        ],
        "bodyContains": "\"next\":null"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The next link should point to the next page only if there is one."
      },
      {
        "level": 2,
        "text": "Add this line after the data line in server.js.\n\nIn server.js:\n```\n  const next = page * perPage < items.length ? `/v1/items?page=${page + 1}&perPage=${perPage}` : null;\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length, next });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst names = [\"Adobo\",\"Pancit\",\"Lumpia\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  const perPage = Number(url.searchParams.get(\"perPage\") ?? 5);\n  const data = items.slice((page - 1) * perPage, page * perPage);\n  const next = page * perPage < items.length ? `/v1/items?page=${page + 1}&perPage=${perPage}` : null;\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length, next });\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "api-design-carinderia"
  },
  {
    "id": "api-api-design-carinderia-7",
    "index": 197,
    "task": "Add a line for previous. This line tells the browser where to go if the user clicks back. The code below does this. Run the checker to test it. The checker will test if page 2 links back to page 1.\n\nIn server.js:\n```\n  const previous = page > 1 ? `/v1/items?page=${page - 1}&perPage=${perPage}` : null;\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length, next, previous });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst names = [\"Adobo\",\"Pancit\",\"Lumpia\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "previous",
        "label": "Page 2 links back to page 1",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/v1/items?page=2"
          }
        ],
        "bodyContains": "\"previous\":\"/v1/items?page=1&perPage=5\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The previous link should point to the previous page only if there is one."
      },
      {
        "level": 2,
        "text": "Add this line after the next line in server.js.\n\nIn server.js:\n```\n  const previous = page > 1 ? `/v1/items?page=${page - 1}&perPage=${perPage}` : null;\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length, next, previous });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst names = [\"Adobo\",\"Pancit\",\"Lumpia\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  const perPage = Number(url.searchParams.get(\"perPage\") ?? 5);\n  const data = items.slice((page - 1) * perPage, page * perPage);\n  const next = page * perPage < items.length ? `/v1/items?page=${page + 1}&perPage=${perPage}` : null;\n  const previous = page > 1 ? `/v1/items?page=${page - 1}&perPage=${perPage}` : null;\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length, next, previous });\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "api-design-carinderia"
  },
  {
    "id": "api-api-design-carinderia-8",
    "index": 198,
    "task": "Add a line to check if the page number is a whole number 1 or more. If not, send a 400 error. The code below does this. Run the checker to test it. The checker will test if GET /v1/items?page=0 answers 400.\n\nIn server.js:\n```\n  if (!Number.isInteger(page) || page < 1) return send(res, 400, { error: \"page must be a whole number 1 or more\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst names = [\"Adobo\",\"Pancit\",\"Lumpia\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "bad",
        "label": "GET /v1/items?page=0 answers 400",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/v1/items?page=0"
          }
        ],
        "status": 400
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Check if the page is a whole number and at least 1 before sending data."
      },
      {
        "level": 2,
        "text": "Add this line after the page line in server.js.\n\nIn server.js:\n```\n  if (!Number.isInteger(page) || page < 1) return send(res, 400, { error: \"page must be a whole number 1 or more\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst names = [\"Adobo\",\"Pancit\",\"Lumpia\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  if (!Number.isInteger(page) || page < 1) return send(res, 400, { error: \"page must be a whole number 1 or more\" });\n  const perPage = Number(url.searchParams.get(\"perPage\") ?? 5);\n  const data = items.slice((page - 1) * perPage, page * perPage);\n  const next = page * perPage < items.length ? `/v1/items?page=${page + 1}&perPage=${perPage}` : null;\n  const previous = page > 1 ? `/v1/items?page=${page - 1}&perPage=${perPage}` : null;\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length, next, previous });\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "api-design-carinderia"
  },
  {
    "id": "api-api-design-carinderia-9",
    "index": 199,
    "task": "Change the perPage line to limit the number of items per page to 10. The code below does this. Run the checker to test it. The checker will test if GET /v1/items?perPage=100 answers perPage 10.\n\nIn server.js:\n```\n  const perPage = Math.min(Number(url.searchParams.get(\"perPage\") ?? 5), 10);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst names = [\"Adobo\",\"Pancit\",\"Lumpia\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "cap",
        "label": "GET /v1/items?perPage=100 answers perPage 10",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/v1/items?perPage=100"
          }
        ],
        "bodyContains": "\"perPage\":10,\"total\":12"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use Math.min to cap the perPage value at 10."
      },
      {
        "level": 2,
        "text": "Change the perPage line in server.js to use the new code.\n\nIn server.js:\n```\n  const perPage = Math.min(Number(url.searchParams.get(\"perPage\") ?? 5), 10);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst names = [\"Adobo\",\"Pancit\",\"Lumpia\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  if (!Number.isInteger(page) || page < 1) return send(res, 400, { error: \"page must be a whole number 1 or more\" });\n  const perPage = Math.min(Number(url.searchParams.get(\"perPage\") ?? 5), 10);\n  const data = items.slice((page - 1) * perPage, page * perPage);\n  const next = page * perPage < items.length ? `/v1/items?page=${page + 1}&perPage=${perPage}` : null;\n  const previous = page > 1 ? `/v1/items?page=${page - 1}&perPage=${perPage}` : null;\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length, next, previous });\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "api-design-carinderia"
  },
  {
    "id": "api-api-design-carinderia-10",
    "index": 200,
    "task": "Change the old /items route to add a Deprecation header. This tells users the route is old and should not be used. The code below does this. Run the checker to test it. The checker will test if GET /items sends Deprecation: true.\n\nIn server.js:\n```\n  if (url.pathname === \"/items\") { res.setHeader(\"Deprecation\", \"true\"); return send(res, 200, items); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Carinderia API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst names = [\"Adobo\",\"Pancit\",\"Lumpia\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "deprecated",
        "label": "GET /items sends Deprecation: true",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "header": {
          "name": "deprecation",
          "value": "true"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Add the header before sending the data for the old route."
      },
      {
        "level": 2,
        "text": "Change the old /items route in server.js to use the new code.\n\nIn server.js:\n```\n  if (url.pathname === \"/items\") { res.setHeader(\"Deprecation\", \"true\"); return send(res, 200, items); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst names = [\"Adobo\",\"Pancit\",\"Lumpia\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  if (!Number.isInteger(page) || page < 1) return send(res, 400, { error: \"page must be a whole number 1 or more\" });\n  const perPage = Math.min(Number(url.searchParams.get(\"perPage\") ?? 5), 10);\n  const data = items.slice((page - 1) * perPage, page * perPage);\n  const next = page * perPage < items.length ? `/v1/items?page=${page + 1}&perPage=${perPage}` : null;\n  const previous = page > 1 ? `/v1/items?page=${page - 1}&perPage=${perPage}` : null;\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length, next, previous });\n  if (url.pathname === \"/items\") { res.setHeader(\"Deprecation\", \"true\"); return send(res, 200, items); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "api-design-carinderia"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: first-server-barangay.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-first-server-barangay-1",
    "index": 201,
    "task": "Open server.js. Change the res.end line to say \"Barangay Office API\". This tells every visitor what your server does. The code below shows what to write. Run the checker to test it.\n\nIn server.js:\n```\n  res.end(\"Barangay Office API\");\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "root",
        "label": "GET / answers Barangay Office API",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/"
          }
        ],
        "status": 200,
        "bodyContains": "Barangay Office API"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Every request gets this message. Make sure it's exactly as shown."
      },
      {
        "level": 2,
        "text": "Put this line inside the handler, right after the req and res lines.\n\nIn server.js:\n```\n  res.end(\"Barangay Office API\");\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Barangay Office API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "first-server-barangay"
  },
  {
    "id": "api-first-server-barangay-2",
    "index": 202,
    "task": "Add one line above res.end. Set the header to say the answer is plain text. This helps browsers and tools know how to read the answer. The code below shows what to write. Run the checker to test it.\n\nIn server.js:\n```\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "type",
        "label": "GET / says its body is plain text",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/"
          }
        ],
        "header": {
          "name": "content-type",
          "value": "text/plain"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Set the header before sending the answer. This tells the receiver the format."
      },
      {
        "level": 2,
        "text": "Put this line right above res.end, inside the handler.\n\nIn server.js:\n```\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.end(\"Barangay Office API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "first-server-barangay"
  },
  {
    "id": "api-first-server-barangay-3",
    "index": 203,
    "task": "Add one line above res.end. Set the X-Place header to \"barangay\". This tells users where the server is. The code below shows what to write. Run the checker to test it.\n\nIn server.js:\n```\n  res.setHeader(\"X-Place\", \"barangay\");\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "custom",
        "label": "GET / sends X-Place: barangay",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/"
          }
        ],
        "header": {
          "name": "x-place",
          "value": "barangay"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "This header is for your own use. It helps you track where the server is."
      },
      {
        "level": 2,
        "text": "Put this line right above res.end, inside the handler.\n\nIn server.js:\n```\n  res.setHeader(\"X-Place\", \"barangay\");\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.setHeader(\"X-Place\", \"barangay\");\n  res.end(\"Barangay Office API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "first-server-barangay"
  },
  {
    "id": "api-first-server-barangay-4",
    "index": 204,
    "task": "Add one line at the top of the handler. Check if the request is for /health. If yes, send \"healthy\" and stop. This lets monitors know the server is working. The code below shows what to write. Run the checker to test it.\n\nIn server.js:\n```\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "health",
        "label": "GET /health answers healthy",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/health"
          }
        ],
        "status": 200,
        "bodyContains": "healthy"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Check the URL first. If it's /health, send \"healthy\" and stop."
      },
      {
        "level": 2,
        "text": "Put this line right after the req and res lines, before any other checks.\n\nIn server.js:\n```\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.setHeader(\"X-Place\", \"barangay\");\n  res.end(\"Barangay Office API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "first-server-barangay"
  },
  {
    "id": "api-first-server-barangay-5",
    "index": 205,
    "task": "Add one line right after the /health check. If the URL is not /, set status 404 and send \"Not found\". This tells users if they asked for something wrong. The code below shows what to write. Run the checker to test it.\n\nIn server.js:\n```\n  if (req.url !== \"/\") { res.statusCode = 404; res.end(\"Not found\"); return; }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "missing",
        "label": "GET /missing answers 404",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/missing"
          }
        ],
        "status": 404
      },
      {
        "id": "root",
        "label": "GET / still answers 200",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/"
          }
        ],
        "status": 200
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Check all URLs except / and /health. If not, send 404."
      },
      {
        "level": 2,
        "text": "Put this line right after the /health check, before res.end.\n\nIn server.js:\n```\n  if (req.url !== \"/\") { res.statusCode = 404; res.end(\"Not found\"); return; }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n  if (req.url !== \"/\") { res.statusCode = 404; res.end(\"Not found\"); return; }\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.setHeader(\"X-Place\", \"barangay\");\n  res.end(\"Barangay Office API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 5,
    "projectId": "first-server-barangay"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: first-server-barangay.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-first-server-barangay-6",
    "index": 206,
    "task": "You will stop other methods from working. Add this line at the very top of the handler. It says: if the method is not GET, send a 405 error. This stops browsers from using POST or PUT. The checker will test this.\n\nIn server.js:\n```\n  if (req.method !== \"GET\") { res.statusCode = 405; res.end(\"Method not allowed\"); return; }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "post",
        "label": "POST / answers 405",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/",
            "body": "{}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 405
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Think of this as a guard: it stops any method except GET."
      },
      {
        "level": 2,
        "text": "Add it at the very top of the handler, right after the imports.\n\nIn server.js:\n```\n  if (req.method !== \"GET\") { res.statusCode = 405; res.end(\"Method not allowed\"); return; }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.method !== \"GET\") { res.statusCode = 405; res.end(\"Method not allowed\"); return; }\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n  if (req.url !== \"/\") { res.statusCode = 404; res.end(\"Not found\"); return; }\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.setHeader(\"X-Place\", \"barangay\");\n  res.end(\"Barangay Office API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "first-server-barangay"
  },
  {
    "id": "api-first-server-barangay-7",
    "index": 207,
    "task": "You will answer /info with a JSON object. Add this line right after the /health line. It sends a JSON object with the name 'Barangay Office'. This helps users know what service they are using. The checker will test this.\n\nIn server.js:\n```\n  if (req.url === \"/info\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify({ name: \"Barangay Office\" })); return; }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "info",
        "label": "GET /info answers JSON with the name",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/info"
          }
        ],
        "bodyContains": "\"name\":\"Barangay Office\"",
        "header": {
          "name": "content-type",
          "value": "application/json"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "This line sends a JSON object with the name of the office."
      },
      {
        "level": 2,
        "text": "Add it right after the /health line, before the 404 line.\n\nIn server.js:\n```\n  if (req.url === \"/info\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify({ name: \"Barangay Office\" })); return; }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.method !== \"GET\") { res.statusCode = 405; res.end(\"Method not allowed\"); return; }\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n  if (req.url === \"/info\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify({ name: \"Barangay Office\" })); return; }\n  if (req.url !== \"/\") { res.statusCode = 404; res.end(\"Not found\"); return; }\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.setHeader(\"X-Place\", \"barangay\");\n  res.end(\"Barangay Office API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "first-server-barangay"
  },
  {
    "id": "api-first-server-barangay-8",
    "index": 208,
    "task": "You will count how many times the server is used. Add a counter above the server. Add a /visits route before the 404 line. This route will show the count. The checker will test this after two requests.\n\nIn server.js:\n```\nlet visits = 0;\n  visits += 1;\n  if (req.url === \"/visits\") { res.end(String(visits)); return; }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "visits",
        "label": "After two requests, GET /visits answers 3",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/"
          },
          {
            "method": "GET",
            "path": "/"
          },
          {
            "method": "GET",
            "path": "/visits"
          }
        ],
        "bodyContains": "3"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Keep a counter that starts at 0 and adds 1 for each request."
      },
      {
        "level": 2,
        "text": "Add the counter above the server, and the /visits route before the 404 line.\n\nIn server.js:\n```\nlet visits = 0;\n  visits += 1;\n  if (req.url === \"/visits\") { res.end(String(visits)); return; }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nlet visits = 0;\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.method !== \"GET\") { res.statusCode = 405; res.end(\"Method not allowed\"); return; }\n  visits += 1;\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n  if (req.url === \"/info\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify({ name: \"Barangay Office\" })); return; }\n  if (req.url === \"/visits\") { res.end(String(visits)); return; }\n  if (req.url !== \"/\") { res.statusCode = 404; res.end(\"Not found\"); return; }\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.setHeader(\"X-Place\", \"barangay\");\n  res.end(\"Barangay Office API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 5,
    "projectId": "first-server-barangay"
  },
  {
    "id": "api-first-server-barangay-9",
    "index": 209,
    "task": "You will tell browsers not to store any answer. Add this line at the very top of the handler. It sets the Cache-Control header to 'no-store'. This means browsers won't save the answer. The checker will test this.\n\nIn server.js:\n```\n  res.setHeader(\"Cache-Control\", \"no-store\");\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "cache",
        "label": "GET / sends Cache-Control: no-store",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/"
          }
        ],
        "header": {
          "name": "cache-control",
          "value": "no-store"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "This line tells browsers not to save any answer."
      },
      {
        "level": 2,
        "text": "Add it at the very top of the handler, right after the method check.\n\nIn server.js:\n```\n  res.setHeader(\"Cache-Control\", \"no-store\");\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nlet visits = 0;\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.setHeader(\"Cache-Control\", \"no-store\");\n  if (req.method !== \"GET\") { res.statusCode = 405; res.end(\"Method not allowed\"); return; }\n  visits += 1;\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n  if (req.url === \"/info\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify({ name: \"Barangay Office\" })); return; }\n  if (req.url === \"/visits\") { res.end(String(visits)); return; }\n  if (req.url !== \"/\") { res.statusCode = 404; res.end(\"Not found\"); return; }\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.setHeader(\"X-Place\", \"barangay\");\n  res.end(\"Barangay Office API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "first-server-barangay"
  },
  {
    "id": "api-first-server-barangay-10",
    "index": 210,
    "task": "You will read a name from the query string at /hello. Add two lines right before the 404 line. The first line creates a URL object. The second line gets the name from the query string, or uses 'friend' if none is given. The checker will test this with and without a name.\n\nIn server.js:\n```\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/hello\") { res.end(`Hello, ${url.searchParams.get(\"name\") ?? \"friend\"}`); return; }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "hello",
        "label": "GET /hello?name=Ana answers Hello, Ana",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/hello?name=Ana"
          }
        ],
        "bodyContains": "Hello, Ana"
      },
      {
        "id": "default",
        "label": "GET /hello answers Hello, friend",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/hello"
          }
        ],
        "bodyContains": "Hello, friend"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use the URL object to get the name from the query string."
      },
      {
        "level": 2,
        "text": "Add these two lines right before the 404 line.\n\nIn server.js:\n```\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/hello\") { res.end(`Hello, ${url.searchParams.get(\"name\") ?? \"friend\"}`); return; }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nlet visits = 0;\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.setHeader(\"Cache-Control\", \"no-store\");\n  if (req.method !== \"GET\") { res.statusCode = 405; res.end(\"Method not allowed\"); return; }\n  visits += 1;\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n  if (req.url === \"/info\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify({ name: \"Barangay Office\" })); return; }\n  if (req.url === \"/visits\") { res.end(String(visits)); return; }\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/hello\") { res.end(`Hello, ${url.searchParams.get(\"name\") ?? \"friend\"}`); return; }\n  if (req.url !== \"/\") { res.statusCode = 404; res.end(\"Not found\"); return; }\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.setHeader(\"X-Place\", \"barangay\");\n  res.end(\"Barangay Office API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 6,
    "projectId": "first-server-barangay"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: json-routes-barangay.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-json-routes-barangay-1",
    "index": 211,
    "task": "You will add a line to handle the /items route. This line sends the whole list as JSON. It goes at the top of the handler. The code below does this. Run the checker to test it.\n\nIn server.js:\n```\n  if (req.url === \"/items\") return send(res, 200, items);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "list",
        "label": "GET /items answers the list as JSON",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "bodyContains": "\"name\":\"Clearance\"",
        "header": {
          "name": "content-type",
          "value": "application/json"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The send helper sends data with the right HTTP status and content type. Use it for every route."
      },
      {
        "level": 2,
        "text": "Put this line right after the first comment in server.js.\n\nIn server.js:\n```\n  if (req.url === \"/items\") return send(res, 200, items);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "json-routes-barangay"
  },
  {
    "id": "api-json-routes-barangay-2",
    "index": 212,
    "task": "You will add a line to handle /items/count. This line sends the count of items as a JSON object. It goes after the /items line. The code below does this. Run the checker to test it.\n\nIn server.js:\n```\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "count",
        "label": "GET /items/count answers {\"count\":3}",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/count"
          }
        ],
        "bodyContains": "{\"count\":3}"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The items array has a length. Use that to count how many items there are."
      },
      {
        "level": 2,
        "text": "Put this line after the /items line in server.js.\n\nIn server.js:\n```\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "json-routes-barangay"
  },
  {
    "id": "api-json-routes-barangay-3",
    "index": 213,
    "task": "You will add a line to handle /items/first. This line sends only the first item as JSON. It goes after the /items/count line. The code below does this. Run the checker to test it.\n\nIn server.js:\n```\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "first",
        "label": "GET /items/first answers Clearance",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/first"
          }
        ],
        "bodyContains": "{\"id\":1,\"name\":\"Clearance\",\"price\":50}"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The first item is at index 0 in the array. Use that to get it."
      },
      {
        "level": 2,
        "text": "Put this line after the /items/count line in server.js.\n\nIn server.js:\n```\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "json-routes-barangay"
  },
  {
    "id": "api-json-routes-barangay-4",
    "index": 214,
    "task": "You will add a line to handle /items/names. This line sends only the names as a JSON array. It goes after the /items/first line. The code below does this. Run the checker to test it.\n\nIn server.js:\n```\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "names",
        "label": "GET /items/names answers the three names",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/names"
          }
        ],
        "bodyContains": "[\"Clearance\",\"Permit\",\"ID\"]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use map to go through each item and pick its name. Then send the names as an array."
      },
      {
        "level": 2,
        "text": "Put this line after the /items/first line in server.js.\n\nIn server.js:\n```\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "json-routes-barangay"
  },
  {
    "id": "api-json-routes-barangay-5",
    "index": 215,
    "task": "You will add a line to handle /items/total. This line sends the sum of all prices as a JSON object. It goes after the /items/names line. The code below does this. Run the checker to test it.\n\nIn server.js:\n```\n  if (req.url === \"/items/total\") return send(res, 200, { total: items.reduce((sum, item) => sum + item.price, 0) });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "total",
        "label": "GET /items/total answers 370",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/total"
          }
        ],
        "bodyContains": "{\"total\":370}"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use reduce to add up all the prices. Start with 0. Then send the total."
      },
      {
        "level": 2,
        "text": "Put this line after the /items/names line in server.js.\n\nIn server.js:\n```\n  if (req.url === \"/items/total\") return send(res, 200, { total: items.reduce((sum, item) => sum + item.price, 0) });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n  if (req.url === \"/items/total\") return send(res, 200, { total: items.reduce((sum, item) => sum + item.price, 0) });\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "json-routes-barangay"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: json-routes-barangay.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-json-routes-barangay-6",
    "index": 216,
    "task": "If someone asks for a path that does not exist, the server must say 404. This is important so users know the request failed. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  send(res, 404, { error: \"Not found\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "missing",
        "label": "GET /nope answers 404 with a JSON error",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/nope"
          }
        ],
        "status": 404,
        "bodyContains": "{\"error\":\"Not found\"}"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "When a path is not found, send a 404 status and a JSON error message."
      },
      {
        "level": 2,
        "text": "Put the code at the end of the handler, right before the 404 line.\n\nIn server.js:\n```\n  send(res, 404, { error: \"Not found\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n  if (req.url === \"/items/total\") return send(res, 200, { total: items.reduce((sum, item) => sum + item.price, 0) });\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "json-routes-barangay"
  },
  {
    "id": "api-json-routes-barangay-7",
    "index": 217,
    "task": "When someone asks for the root path, the server must return a JSON object with the API's name and its routes. This helps users understand what the API offers. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (req.url === \"/\") return send(res, 200, { name: \"Barangay Office\", routes: [\"/items\"] });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "root",
        "label": "GET / lists the routes",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/"
          }
        ],
        "status": 200,
        "bodyContains": "\"routes\":[\"/items\"]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Check if the URL is exactly \"/\". If yes, return a JSON object with the name and routes."
      },
      {
        "level": 2,
        "text": "Put the code at the top of the handler, right after the first line.\n\nIn server.js:\n```\n  if (req.url === \"/\") return send(res, 200, { name: \"Barangay Office\", routes: [\"/items\"] });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/\") return send(res, 200, { name: \"Barangay Office\", routes: [\"/items\"] });\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n  if (req.url === \"/items/total\") return send(res, 200, { total: items.reduce((sum, item) => sum + item.price, 0) });\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "json-routes-barangay"
  },
  {
    "id": "api-json-routes-barangay-8",
    "index": 218,
    "task": "When someone asks for /items/cheapest, the server must return the item with the lowest price. This helps users find the cheapest item quickly. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (req.url === \"/items/cheapest\") return send(res, 200, [...items].sort((a, b) => a.price - b.price)[0]);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "cheapest",
        "label": "GET /items/cheapest answers ID",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/cheapest"
          }
        ],
        "bodyContains": "\"name\":\"ID\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Sort the items by price and pick the first one. Use the filter method if needed."
      },
      {
        "level": 2,
        "text": "Put the code before the 404 line, right after the /items route.\n\nIn server.js:\n```\n  if (req.url === \"/items/cheapest\") return send(res, 200, [...items].sort((a, b) => a.price - b.price)[0]);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/\") return send(res, 200, { name: \"Barangay Office\", routes: [\"/items\"] });\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n  if (req.url === \"/items/total\") return send(res, 200, { total: items.reduce((sum, item) => sum + item.price, 0) });\n  if (req.url === \"/items/cheapest\") return send(res, 200, [...items].sort((a, b) => a.price - b.price)[0]);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 5,
    "projectId": "json-routes-barangay"
  },
  {
    "id": "api-json-routes-barangay-9",
    "index": 219,
    "task": "When someone asks for /items/pretty, the server must return the items as indented JSON. This makes the data easier to read. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (req.url === \"/items/pretty\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify(items, null, 2)); return; }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "pretty",
        "label": "GET /items/pretty answers indented JSON",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/pretty"
          }
        ],
        "bodyContains": "[\n  {\n    \"id\": 1"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Set the Content-Type header to application/json. Then send the JSON with 2 spaces for indentation."
      },
      {
        "level": 2,
        "text": "Put the code before the 404 line, right after the /items route.\n\nIn server.js:\n```\n  if (req.url === \"/items/pretty\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify(items, null, 2)); return; }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/\") return send(res, 200, { name: \"Barangay Office\", routes: [\"/items\"] });\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n  if (req.url === \"/items/total\") return send(res, 200, { total: items.reduce((sum, item) => sum + item.price, 0) });\n  if (req.url === \"/items/cheapest\") return send(res, 200, [...items].sort((a, b) => a.price - b.price)[0]);\n  if (req.url === \"/items/pretty\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify(items, null, 2)); return; }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 5,
    "projectId": "json-routes-barangay"
  },
  {
    "id": "api-json-routes-barangay-10",
    "index": 220,
    "task": "When someone asks for /items/expensive, the server must return only items priced over 40. This helps users find expensive items. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (req.url === \"/items/expensive\") return send(res, 200, items.filter((item) => item.price > 40));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "expensive",
        "label": "GET /items/expensive answers Clearance and Permit",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/expensive"
          }
        ],
        "bodyContains": "\"name\":\"Clearance\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use the filter method to keep only items with price greater than 40."
      },
      {
        "level": 2,
        "text": "Put the code before the 404 line, right after the /items route.\n\nIn server.js:\n```\n  if (req.url === \"/items/expensive\") return send(res, 200, items.filter((item) => item.price > 40));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/\") return send(res, 200, { name: \"Barangay Office\", routes: [\"/items\"] });\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n  if (req.url === \"/items/total\") return send(res, 200, { total: items.reduce((sum, item) => sum + item.price, 0) });\n  if (req.url === \"/items/cheapest\") return send(res, 200, [...items].sort((a, b) => a.price - b.price)[0]);\n  if (req.url === \"/items/pretty\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify(items, null, 2)); return; }\n  if (req.url === \"/items/expensive\") return send(res, 200, items.filter((item) => item.price > 40));\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "json-routes-barangay"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: request-bodies-barangay.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-request-bodies-barangay-1",
    "index": 221,
    "task": "You will read what the client sends. The code below reads each part and joins them. Add this code above the server. Then add a route for /echo at the top of the handler. This lets you test reading text from the client.\n\nIn server.js:\n```\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "echo",
        "label": "POST /echo with hello answers it back",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/echo",
            "body": "hello",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "bodyContains": "\"received\":\"hello\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The code reads chunks one by one until the client stops sending."
      },
      {
        "level": 2,
        "text": "Add the route /echo at the top of the handler section in server.js.\n\nIn server.js:\n```\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "request-bodies-barangay"
  },
  {
    "id": "api-request-bodies-barangay-2",
    "index": 222,
    "task": "You will turn text into an object. Add one line after the /echo route. This line uses JSON.parse to turn the text into an object. The checker will test if it works.\n\nIn server.js:\n```\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "json",
        "label": "POST /echo-json answers the object it received",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/echo-json",
            "body": "{\"name\":\"Ana\"}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "bodyContains": "\"name\":\"Ana\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use JSON.parse to turn the text into an object. It needs to be a string first."
      },
      {
        "level": 2,
        "text": "Add this line right after the /echo route in server.js.\n\nIn server.js:\n```\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "request-bodies-barangay"
  },
  {
    "id": "api-request-bodies-barangay-3",
    "index": 223,
    "task": "You will answer POST /items with status 201. Replace the line inside createItem with two lines. The first line parses the text into an object. The second line sends the object back with status 201. The checker will test this.\n\nIn server.js:\n```\n  const data = JSON.parse(await readBody(req));\n  return send(res, 201, data);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "created",
        "label": "POST /items answers 201 with the item",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "{\"name\":\"Tea\",\"price\":12}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 201,
        "bodyContains": "\"name\":\"Tea\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Parse the text into an object first, then send it back with status 201."
      },
      {
        "level": 2,
        "text": "Replace the single line in createItem with these two lines.\n\nIn server.js:\n```\n  const data = JSON.parse(await readBody(req));\n  return send(res, 201, data);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const data = JSON.parse(await readBody(req));\n  return send(res, 201, data);\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "request-bodies-barangay"
  },
  {
    "id": "api-request-bodies-barangay-4",
    "index": 224,
    "task": "You will store the new item so it shows up in the list. Add one line inside createItem before the return. This line adds the item to the items array. The checker will test if it appears in the list.\n\nIn server.js:\n```\n  items.push(data);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "stored",
        "label": "After POST /items, GET /items includes Tea",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "{\"name\":\"Tea\",\"price\":12}",
            "headers": {
              "Content-Type": "application/json"
            }
          },
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "bodyContains": "\"name\":\"Tea\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Add the item to the items array before sending the response."
      },
      {
        "level": 2,
        "text": "Add this line right before the return in createItem.\n\nIn server.js:\n```\n  items.push(data);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const data = JSON.parse(await readBody(req));\n  items.push(data);\n  return send(res, 201, data);\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "request-bodies-barangay"
  },
  {
    "id": "api-request-bodies-barangay-5",
    "index": 225,
    "task": "You will give each new item an id one higher than the last. Change createItem so each new item gets the next id. Add the id to the item object, then push it. The checker will test if the new item has id 4.\n\nIn server.js:\n```\n  const item = { id: items.length + 1, ...data };\n  items.push(item);\n  return send(res, 201, item);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "id",
        "label": "The new item answers with id 4",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "{\"name\":\"Tea\",\"price\":12}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "bodyContains": "\"id\":4"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use items.length + 1 to get the next id. Add it to the item object."
      },
      {
        "level": 2,
        "text": "Replace the createItem code with these three lines.\n\nIn server.js:\n```\n  const item = { id: items.length + 1, ...data };\n  items.push(item);\n  return send(res, 201, item);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const data = JSON.parse(await readBody(req));\n  const item = { id: items.length + 1, ...data };\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 5,
    "projectId": "request-bodies-barangay"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: request-bodies-barangay.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-request-bodies-barangay-6",
    "index": 226,
    "task": "If the body is not JSON, the server crashes. You fix this by catching the error. The code below stops the crash and sends a 400 error. Run the checker to test it.\n\nIn server.js:\n```\n  let data;\n  try { data = JSON.parse(await readBody(req)); } catch { return send(res, 400, { error: \"Body must be JSON\" }); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "bad",
        "label": "POST /items with broken JSON answers 400",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "not json",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 400,
        "bodyContains": "Body must be JSON"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "If JSON.parse fails, you must stop the server and send a 400 error."
      },
      {
        "level": 2,
        "text": "Put the code below after the try block, inside the catch block.\n\nIn server.js:\n```\n  let data;\n  try { data = JSON.parse(await readBody(req)); } catch { return send(res, 400, { error: \"Body must be JSON\" }); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  let data;\n  try { data = JSON.parse(await readBody(req)); } catch { return send(res, 400, { error: \"Body must be JSON\" }); }\n  const item = { id: items.length + 1, ...data };\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "request-bodies-barangay"
  },
  {
    "id": "api-request-bodies-barangay-7",
    "index": 227,
    "task": "If the name is missing or empty, the server lets it go. You fix this by checking the name. The code below checks if the name is a string and not empty. Run the checker to test it.\n\nIn server.js:\n```\n  if (typeof data.name !== \"string\" || !data.name.trim()) return send(res, 422, { error: \"name is required\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "noname",
        "label": "POST /items without a name answers 422",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "{\"price\":5}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 422,
        "bodyContains": "name is required"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Check if the name is a string and not empty before letting the server continue."
      },
      {
        "level": 2,
        "text": "Put the code below after the try block, before the price check.\n\nIn server.js:\n```\n  if (typeof data.name !== \"string\" || !data.name.trim()) return send(res, 422, { error: \"name is required\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  let data;\n  try { data = JSON.parse(await readBody(req)); } catch { return send(res, 400, { error: \"Body must be JSON\" }); }\n  if (typeof data.name !== \"string\" || !data.name.trim()) return send(res, 422, { error: \"name is required\" });\n  const item = { id: items.length + 1, ...data };\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "request-bodies-barangay"
  },
  {
    "id": "api-request-bodies-barangay-8",
    "index": 228,
    "task": "If the price is not a number or less than zero, the server lets it go. You fix this by checking the price. The code below checks if the price is a number and not negative. Run the checker to test it.\n\nIn server.js:\n```\n  if (!Number.isFinite(data.price) || data.price < 0) return send(res, 422, { error: \"price must be a number 0 or more\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "badprice",
        "label": "POST /items with price five answers 422",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "{\"name\":\"Tea\",\"price\":\"five\"}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 422,
        "bodyContains": "price must be a number"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Check if the price is a number and not less than zero before letting the server continue."
      },
      {
        "level": 2,
        "text": "Put the code below after the name check, before building the item.\n\nIn server.js:\n```\n  if (!Number.isFinite(data.price) || data.price < 0) return send(res, 422, { error: \"price must be a number 0 or more\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  let data;\n  try { data = JSON.parse(await readBody(req)); } catch { return send(res, 400, { error: \"Body must be JSON\" }); }\n  if (typeof data.name !== \"string\" || !data.name.trim()) return send(res, 422, { error: \"name is required\" });\n  if (!Number.isFinite(data.price) || data.price < 0) return send(res, 422, { error: \"price must be a number 0 or more\" });\n  const item = { id: items.length + 1, ...data };\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "request-bodies-barangay"
  },
  {
    "id": "api-request-bodies-barangay-9",
    "index": 229,
    "task": "If extra fields are in the body, the server stores them. You fix this by trimming the name and keeping only known fields. The code below builds the item with only name and price. Run the checker to test it.\n\nIn server.js:\n```\n  const item = { id: items.length + 1, name: data.name.trim(), price: data.price };\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "trim",
        "label": "The new item is stored with a trimmed name and no extra fields",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "{\"name\":\"  Tea  \",\"price\":12,\"secret\":\"x\"}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "bodyContains": "{\"id\":4,\"name\":\"Tea\",\"price\":12}"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Only keep the name and price fields. Trim the name to remove spaces."
      },
      {
        "level": 2,
        "text": "Put the code below where the item is built, replacing the old line.\n\nIn server.js:\n```\n  const item = { id: items.length + 1, name: data.name.trim(), price: data.price };\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  let data;\n  try { data = JSON.parse(await readBody(req)); } catch { return send(res, 400, { error: \"Body must be JSON\" }); }\n  if (typeof data.name !== \"string\" || !data.name.trim()) return send(res, 422, { error: \"name is required\" });\n  if (!Number.isFinite(data.price) || data.price < 0) return send(res, 422, { error: \"price must be a number 0 or more\" });\n  const item = { id: items.length + 1, name: data.name.trim(), price: data.price };\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "request-bodies-barangay"
  },
  {
    "id": "api-request-bodies-barangay-10",
    "index": 230,
    "task": "The server does not tell where the new item is. You fix this by adding a Location header. The code below adds the URL of the new item. Run the checker to test it.\n\nIn server.js:\n```\n  res.setHeader(\"Location\", `/items/${item.id}`);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "location",
        "label": "POST /items sends Location: /items/4",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "{\"name\":\"Tea\",\"price\":12}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "header": {
          "name": "location",
          "value": "/items/4"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Add a Location header with the URL of the new item before returning the response."
      },
      {
        "level": 2,
        "text": "Put the code below before the return statement in createItem.\n\nIn server.js:\n```\n  res.setHeader(\"Location\", `/items/${item.id}`);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  let data;\n  try { data = JSON.parse(await readBody(req)); } catch { return send(res, 400, { error: \"Body must be JSON\" }); }\n  if (typeof data.name !== \"string\" || !data.name.trim()) return send(res, 422, { error: \"name is required\" });\n  if (!Number.isFinite(data.price) || data.price < 0) return send(res, 422, { error: \"price must be a number 0 or more\" });\n  const item = { id: items.length + 1, name: data.name.trim(), price: data.price };\n  items.push(item);\n  res.setHeader(\"Location\", `/items/${item.id}`);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "request-bodies-barangay"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: one-item-barangay.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-one-item-barangay-1",
    "index": 231,
    "task": "You will read the item ID from the path. The code below finds the ID in /items/2. Add these two lines at the top of the handler. This lets you get one item by its ID. Run the checker to test it.\n\nIn server.js:\n```\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") return send(res, 200, items.find((item) => item.id === Number(match[1])));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "one",
        "label": "GET /items/2 answers Permit",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/2"
          }
        ],
        "bodyContains": "\"name\":\"Permit\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Look for the pattern in the URL to get the ID."
      },
      {
        "level": 2,
        "text": "Add the code at the top of the handler, right after the imports.\n\nIn server.js:\n```\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") return send(res, 200, items.find((item) => item.id === Number(match[1])));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") return send(res, 200, items.find((item) => item.id === Number(match[1])));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "one-item-barangay"
  },
  {
    "id": "api-one-item-barangay-2",
    "index": 232,
    "task": "Now add a line to check if the item exists. Change the GET line to send 404 if not found. This makes your server answer properly when asked for a missing item. Run the checker to test it.\n\nIn server.js:\n```\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "none",
        "label": "GET /items/99 answers 404",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/99"
          }
        ],
        "status": 404,
        "bodyContains": "Item not found"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Check if the item exists before sending the answer."
      },
      {
        "level": 2,
        "text": "Add the found line and change the GET line right after it.\n\nIn server.js:\n```\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "one-item-barangay"
  },
  {
    "id": "api-one-item-barangay-3",
    "index": 233,
    "task": "Add a line to delete an item when the method is DELETE. Set the status to 204 and end the response. This tells the client the item was removed. Run the checker to test it.\n\nIn server.js:\n```\n  if (found && req.method === \"DELETE\") { items.splice(items.indexOf(found), 1); res.statusCode = 204; return res.end(); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "deleted",
        "label": "DELETE /items/1 answers 204",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "DELETE",
            "path": "/items/1"
          }
        ],
        "status": 204
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Delete the item only if it exists and the method is DELETE."
      },
      {
        "level": 2,
        "text": "Add the code right after the GET line, under the found check.\n\nIn server.js:\n```\n  if (found && req.method === \"DELETE\") { items.splice(items.indexOf(found), 1); res.statusCode = 204; return res.end(); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"DELETE\") { items.splice(items.indexOf(found), 1); res.statusCode = 204; return res.end(); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "one-item-barangay"
  },
  {
    "id": "api-one-item-barangay-4",
    "index": 234,
    "task": "Add a line to answer 404 if you try to delete a non-existent item. This stops errors when the item is not found. Run the checker to test it.\n\nIn server.js:\n```\n  if (match && req.method === \"DELETE\") return send(res, 404, { error: \"Item not found\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "gone",
        "label": "DELETE /items/99 answers 404 Item not found",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "DELETE",
            "path": "/items/99"
          }
        ],
        "status": 404,
        "bodyContains": "Item not found"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Check if the item exists before deleting it."
      },
      {
        "level": 2,
        "text": "Add the code right after the DELETE line, before the delete action.\n\nIn server.js:\n```\n  if (match && req.method === \"DELETE\") return send(res, 404, { error: \"Item not found\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"DELETE\") { items.splice(items.indexOf(found), 1); res.statusCode = 204; return res.end(); }\n  if (match && req.method === \"DELETE\") return send(res, 404, { error: \"Item not found\" });\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "one-item-barangay"
  },
  {
    "id": "api-one-item-barangay-5",
    "index": 235,
    "task": "Add a line to handle PUT requests. Replace the item's fields with the new data. Send back the updated item. This lets clients update items. Run the checker to test it.\n\nIn server.js:\n```\n  if (found && req.method === \"PUT\") { Object.assign(found, JSON.parse(await readBody(req))); return send(res, 200, found); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "put",
        "label": "PUT /items/1 with price 99 answers the new price",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "PUT",
            "path": "/items/1",
            "body": "{\"price\":99}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "bodyContains": "\"price\":99"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use PUT to update the item's fields with the body data."
      },
      {
        "level": 2,
        "text": "Add the code after the DELETE lines, under the found check.\n\nIn server.js:\n```\n  if (found && req.method === \"PUT\") { Object.assign(found, JSON.parse(await readBody(req))); return send(res, 200, found); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"DELETE\") { items.splice(items.indexOf(found), 1); res.statusCode = 204; return res.end(); }\n  if (match && req.method === \"DELETE\") return send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"PUT\") { Object.assign(found, JSON.parse(await readBody(req))); return send(res, 200, found); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 5,
    "projectId": "one-item-barangay"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: one-item-barangay.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-one-item-barangay-6",
    "index": 236,
    "task": "You will change the PUT line in server.js. The client may send a different id, but your server must keep the original id. This keeps the item's identity safe. The code below shows the change. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (found && req.method === \"PUT\") { Object.assign(found, JSON.parse(await readBody(req)), { id: found.id }); return send(res, 200, found); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "keep",
        "label": "After PUT with id 50, GET /items/1 still works",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "PUT",
            "path": "/items/1",
            "body": "{\"id\":50,\"price\":1}",
            "headers": {
              "Content-Type": "application/json"
            }
          },
          {
            "method": "GET",
            "path": "/items/1"
          }
        ],
        "status": 200,
        "bodyContains": "\"id\":1"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The client may send a new id, but your server must not change the item's original id."
      },
      {
        "level": 2,
        "text": "Add this change before the PUT line in server.js.\n\nIn server.js:\n```\n  if (found && req.method === \"PUT\") { Object.assign(found, JSON.parse(await readBody(req)), { id: found.id }); return send(res, 200, found); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"DELETE\") { items.splice(items.indexOf(found), 1); res.statusCode = 204; return res.end(); }\n  if (match && req.method === \"DELETE\") return send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"PUT\") { Object.assign(found, JSON.parse(await readBody(req)), { id: found.id }); return send(res, 200, found); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "one-item-barangay"
  },
  {
    "id": "api-one-item-barangay-7",
    "index": 237,
    "task": "You will add one line before the PUT line. This line checks if the client sends a body that is not JSON. If so, the server answers 415. This stops bad data from breaking your server. The code below shows the change. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (found && req.method === \"PUT\" && !req.headers[\"content-type\"]?.includes(\"application/json\")) return send(res, 415, { error: \"Send JSON\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "type",
        "label": "PUT with a text/plain body answers 415",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "PUT",
            "path": "/items/1",
            "body": "price=99",
            "headers": {
              "Content-Type": "text/plain"
            }
          }
        ],
        "status": 415
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "If the client sends a body that is not JSON, your server must answer 415."
      },
      {
        "level": 2,
        "text": "Add this line before the PUT line in server.js.\n\nIn server.js:\n```\n  if (found && req.method === \"PUT\" && !req.headers[\"content-type\"]?.includes(\"application/json\")) return send(res, 415, { error: \"Send JSON\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"DELETE\") { items.splice(items.indexOf(found), 1); res.statusCode = 204; return res.end(); }\n  if (match && req.method === \"DELETE\") return send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"PUT\" && !req.headers[\"content-type\"]?.includes(\"application/json\")) return send(res, 415, { error: \"Send JSON\" });\n  if (found && req.method === \"PUT\") { Object.assign(found, JSON.parse(await readBody(req)), { id: found.id }); return send(res, 200, found); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "one-item-barangay"
  },
  {
    "id": "api-one-item-barangay-8",
    "index": 238,
    "task": "You will add one line before the 404 line. This line handles HEAD requests to /items. It sends only the X-Total-Count header and no body. This lets clients know how many items exist. The code below shows the change. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (req.url === \"/items\" && req.method === \"HEAD\") { res.setHeader(\"X-Total-Count\", String(items.length)); return res.end(); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "head",
        "label": "HEAD /items sends X-Total-Count: 3",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "HEAD",
            "path": "/items"
          }
        ],
        "header": {
          "name": "x-total-count",
          "value": "3"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "HEAD requests ask for headers only. Your server must send X-Total-Count and no body."
      },
      {
        "level": 2,
        "text": "Add this line before the 404 line in server.js.\n\nIn server.js:\n```\n  if (req.url === \"/items\" && req.method === \"HEAD\") { res.setHeader(\"X-Total-Count\", String(items.length)); return res.end(); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"DELETE\") { items.splice(items.indexOf(found), 1); res.statusCode = 204; return res.end(); }\n  if (match && req.method === \"DELETE\") return send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"PUT\" && !req.headers[\"content-type\"]?.includes(\"application/json\")) return send(res, 415, { error: \"Send JSON\" });\n  if (found && req.method === \"PUT\") { Object.assign(found, JSON.parse(await readBody(req)), { id: found.id }); return send(res, 200, found); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"HEAD\") { res.setHeader(\"X-Total-Count\", String(items.length)); return res.end(); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "one-item-barangay"
  },
  {
    "id": "api-one-item-barangay-9",
    "index": 239,
    "task": "You will add one line after the PUT line. This line handles OPTIONS requests. It sends an Allow header listing the allowed methods: GET, PUT, DELETE. This tells clients what they can do. The code below shows the change. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (match && req.method === \"OPTIONS\") { res.setHeader(\"Allow\", \"GET, PUT, DELETE\"); res.statusCode = 204; return res.end(); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "options",
        "label": "OPTIONS /items/1 lists GET, PUT, DELETE",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "OPTIONS",
            "path": "/items/1"
          }
        ],
        "header": {
          "name": "allow",
          "value": "GET, PUT, DELETE"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "OPTIONS requests ask what methods are allowed. Your server must list GET, PUT, DELETE."
      },
      {
        "level": 2,
        "text": "Add this line after the PUT line in server.js.\n\nIn server.js:\n```\n  if (match && req.method === \"OPTIONS\") { res.setHeader(\"Allow\", \"GET, PUT, DELETE\"); res.statusCode = 204; return res.end(); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"DELETE\") { items.splice(items.indexOf(found), 1); res.statusCode = 204; return res.end(); }\n  if (match && req.method === \"DELETE\") return send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"PUT\" && !req.headers[\"content-type\"]?.includes(\"application/json\")) return send(res, 415, { error: \"Send JSON\" });\n  if (found && req.method === \"PUT\") { Object.assign(found, JSON.parse(await readBody(req)), { id: found.id }); return send(res, 200, found); }\n  if (match && req.method === \"OPTIONS\") { res.setHeader(\"Allow\", \"GET, PUT, DELETE\"); res.statusCode = 204; return res.end(); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"HEAD\") { res.setHeader(\"X-Total-Count\", String(items.length)); return res.end(); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "one-item-barangay"
  },
  {
    "id": "api-one-item-barangay-10",
    "index": 240,
    "task": "You will add one line after the OPTIONS line. This line handles any other method on a single item. It answers 405 with an error message. This stops clients from using forbidden methods. The code below shows the change. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (match) return send(res, 405, { error: \"Method not allowed\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "post",
        "label": "POST /items/1 answers 405",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items/1",
            "body": "{}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 405
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "If the client uses a method not allowed, your server must answer 405."
      },
      {
        "level": 2,
        "text": "Add this line after the OPTIONS line in server.js.\n\nIn server.js:\n```\n  if (match) return send(res, 405, { error: \"Method not allowed\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"DELETE\") { items.splice(items.indexOf(found), 1); res.statusCode = 204; return res.end(); }\n  if (match && req.method === \"DELETE\") return send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"PUT\" && !req.headers[\"content-type\"]?.includes(\"application/json\")) return send(res, 415, { error: \"Send JSON\" });\n  if (found && req.method === \"PUT\") { Object.assign(found, JSON.parse(await readBody(req)), { id: found.id }); return send(res, 200, found); }\n  if (match && req.method === \"OPTIONS\") { res.setHeader(\"Allow\", \"GET, PUT, DELETE\"); res.statusCode = 204; return res.end(); }\n  if (match) return send(res, 405, { error: \"Method not allowed\" });\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"HEAD\") { res.setHeader(\"X-Total-Count\", String(items.length)); return res.end(); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "one-item-barangay"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: query-strings-barangay.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-query-strings-barangay-1",
    "index": 241,
    "task": "You add two lines after the url line. You change the /items line to send the list. This lets you filter items by price. The code below does that. Run the checker to test it.\n\nIn server.js:\n```\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.pathname === \"/items\") return send(res, 200, list);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "max",
        "label": "GET /items?max=50 answers only the items up to 50",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items?max=50"
          }
        ],
        "bodyContains": "[{\"id\":1,\"name\":\"Clearance\",\"price\":50},{\"id\":3,\"name\":\"ID\",\"price\":20}]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "You filter items by checking if their price is less than or equal to the max value."
      },
      {
        "level": 2,
        "text": "Add the code after the url line and before the /items line.\n\nIn server.js:\n```\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.pathname === \"/items\") return send(res, 200, list);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.pathname === \"/items\") return send(res, 200, list);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "query-strings-barangay"
  },
  {
    "id": "api-query-strings-barangay-2",
    "index": 242,
    "task": "You add one line after the max line. This lets you filter items by minimum price. The code below does that. Run the checker to test it.\n\nIn server.js:\n```\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "min",
        "label": "GET /items?min=50 answers only the items from 50",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items?min=50"
          }
        ],
        "bodyContains": "[{\"id\":1,\"name\":\"Clearance\",\"price\":50},{\"id\":2,\"name\":\"Permit\",\"price\":300}]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "You check if the item's price is greater than or equal to the min value."
      },
      {
        "level": 2,
        "text": "Add the code after the max line and before the q line.\n\nIn server.js:\n```\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n  if (url.pathname === \"/items\") return send(res, 200, list);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "query-strings-barangay"
  },
  {
    "id": "api-query-strings-barangay-3",
    "index": 243,
    "task": "You add one line after the min line. This lets you search for items by name, ignoring case. The code below does that. Run the checker to test it.\n\nIn server.js:\n```\n  if (url.searchParams.has(\"q\")) list = list.filter((item) => item.name.toLowerCase().includes(url.searchParams.get(\"q\").toLowerCase()));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "q",
        "label": "GET /items?q=erm finds Permit",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items?q=erm"
          }
        ],
        "bodyContains": "[{\"id\":2,\"name\":\"Permit\",\"price\":300}]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "You convert both the item name and search term to lowercase before comparing."
      },
      {
        "level": 2,
        "text": "Add the code after the min line and before the sort line.\n\nIn server.js:\n```\n  if (url.searchParams.has(\"q\")) list = list.filter((item) => item.name.toLowerCase().includes(url.searchParams.get(\"q\").toLowerCase()));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n  if (url.searchParams.has(\"q\")) list = list.filter((item) => item.name.toLowerCase().includes(url.searchParams.get(\"q\").toLowerCase()));\n  if (url.pathname === \"/items\") return send(res, 200, list);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "query-strings-barangay"
  },
  {
    "id": "api-query-strings-barangay-4",
    "index": 244,
    "task": "You add one line after the q line. This sorts items from cheapest to most expensive. The code below does that. Run the checker to test it.\n\nIn server.js:\n```\n  if (url.searchParams.get(\"sort\") === \"price\") list = [...list].sort((a, b) => a.price - b.price);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "asc",
        "label": "GET /items?sort=price answers cheapest first",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items?sort=price"
          }
        ],
        "bodyContains": "[{\"id\":3,\"name\":\"ID\",\"price\":20},{\"id\":1,\"name\":\"Clearance\",\"price\":50},{\"id\":2,\"name\":\"Permit\",\"price\":300}]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "You sort the list using the price property, from lowest to highest."
      },
      {
        "level": 2,
        "text": "Add the code after the q line and before the -price line.\n\nIn server.js:\n```\n  if (url.searchParams.get(\"sort\") === \"price\") list = [...list].sort((a, b) => a.price - b.price);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n  if (url.searchParams.has(\"q\")) list = list.filter((item) => item.name.toLowerCase().includes(url.searchParams.get(\"q\").toLowerCase()));\n  if (url.searchParams.get(\"sort\") === \"price\") list = [...list].sort((a, b) => a.price - b.price);\n  if (url.pathname === \"/items\") return send(res, 200, list);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "query-strings-barangay"
  },
  {
    "id": "api-query-strings-barangay-5",
    "index": 245,
    "task": "You add one line after the sort line. This sorts items from most expensive to cheapest. The code below does that. Run the checker to test it.\n\nIn server.js:\n```\n  if (url.searchParams.get(\"sort\") === \"-price\") list = [...list].sort((a, b) => b.price - a.price);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "desc",
        "label": "GET /items?sort=-price answers most expensive first",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items?sort=-price"
          }
        ],
        "bodyContains": "[{\"id\":2,\"name\":\"Permit\",\"price\":300},{\"id\":1,\"name\":\"Clearance\",\"price\":50},{\"id\":3,\"name\":\"ID\",\"price\":20}]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "You sort the list using the price property, from highest to lowest."
      },
      {
        "level": 2,
        "text": "Add the code after the sort line and before the end of the code.\n\nIn server.js:\n```\n  if (url.searchParams.get(\"sort\") === \"-price\") list = [...list].sort((a, b) => b.price - a.price);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n  if (url.searchParams.has(\"q\")) list = list.filter((item) => item.name.toLowerCase().includes(url.searchParams.get(\"q\").toLowerCase()));\n  if (url.searchParams.get(\"sort\") === \"price\") list = [...list].sort((a, b) => a.price - b.price);\n  if (url.searchParams.get(\"sort\") === \"-price\") list = [...list].sort((a, b) => b.price - a.price);\n  if (url.pathname === \"/items\") return send(res, 200, list);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "query-strings-barangay"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: query-strings-barangay.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-query-strings-barangay-6",
    "index": 246,
    "task": "You add a line to limit the list. This line checks if the user asks for a limit. If yes, it cuts the list to that number. This helps the barangay office show only the first few items. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\n  if (url.searchParams.has(\"limit\")) list = list.slice(0, Number(url.searchParams.get(\"limit\")));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "limit",
        "label": "GET /items?sort=price&limit=1 answers only the cheapest item",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items?sort=price&limit=1"
          }
        ],
        "bodyContains": "[{\"id\":3,\"name\":\"ID\",\"price\":20}]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Think of limit like a stop sign for the list. It stops at the number asked."
      },
      {
        "level": 2,
        "text": "Add this line after the second sort line in server.js.\n\nIn server.js:\n```\n  if (url.searchParams.has(\"limit\")) list = list.slice(0, Number(url.searchParams.get(\"limit\")));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n  if (url.searchParams.has(\"q\")) list = list.filter((item) => item.name.toLowerCase().includes(url.searchParams.get(\"q\").toLowerCase()));\n  if (url.searchParams.get(\"sort\") === \"price\") list = [...list].sort((a, b) => a.price - b.price);\n  if (url.searchParams.get(\"sort\") === \"-price\") list = [...list].sort((a, b) => b.price - a.price);\n  if (url.searchParams.has(\"limit\")) list = list.slice(0, Number(url.searchParams.get(\"limit\")));\n  if (url.pathname === \"/items\") return send(res, 200, list);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "query-strings-barangay"
  },
  {
    "id": "api-query-strings-barangay-7",
    "index": 247,
    "task": "You add a line to show only names. This line checks if the user asks for fields=name. If yes, it changes each item to show only its name. This helps the barangay office show only names. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\n  if (url.searchParams.get(\"fields\") === \"name\") list = list.map((item) => ({ name: item.name }));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "fields",
        "label": "GET /items?fields=name answers names only",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items?fields=name"
          }
        ],
        "bodyContains": "[{\"name\":\"Clearance\"},{\"name\":\"Permit\"},{\"name\":\"ID\"}]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "You are filtering the data to keep only the name part."
      },
      {
        "level": 2,
        "text": "Add this line after the limit line in server.js.\n\nIn server.js:\n```\n  if (url.searchParams.get(\"fields\") === \"name\") list = list.map((item) => ({ name: item.name }));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n  if (url.searchParams.has(\"q\")) list = list.filter((item) => item.name.toLowerCase().includes(url.searchParams.get(\"q\").toLowerCase()));\n  if (url.searchParams.get(\"sort\") === \"price\") list = [...list].sort((a, b) => a.price - b.price);\n  if (url.searchParams.get(\"sort\") === \"-price\") list = [...list].sort((a, b) => b.price - a.price);\n  if (url.searchParams.has(\"limit\")) list = list.slice(0, Number(url.searchParams.get(\"limit\")));\n  if (url.searchParams.get(\"fields\") === \"name\") list = list.map((item) => ({ name: item.name }));\n  if (url.pathname === \"/items\") return send(res, 200, list);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "query-strings-barangay"
  },
  {
    "id": "api-query-strings-barangay-8",
    "index": 248,
    "task": "You add a line to catch bad numbers. This line checks if the user asks for max but gives a letter. If yes, it sends back a 400 error. This helps the barangay office stop bad requests. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\n  if (url.searchParams.has(\"max\") && Number.isNaN(Number(url.searchParams.get(\"max\")))) return send(res, 400, { error: \"max must be a number\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "bad",
        "label": "GET /items?max=abc answers 400",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items?max=abc"
          }
        ],
        "status": 400
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "If max is not a number, the server should say it's wrong."
      },
      {
        "level": 2,
        "text": "Add this line right after the url line in server.js.\n\nIn server.js:\n```\n  if (url.searchParams.has(\"max\") && Number.isNaN(Number(url.searchParams.get(\"max\")))) return send(res, 400, { error: \"max must be a number\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.searchParams.has(\"max\") && Number.isNaN(Number(url.searchParams.get(\"max\")))) return send(res, 400, { error: \"max must be a number\" });\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n  if (url.searchParams.has(\"q\")) list = list.filter((item) => item.name.toLowerCase().includes(url.searchParams.get(\"q\").toLowerCase()));\n  if (url.searchParams.get(\"sort\") === \"price\") list = [...list].sort((a, b) => a.price - b.price);\n  if (url.searchParams.get(\"sort\") === \"-price\") list = [...list].sort((a, b) => b.price - a.price);\n  if (url.searchParams.has(\"limit\")) list = list.slice(0, Number(url.searchParams.get(\"limit\")));\n  if (url.searchParams.get(\"fields\") === \"name\") list = list.map((item) => ({ name: item.name }));\n  if (url.pathname === \"/items\") return send(res, 200, list);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "query-strings-barangay"
  },
  {
    "id": "api-query-strings-barangay-9",
    "index": 249,
    "task": "You change the /items line to send a header. This header tells the user how many items match. This helps the barangay office know how many items they have. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\n  if (url.pathname === \"/items\") { res.setHeader(\"X-Total-Count\", String(list.length)); return send(res, 200, list); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "count",
        "label": "GET /items?max=50 sends X-Total-Count: 2",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items?max=50"
          }
        ],
        "header": {
          "name": "x-total-count",
          "value": "2"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "You add a header called X-Total-Count to show the total number."
      },
      {
        "level": 2,
        "text": "Change the /items line in server.js to include this header.\n\nIn server.js:\n```\n  if (url.pathname === \"/items\") { res.setHeader(\"X-Total-Count\", String(list.length)); return send(res, 200, list); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.searchParams.has(\"max\") && Number.isNaN(Number(url.searchParams.get(\"max\")))) return send(res, 400, { error: \"max must be a number\" });\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n  if (url.searchParams.has(\"q\")) list = list.filter((item) => item.name.toLowerCase().includes(url.searchParams.get(\"q\").toLowerCase()));\n  if (url.searchParams.get(\"sort\") === \"price\") list = [...list].sort((a, b) => a.price - b.price);\n  if (url.searchParams.get(\"sort\") === \"-price\") list = [...list].sort((a, b) => b.price - a.price);\n  if (url.searchParams.has(\"limit\")) list = list.slice(0, Number(url.searchParams.get(\"limit\")));\n  if (url.searchParams.get(\"fields\") === \"name\") list = list.map((item) => ({ name: item.name }));\n  if (url.pathname === \"/items\") { res.setHeader(\"X-Total-Count\", String(list.length)); return send(res, 200, list); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "query-strings-barangay"
  },
  {
    "id": "api-query-strings-barangay-10",
    "index": 250,
    "task": "You add a line to show what filters are allowed. This line checks if the user asks for /items/options. If yes, it sends back a list of filters. This helps the barangay office know what they can ask for. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\n  if (url.pathname === \"/items/options\") return send(res, 200, { filters: [\"max\", \"min\", \"q\"], sort: [\"price\", \"-price\"], other: [\"limit\", \"fields\"] });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "options",
        "label": "GET /items/options lists the filters",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/options"
          }
        ],
        "bodyContains": "\"filters\":[\"max\",\"min\",\"q\"]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The /items/options path tells users what filters they can use."
      },
      {
        "level": 2,
        "text": "Add this line after the 400 line in server.js.\n\nIn server.js:\n```\n  if (url.pathname === \"/items/options\") return send(res, 200, { filters: [\"max\", \"min\", \"q\"], sort: [\"price\", \"-price\"], other: [\"limit\", \"fields\"] });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.searchParams.has(\"max\") && Number.isNaN(Number(url.searchParams.get(\"max\")))) return send(res, 400, { error: \"max must be a number\" });\n  if (url.pathname === \"/items/options\") return send(res, 200, { filters: [\"max\", \"min\", \"q\"], sort: [\"price\", \"-price\"], other: [\"limit\", \"fields\"] });\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n  if (url.searchParams.has(\"q\")) list = list.filter((item) => item.name.toLowerCase().includes(url.searchParams.get(\"q\").toLowerCase()));\n  if (url.searchParams.get(\"sort\") === \"price\") list = [...list].sort((a, b) => a.price - b.price);\n  if (url.searchParams.get(\"sort\") === \"-price\") list = [...list].sort((a, b) => b.price - a.price);\n  if (url.searchParams.has(\"limit\")) list = list.slice(0, Number(url.searchParams.get(\"limit\")));\n  if (url.searchParams.get(\"fields\") === \"name\") list = list.map((item) => ({ name: item.name }));\n  if (url.pathname === \"/items\") { res.setHeader(\"X-Total-Count\", String(list.length)); return send(res, 200, list); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "query-strings-barangay"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: middleware-barangay.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-middleware-barangay-1",
    "index": 251,
    "task": "You will add a counter to track request numbers. This counter starts at 1. You will also add a function called addRequestId. This function adds a header to every answer. The header is named X-Request-Id. You will call this function right after the handler starts. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\n  addRequestId(req, res);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "id",
        "label": "The first answer sends X-Request-Id: 1",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "header": {
          "name": "x-request-id",
          "value": "1"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Think of the counter as a small notebook that counts each request."
      },
      {
        "level": 2,
        "text": "Add the function above the server, then call it right after the handler starts.\n\nIn server.js:\n```\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\n  addRequestId(req, res);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  addRequestId(req, res);\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "middleware-barangay"
  },
  {
    "id": "api-middleware-barangay-2",
    "index": 252,
    "task": "You will add a new function called addPlace. This function adds a header named X-Place. The value is always barangay. You will call this function after addRequestId. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"barangay\"); }\n  addPlace(req, res);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "place",
        "label": "Answers send X-Place: barangay",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "header": {
          "name": "x-place",
          "value": "barangay"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The header X-Place tells where the request came from, like barangay."
      },
      {
        "level": 2,
        "text": "Add the function after addRequestId, then call it in the same spot.\n\nIn server.js:\n```\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"barangay\"); }\n  addPlace(req, res);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"barangay\"); }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  addRequestId(req, res);\n  addPlace(req, res);\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "middleware-barangay"
  },
  {
    "id": "api-middleware-barangay-3",
    "index": 253,
    "task": "You will make a list called middleware. This list holds all the functions you want to run. You will replace the two calls with a loop that runs each function in the list. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\nconst middleware = [addRequestId, addPlace];\n  for (const step of middleware) step(req, res);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "list",
        "label": "server.js keeps its middleware in one list",
        "kind": "local-file-contains",
        "path": "server.js",
        "value": "const middleware = [addRequestId, addPlace];"
      },
      {
        "id": "still",
        "label": "Answers still send X-Place",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "header": {
          "name": "x-place",
          "value": "barangay"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The list holds all the functions you want to run, like a checklist."
      },
      {
        "level": 2,
        "text": "Replace the two calls with a for loop that runs each function in the list.\n\nIn server.js:\n```\nconst middleware = [addRequestId, addPlace];\n  for (const step of middleware) step(req, res);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"barangay\"); }\nconst middleware = [addRequestId, addPlace];\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  for (const step of middleware) step(req, res);\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "middleware-barangay"
  },
  {
    "id": "api-middleware-barangay-4",
    "index": 254,
    "task": "You will add a new function called addPoweredBy. This function adds a header named X-Powered-By. The value is Node built-ins. You will add this function to the middleware list. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\nfunction addPoweredBy(req, res) { res.setHeader(\"X-Powered-By\", \"Node built-ins\"); }\nconst middleware = [addRequestId, addPlace, addPoweredBy];\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "powered",
        "label": "Answers send X-Powered-By: Node built-ins",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "header": {
          "name": "x-powered-by",
          "value": "Node built-ins"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The header X-Powered-By tells what tools made the answer, like Node built-ins."
      },
      {
        "level": 2,
        "text": "Add the function to the middleware list, then call it in the loop.\n\nIn server.js:\n```\nfunction addPoweredBy(req, res) { res.setHeader(\"X-Powered-By\", \"Node built-ins\"); }\nconst middleware = [addRequestId, addPlace, addPoweredBy];\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"barangay\"); }\nfunction addPoweredBy(req, res) { res.setHeader(\"X-Powered-By\", \"Node built-ins\"); }\nconst middleware = [addRequestId, addPlace, addPoweredBy];\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  for (const step of middleware) step(req, res);\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "middleware-barangay"
  },
  {
    "id": "api-middleware-barangay-5",
    "index": 255,
    "task": "You will add a new function called allowBrowsers. This function adds a header named Access-Control-Allow-Origin. The value is *. This lets web pages from other sites call your API. You will add this function to the middleware list. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\nfunction allowBrowsers(req, res) { res.setHeader(\"Access-Control-Allow-Origin\", \"*\"); }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers];\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "cors",
        "label": "Answers send Access-Control-Allow-Origin: *",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "header": {
          "name": "access-control-allow-origin",
          "value": "*"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The header Access-Control-Allow-Origin lets other websites use your API."
      },
      {
        "level": 2,
        "text": "Add the function to the middleware list, then call it in the loop.\n\nIn server.js:\n```\nfunction allowBrowsers(req, res) { res.setHeader(\"Access-Control-Allow-Origin\", \"*\"); }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers];\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"barangay\"); }\nfunction addPoweredBy(req, res) { res.setHeader(\"X-Powered-By\", \"Node built-ins\"); }\nfunction allowBrowsers(req, res) { res.setHeader(\"Access-Control-Allow-Origin\", \"*\"); }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers];\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  for (const step of middleware) step(req, res);\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "middleware-barangay"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: middleware-barangay.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-middleware-barangay-6",
    "index": 256,
    "task": "You add a function called requireKey. It checks if the request has the right API key. If not, it sends a 401 error. You put this function in the middleware list. Then, if any middleware returns true, the loop stops early. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\nfunction requireKey(req, res) { if (req.headers[\"x-api-key\"] !== \"secret123\") { send(res, 401, { error: \"Missing or wrong API key\" }); return true; } }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers, requireKey];\n  for (const step of middleware) if (step(req, res)) return;\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "nokey",
        "label": "GET /items without a key answers 401",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "status": 401
      },
      {
        "id": "key",
        "label": "GET /items with the key answers 200",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items",
            "headers": {
              "x-api-key": "secret123"
            }
          }
        ],
        "status": 200
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The key check must come after the browser allowances so it doesn't block preflight requests."
      },
      {
        "level": 2,
        "text": "Add the requireKey function to the middleware array right after addPoweredBy.\n\nIn server.js:\n```\nfunction requireKey(req, res) { if (req.headers[\"x-api-key\"] !== \"secret123\") { send(res, 401, { error: \"Missing or wrong API key\" }); return true; } }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers, requireKey];\n  for (const step of middleware) if (step(req, res)) return;\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"barangay\"); }\nfunction addPoweredBy(req, res) { res.setHeader(\"X-Powered-By\", \"Node built-ins\"); }\nfunction allowBrowsers(req, res) { res.setHeader(\"Access-Control-Allow-Origin\", \"*\"); }\nfunction requireKey(req, res) { if (req.headers[\"x-api-key\"] !== \"secret123\") { send(res, 401, { error: \"Missing or wrong API key\" }); return true; } }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers, requireKey];\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  for (const step of middleware) if (step(req, res)) return;\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "middleware-barangay"
  },
  {
    "id": "api-middleware-barangay-7",
    "index": 257,
    "task": "You add a line at the top of the handler to check if the URL is /health. If yes, you send a 200 OK response right away. This lets monitors check the server without needing a key. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (req.url === \"/health\") return send(res, 200, { ok: true });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "health",
        "label": "GET /health answers without a key",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/health"
          }
        ],
        "status": 200,
        "bodyContains": "\"ok\":true"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "This check must be before the middleware loop so it doesn't get blocked by key checks."
      },
      {
        "level": 2,
        "text": "Add the if (req.url === \"/health\") line right after the middleware loop starts.\n\nIn server.js:\n```\n  if (req.url === \"/health\") return send(res, 200, { ok: true });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"barangay\"); }\nfunction addPoweredBy(req, res) { res.setHeader(\"X-Powered-By\", \"Node built-ins\"); }\nfunction allowBrowsers(req, res) { res.setHeader(\"Access-Control-Allow-Origin\", \"*\"); }\nfunction requireKey(req, res) { if (req.headers[\"x-api-key\"] !== \"secret123\") { send(res, 401, { error: \"Missing or wrong API key\" }); return true; } }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers, requireKey];\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/health\") return send(res, 200, { ok: true });\n  for (const step of middleware) if (step(req, res)) return;\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "middleware-barangay"
  },
  {
    "id": "api-middleware-barangay-8",
    "index": 258,
    "task": "You change the allowBrowsers function to handle OPTIONS requests. When the method is OPTIONS, you send a 204 status and stop. This lets browsers ask for permissions before sending data. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\nfunction allowBrowsers(req, res) { res.setHeader(\"Access-Control-Allow-Origin\", \"*\"); if (req.method === \"OPTIONS\") { res.statusCode = 204; res.end(); return true; } }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "preflight",
        "label": "OPTIONS /items answers 204 without a key",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "OPTIONS",
            "path": "/items"
          }
        ],
        "status": 204
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The OPTIONS response must come before the key check so the browser can ask for permission first."
      },
      {
        "level": 2,
        "text": "Change the allowBrowsers function to return true when req.method === \"OPTIONS\".\n\nIn server.js:\n```\nfunction allowBrowsers(req, res) { res.setHeader(\"Access-Control-Allow-Origin\", \"*\"); if (req.method === \"OPTIONS\") { res.statusCode = 204; res.end(); return true; } }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"barangay\"); }\nfunction addPoweredBy(req, res) { res.setHeader(\"X-Powered-By\", \"Node built-ins\"); }\nfunction allowBrowsers(req, res) { res.setHeader(\"Access-Control-Allow-Origin\", \"*\"); if (req.method === \"OPTIONS\") { res.statusCode = 204; res.end(); return true; } }\nfunction requireKey(req, res) { if (req.headers[\"x-api-key\"] !== \"secret123\") { send(res, 401, { error: \"Missing or wrong API key\" }); return true; } }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers, requireKey];\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/health\") return send(res, 200, { ok: true });\n  for (const step of middleware) if (step(req, res)) return;\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "middleware-barangay"
  },
  {
    "id": "api-middleware-barangay-9",
    "index": 259,
    "task": "You add a line after the middleware loop to handle /stats. When the URL is /stats, you send back how many requests were served. You use nextId - 1 to count them. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (req.url === \"/stats\") return send(res, 200, { served: nextId - 1 });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "stats",
        "label": "After two requests, GET /stats answers served 3",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items",
            "headers": {
              "x-api-key": "secret123"
            }
          },
          {
            "method": "GET",
            "path": "/items",
            "headers": {
              "x-api-key": "secret123"
            }
          },
          {
            "method": "GET",
            "path": "/stats",
            "headers": {
              "x-api-key": "secret123"
            }
          }
        ],
        "bodyContains": "\"served\":3"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "This line must come after the middleware loop so it doesn't get blocked by key checks."
      },
      {
        "level": 2,
        "text": "Add the if (req.url === \"/stats\") line right after the for loop ends.\n\nIn server.js:\n```\n  if (req.url === \"/stats\") return send(res, 200, { served: nextId - 1 });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"barangay\"); }\nfunction addPoweredBy(req, res) { res.setHeader(\"X-Powered-By\", \"Node built-ins\"); }\nfunction allowBrowsers(req, res) { res.setHeader(\"Access-Control-Allow-Origin\", \"*\"); if (req.method === \"OPTIONS\") { res.statusCode = 204; res.end(); return true; } }\nfunction requireKey(req, res) { if (req.headers[\"x-api-key\"] !== \"secret123\") { send(res, 401, { error: \"Missing or wrong API key\" }); return true; } }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers, requireKey];\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/health\") return send(res, 200, { ok: true });\n  for (const step of middleware) if (step(req, res)) return;\n  if (req.url === \"/stats\") return send(res, 200, { served: nextId - 1 });\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "middleware-barangay"
  },
  {
    "id": "api-middleware-barangay-10",
    "index": 260,
    "task": "You add a secureHeaders function that sets the X-Content-Type-Options header to nosniff. This helps prevent attacks. You add this function to the middleware list. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\nfunction secureHeaders(req, res) { res.setHeader(\"X-Content-Type-Options\", \"nosniff\"); }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers, secureHeaders, requireKey];\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "nosniff",
        "label": "Answers send X-Content-Type-Options: nosniff",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items",
            "headers": {
              "x-api-key": "secret123"
            }
          }
        ],
        "header": {
          "name": "x-content-type-options",
          "value": "nosniff"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "This header must be set before any response is sent, so put it early in the middleware list."
      },
      {
        "level": 2,
        "text": "Add the secureHeaders function to the middleware array right after allowBrowsers.\n\nIn server.js:\n```\nfunction secureHeaders(req, res) { res.setHeader(\"X-Content-Type-Options\", \"nosniff\"); }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers, secureHeaders, requireKey];\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"barangay\"); }\nfunction addPoweredBy(req, res) { res.setHeader(\"X-Powered-By\", \"Node built-ins\"); }\nfunction allowBrowsers(req, res) { res.setHeader(\"Access-Control-Allow-Origin\", \"*\"); if (req.method === \"OPTIONS\") { res.statusCode = 204; res.end(); return true; } }\nfunction requireKey(req, res) { if (req.headers[\"x-api-key\"] !== \"secret123\") { send(res, 401, { error: \"Missing or wrong API key\" }); return true; } }\nfunction secureHeaders(req, res) { res.setHeader(\"X-Content-Type-Options\", \"nosniff\"); }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers, secureHeaders, requireKey];\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/health\") return send(res, 200, { ok: true });\n  for (const step of middleware) if (step(req, res)) return;\n  if (req.url === \"/stats\") return send(res, 200, { served: nextId - 1 });\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "middleware-barangay"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: sqlite-storage-barangay.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-sqlite-storage-barangay-1",
    "index": 261,
    "task": "Open an in-memory SQLite database. Make a table called items. Answer GET /items from it. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "empty",
        "label": "GET /items answers an empty list from the database",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "status": 200,
        "bodyContains": "[]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "You need to create a database and a table before you can answer requests."
      },
      {
        "level": 2,
        "text": "Add the code above createItem and before the 404 line.\n\nIn server.js:\n```\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "sqlite-storage-barangay"
  },
  {
    "id": "api-sqlite-storage-barangay-2",
    "index": 262,
    "task": "Prepare an INSERT statement to add items. Add three starting items: Clearance, Permit, and ID. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Clearance\",50],[\"Permit\",300],[\"ID\",20]]) insert.run(name, price);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "rows",
        "label": "GET /items includes Clearance from the database",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "bodyContains": "\"name\":\"Clearance\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "You need to add data to the table before the server can show it."
      },
      {
        "level": 2,
        "text": "Add the code after the CREATE TABLE line.\n\nIn server.js:\n```\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Clearance\",50],[\"Permit\",300],[\"ID\",20]]) insert.run(name, price);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Clearance\",50],[\"Permit\",300],[\"ID\",20]]) insert.run(name, price);\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "sqlite-storage-barangay"
  },
  {
    "id": "api-sqlite-storage-barangay-3",
    "index": 263,
    "task": "Change the SQL in GET /items to sort rows by price. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items ORDER BY price\").all());\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "ordered",
        "label": "GET /items lists the items cheapest first, starting with ID",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "bodyContains": "[{\"id\":3,\"name\":\"ID\",\"price\":20},{\"id\":1,\"name\":\"Clearance\",\"price\":50},{\"id\":2,\"name\":\"Permit\",\"price\":300}]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "You need to tell the database to sort by price to show cheapest first."
      },
      {
        "level": 2,
        "text": "Change the SQL in the GET /items line.\n\nIn server.js:\n```\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items ORDER BY price\").all());\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Clearance\",50],[\"Permit\",300],[\"ID\",20]]) insert.run(name, price);\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items ORDER BY price\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 2,
    "projectId": "sqlite-storage-barangay"
  },
  {
    "id": "api-sqlite-storage-barangay-4",
    "index": 264,
    "task": "Add a route to count rows. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "count",
        "label": "GET /items/count answers {\"count\":3}",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/count"
          }
        ],
        "bodyContains": "{\"count\":3}"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "You need to count rows to know how many items are in the database."
      },
      {
        "level": 2,
        "text": "Add the code after the GET /items line.\n\nIn server.js:\n```\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Clearance\",50],[\"Permit\",300],[\"ID\",20]]) insert.run(name, price);\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items ORDER BY price\").all());\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 2,
    "projectId": "sqlite-storage-barangay"
  },
  {
    "id": "api-sqlite-storage-barangay-5",
    "index": 265,
    "task": "Answer one row by id. Pass the id as a ? parameter. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match) return send(res, 200, db.prepare(\"SELECT * FROM items WHERE id = ?\").get(Number(match[1])));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "one",
        "label": "GET /items/2 answers Permit",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/2"
          }
        ],
        "bodyContains": "\"name\":\"Permit\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "You need to match the URL to get the id and use it to find a row."
      },
      {
        "level": 2,
        "text": "Add the code before the 404 line.\n\nIn server.js:\n```\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match) return send(res, 200, db.prepare(\"SELECT * FROM items WHERE id = ?\").get(Number(match[1])));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Clearance\",50],[\"Permit\",300],[\"ID\",20]]) insert.run(name, price);\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items ORDER BY price\").all());\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match) return send(res, 200, db.prepare(\"SELECT * FROM items WHERE id = ?\").get(Number(match[1])));\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "sqlite-storage-barangay"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: sqlite-storage-barangay.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-sqlite-storage-barangay-6",
    "index": 266,
    "task": "When the database has no row with that id, the server must answer 404. Change the one-row line. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\n  if (match) { const row = db.prepare(\"SELECT * FROM items WHERE id = ?\").get(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "none",
        "label": "GET /items/99 answers 404",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/99"
          }
        ],
        "status": 404
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "If the row is not found, return a 404 error with a message."
      },
      {
        "level": 2,
        "text": "Put the code in server.js, right after the line that checks for a match.\n\nIn server.js:\n```\n  if (match) { const row = db.prepare(\"SELECT * FROM items WHERE id = ?\").get(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Clearance\",50],[\"Permit\",300],[\"ID\",20]]) insert.run(name, price);\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items ORDER BY price\").all());\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match) { const row = db.prepare(\"SELECT * FROM items WHERE id = ?\").get(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "sqlite-storage-barangay"
  },
  {
    "id": "api-sqlite-storage-barangay-7",
    "index": 267,
    "task": "When you POST an item, the server must insert it and answer with the id the database gave it. Replace the line inside createItem with three lines. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\n  const data = JSON.parse(await readBody(req));\n  const result = insert.run(data.name, data.price);\n  return send(res, 201, { id: Number(result.lastInsertRowid), ...data });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "insert",
        "label": "POST /items answers 201 with id 4",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "{\"name\":\"Tea\",\"price\":12}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 201,
        "bodyContains": "\"id\":4"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Parse the JSON body, insert the data, then send back the id and data."
      },
      {
        "level": 2,
        "text": "Put the code in server.js, inside the createItem function.\n\nIn server.js:\n```\n  const data = JSON.parse(await readBody(req));\n  const result = insert.run(data.name, data.price);\n  return send(res, 201, { id: Number(result.lastInsertRowid), ...data });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Clearance\",50],[\"Permit\",300],[\"ID\",20]]) insert.run(name, price);\nasync function createItem(req, res) {\n  const data = JSON.parse(await readBody(req));\n  const result = insert.run(data.name, data.price);\n  return send(res, 201, { id: Number(result.lastInsertRowid), ...data });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items ORDER BY price\").all());\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match) { const row = db.prepare(\"SELECT * FROM items WHERE id = ?\").get(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "sqlite-storage-barangay"
  },
  {
    "id": "api-sqlite-storage-barangay-8",
    "index": 268,
    "task": "Add a route to get the sum of all prices. Add one route after the count route. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\n  if (req.url === \"/items/total\") return send(res, 200, db.prepare(\"SELECT SUM(price) AS total FROM items\").get());\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "sum",
        "label": "GET /items/total answers the sum of prices",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/total"
          }
        ],
        "bodyContains": "{\"total\":370}"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use SQL SUM to add all prices from the items table."
      },
      {
        "level": 2,
        "text": "Put the code in server.js, after the route for counting items.\n\nIn server.js:\n```\n  if (req.url === \"/items/total\") return send(res, 200, db.prepare(\"SELECT SUM(price) AS total FROM items\").get());\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Clearance\",50],[\"Permit\",300],[\"ID\",20]]) insert.run(name, price);\nasync function createItem(req, res) {\n  const data = JSON.parse(await readBody(req));\n  const result = insert.run(data.name, data.price);\n  return send(res, 201, { id: Number(result.lastInsertRowid), ...data });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items ORDER BY price\").all());\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n  if (req.url === \"/items/total\") return send(res, 200, db.prepare(\"SELECT SUM(price) AS total FROM items\").get());\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match) { const row = db.prepare(\"SELECT * FROM items WHERE id = ?\").get(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "sqlite-storage-barangay"
  },
  {
    "id": "api-sqlite-storage-barangay-9",
    "index": 269,
    "task": "Search for items by name using LIKE. Add two lines after the total route. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items/search\") return send(res, 200, db.prepare(\"SELECT * FROM items WHERE name LIKE ?\").all(`%${url.searchParams.get(\"q\") ?? \"\"}%`));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "like",
        "label": "GET /items/search?q=D finds ID",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/search?q=D"
          }
        ],
        "bodyContains": "\"name\":\"ID\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use the search parameter 'q' to find names that match the text."
      },
      {
        "level": 2,
        "text": "Put the code in server.js, after the route for getting the total.\n\nIn server.js:\n```\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items/search\") return send(res, 200, db.prepare(\"SELECT * FROM items WHERE name LIKE ?\").all(`%${url.searchParams.get(\"q\") ?? \"\"}%`));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Clearance\",50],[\"Permit\",300],[\"ID\",20]]) insert.run(name, price);\nasync function createItem(req, res) {\n  const data = JSON.parse(await readBody(req));\n  const result = insert.run(data.name, data.price);\n  return send(res, 201, { id: Number(result.lastInsertRowid), ...data });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items ORDER BY price\").all());\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n  if (req.url === \"/items/total\") return send(res, 200, db.prepare(\"SELECT SUM(price) AS total FROM items\").get());\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items/search\") return send(res, 200, db.prepare(\"SELECT * FROM items WHERE name LIKE ?\").all(`%${url.searchParams.get(\"q\") ?? \"\"}%`));\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match) { const row = db.prepare(\"SELECT * FROM items WHERE id = ?\").get(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "sqlite-storage-barangay"
  },
  {
    "id": "api-sqlite-storage-barangay-10",
    "index": 270,
    "task": "Keep the data in a file named by DB_FILE. Create the table and rows only once. Change the open, CREATE TABLE, and starting-rows lines. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\nconst db = new DatabaseSync(process.env.DB_FILE ?? \":memory:\");\ndb.exec(\"CREATE TABLE IF NOT EXISTS items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nif (db.prepare(\"SELECT COUNT(*) AS count FROM items\").get().count === 0) for (const [name, price] of [[\"Clearance\",50],[\"Permit\",300],[\"ID\",20]]) insert.run(name, price);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "file",
        "label": "With DB_FILE=shop.db the server answers from a file",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "env": {
          "DB_FILE": "shop.db"
        },
        "status": 200,
        "bodyContains": "\"name\":\"Clearance\""
      },
      {
        "id": "saved",
        "label": "shop.db exists on disk",
        "kind": "local-file-exists",
        "path": "shop.db"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use process.env.DB_FILE to set the file name. Create the table if it doesn't exist."
      },
      {
        "level": 2,
        "text": "Put the code in server.js, at the top where the database is opened.\n\nIn server.js:\n```\nconst db = new DatabaseSync(process.env.DB_FILE ?? \":memory:\");\ndb.exec(\"CREATE TABLE IF NOT EXISTS items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nif (db.prepare(\"SELECT COUNT(*) AS count FROM items\").get().count === 0) for (const [name, price] of [[\"Clearance\",50],[\"Permit\",300],[\"ID\",20]]) insert.run(name, price);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(process.env.DB_FILE ?? \":memory:\");\ndb.exec(\"CREATE TABLE IF NOT EXISTS items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nif (db.prepare(\"SELECT COUNT(*) AS count FROM items\").get().count === 0) for (const [name, price] of [[\"Clearance\",50],[\"Permit\",300],[\"ID\",20]]) insert.run(name, price);\nasync function createItem(req, res) {\n  const data = JSON.parse(await readBody(req));\n  const result = insert.run(data.name, data.price);\n  return send(res, 201, { id: Number(result.lastInsertRowid), ...data });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items ORDER BY price\").all());\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n  if (req.url === \"/items/total\") return send(res, 200, db.prepare(\"SELECT SUM(price) AS total FROM items\").get());\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items/search\") return send(res, 200, db.prepare(\"SELECT * FROM items WHERE name LIKE ?\").all(`%${url.searchParams.get(\"q\") ?? \"\"}%`));\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match) { const row = db.prepare(\"SELECT * FROM items WHERE id = ?\").get(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 5,
    "projectId": "sqlite-storage-barangay"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: sqlite-changes-barangay.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-sqlite-changes-barangay-1",
    "index": 271,
    "task": "You will delete a row in the barangay office database. The code below deletes the row and sends back 204 No Content. This tells the user the row was deleted without extra info. Run the checker to test it.\n\nIn server.js:\n```\n  db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  res.statusCode = 204;\n  return res.end();\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Clearance\",50],[\"Permit\",300],[\"ID\",20]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "deleted",
        "label": "DELETE /items/1 answers 204",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "DELETE",
            "path": "/items/1"
          }
        ],
        "status": 204
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The server must send 204 after deleting the row. No body is needed."
      },
      {
        "level": 2,
        "text": "Put the three lines inside the deleteItem function in server.js.\n\nIn server.js:\n```\n  db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  res.statusCode = 204;\n  return res.end();\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Clearance\",50],[\"Permit\",300],[\"ID\",20]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  res.statusCode = 204;\n  return res.end();\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "sqlite-changes-barangay"
  },
  {
    "id": "api-sqlite-changes-barangay-2",
    "index": 272,
    "task": "Now check if the row exists before deleting. If no row is deleted, send 404. This stops errors when the user asks to delete something that doesn't exist. Run the checker to test it.\n\nIn server.js:\n```\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Clearance\",50],[\"Permit\",300],[\"ID\",20]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "none",
        "label": "DELETE /items/99 answers 404",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "DELETE",
            "path": "/items/99"
          }
        ],
        "status": 404
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use result.changes to check how many rows were affected. If zero, the row doesn't exist."
      },
      {
        "level": 2,
        "text": "Put the two lines after the DELETE line inside the deleteItem function in server.js.\n\nIn server.js:\n```\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Clearance\",50],[\"Permit\",300],[\"ID\",20]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  res.statusCode = 204;\n  return res.end();\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "sqlite-changes-barangay"
  },
  {
    "id": "api-sqlite-changes-barangay-3",
    "index": 273,
    "task": "You will update a row's price. The code below changes the price and sends back the updated row. This helps the user see the new price. Run the checker to test it.\n\nIn server.js:\n```\n  const data = JSON.parse(await readBody(req));\n  db.prepare(\"UPDATE items SET price = ? WHERE id = ?\").run(data.price, id);\n  return send(res, 200, one(id));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Clearance\",50],[\"Permit\",300],[\"ID\",20]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "updated",
        "label": "PUT /items/1 with price 99 answers the new price",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "PUT",
            "path": "/items/1",
            "body": "{\"price\":99}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "bodyContains": "\"price\":99"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The server must send the updated row after changing the price."
      },
      {
        "level": 2,
        "text": "Put the three lines inside the updateItem function in server.js.\n\nIn server.js:\n```\n  const data = JSON.parse(await readBody(req));\n  db.prepare(\"UPDATE items SET price = ? WHERE id = ?\").run(data.price, id);\n  return send(res, 200, one(id));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Clearance\",50],[\"Permit\",300],[\"ID\",20]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  res.statusCode = 204;\n  return res.end();\n}\nasync function updateItem(req, res, id) {\n  const data = JSON.parse(await readBody(req));\n  db.prepare(\"UPDATE items SET price = ? WHERE id = ?\").run(data.price, id);\n  return send(res, 200, one(id));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "sqlite-changes-barangay"
  },
  {
    "id": "api-sqlite-changes-barangay-4",
    "index": 274,
    "task": "Now check if the row exists before updating. If no row is updated, send 404. This stops errors when the user asks to update something that doesn't exist. Run the checker to test it.\n\nIn server.js:\n```\n  const result = db.prepare(\"UPDATE items SET price = ? WHERE id = ?\").run(data.price, id);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Clearance\",50],[\"Permit\",300],[\"ID\",20]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "none",
        "label": "PUT /items/99 answers 404",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "PUT",
            "path": "/items/99",
            "body": "{\"price\":1}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 404
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use result.changes to check how many rows were affected. If zero, the row doesn't exist."
      },
      {
        "level": 2,
        "text": "Put the two lines after the UPDATE line inside the updateItem function in server.js.\n\nIn server.js:\n```\n  const result = db.prepare(\"UPDATE items SET price = ? WHERE id = ?\").run(data.price, id);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Clearance\",50],[\"Permit\",300],[\"ID\",20]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  res.statusCode = 204;\n  return res.end();\n}\nasync function updateItem(req, res, id) {\n  const data = JSON.parse(await readBody(req));\n  const result = db.prepare(\"UPDATE items SET price = ? WHERE id = ?\").run(data.price, id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  return send(res, 200, one(id));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "sqlite-changes-barangay"
  },
  {
    "id": "api-sqlite-changes-barangay-5",
    "index": 275,
    "task": "You will check if the price is a whole number 0 or more. If not, send 422. This stops bad prices from entering the system. Run the checker to test it.\n\nIn server.js:\n```\n  if (!Number.isInteger(data.price) || data.price < 0) return send(res, 422, { error: \"price must be a whole number 0 or more\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Clearance\",50],[\"Permit\",300],[\"ID\",20]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "bad",
        "label": "PUT /items/1 with price -5 answers 422",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "PUT",
            "path": "/items/1",
            "body": "{\"price\":-5}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 422
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use Number.isInteger to check if the price is a whole number. Use data.price < 0 to check if it's negative."
      },
      {
        "level": 2,
        "text": "Put the one line after the JSON.parse line inside the updateItem function in server.js.\n\nIn server.js:\n```\n  if (!Number.isInteger(data.price) || data.price < 0) return send(res, 422, { error: \"price must be a whole number 0 or more\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Clearance\",50],[\"Permit\",300],[\"ID\",20]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  res.statusCode = 204;\n  return res.end();\n}\nasync function updateItem(req, res, id) {\n  const data = JSON.parse(await readBody(req));\n  if (!Number.isInteger(data.price) || data.price < 0) return send(res, 422, { error: \"price must be a whole number 0 or more\" });\n  const result = db.prepare(\"UPDATE items SET price = ? WHERE id = ?\").run(data.price, id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  return send(res, 200, one(id));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "sqlite-changes-barangay"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: sqlite-changes-barangay.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-sqlite-changes-barangay-6",
    "index": 276,
    "task": "You will change the price check. It now stops if the price is not a whole number. You will let it update the name or price even if the other is not sent. This keeps the old data. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (data.price !== undefined && (!Number.isInteger(data.price) || data.price < 0)) return send(res, 422, { error: \"price must be a whole number 0 or more\" });\n  const result = db.prepare(\"UPDATE items SET name = COALESCE(?, name), price = COALESCE(?, price) WHERE id = ?\").run(data.name ?? null, data.price ?? null, id);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Clearance\",50],[\"Permit\",300],[\"ID\",20]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "name",
        "label": "PUT /items/1 with only a name keeps the price",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "PUT",
            "path": "/items/1",
            "body": "{\"name\":\"Tea\"}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "bodyContains": "\"name\":\"Tea\",\"price\":50"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The UPDATE line uses COALESCE to keep old data if no new value is sent."
      },
      {
        "level": 2,
        "text": "Add the new UPDATE line after the price check in server.js.\n\nIn server.js:\n```\n  if (data.price !== undefined && (!Number.isInteger(data.price) || data.price < 0)) return send(res, 422, { error: \"price must be a whole number 0 or more\" });\n  const result = db.prepare(\"UPDATE items SET name = COALESCE(?, name), price = COALESCE(?, price) WHERE id = ?\").run(data.name ?? null, data.price ?? null, id);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Clearance\",50],[\"Permit\",300],[\"ID\",20]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  res.statusCode = 204;\n  return res.end();\n}\nasync function updateItem(req, res, id) {\n  const data = JSON.parse(await readBody(req));\n  if (data.price !== undefined && (!Number.isInteger(data.price) || data.price < 0)) return send(res, 422, { error: \"price must be a whole number 0 or more\" });\n  const result = db.prepare(\"UPDATE items SET name = COALESCE(?, name), price = COALESCE(?, price) WHERE id = ?\").run(data.name ?? null, data.price ?? null, id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  return send(res, 200, one(id));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "sqlite-changes-barangay"
  },
  {
    "id": "api-sqlite-changes-barangay-7",
    "index": 277,
    "task": "You will add a new route for raising prices. It runs a transaction to update every row. This keeps the database safe. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (req.url === \"/items/raise\" && req.method === \"POST\") { db.exec(\"BEGIN\"); db.prepare(\"UPDATE items SET price = price + 1\").run(); db.exec(\"COMMIT\"); return send(res, 200, db.prepare(\"SELECT * FROM items\").all()); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Clearance\",50],[\"Permit\",300],[\"ID\",20]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "raise",
        "label": "POST /items/raise answers the raised prices",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items/raise",
            "body": "{}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "bodyContains": "\"price\":51"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The transaction uses BEGIN, UPDATE, and COMMIT to keep changes safe."
      },
      {
        "level": 2,
        "text": "Add this route after the PUT route in server.js.\n\nIn server.js:\n```\n  if (req.url === \"/items/raise\" && req.method === \"POST\") { db.exec(\"BEGIN\"); db.prepare(\"UPDATE items SET price = price + 1\").run(); db.exec(\"COMMIT\"); return send(res, 200, db.prepare(\"SELECT * FROM items\").all()); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Clearance\",50],[\"Permit\",300],[\"ID\",20]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  res.statusCode = 204;\n  return res.end();\n}\nasync function updateItem(req, res, id) {\n  const data = JSON.parse(await readBody(req));\n  if (data.price !== undefined && (!Number.isInteger(data.price) || data.price < 0)) return send(res, 422, { error: \"price must be a whole number 0 or more\" });\n  const result = db.prepare(\"UPDATE items SET name = COALESCE(?, name), price = COALESCE(?, price) WHERE id = ?\").run(data.name ?? null, data.price ?? null, id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  return send(res, 200, one(id));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items/raise\" && req.method === \"POST\") { db.exec(\"BEGIN\"); db.prepare(\"UPDATE items SET price = price + 1\").run(); db.exec(\"COMMIT\"); return send(res, 200, db.prepare(\"SELECT * FROM items\").all()); }\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 5,
    "projectId": "sqlite-changes-barangay"
  },
  {
    "id": "api-sqlite-changes-barangay-8",
    "index": 278,
    "task": "You will add a route to count rows. This helps see how many items are left after a delete. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Clearance\",50],[\"Permit\",300],[\"ID\",20]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "count",
        "label": "After DELETE /items/1, GET /items/count answers 2",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "DELETE",
            "path": "/items/1"
          },
          {
            "method": "GET",
            "path": "/items/count"
          }
        ],
        "bodyContains": "{\"count\":2}"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The route counts rows with COUNT(*) and sends the number."
      },
      {
        "level": 2,
        "text": "Add this route after the raise route in server.js.\n\nIn server.js:\n```\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Clearance\",50],[\"Permit\",300],[\"ID\",20]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  res.statusCode = 204;\n  return res.end();\n}\nasync function updateItem(req, res, id) {\n  const data = JSON.parse(await readBody(req));\n  if (data.price !== undefined && (!Number.isInteger(data.price) || data.price < 0)) return send(res, 422, { error: \"price must be a whole number 0 or more\" });\n  const result = db.prepare(\"UPDATE items SET name = COALESCE(?, name), price = COALESCE(?, price) WHERE id = ?\").run(data.name ?? null, data.price ?? null, id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  return send(res, 200, one(id));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items/raise\" && req.method === \"POST\") { db.exec(\"BEGIN\"); db.prepare(\"UPDATE items SET price = price + 1\").run(); db.exec(\"COMMIT\"); return send(res, 200, db.prepare(\"SELECT * FROM items\").all()); }\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "sqlite-changes-barangay"
  },
  {
    "id": "api-sqlite-changes-barangay-9",
    "index": 279,
    "task": "You will change the GET /items route to send a header. The header says how many rows are there. This helps clients know the total. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (req.url === \"/items\") { const rows = db.prepare(\"SELECT * FROM items\").all(); res.setHeader(\"X-Total-Count\", String(rows.length)); return send(res, 200, rows); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Clearance\",50],[\"Permit\",300],[\"ID\",20]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "header",
        "label": "GET /items sends X-Total-Count: 3",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "header": {
          "name": "x-total-count",
          "value": "3"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use res.setHeader to add the X-Total-Count header with the row count."
      },
      {
        "level": 2,
        "text": "Change the GET /items block in server.js to include the header.\n\nIn server.js:\n```\n  if (req.url === \"/items\") { const rows = db.prepare(\"SELECT * FROM items\").all(); res.setHeader(\"X-Total-Count\", String(rows.length)); return send(res, 200, rows); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Clearance\",50],[\"Permit\",300],[\"ID\",20]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  res.statusCode = 204;\n  return res.end();\n}\nasync function updateItem(req, res, id) {\n  const data = JSON.parse(await readBody(req));\n  if (data.price !== undefined && (!Number.isInteger(data.price) || data.price < 0)) return send(res, 422, { error: \"price must be a whole number 0 or more\" });\n  const result = db.prepare(\"UPDATE items SET name = COALESCE(?, name), price = COALESCE(?, price) WHERE id = ?\").run(data.name ?? null, data.price ?? null, id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  return send(res, 200, one(id));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items/raise\" && req.method === \"POST\") { db.exec(\"BEGIN\"); db.prepare(\"UPDATE items SET price = price + 1\").run(); db.exec(\"COMMIT\"); return send(res, 200, db.prepare(\"SELECT * FROM items\").all()); }\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") { const rows = db.prepare(\"SELECT * FROM items\").all(); res.setHeader(\"X-Total-Count\", String(rows.length)); return send(res, 200, rows); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "sqlite-changes-barangay"
  },
  {
    "id": "api-sqlite-changes-barangay-10",
    "index": 280,
    "task": "You will add a route to search by exact name. This stops bad text from changing the SQL. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items/search\") return send(res, 200, db.prepare(\"SELECT * FROM items WHERE name = ?\").all(url.searchParams.get(\"name\") ?? \"\"));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Clearance\",50],[\"Permit\",300],[\"ID\",20]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "found",
        "label": "GET /items/search?name=Permit finds it",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/search?name=Permit"
          }
        ],
        "bodyContains": "\"name\":\"Permit\""
      },
      {
        "id": "safe",
        "label": "A search for ' OR '1'='1 finds nothing",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/search?name=%27%20OR%20%271%27%3D%271"
          }
        ],
        "status": 200,
        "bodyContains": "[]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use URL to get the search parameter and pass it to the SQL query."
      },
      {
        "level": 2,
        "text": "Add the two lines after the count route in server.js.\n\nIn server.js:\n```\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items/search\") return send(res, 200, db.prepare(\"SELECT * FROM items WHERE name = ?\").all(url.searchParams.get(\"name\") ?? \"\"));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Clearance\",50],[\"Permit\",300],[\"ID\",20]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  res.statusCode = 204;\n  return res.end();\n}\nasync function updateItem(req, res, id) {\n  const data = JSON.parse(await readBody(req));\n  if (data.price !== undefined && (!Number.isInteger(data.price) || data.price < 0)) return send(res, 422, { error: \"price must be a whole number 0 or more\" });\n  const result = db.prepare(\"UPDATE items SET name = COALESCE(?, name), price = COALESCE(?, price) WHERE id = ?\").run(data.name ?? null, data.price ?? null, id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  return send(res, 200, one(id));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items/raise\" && req.method === \"POST\") { db.exec(\"BEGIN\"); db.prepare(\"UPDATE items SET price = price + 1\").run(); db.exec(\"COMMIT\"); return send(res, 200, db.prepare(\"SELECT * FROM items\").all()); }\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items/search\") return send(res, 200, db.prepare(\"SELECT * FROM items WHERE name = ?\").all(url.searchParams.get(\"name\") ?? \"\"));\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") { const rows = db.prepare(\"SELECT * FROM items\").all(); res.setHeader(\"X-Total-Count\", String(rows.length)); return send(res, 200, rows); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 5,
    "projectId": "sqlite-changes-barangay"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: errors-barangay.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-errors-barangay-1",
    "index": 281,
    "task": "Add a route for /boom at the top of handle. This lets you test error handling. Change the handle call at the bottom to catch errors. This stops the server from crashing. The code below does this. Run the checker to test it.\n\nIn server.js:\n```\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  handle(req, res).catch(() => send(res, 500, { error: \"Something went wrong\" }));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res);\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "500",
        "label": "GET /boom answers 500",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/boom"
          }
        ],
        "status": 500,
        "bodyContains": "Something went wrong"
      },
      {
        "id": "alive",
        "label": "The server keeps answering after an error",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/boom"
          },
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "status": 200
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Throw an error to see how the server responds."
      },
      {
        "level": 2,
        "text": "Put the code in server.js, right after the handle function starts.\n\nIn server.js:\n```\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  handle(req, res).catch(() => send(res, 500, { error: \"Something went wrong\" }));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res).catch(() => send(res, 500, { error: \"Something went wrong\" }));\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "errors-barangay"
  },
  {
    "id": "api-errors-barangay-2",
    "index": 282,
    "task": "Add a counter variable above handle. This counts errors. Change the catch to include the counter. This helps track how many errors happen. The code below does this. Run the checker to test it.\n\nIn server.js:\n```\nlet errorCount = 0;\n  handle(req, res).catch(() => send(res, 500, { error: \"Something went wrong\", id: ++errorCount }));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res);\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "id",
        "label": "The first error answers id 1",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/boom"
          }
        ],
        "bodyContains": "\"id\":1"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use a variable to count each error you throw."
      },
      {
        "level": 2,
        "text": "Put the code in server.js, right after the errorCount line.\n\nIn server.js:\n```\nlet errorCount = 0;\n  handle(req, res).catch(() => send(res, 500, { error: \"Something went wrong\", id: ++errorCount }));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet errorCount = 0;\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res).catch(() => send(res, 500, { error: \"Something went wrong\", id: ++errorCount }));\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "errors-barangay"
  },
  {
    "id": "api-errors-barangay-3",
    "index": 283,
    "task": "Add an HttpError class to define custom error messages. Add a /teapot route to test it. Change the catch to use this class. This lets you send specific error codes. The code below does this. Run the checker to test it.\n\nIn server.js:\n```\nclass HttpError extends Error { constructor(status, message) { super(message); this.status = status; } }\n  if (req.url === \"/teapot\") throw new HttpError(418, \"I am a teapot\");\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", id: ++errorCount }));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res);\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "teapot",
        "label": "GET /teapot answers 418",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/teapot"
          }
        ],
        "status": 418,
        "bodyContains": "I am a teapot"
      },
      {
        "id": "hidden",
        "label": "Unexpected errors still hide their details",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/boom"
          }
        ],
        "status": 500,
        "bodyContains": "Something went wrong"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Create a class to hold error codes and messages."
      },
      {
        "level": 2,
        "text": "Put the code in server.js, right after the HttpError class.\n\nIn server.js:\n```\nclass HttpError extends Error { constructor(status, message) { super(message); this.status = status; } }\n  if (req.url === \"/teapot\") throw new HttpError(418, \"I am a teapot\");\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", id: ++errorCount }));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet errorCount = 0;\nclass HttpError extends Error { constructor(status, message) { super(message); this.status = status; } }\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  if (req.url === \"/teapot\") throw new HttpError(418, \"I am a teapot\");\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", id: ++errorCount }));\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "errors-barangay"
  },
  {
    "id": "api-errors-barangay-4",
    "index": 284,
    "task": "Add a readJson helper to check if the body is valid JSON. Use it in createItem to throw an error if it's not. This answers 400 for bad JSON. The code below does this. Run the checker to test it.\n\nIn server.js:\n```\nasync function readJson(req) { const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\n  const item = await readJson(req);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res);\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "400",
        "label": "POST /items with broken JSON answers 400",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "not json",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 400
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Check the body before you use it. If it's not JSON, throw an error."
      },
      {
        "level": 2,
        "text": "Put the code in server.js, right after the handle function.\n\nIn server.js:\n```\nasync function readJson(req) { const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\n  const item = await readJson(req);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet errorCount = 0;\nclass HttpError extends Error { constructor(status, message) { super(message); this.status = status; } }\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function readJson(req) { const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\nasync function createItem(req, res) {\n  const item = await readJson(req);\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  if (req.url === \"/teapot\") throw new HttpError(418, \"I am a teapot\");\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", id: ++errorCount }));\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "errors-barangay"
  },
  {
    "id": "api-errors-barangay-5",
    "index": 285,
    "task": "Add a route before GET /items to check the HTTP method. If it's not GET or POST, throw an HttpError with 405. This answers 405 with an Allow header. The code below does this. Run the checker to test it.\n\nIn server.js:\n```\n  if (req.url === \"/items\" && ![\"GET\", \"POST\"].includes(req.method)) { res.setHeader(\"Allow\", \"GET, POST\"); throw new HttpError(405, \"Method not allowed\"); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res);\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "405",
        "label": "DELETE /items answers 405 with Allow: GET, POST",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "DELETE",
            "path": "/items"
          }
        ],
        "status": 405,
        "header": {
          "name": "allow",
          "value": "GET, POST"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Check the method before you process the request. If it's wrong, throw an error."
      },
      {
        "level": 2,
        "text": "Put the code in server.js, right before the GET /items line.\n\nIn server.js:\n```\n  if (req.url === \"/items\" && ![\"GET\", \"POST\"].includes(req.method)) { res.setHeader(\"Allow\", \"GET, POST\"); throw new HttpError(405, \"Method not allowed\"); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet errorCount = 0;\nclass HttpError extends Error { constructor(status, message) { super(message); this.status = status; } }\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function readJson(req) { const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\nasync function createItem(req, res) {\n  const item = await readJson(req);\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  if (req.url === \"/teapot\") throw new HttpError(418, \"I am a teapot\");\n  if (req.url === \"/items\" && ![\"GET\", \"POST\"].includes(req.method)) { res.setHeader(\"Allow\", \"GET, POST\"); throw new HttpError(405, \"Method not allowed\"); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", id: ++errorCount }));\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "errors-barangay"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: errors-barangay.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-errors-barangay-6",
    "index": 286,
    "task": "You will change the readBody helper. This helper reads the body of a request. When the body is over 1,000 characters, it throws an error with status 413. This tells the user the body is too big. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) { text += chunk; if (text.length > 1000) throw new HttpError(413, \"Body too large\"); } return text; }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res);\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "413",
        "label": "A 2,000-character body answers 413",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "{\"name\":\"\",\"price\":1}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 413
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Think of 413 as 'too big', the body must be smaller than 1,000 characters."
      },
      {
        "level": 2,
        "text": "Put the code inside the readBody function in server.js.\n\nIn server.js:\n```\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) { text += chunk; if (text.length > 1000) throw new HttpError(413, \"Body too large\"); } return text; }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet errorCount = 0;\nclass HttpError extends Error { constructor(status, message) { super(message); this.status = status; } }\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) { text += chunk; if (text.length > 1000) throw new HttpError(413, \"Body too large\"); } return text; }\nasync function readJson(req) { const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\nasync function createItem(req, res) {\n  const item = await readJson(req);\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  if (req.url === \"/teapot\") throw new HttpError(418, \"I am a teapot\");\n  if (req.url === \"/items\" && ![\"GET\", \"POST\"].includes(req.method)) { res.setHeader(\"Allow\", \"GET, POST\"); throw new HttpError(405, \"Method not allowed\"); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", id: ++errorCount }));\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "errors-barangay"
  },
  {
    "id": "api-errors-barangay-7",
    "index": 287,
    "task": "You will change the readJson helper. This helper checks if the request is marked as JSON. If not, it throws an error with status 415. This tells the user to send JSON. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\nasync function readJson(req) { if (!req.headers[\"content-type\"]?.includes(\"application/json\")) throw new HttpError(415, \"Send JSON\"); const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res);\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "415",
        "label": "POST /items with text/plain answers 415",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "name=Tea",
            "headers": {
              "Content-Type": "text/plain"
            }
          }
        ],
        "status": 415
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "415 means 'wrong format', the request must say it is JSON."
      },
      {
        "level": 2,
        "text": "Put the code inside the readJson function in server.js.\n\nIn server.js:\n```\nasync function readJson(req) { if (!req.headers[\"content-type\"]?.includes(\"application/json\")) throw new HttpError(415, \"Send JSON\"); const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet errorCount = 0;\nclass HttpError extends Error { constructor(status, message) { super(message); this.status = status; } }\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) { text += chunk; if (text.length > 1000) throw new HttpError(413, \"Body too large\"); } return text; }\nasync function readJson(req) { if (!req.headers[\"content-type\"]?.includes(\"application/json\")) throw new HttpError(415, \"Send JSON\"); const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\nasync function createItem(req, res) {\n  const item = await readJson(req);\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  if (req.url === \"/teapot\") throw new HttpError(418, \"I am a teapot\");\n  if (req.url === \"/items\" && ![\"GET\", \"POST\"].includes(req.method)) { res.setHeader(\"Allow\", \"GET, POST\"); throw new HttpError(405, \"Method not allowed\"); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", id: ++errorCount }));\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "errors-barangay"
  },
  {
    "id": "api-errors-barangay-8",
    "index": 288,
    "task": "You will add one line in the createItem function. This line checks if an item with that name already exists. If it does, it throws an error with status 409. This tells the user the name is taken. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (items.some((existing) => existing.name === item.name)) throw new HttpError(409, \"Name already exists\");\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res);\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "409",
        "label": "POST /items with Clearance again answers 409",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "{\"name\":\"Clearance\",\"price\":1}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 409
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "409 means 'conflict', the item name is already in use."
      },
      {
        "level": 2,
        "text": "Put the code right after you read the item in createItem.\n\nIn server.js:\n```\n  if (items.some((existing) => existing.name === item.name)) throw new HttpError(409, \"Name already exists\");\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet errorCount = 0;\nclass HttpError extends Error { constructor(status, message) { super(message); this.status = status; } }\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) { text += chunk; if (text.length > 1000) throw new HttpError(413, \"Body too large\"); } return text; }\nasync function readJson(req) { if (!req.headers[\"content-type\"]?.includes(\"application/json\")) throw new HttpError(415, \"Send JSON\"); const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\nasync function createItem(req, res) {\n  const item = await readJson(req);\n  if (items.some((existing) => existing.name === item.name)) throw new HttpError(409, \"Name already exists\");\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  if (req.url === \"/teapot\") throw new HttpError(418, \"I am a teapot\");\n  if (req.url === \"/items\" && ![\"GET\", \"POST\"].includes(req.method)) { res.setHeader(\"Allow\", \"GET, POST\"); throw new HttpError(405, \"Method not allowed\"); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", id: ++errorCount }));\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "errors-barangay"
  },
  {
    "id": "api-errors-barangay-9",
    "index": 289,
    "task": "You will change the catch block at the bottom. This block handles errors. Now it adds the status code inside the error body. This helps the user see what went wrong. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", status: error.status ?? 500, id: ++errorCount }));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res);\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "status",
        "label": "The /teapot error body includes status 418",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/teapot"
          }
        ],
        "bodyContains": "\"status\":418"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The error body must include the status code, like 'status: 418'."
      },
      {
        "level": 2,
        "text": "Put the code inside the catch block at the bottom of server.js.\n\nIn server.js:\n```\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", status: error.status ?? 500, id: ++errorCount }));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet errorCount = 0;\nclass HttpError extends Error { constructor(status, message) { super(message); this.status = status; } }\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) { text += chunk; if (text.length > 1000) throw new HttpError(413, \"Body too large\"); } return text; }\nasync function readJson(req) { if (!req.headers[\"content-type\"]?.includes(\"application/json\")) throw new HttpError(415, \"Send JSON\"); const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\nasync function createItem(req, res) {\n  const item = await readJson(req);\n  if (items.some((existing) => existing.name === item.name)) throw new HttpError(409, \"Name already exists\");\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  if (req.url === \"/teapot\") throw new HttpError(418, \"I am a teapot\");\n  if (req.url === \"/items\" && ![\"GET\", \"POST\"].includes(req.method)) { res.setHeader(\"Allow\", \"GET, POST\"); throw new HttpError(405, \"Method not allowed\"); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", status: error.status ?? 500, id: ++errorCount }));\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "errors-barangay"
  },
  {
    "id": "api-errors-barangay-10",
    "index": 290,
    "task": "You will replace the last line of handle. This line now throws an error with status 404 and a message that says what method and path were used. This tells the user the route does not exist. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  throw new HttpError(404, `No route for ${req.method} ${req.url}`);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res);\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "route",
        "label": "GET /nope answers No route for GET /nope",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/nope"
          }
        ],
        "status": 404,
        "bodyContains": "No route for GET /nope"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "404 means 'not found', the route does not exist for that method and path."
      },
      {
        "level": 2,
        "text": "Put the code at the very end of the handle function in server.js.\n\nIn server.js:\n```\n  throw new HttpError(404, `No route for ${req.method} ${req.url}`);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Clearance\", price: 50 }, { id: 2, name: \"Permit\", price: 300 }, { id: 3, name: \"ID\", price: 20 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet errorCount = 0;\nclass HttpError extends Error { constructor(status, message) { super(message); this.status = status; } }\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) { text += chunk; if (text.length > 1000) throw new HttpError(413, \"Body too large\"); } return text; }\nasync function readJson(req) { if (!req.headers[\"content-type\"]?.includes(\"application/json\")) throw new HttpError(415, \"Send JSON\"); const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\nasync function createItem(req, res) {\n  const item = await readJson(req);\n  if (items.some((existing) => existing.name === item.name)) throw new HttpError(409, \"Name already exists\");\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  if (req.url === \"/teapot\") throw new HttpError(418, \"I am a teapot\");\n  if (req.url === \"/items\" && ![\"GET\", \"POST\"].includes(req.method)) { res.setHeader(\"Allow\", \"GET, POST\"); throw new HttpError(405, \"Method not allowed\"); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  throw new HttpError(404, `No route for ${req.method} ${req.url}`);\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", status: error.status ?? 500, id: ++errorCount }));\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "errors-barangay"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: api-design-barangay.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-api-design-barangay-1",
    "index": 291,
    "task": "You add a route for /v1/items. This lets the API answer requests for the list. The code below goes after the url line in server.js. Run the checker to test it.\n\nIn server.js:\n```\n  if (url.pathname === \"/v1/items\") return send(res, 200, items);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst names = [\"Clearance\",\"Permit\",\"ID\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "v1",
        "label": "GET /v1/items answers the list",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/v1/items"
          }
        ],
        "status": 200,
        "bodyContains": "\"name\":\"Clearance 1\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The route checks the URL path. If it matches, it sends the list."
      },
      {
        "level": 2,
        "text": "Put the code after the url line in server.js.\n\nIn server.js:\n```\n  if (url.pathname === \"/v1/items\") return send(res, 200, items);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst names = [\"Clearance\",\"Permit\",\"ID\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/v1/items\") return send(res, 200, items);\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "api-design-barangay"
  },
  {
    "id": "api-api-design-barangay-2",
    "index": 292,
    "task": "You add a page variable to get the page number from the URL. You change the route to show only five items per page. The code below goes after the url line. Run the checker to test it.\n\nIn server.js:\n```\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  if (url.pathname === \"/v1/items\") return send(res, 200, items.slice((page - 1) * 5, page * 5));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst names = [\"Clearance\",\"Permit\",\"ID\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "page2",
        "label": "GET /v1/items?page=2 answers items 6 to 10",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/v1/items?page=2"
          }
        ],
        "bodyContains": "[{\"id\":6,\"name\":\"ID 6\",\"price\":30},{\"id\":7,\"name\":\"Clearance 7\",\"price\":35},{\"id\":8,\"name\":\"Permit 8\",\"price\":40},{\"id\":9,\"name\":\"ID 9\",\"price\":45},{\"id\":10,\"name\":\"Clearance 10\",\"price\":50}]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The page number comes from the ?page query. Use it to slice the items."
      },
      {
        "level": 2,
        "text": "Put the code after the url line in server.js.\n\nIn server.js:\n```\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  if (url.pathname === \"/v1/items\") return send(res, 200, items.slice((page - 1) * 5, page * 5));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst names = [\"Clearance\",\"Permit\",\"ID\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  if (url.pathname === \"/v1/items\") return send(res, 200, items.slice((page - 1) * 5, page * 5));\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "api-design-barangay"
  },
  {
    "id": "api-api-design-barangay-3",
    "index": 293,
    "task": "You add a perPage variable to let the client choose how many items per page. You change the route to use this number. The code below goes after the url line. Run the checker to test it.\n\nIn server.js:\n```\n  const perPage = Number(url.searchParams.get(\"perPage\") ?? 5);\n  if (url.pathname === \"/v1/items\") return send(res, 200, items.slice((page - 1) * perPage, page * perPage));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst names = [\"Clearance\",\"Permit\",\"ID\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "per",
        "label": "GET /v1/items?perPage=3 answers three items",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/v1/items?perPage=3"
          }
        ],
        "bodyContains": "[{\"id\":1,\"name\":\"Clearance 1\",\"price\":5},{\"id\":2,\"name\":\"Permit 2\",\"price\":10},{\"id\":3,\"name\":\"ID 3\",\"price\":15}]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The perPage number comes from the ?perPage query. Use it to slice the items."
      },
      {
        "level": 2,
        "text": "Put the code after the url line in server.js.\n\nIn server.js:\n```\n  const perPage = Number(url.searchParams.get(\"perPage\") ?? 5);\n  if (url.pathname === \"/v1/items\") return send(res, 200, items.slice((page - 1) * perPage, page * perPage));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst names = [\"Clearance\",\"Permit\",\"ID\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  const perPage = Number(url.searchParams.get(\"perPage\") ?? 5);\n  if (url.pathname === \"/v1/items\") return send(res, 200, items.slice((page - 1) * perPage, page * perPage));\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "api-design-barangay"
  },
  {
    "id": "api-api-design-barangay-4",
    "index": 294,
    "task": "You add a data variable to hold the sliced items. You change the route to send the page, perPage, and data in one object. The code below goes after the url line. Run the checker to test it.\n\nIn server.js:\n```\n  const data = items.slice((page - 1) * perPage, page * perPage);\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst names = [\"Clearance\",\"Permit\",\"ID\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "envelope",
        "label": "The answer says page 2 and perPage 5",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/v1/items?page=2"
          }
        ],
        "bodyContains": "\"page\":2,\"perPage\":5"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Wrap the sliced items in an object with data, page, and perPage."
      },
      {
        "level": 2,
        "text": "Put the code after the url line in server.js.\n\nIn server.js:\n```\n  const data = items.slice((page - 1) * perPage, page * perPage);\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst names = [\"Clearance\",\"Permit\",\"ID\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  const perPage = Number(url.searchParams.get(\"perPage\") ?? 5);\n  const data = items.slice((page - 1) * perPage, page * perPage);\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage });\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "api-design-barangay"
  },
  {
    "id": "api-api-design-barangay-5",
    "index": 295,
    "task": "You add a total field to the answer. It shows how many items are in the list. The code below changes the route. Run the checker to test it.\n\nIn server.js:\n```\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst names = [\"Clearance\",\"Permit\",\"ID\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "total",
        "label": "The answer says total 12",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/v1/items"
          }
        ],
        "bodyContains": "\"total\":12"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Add total: items.length to the object sent in the route."
      },
      {
        "level": 2,
        "text": "Put the code after the url line in server.js.\n\nIn server.js:\n```\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst names = [\"Clearance\",\"Permit\",\"ID\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  const perPage = Number(url.searchParams.get(\"perPage\") ?? 5);\n  const data = items.slice((page - 1) * perPage, page * perPage);\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length });\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "api-design-barangay"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: api-design-barangay.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-api-design-barangay-6",
    "index": 296,
    "task": "Add a line for next. This line tells the browser where to go for the next page. The code below does that. Run the checker to test it. The checker will test if page 1 links to page 2 and if the last page has no next link.\n\nIn server.js:\n```\n  const next = page * perPage < items.length ? `/v1/items?page=${page + 1}&perPage=${perPage}` : null;\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length, next });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst names = [\"Clearance\",\"Permit\",\"ID\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "next",
        "label": "Page 1 links to page 2",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/v1/items"
          }
        ],
        "bodyContains": "\"next\":\"/v1/items?page=2&perPage=5\""
      },
      {
        "id": "last",
        "label": "The last page has no next link",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/v1/items?page=3"
          }
        ],
        "bodyContains": "\"next\":null"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The next link should only appear if there is a next page. If not, it should be null."
      },
      {
        "level": 2,
        "text": "Put the code after the line that sets the data array, before the return statement.\n\nIn server.js:\n```\n  const next = page * perPage < items.length ? `/v1/items?page=${page + 1}&perPage=${perPage}` : null;\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length, next });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst names = [\"Clearance\",\"Permit\",\"ID\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  const perPage = Number(url.searchParams.get(\"perPage\") ?? 5);\n  const data = items.slice((page - 1) * perPage, page * perPage);\n  const next = page * perPage < items.length ? `/v1/items?page=${page + 1}&perPage=${perPage}` : null;\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length, next });\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "api-design-barangay"
  },
  {
    "id": "api-api-design-barangay-7",
    "index": 297,
    "task": "Add a line for previous. This line tells the browser where to go for the previous page. The code below does that. Run the checker to test it. The checker will test if page 2 links back to page 1.\n\nIn server.js:\n```\n  const previous = page > 1 ? `/v1/items?page=${page - 1}&perPage=${perPage}` : null;\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length, next, previous });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst names = [\"Clearance\",\"Permit\",\"ID\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "previous",
        "label": "Page 2 links back to page 1",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/v1/items?page=2"
          }
        ],
        "bodyContains": "\"previous\":\"/v1/items?page=1&perPage=5\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The previous link should only appear if there is a previous page. If not, it should be null."
      },
      {
        "level": 2,
        "text": "Put the code after the next line, before the return statement.\n\nIn server.js:\n```\n  const previous = page > 1 ? `/v1/items?page=${page - 1}&perPage=${perPage}` : null;\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length, next, previous });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst names = [\"Clearance\",\"Permit\",\"ID\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  const perPage = Number(url.searchParams.get(\"perPage\") ?? 5);\n  const data = items.slice((page - 1) * perPage, page * perPage);\n  const next = page * perPage < items.length ? `/v1/items?page=${page + 1}&perPage=${perPage}` : null;\n  const previous = page > 1 ? `/v1/items?page=${page - 1}&perPage=${perPage}` : null;\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length, next, previous });\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "api-design-barangay"
  },
  {
    "id": "api-api-design-barangay-8",
    "index": 298,
    "task": "Add a line to check if the page number is a whole number 1 or more. If not, send a 400 error. The code below does that. Run the checker to test it. The checker will test if asking for page 0 gives a 400 error.\n\nIn server.js:\n```\n  if (!Number.isInteger(page) || page < 1) return send(res, 400, { error: \"page must be a whole number 1 or more\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst names = [\"Clearance\",\"Permit\",\"ID\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "bad",
        "label": "GET /v1/items?page=0 answers 400",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/v1/items?page=0"
          }
        ],
        "status": 400
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "If the page number is not a whole number or less than 1, the server must reject it with a 400 error."
      },
      {
        "level": 2,
        "text": "Put the code after the line that sets the page variable, before the data line.\n\nIn server.js:\n```\n  if (!Number.isInteger(page) || page < 1) return send(res, 400, { error: \"page must be a whole number 1 or more\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst names = [\"Clearance\",\"Permit\",\"ID\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  if (!Number.isInteger(page) || page < 1) return send(res, 400, { error: \"page must be a whole number 1 or more\" });\n  const perPage = Number(url.searchParams.get(\"perPage\") ?? 5);\n  const data = items.slice((page - 1) * perPage, page * perPage);\n  const next = page * perPage < items.length ? `/v1/items?page=${page + 1}&perPage=${perPage}` : null;\n  const previous = page > 1 ? `/v1/items?page=${page - 1}&perPage=${perPage}` : null;\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length, next, previous });\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "api-design-barangay"
  },
  {
    "id": "api-api-design-barangay-9",
    "index": 299,
    "task": "Change the perPage line to limit it to 10. The code below does that. Run the checker to test it. The checker will test if asking for 100 items per page still returns 10.\n\nIn server.js:\n```\n  const perPage = Math.min(Number(url.searchParams.get(\"perPage\") ?? 5), 10);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst names = [\"Clearance\",\"Permit\",\"ID\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "cap",
        "label": "GET /v1/items?perPage=100 answers perPage 10",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/v1/items?perPage=100"
          }
        ],
        "bodyContains": "\"perPage\":10,\"total\":12"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The server must never send more than 10 items per page, no matter what the client asks."
      },
      {
        "level": 2,
        "text": "Replace the current perPage line with the code below.\n\nIn server.js:\n```\n  const perPage = Math.min(Number(url.searchParams.get(\"perPage\") ?? 5), 10);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst names = [\"Clearance\",\"Permit\",\"ID\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  if (!Number.isInteger(page) || page < 1) return send(res, 400, { error: \"page must be a whole number 1 or more\" });\n  const perPage = Math.min(Number(url.searchParams.get(\"perPage\") ?? 5), 10);\n  const data = items.slice((page - 1) * perPage, page * perPage);\n  const next = page * perPage < items.length ? `/v1/items?page=${page + 1}&perPage=${perPage}` : null;\n  const previous = page > 1 ? `/v1/items?page=${page - 1}&perPage=${perPage}` : null;\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length, next, previous });\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "api-design-barangay"
  },
  {
    "id": "api-api-design-barangay-10",
    "index": 300,
    "task": "Change the old /items route to mark it as deprecated. Add a header to tell the browser it's old. The code below does that. Run the checker to test it. The checker will test if the old route sends a Deprecation header.\n\nIn server.js:\n```\n  if (url.pathname === \"/items\") { res.setHeader(\"Deprecation\", \"true\"); return send(res, 200, items); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Barangay Office API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst names = [\"Clearance\",\"Permit\",\"ID\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "deprecated",
        "label": "GET /items sends Deprecation: true",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "header": {
          "name": "deprecation",
          "value": "true"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The old route must send a Deprecation header to tell users it's outdated."
      },
      {
        "level": 2,
        "text": "Put the code after the line that checks if the path is /items.\n\nIn server.js:\n```\n  if (url.pathname === \"/items\") { res.setHeader(\"Deprecation\", \"true\"); return send(res, 200, items); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst names = [\"Clearance\",\"Permit\",\"ID\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  if (!Number.isInteger(page) || page < 1) return send(res, 400, { error: \"page must be a whole number 1 or more\" });\n  const perPage = Math.min(Number(url.searchParams.get(\"perPage\") ?? 5), 10);\n  const data = items.slice((page - 1) * perPage, page * perPage);\n  const next = page * perPage < items.length ? `/v1/items?page=${page + 1}&perPage=${perPage}` : null;\n  const previous = page > 1 ? `/v1/items?page=${page - 1}&perPage=${perPage}` : null;\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length, next, previous });\n  if (url.pathname === \"/items\") { res.setHeader(\"Deprecation\", \"true\"); return send(res, 200, items); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "api-design-barangay"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: first-server-school-club.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-first-server-school-club-1",
    "index": 301,
    "task": "Your server must answer every request with the text 'School Club API'.\nChange the line in server.js that says res.end(\"School Club API\");.\nThis tells the server what to send back to anyone who visits it.\nRun the checker to confirm your server answers correctly.\n\nIn server.js:\n```\n  res.end(\"School Club API\");\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "root",
        "label": "GET / answers School Club API",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/"
          }
        ],
        "status": 200,
        "bodyContains": "School Club API"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The server must send the same message for every request."
      },
      {
        "level": 2,
        "text": "Put the code in server.js where it says res.end(\"School Club API\");\n\nIn server.js:\n```\n  res.end(\"School Club API\");\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"School Club API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "first-server-school-club"
  },
  {
    "id": "api-first-server-school-club-2",
    "index": 302,
    "task": "Your server must say the answer is plain text.\nAdd one line above res.end in server.js to set the header.\nThis tells the browser or tool how to read the answer.\nRun the checker to confirm your server sends plain text.\n\nIn server.js:\n```\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "type",
        "label": "GET / says its body is plain text",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/"
          }
        ],
        "header": {
          "name": "content-type",
          "value": "text/plain"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Set the header to tell the receiver the type of answer."
      },
      {
        "level": 2,
        "text": "Put the code in server.js right above the res.end line.\n\nIn server.js:\n```\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.end(\"School Club API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "first-server-school-club"
  },
  {
    "id": "api-first-server-school-club-3",
    "index": 303,
    "task": "Your server must add a custom header named X-Place.\nAdd one line above res.end in server.js to set it to 'school-club'.\nThis helps tools know where the server is located.\nRun the checker to confirm your server sends the X-Place header.\n\nIn server.js:\n```\n  res.setHeader(\"X-Place\", \"school-club\");\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "custom",
        "label": "GET / sends X-Place: school-club",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/"
          }
        ],
        "header": {
          "name": "x-place",
          "value": "school-club"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Add the header to help tools identify your server's location."
      },
      {
        "level": 2,
        "text": "Put the code in server.js right above the res.end line.\n\nIn server.js:\n```\n  res.setHeader(\"X-Place\", \"school-club\");\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.setHeader(\"X-Place\", \"school-club\");\n  res.end(\"School Club API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "first-server-school-club"
  },
  {
    "id": "api-first-server-school-club-4",
    "index": 304,
    "task": "Your server must answer /health with the word 'healthy'.\nAdd one line at the top of the handler to check if the request is for /health.\nThis lets a monitor know the server is running.\nRun the checker to confirm your server answers /health correctly.\n\nIn server.js:\n```\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "health",
        "label": "GET /health answers healthy",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/health"
          }
        ],
        "status": 200,
        "bodyContains": "healthy"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Check the URL to see if it is /health, then send 'healthy'."
      },
      {
        "level": 2,
        "text": "Put the code in server.js right at the top of the handler.\n\nIn server.js:\n```\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.setHeader(\"X-Place\", \"school-club\");\n  res.end(\"School Club API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "first-server-school-club"
  },
  {
    "id": "api-first-server-school-club-5",
    "index": 305,
    "task": "Your server must answer any other path with status 404 Not found.\nAdd one line right after the /health check to handle unknown paths.\nThis tells users the path does not exist.\nRun the checker to confirm your server sends 404 for unknown paths.\n\nIn server.js:\n```\n  if (req.url !== \"/\") { res.statusCode = 404; res.end(\"Not found\"); return; }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "missing",
        "label": "GET /missing answers 404",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/missing"
          }
        ],
        "status": 404
      },
      {
        "id": "root",
        "label": "GET / still answers 200",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/"
          }
        ],
        "status": 200
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Check if the URL is not /, then send 404 and 'Not found'."
      },
      {
        "level": 2,
        "text": "Put the code in server.js right after the /health check.\n\nIn server.js:\n```\n  if (req.url !== \"/\") { res.statusCode = 404; res.end(\"Not found\"); return; }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n  if (req.url !== \"/\") { res.statusCode = 404; res.end(\"Not found\"); return; }\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.setHeader(\"X-Place\", \"school-club\");\n  res.end(\"School Club API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "first-server-school-club"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: first-server-school-club.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-first-server-school-club-6",
    "index": 306,
    "task": "Your server must refuse any method other than GET. Add this line at the very top of the handler. This stops bad requests. The code below does that. Run the checker to confirm.\n\nIn server.js:\n```\n  if (req.method !== \"GET\") { res.statusCode = 405; res.end(\"Method not allowed\"); return; }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "post",
        "label": "POST / answers 405",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/",
            "body": "{}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 405
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Add a check for the request method before any other code."
      },
      {
        "level": 2,
        "text": "Place this line at the very top of the handler function in server.js.\n\nIn server.js:\n```\n  if (req.method !== \"GET\") { res.statusCode = 405; res.end(\"Method not allowed\"); return; }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.method !== \"GET\") { res.statusCode = 405; res.end(\"Method not allowed\"); return; }\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n  if (req.url !== \"/\") { res.statusCode = 404; res.end(\"Not found\"); return; }\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.setHeader(\"X-Place\", \"school-club\");\n  res.end(\"School Club API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "first-server-school-club"
  },
  {
    "id": "api-first-server-school-club-7",
    "index": 307,
    "task": "Your server must answer /info with JSON. Add this line right after the /health line. This tells the browser it's sending data. The code below does that. Run the checker to confirm.\n\nIn server.js:\n```\n  if (req.url === \"/info\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify({ name: \"School Club\" })); return; }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "info",
        "label": "GET /info answers JSON with the name",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/info"
          }
        ],
        "bodyContains": "\"name\":\"School Club\"",
        "header": {
          "name": "content-type",
          "value": "application/json"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Add a new route for /info that sends a JSON object."
      },
      {
        "level": 2,
        "text": "Place this line right after the /health route in server.js.\n\nIn server.js:\n```\n  if (req.url === \"/info\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify({ name: \"School Club\" })); return; }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.method !== \"GET\") { res.statusCode = 405; res.end(\"Method not allowed\"); return; }\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n  if (req.url === \"/info\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify({ name: \"School Club\" })); return; }\n  if (req.url !== \"/\") { res.statusCode = 404; res.end(\"Not found\"); return; }\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.setHeader(\"X-Place\", \"school-club\");\n  res.end(\"School Club API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "first-server-school-club"
  },
  {
    "id": "api-first-server-school-club-8",
    "index": 308,
    "task": "Your server must count requests and show the count at /visits. Add a counter above the server. Add a route for /visits before the 404 line. This tracks how many people visit. The code below does that. Run the checker to confirm.\n\nIn server.js:\n```\nlet visits = 0;\n  visits += 1;\n  if (req.url === \"/visits\") { res.end(String(visits)); return; }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "visits",
        "label": "After two requests, GET /visits answers 3",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/"
          },
          {
            "method": "GET",
            "path": "/"
          },
          {
            "method": "GET",
            "path": "/visits"
          }
        ],
        "bodyContains": "3"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Declare a counter variable above the server and increment it each time a request comes in."
      },
      {
        "level": 2,
        "text": "Add the /visits route before the 404 line in server.js.\n\nIn server.js:\n```\nlet visits = 0;\n  visits += 1;\n  if (req.url === \"/visits\") { res.end(String(visits)); return; }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nlet visits = 0;\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.method !== \"GET\") { res.statusCode = 405; res.end(\"Method not allowed\"); return; }\n  visits += 1;\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n  if (req.url === \"/info\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify({ name: \"School Club\" })); return; }\n  if (req.url === \"/visits\") { res.end(String(visits)); return; }\n  if (req.url !== \"/\") { res.statusCode = 404; res.end(\"Not found\"); return; }\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.setHeader(\"X-Place\", \"school-club\");\n  res.end(\"School Club API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 5,
    "projectId": "first-server-school-club"
  },
  {
    "id": "api-first-server-school-club-9",
    "index": 309,
    "task": "Your server must tell browsers not to store any answer. Add this line at the very top of the handler. This stops browsers from saving your answers. The code below does that. Run the checker to confirm.\n\nIn server.js:\n```\n  res.setHeader(\"Cache-Control\", \"no-store\");\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "cache",
        "label": "GET / sends Cache-Control: no-store",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/"
          }
        ],
        "header": {
          "name": "cache-control",
          "value": "no-store"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Set the Cache-Control header to no-store at the start of the handler."
      },
      {
        "level": 2,
        "text": "Place this line at the very top of the handler function in server.js.\n\nIn server.js:\n```\n  res.setHeader(\"Cache-Control\", \"no-store\");\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nlet visits = 0;\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.setHeader(\"Cache-Control\", \"no-store\");\n  if (req.method !== \"GET\") { res.statusCode = 405; res.end(\"Method not allowed\"); return; }\n  visits += 1;\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n  if (req.url === \"/info\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify({ name: \"School Club\" })); return; }\n  if (req.url === \"/visits\") { res.end(String(visits)); return; }\n  if (req.url !== \"/\") { res.statusCode = 404; res.end(\"Not found\"); return; }\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.setHeader(\"X-Place\", \"school-club\");\n  res.end(\"School Club API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "first-server-school-club"
  },
  {
    "id": "api-first-server-school-club-10",
    "index": 310,
    "task": "Your server must read a name from the query string at /hello, with 'friend' as the default. Add these two lines right before the 404 line. This lets users say hello with their name. The code below does that. Run the checker to confirm.\n\nIn server.js:\n```\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/hello\") { res.end(`Hello, ${url.searchParams.get(\"name\") ?? \"friend\"}`); return; }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "hello",
        "label": "GET /hello?name=Ana answers Hello, Ana",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/hello?name=Ana"
          }
        ],
        "bodyContains": "Hello, Ana"
      },
      {
        "id": "default",
        "label": "GET /hello answers Hello, friend",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/hello"
          }
        ],
        "bodyContains": "Hello, friend"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use URL to parse the query string and get the name from it."
      },
      {
        "level": 2,
        "text": "Place these lines right before the 404 line in server.js.\n\nIn server.js:\n```\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/hello\") { res.end(`Hello, ${url.searchParams.get(\"name\") ?? \"friend\"}`); return; }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nlet visits = 0;\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.setHeader(\"Cache-Control\", \"no-store\");\n  if (req.method !== \"GET\") { res.statusCode = 405; res.end(\"Method not allowed\"); return; }\n  visits += 1;\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n  if (req.url === \"/info\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify({ name: \"School Club\" })); return; }\n  if (req.url === \"/visits\") { res.end(String(visits)); return; }\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/hello\") { res.end(`Hello, ${url.searchParams.get(\"name\") ?? \"friend\"}`); return; }\n  if (req.url !== \"/\") { res.statusCode = 404; res.end(\"Not found\"); return; }\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.setHeader(\"X-Place\", \"school-club\");\n  res.end(\"School Club API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 5,
    "projectId": "first-server-school-club"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: json-routes-school-club.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-json-routes-school-club-1",
    "index": 311,
    "task": "You will add a line to handle the /items route. This line sends the whole list as JSON. It goes at the top of the handler. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\n  if (req.url === \"/items\") return send(res, 200, items);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "list",
        "label": "GET /items answers the list as JSON",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "bodyContains": "\"name\":\"Shirt\"",
        "header": {
          "name": "content-type",
          "value": "application/json"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The send helper sends data with a status code. It's like sending a message."
      },
      {
        "level": 2,
        "text": "Put this line right after the first line in the handler, where the other routes start.\n\nIn server.js:\n```\n  if (req.url === \"/items\") return send(res, 200, items);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "json-routes-school-club"
  },
  {
    "id": "api-json-routes-school-club-2",
    "index": 312,
    "task": "You will add a line to handle /items/count. This line sends how many items there are. It goes after the /items line. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "count",
        "label": "GET /items/count answers {\"count\":3}",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/count"
          }
        ],
        "bodyContains": "{\"count\":3}"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "You use the length of the items array to count them. It's like counting students."
      },
      {
        "level": 2,
        "text": "Put this line right after the /items line, before the /items/first line.\n\nIn server.js:\n```\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "json-routes-school-club"
  },
  {
    "id": "api-json-routes-school-club-3",
    "index": 313,
    "task": "You will add a line to handle /items/first. This line sends only the first item. It goes after the /items/count line. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "first",
        "label": "GET /items/first answers Shirt",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/first"
          }
        ],
        "bodyContains": "{\"id\":1,\"name\":\"Shirt\",\"price\":250}"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "You use the index 0 to get the first item. It's like picking the first student in line."
      },
      {
        "level": 2,
        "text": "Put this line right after the /items/count line, before the /items/names line.\n\nIn server.js:\n```\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "json-routes-school-club"
  },
  {
    "id": "api-json-routes-school-club-4",
    "index": 314,
    "task": "You will add a line to handle /items/names. This line sends only the names using map. It goes after the /items/first line. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "names",
        "label": "GET /items/names answers the three names",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/names"
          }
        ],
        "bodyContains": "[\"Shirt\",\"Pin\",\"Badge\"]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The map function changes each item to its name. It's like making a list of names only."
      },
      {
        "level": 2,
        "text": "Put this line right after the /items/first line, before the /items/total line.\n\nIn server.js:\n```\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "json-routes-school-club"
  },
  {
    "id": "api-json-routes-school-club-5",
    "index": 315,
    "task": "You will add a line to handle /items/total. This line sends the sum of all prices using reduce. It goes after the /items/names line. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\n  if (req.url === \"/items/total\") return send(res, 200, { total: items.reduce((sum, item) => sum + item.price, 0) });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "total",
        "label": "GET /items/total answers 325",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/total"
          }
        ],
        "bodyContains": "{\"total\":325}"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The reduce function adds up all the prices. It's like adding up all the money."
      },
      {
        "level": 2,
        "text": "Put this line right after the /items/names line. It's the last route.\n\nIn server.js:\n```\n  if (req.url === \"/items/total\") return send(res, 200, { total: items.reduce((sum, item) => sum + item.price, 0) });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n  if (req.url === \"/items/total\") return send(res, 200, { total: items.reduce((sum, item) => sum + item.price, 0) });\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "json-routes-school-club"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: json-routes-school-club.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-json-routes-school-club-6",
    "index": 316,
    "task": "You will fix how the server answers when someone asks for a path that does not exist. This is important so users get a clear error message. The code below replaces the last line of the handler. Run the checker to test it.\n\nIn server.js:\n```\n  send(res, 404, { error: \"Not found\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "missing",
        "label": "GET /nope answers 404 with a JSON error",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/nope"
          }
        ],
        "status": 404,
        "bodyContains": "{\"error\":\"Not found\"}"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "You are making the server say 'Not found' for any unknown path."
      },
      {
        "level": 2,
        "text": "Put the code at the end of the handler, right before the 404 line.\n\nIn server.js:\n```\n  send(res, 404, { error: \"Not found\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n  if (req.url === \"/items/total\") return send(res, 200, { total: items.reduce((sum, item) => sum + item.price, 0) });\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "json-routes-school-club"
  },
  {
    "id": "api-json-routes-school-club-7",
    "index": 317,
    "task": "You will add a special rule for the root path. When someone visits /, the server will show the API's name and its routes. The code below goes at the top of the handler. Run the checker to test it.\n\nIn server.js:\n```\n  if (req.url === \"/\") return send(res, 200, { name: \"School Club\", routes: [\"/items\"] });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "root",
        "label": "GET / lists the routes",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/"
          }
        ],
        "status": 200,
        "bodyContains": "\"routes\":[\"/items\"]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "You are telling the server to show its name and routes when someone visits the main page."
      },
      {
        "level": 2,
        "text": "Put the code at the top of the handler, before any other checks.\n\nIn server.js:\n```\n  if (req.url === \"/\") return send(res, 200, { name: \"School Club\", routes: [\"/items\"] });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/\") return send(res, 200, { name: \"School Club\", routes: [\"/items\"] });\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n  if (req.url === \"/items/total\") return send(res, 200, { total: items.reduce((sum, item) => sum + item.price, 0) });\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "json-routes-school-club"
  },
  {
    "id": "api-json-routes-school-club-8",
    "index": 318,
    "task": "You will add a new route to find the cheapest item. The server will sort all items by price and return the lowest one. The code below goes before the 404 line. Run the checker to test it.\n\nIn server.js:\n```\n  if (req.url === \"/items/cheapest\") return send(res, 200, [...items].sort((a, b) => a.price - b.price)[0]);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "cheapest",
        "label": "GET /items/cheapest answers Pin",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/cheapest"
          }
        ],
        "bodyContains": "\"name\":\"Pin\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "You are letting the server find the item with the lowest price."
      },
      {
        "level": 2,
        "text": "Put the code before the 404 line, after the / route check.\n\nIn server.js:\n```\n  if (req.url === \"/items/cheapest\") return send(res, 200, [...items].sort((a, b) => a.price - b.price)[0]);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/\") return send(res, 200, { name: \"School Club\", routes: [\"/items\"] });\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n  if (req.url === \"/items/total\") return send(res, 200, { total: items.reduce((sum, item) => sum + item.price, 0) });\n  if (req.url === \"/items/cheapest\") return send(res, 200, [...items].sort((a, b) => a.price - b.price)[0]);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 5,
    "projectId": "json-routes-school-club"
  },
  {
    "id": "api-json-routes-school-club-9",
    "index": 319,
    "task": "You will add a route to show items in a readable format. The server will send JSON with extra spaces for easier reading. The code below goes before the 404 line. Run the checker to test it.\n\nIn server.js:\n```\n  if (req.url === \"/items/pretty\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify(items, null, 2)); return; }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "pretty",
        "label": "GET /items/pretty answers indented JSON",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/pretty"
          }
        ],
        "bodyContains": "[\n  {\n    \"id\": 1"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "You are formatting the JSON so it's easier for people to read."
      },
      {
        "level": 2,
        "text": "Put the code before the 404 line, after the / route check.\n\nIn server.js:\n```\n  if (req.url === \"/items/pretty\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify(items, null, 2)); return; }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/\") return send(res, 200, { name: \"School Club\", routes: [\"/items\"] });\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n  if (req.url === \"/items/total\") return send(res, 200, { total: items.reduce((sum, item) => sum + item.price, 0) });\n  if (req.url === \"/items/cheapest\") return send(res, 200, [...items].sort((a, b) => a.price - b.price)[0]);\n  if (req.url === \"/items/pretty\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify(items, null, 2)); return; }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 5,
    "projectId": "json-routes-school-club"
  },
  {
    "id": "api-json-routes-school-club-10",
    "index": 320,
    "task": "You will add a route to show only expensive items. The server will filter items that cost more than 40. The code below goes before the 404 line. Run the checker to test it.\n\nIn server.js:\n```\n  if (req.url === \"/items/expensive\") return send(res, 200, items.filter((item) => item.price > 40));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "expensive",
        "label": "GET /items/expensive answers Shirt and Badge",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/expensive"
          }
        ],
        "bodyContains": "\"name\":\"Shirt\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "You are letting the server show only items that cost more than 40."
      },
      {
        "level": 2,
        "text": "Put the code before the 404 line, after the / route check.\n\nIn server.js:\n```\n  if (req.url === \"/items/expensive\") return send(res, 200, items.filter((item) => item.price > 40));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/\") return send(res, 200, { name: \"School Club\", routes: [\"/items\"] });\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n  if (req.url === \"/items/total\") return send(res, 200, { total: items.reduce((sum, item) => sum + item.price, 0) });\n  if (req.url === \"/items/cheapest\") return send(res, 200, [...items].sort((a, b) => a.price - b.price)[0]);\n  if (req.url === \"/items/pretty\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify(items, null, 2)); return; }\n  if (req.url === \"/items/expensive\") return send(res, 200, items.filter((item) => item.price > 40));\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 5,
    "projectId": "json-routes-school-club"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: request-bodies-school-club.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-request-bodies-school-club-1",
    "index": 321,
    "task": "You will read what the client sends. The code below reads each chunk of text. It joins them into one string. Then it returns that string. This helps you echo back what the client sends. Add this code above the server. Then add the /echo route at the top of the handler. Run the checker to confirm it works.\n\nIn server.js:\n```\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "echo",
        "label": "POST /echo with hello answers it back",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/echo",
            "body": "hello",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "bodyContains": "\"received\":\"hello\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Read each chunk from the request and join them into one string."
      },
      {
        "level": 2,
        "text": "Add the code above the server and the route at the top of the handler.\n\nIn server.js:\n```\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "request-bodies-school-club"
  },
  {
    "id": "api-request-bodies-school-club-2",
    "index": 322,
    "task": "You will turn the text into a JSON object. Use JSON.parse to do this. Add one line after the /echo route. This line parses the text into an object. Then it sends that object back. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "json",
        "label": "POST /echo-json answers the object it received",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/echo-json",
            "body": "{\"name\":\"Ana\"}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "bodyContains": "\"name\":\"Ana\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use JSON.parse to turn the text into an object."
      },
      {
        "level": 2,
        "text": "Add the line after the /echo route.\n\nIn server.js:\n```\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "request-bodies-school-club"
  },
  {
    "id": "api-request-bodies-school-club-3",
    "index": 323,
    "task": "You will answer POST /items with status 201. This means created. Add two lines inside createItem. First, parse the text into an object. Then send that object back with status 201. Run the checker to confirm it works.\n\nIn server.js:\n```\n  const data = JSON.parse(await readBody(req));\n  return send(res, 201, data);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "created",
        "label": "POST /items answers 201 with the item",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "{\"name\":\"Tea\",\"price\":12}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 201,
        "bodyContains": "\"name\":\"Tea\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Parse the text into an object and send it back with status 201."
      },
      {
        "level": 2,
        "text": "Replace the line inside createItem with these two lines.\n\nIn server.js:\n```\n  const data = JSON.parse(await readBody(req));\n  return send(res, 201, data);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const data = JSON.parse(await readBody(req));\n  return send(res, 201, data);\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "request-bodies-school-club"
  },
  {
    "id": "api-request-bodies-school-club-4",
    "index": 324,
    "task": "You will store the new item so it shows up in the list. Add one line inside createItem. This line adds the item to the items array. Then the GET /items route will show it. Run the checker to confirm it works.\n\nIn server.js:\n```\n  items.push(data);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "stored",
        "label": "After POST /items, GET /items includes Tea",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "{\"name\":\"Tea\",\"price\":12}",
            "headers": {
              "Content-Type": "application/json"
            }
          },
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "bodyContains": "\"name\":\"Tea\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Add the item to the items array so it appears in the list."
      },
      {
        "level": 2,
        "text": "Add the line before the return inside createItem.\n\nIn server.js:\n```\n  items.push(data);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const data = JSON.parse(await readBody(req));\n  items.push(data);\n  return send(res, 201, data);\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "request-bodies-school-club"
  },
  {
    "id": "api-request-bodies-school-club-5",
    "index": 325,
    "task": "You will give each new item an id one higher than the last. Change createItem. First, make a new item with id: items.length + 1. Then add it to the array. Finally, send it back with status 201. Run the checker to confirm it works.\n\nIn server.js:\n```\n  const item = { id: items.length + 1, ...data };\n  items.push(item);\n  return send(res, 201, item);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "id",
        "label": "The new item answers with id 4",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "{\"name\":\"Tea\",\"price\":12}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "bodyContains": "\"id\":4"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Give each new item an id one higher than the last."
      },
      {
        "level": 2,
        "text": "Change createItem to assign the next id and add it to the array.\n\nIn server.js:\n```\n  const item = { id: items.length + 1, ...data };\n  items.push(item);\n  return send(res, 201, item);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const data = JSON.parse(await readBody(req));\n  const item = { id: items.length + 1, ...data };\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 5,
    "projectId": "request-bodies-school-club"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: request-bodies-school-club.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-request-bodies-school-club-6",
    "index": 326,
    "task": "When the body is not JSON, the server crashes. You fix this by catching the error. The code below shows how. Run the checker to test it.\n\nIn server.js:\n```\n  let data;\n  try { data = JSON.parse(await readBody(req)); } catch { return send(res, 400, { error: \"Body must be JSON\" }); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "bad",
        "label": "POST /items with broken JSON answers 400",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "not json",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 400,
        "bodyContains": "Body must be JSON"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "If the body is not JSON, the server should not crash. It should send a 400 error instead."
      },
      {
        "level": 2,
        "text": "Put the code below after the line that reads the body. It replaces the old JSON.parse line.\n\nIn server.js:\n```\n  let data;\n  try { data = JSON.parse(await readBody(req)); } catch { return send(res, 400, { error: \"Body must be JSON\" }); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  let data;\n  try { data = JSON.parse(await readBody(req)); } catch { return send(res, 400, { error: \"Body must be JSON\" }); }\n  const item = { id: items.length + 1, ...data };\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "request-bodies-school-club"
  },
  {
    "id": "api-request-bodies-school-club-7",
    "index": 327,
    "task": "If the name is missing or empty, the server should answer 422. The code below checks for that. Run the checker to test it.\n\nIn server.js:\n```\n  if (typeof data.name !== \"string\" || !data.name.trim()) return send(res, 422, { error: \"name is required\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "noname",
        "label": "POST /items without a name answers 422",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "{\"price\":5}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 422,
        "bodyContains": "name is required"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Check if the name is a string and not empty. If it is not, send 422 with an error message."
      },
      {
        "level": 2,
        "text": "Put the code below after the try block. It checks the name right after parsing.\n\nIn server.js:\n```\n  if (typeof data.name !== \"string\" || !data.name.trim()) return send(res, 422, { error: \"name is required\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  let data;\n  try { data = JSON.parse(await readBody(req)); } catch { return send(res, 400, { error: \"Body must be JSON\" }); }\n  if (typeof data.name !== \"string\" || !data.name.trim()) return send(res, 422, { error: \"name is required\" });\n  const item = { id: items.length + 1, ...data };\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "request-bodies-school-club"
  },
  {
    "id": "api-request-bodies-school-club-8",
    "index": 328,
    "task": "If the price is not a number or is less than zero, the server should answer 422. The code below checks for that. Run the checker to test it.\n\nIn server.js:\n```\n  if (!Number.isFinite(data.price) || data.price < 0) return send(res, 422, { error: \"price must be a number 0 or more\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "badprice",
        "label": "POST /items with price five answers 422",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "{\"name\":\"Tea\",\"price\":\"five\"}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 422,
        "bodyContains": "price must be a number"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Check if the price is a valid number and not negative. If not, send 422 with an error message."
      },
      {
        "level": 2,
        "text": "Put the code below after the name check. It checks the price right after the name.\n\nIn server.js:\n```\n  if (!Number.isFinite(data.price) || data.price < 0) return send(res, 422, { error: \"price must be a number 0 or more\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  let data;\n  try { data = JSON.parse(await readBody(req)); } catch { return send(res, 400, { error: \"Body must be JSON\" }); }\n  if (typeof data.name !== \"string\" || !data.name.trim()) return send(res, 422, { error: \"name is required\" });\n  if (!Number.isFinite(data.price) || data.price < 0) return send(res, 422, { error: \"price must be a number 0 or more\" });\n  const item = { id: items.length + 1, ...data };\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "request-bodies-school-club"
  },
  {
    "id": "api-request-bodies-school-club-9",
    "index": 329,
    "task": "The server should keep only the known fields and trim spaces from the name. The code below builds the item correctly. Run the checker to test it.\n\nIn server.js:\n```\n  const item = { id: items.length + 1, name: data.name.trim(), price: data.price };\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "trim",
        "label": "The new item is stored with a trimmed name and no extra fields",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "{\"name\":\"  Tea  \",\"price\":12,\"secret\":\"x\"}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "bodyContains": "{\"id\":4,\"name\":\"Tea\",\"price\":12}"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Only use the name and price from the body. Trim any extra spaces from the name."
      },
      {
        "level": 2,
        "text": "Put the code below where the item is built. It replaces the old item creation line.\n\nIn server.js:\n```\n  const item = { id: items.length + 1, name: data.name.trim(), price: data.price };\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  let data;\n  try { data = JSON.parse(await readBody(req)); } catch { return send(res, 400, { error: \"Body must be JSON\" }); }\n  if (typeof data.name !== \"string\" || !data.name.trim()) return send(res, 422, { error: \"name is required\" });\n  if (!Number.isFinite(data.price) || data.price < 0) return send(res, 422, { error: \"price must be a number 0 or more\" });\n  const item = { id: items.length + 1, name: data.name.trim(), price: data.price };\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "request-bodies-school-club"
  },
  {
    "id": "api-request-bodies-school-club-10",
    "index": 330,
    "task": "The server should send a Location header with the new item's URL. The code below adds that header. Run the checker to test it.\n\nIn server.js:\n```\n  res.setHeader(\"Location\", `/items/${item.id}`);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "location",
        "label": "POST /items sends Location: /items/4",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "{\"name\":\"Tea\",\"price\":12}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "header": {
          "name": "location",
          "value": "/items/4"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "After creating the item, send a Location header with the new item's URL. This helps clients find it."
      },
      {
        "level": 2,
        "text": "Put the code below before the return in createItem. It adds the header right before sending the response.\n\nIn server.js:\n```\n  res.setHeader(\"Location\", `/items/${item.id}`);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  let data;\n  try { data = JSON.parse(await readBody(req)); } catch { return send(res, 400, { error: \"Body must be JSON\" }); }\n  if (typeof data.name !== \"string\" || !data.name.trim()) return send(res, 422, { error: \"name is required\" });\n  if (!Number.isFinite(data.price) || data.price < 0) return send(res, 422, { error: \"price must be a number 0 or more\" });\n  const item = { id: items.length + 1, name: data.name.trim(), price: data.price };\n  items.push(item);\n  res.setHeader(\"Location\", `/items/${item.id}`);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "request-bodies-school-club"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: one-item-school-club.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-one-item-school-club-1",
    "index": 331,
    "task": "You will read the item ID from the path. The code below finds the ID in the URL. Add it at the top of the handler. This lets you get one item by its number. Run the checker to test it.\n\nIn server.js:\n```\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") return send(res, 200, items.find((item) => item.id === Number(match[1])));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "one",
        "label": "GET /items/2 answers Pin",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/2"
          }
        ],
        "bodyContains": "\"name\":\"Pin\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Look for the pattern in the URL to get the ID."
      },
      {
        "level": 2,
        "text": "Add the code at the top of the handler, right after the imports.\n\nIn server.js:\n```\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") return send(res, 200, items.find((item) => item.id === Number(match[1])));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") return send(res, 200, items.find((item) => item.id === Number(match[1])));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "one-item-school-club"
  },
  {
    "id": "api-one-item-school-club-2",
    "index": 332,
    "task": "You will check if the item exists. The code below checks if the item is found. If not, it answers 404. Add this line after the first line. Run the checker to test it.\n\nIn server.js:\n```\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "none",
        "label": "GET /items/99 answers 404",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/99"
          }
        ],
        "status": 404,
        "bodyContains": "Item not found"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "If the item is not found, send a 404 error."
      },
      {
        "level": 2,
        "text": "Add the code after the first line, before the GET check.\n\nIn server.js:\n```\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "one-item-school-club"
  },
  {
    "id": "api-one-item-school-club-3",
    "index": 333,
    "task": "You will delete an item. The code below removes the item and answers 204. Add it after the GET check. Run the checker to test it.\n\nIn server.js:\n```\n  if (found && req.method === \"DELETE\") { items.splice(items.indexOf(found), 1); res.statusCode = 204; return res.end(); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "deleted",
        "label": "DELETE /items/1 answers 204",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "DELETE",
            "path": "/items/1"
          }
        ],
        "status": 204
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Delete the item by removing it from the array."
      },
      {
        "level": 2,
        "text": "Add the code after the GET check, before the 404 check.\n\nIn server.js:\n```\n  if (found && req.method === \"DELETE\") { items.splice(items.indexOf(found), 1); res.statusCode = 204; return res.end(); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"DELETE\") { items.splice(items.indexOf(found), 1); res.statusCode = 204; return res.end(); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "one-item-school-club"
  },
  {
    "id": "api-one-item-school-club-4",
    "index": 334,
    "task": "You will answer 404 when deleting a non-existent item. The code below checks if the item exists before deleting. Add it after the DELETE line. Run the checker to test it.\n\nIn server.js:\n```\n  if (match && req.method === \"DELETE\") return send(res, 404, { error: \"Item not found\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "gone",
        "label": "DELETE /items/99 answers 404 Item not found",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "DELETE",
            "path": "/items/99"
          }
        ],
        "status": 404,
        "bodyContains": "Item not found"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "If the item does not exist, send a 404 error."
      },
      {
        "level": 2,
        "text": "Add the code after the DELETE line, before the 204 line.\n\nIn server.js:\n```\n  if (match && req.method === \"DELETE\") return send(res, 404, { error: \"Item not found\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"DELETE\") { items.splice(items.indexOf(found), 1); res.statusCode = 204; return res.end(); }\n  if (match && req.method === \"DELETE\") return send(res, 404, { error: \"Item not found\" });\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "one-item-school-club"
  },
  {
    "id": "api-one-item-school-club-5",
    "index": 335,
    "task": "You will update an item with PUT. The code below replaces the item's fields and answers the updated item. Add it after the DELETE lines. Run the checker to test it.\n\nIn server.js:\n```\n  if (found && req.method === \"PUT\") { Object.assign(found, JSON.parse(await readBody(req))); return send(res, 200, found); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "put",
        "label": "PUT /items/1 with price 99 answers the new price",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "PUT",
            "path": "/items/1",
            "body": "{\"price\":99}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "bodyContains": "\"price\":99"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use PUT to change the item's data."
      },
      {
        "level": 2,
        "text": "Add the code after the DELETE lines, before the 404 line.\n\nIn server.js:\n```\n  if (found && req.method === \"PUT\") { Object.assign(found, JSON.parse(await readBody(req))); return send(res, 200, found); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"DELETE\") { items.splice(items.indexOf(found), 1); res.statusCode = 204; return res.end(); }\n  if (match && req.method === \"DELETE\") return send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"PUT\") { Object.assign(found, JSON.parse(await readBody(req))); return send(res, 200, found); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "one-item-school-club"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: one-item-school-club.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-one-item-school-club-6",
    "index": 336,
    "task": "You will change the PUT line in server.js. This line handles when a client sends data to update an item. The client might send a different id, but you must keep the original id. This is important because the club's system uses the original id to find the item. The code below shows what to change. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (found && req.method === \"PUT\") { Object.assign(found, JSON.parse(await readBody(req)), { id: found.id }); return send(res, 200, found); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "keep",
        "label": "After PUT with id 50, GET /items/1 still works",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "PUT",
            "path": "/items/1",
            "body": "{\"id\":50,\"price\":1}",
            "headers": {
              "Content-Type": "application/json"
            }
          },
          {
            "method": "GET",
            "path": "/items/1"
          }
        ],
        "status": 200,
        "bodyContains": "\"id\":1"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The client might send a new id, but you must keep the old one to match the club's system."
      },
      {
        "level": 2,
        "text": "Change the PUT line in server.js, it's the line that handles updating items.\n\nIn server.js:\n```\n  if (found && req.method === \"PUT\") { Object.assign(found, JSON.parse(await readBody(req)), { id: found.id }); return send(res, 200, found); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"DELETE\") { items.splice(items.indexOf(found), 1); res.statusCode = 204; return res.end(); }\n  if (match && req.method === \"DELETE\") return send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"PUT\") { Object.assign(found, JSON.parse(await readBody(req)), { id: found.id }); return send(res, 200, found); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "one-item-school-club"
  },
  {
    "id": "api-one-item-school-club-7",
    "index": 337,
    "task": "You will add one line before the PUT line. This line checks if the client sent data marked as JSON. If not, the server answers 415. This is important because the club's system only accepts JSON. The code below shows what to add. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (found && req.method === \"PUT\" && !req.headers[\"content-type\"]?.includes(\"application/json\")) return send(res, 415, { error: \"Send JSON\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "type",
        "label": "PUT with a text/plain body answers 415",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "PUT",
            "path": "/items/1",
            "body": "price=99",
            "headers": {
              "Content-Type": "text/plain"
            }
          }
        ],
        "status": 415
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "If the client sends data not marked as JSON, the server must answer 415."
      },
      {
        "level": 2,
        "text": "Add this line before the PUT line in server.js, the page will show the code after your text.\n\nIn server.js:\n```\n  if (found && req.method === \"PUT\" && !req.headers[\"content-type\"]?.includes(\"application/json\")) return send(res, 415, { error: \"Send JSON\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"DELETE\") { items.splice(items.indexOf(found), 1); res.statusCode = 204; return res.end(); }\n  if (match && req.method === \"DELETE\") return send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"PUT\" && !req.headers[\"content-type\"]?.includes(\"application/json\")) return send(res, 415, { error: \"Send JSON\" });\n  if (found && req.method === \"PUT\") { Object.assign(found, JSON.parse(await readBody(req)), { id: found.id }); return send(res, 200, found); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "one-item-school-club"
  },
  {
    "id": "api-one-item-school-club-8",
    "index": 338,
    "task": "You will add one line before the 404 line. This line handles HEAD requests to /items. It sends only the X-Total-Count header and no body. This is important because the club's system uses HEAD to count items without downloading them. The code below shows what to add. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (req.url === \"/items\" && req.method === \"HEAD\") { res.setHeader(\"X-Total-Count\", String(items.length)); return res.end(); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "head",
        "label": "HEAD /items sends X-Total-Count: 3",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "HEAD",
            "path": "/items"
          }
        ],
        "header": {
          "name": "x-total-count",
          "value": "3"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "HEAD requests ask for item count without the item data, send only the header."
      },
      {
        "level": 2,
        "text": "Add this line before the 404 line in server.js, the page will show the code after your text.\n\nIn server.js:\n```\n  if (req.url === \"/items\" && req.method === \"HEAD\") { res.setHeader(\"X-Total-Count\", String(items.length)); return res.end(); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"DELETE\") { items.splice(items.indexOf(found), 1); res.statusCode = 204; return res.end(); }\n  if (match && req.method === \"DELETE\") return send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"PUT\" && !req.headers[\"content-type\"]?.includes(\"application/json\")) return send(res, 415, { error: \"Send JSON\" });\n  if (found && req.method === \"PUT\") { Object.assign(found, JSON.parse(await readBody(req)), { id: found.id }); return send(res, 200, found); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"HEAD\") { res.setHeader(\"X-Total-Count\", String(items.length)); return res.end(); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "one-item-school-club"
  },
  {
    "id": "api-one-item-school-club-9",
    "index": 339,
    "task": "You will add one line after the PUT line. This line handles OPTIONS requests to any item. It sends an Allow header listing GET, PUT, DELETE. This is important because the club's system uses OPTIONS to check allowed methods. The code below shows what to add. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (match && req.method === \"OPTIONS\") { res.setHeader(\"Allow\", \"GET, PUT, DELETE\"); res.statusCode = 204; return res.end(); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "options",
        "label": "OPTIONS /items/1 lists GET, PUT, DELETE",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "OPTIONS",
            "path": "/items/1"
          }
        ],
        "header": {
          "name": "allow",
          "value": "GET, PUT, DELETE"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "OPTIONS requests ask what methods are allowed, answer with GET, PUT, DELETE."
      },
      {
        "level": 2,
        "text": "Add this line after the PUT line in server.js, the page will show the code after your text.\n\nIn server.js:\n```\n  if (match && req.method === \"OPTIONS\") { res.setHeader(\"Allow\", \"GET, PUT, DELETE\"); res.statusCode = 204; return res.end(); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"DELETE\") { items.splice(items.indexOf(found), 1); res.statusCode = 204; return res.end(); }\n  if (match && req.method === \"DELETE\") return send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"PUT\" && !req.headers[\"content-type\"]?.includes(\"application/json\")) return send(res, 415, { error: \"Send JSON\" });\n  if (found && req.method === \"PUT\") { Object.assign(found, JSON.parse(await readBody(req)), { id: found.id }); return send(res, 200, found); }\n  if (match && req.method === \"OPTIONS\") { res.setHeader(\"Allow\", \"GET, PUT, DELETE\"); res.statusCode = 204; return res.end(); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"HEAD\") { res.setHeader(\"X-Total-Count\", String(items.length)); return res.end(); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "one-item-school-club"
  },
  {
    "id": "api-one-item-school-club-10",
    "index": 340,
    "task": "You will add one line after the OPTIONS line. This line answers 405 for any other method on an item. This is important because the club's system only allows GET, PUT, DELETE. The code below shows what to add. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (match) return send(res, 405, { error: \"Method not allowed\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "post",
        "label": "POST /items/1 answers 405",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items/1",
            "body": "{}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 405
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "If the client uses a method not allowed, the server must answer 405."
      },
      {
        "level": 2,
        "text": "Add this line after the OPTIONS line in server.js, the page will show the code after your text.\n\nIn server.js:\n```\n  if (match) return send(res, 405, { error: \"Method not allowed\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  const found = match && items.find((item) => item.id === Number(match[1]));\n  if (match && req.method === \"GET\") return found ? send(res, 200, found) : send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"DELETE\") { items.splice(items.indexOf(found), 1); res.statusCode = 204; return res.end(); }\n  if (match && req.method === \"DELETE\") return send(res, 404, { error: \"Item not found\" });\n  if (found && req.method === \"PUT\" && !req.headers[\"content-type\"]?.includes(\"application/json\")) return send(res, 415, { error: \"Send JSON\" });\n  if (found && req.method === \"PUT\") { Object.assign(found, JSON.parse(await readBody(req)), { id: found.id }); return send(res, 200, found); }\n  if (match && req.method === \"OPTIONS\") { res.setHeader(\"Allow\", \"GET, PUT, DELETE\"); res.statusCode = 204; return res.end(); }\n  if (match) return send(res, 405, { error: \"Method not allowed\" });\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"HEAD\") { res.setHeader(\"X-Total-Count\", String(items.length)); return res.end(); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "one-item-school-club"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: query-strings-school-club.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-query-strings-school-club-1",
    "index": 341,
    "task": "You will add code to filter items by price. The code goes after the url line. Change the /items line to send the filtered list. This lets users see only items up to a certain price. The code below does that. Run the checker to test it.\n\nIn server.js:\n```\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.pathname === \"/items\") return send(res, 200, list);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "max",
        "label": "GET /items?max=45 answers only the items up to 45",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items?max=45"
          }
        ],
        "bodyContains": "[{\"id\":2,\"name\":\"Pin\",\"price\":30},{\"id\":3,\"name\":\"Badge\",\"price\":45}]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Think of max as the highest price you want to show."
      },
      {
        "level": 2,
        "text": "Put the code after the url line, right before the /items line.\n\nIn server.js:\n```\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.pathname === \"/items\") return send(res, 200, list);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.pathname === \"/items\") return send(res, 200, list);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "query-strings-school-club"
  },
  {
    "id": "api-query-strings-school-club-2",
    "index": 342,
    "task": "You will add code to filter items by minimum price. The code goes after the max filter line. This lets users see only items at or above a certain price. The code below does that. Run the checker to test it.\n\nIn server.js:\n```\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "min",
        "label": "GET /items?min=45 answers only the items from 45",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items?min=45"
          }
        ],
        "bodyContains": "[{\"id\":1,\"name\":\"Shirt\",\"price\":250},{\"id\":3,\"name\":\"Badge\",\"price\":45}]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Think of min as the lowest price you want to show."
      },
      {
        "level": 2,
        "text": "Put the code after the max filter line, right before the q line.\n\nIn server.js:\n```\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n  if (url.pathname === \"/items\") return send(res, 200, list);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "query-strings-school-club"
  },
  {
    "id": "api-query-strings-school-club-3",
    "index": 343,
    "task": "You will add code to search item names. The code goes after the min filter line. This lets users search names, ignoring if they are uppercase or lowercase. The code below does that. Run the checker to test it.\n\nIn server.js:\n```\n  if (url.searchParams.has(\"q\")) list = list.filter((item) => item.name.toLowerCase().includes(url.searchParams.get(\"q\").toLowerCase()));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "q",
        "label": "GET /items?q=in finds Pin",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items?q=in"
          }
        ],
        "bodyContains": "[{\"id\":2,\"name\":\"Pin\",\"price\":30}]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Think of q as the search word the user types."
      },
      {
        "level": 2,
        "text": "Put the code after the min filter line, right before the sort line.\n\nIn server.js:\n```\n  if (url.searchParams.has(\"q\")) list = list.filter((item) => item.name.toLowerCase().includes(url.searchParams.get(\"q\").toLowerCase()));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n  if (url.searchParams.has(\"q\")) list = list.filter((item) => item.name.toLowerCase().includes(url.searchParams.get(\"q\").toLowerCase()));\n  if (url.pathname === \"/items\") return send(res, 200, list);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "query-strings-school-club"
  },
  {
    "id": "api-query-strings-school-club-4",
    "index": 344,
    "task": "You will add code to sort items by price from cheapest to most expensive. The code goes after the q search line. This lets users see items sorted by price. The code below does that. Run the checker to test it.\n\nIn server.js:\n```\n  if (url.searchParams.get(\"sort\") === \"price\") list = [...list].sort((a, b) => a.price - b.price);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "asc",
        "label": "GET /items?sort=price answers cheapest first",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items?sort=price"
          }
        ],
        "bodyContains": "[{\"id\":2,\"name\":\"Pin\",\"price\":30},{\"id\":3,\"name\":\"Badge\",\"price\":45},{\"id\":1,\"name\":\"Shirt\",\"price\":250}]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Think of sort=price as asking for cheapest first."
      },
      {
        "level": 2,
        "text": "Put the code after the q search line, right before the -price line.\n\nIn server.js:\n```\n  if (url.searchParams.get(\"sort\") === \"price\") list = [...list].sort((a, b) => a.price - b.price);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n  if (url.searchParams.has(\"q\")) list = list.filter((item) => item.name.toLowerCase().includes(url.searchParams.get(\"q\").toLowerCase()));\n  if (url.searchParams.get(\"sort\") === \"price\") list = [...list].sort((a, b) => a.price - b.price);\n  if (url.pathname === \"/items\") return send(res, 200, list);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "query-strings-school-club"
  },
  {
    "id": "api-query-strings-school-club-5",
    "index": 345,
    "task": "You will add code to sort items by price from most expensive to cheapest. The code goes after the sort=price line. This lets users see items sorted by price in reverse. The code below does that. Run the checker to test it.\n\nIn server.js:\n```\n  if (url.searchParams.get(\"sort\") === \"-price\") list = [...list].sort((a, b) => b.price - a.price);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "desc",
        "label": "GET /items?sort=-price answers most expensive first",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items?sort=-price"
          }
        ],
        "bodyContains": "[{\"id\":1,\"name\":\"Shirt\",\"price\":250},{\"id\":3,\"name\":\"Badge\",\"price\":45},{\"id\":2,\"name\":\"Pin\",\"price\":30}]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Think of sort=-price as asking for most expensive first."
      },
      {
        "level": 2,
        "text": "Put the code after the sort=price line, right before the end of the list.\n\nIn server.js:\n```\n  if (url.searchParams.get(\"sort\") === \"-price\") list = [...list].sort((a, b) => b.price - a.price);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n  if (url.searchParams.has(\"q\")) list = list.filter((item) => item.name.toLowerCase().includes(url.searchParams.get(\"q\").toLowerCase()));\n  if (url.searchParams.get(\"sort\") === \"price\") list = [...list].sort((a, b) => a.price - b.price);\n  if (url.searchParams.get(\"sort\") === \"-price\") list = [...list].sort((a, b) => b.price - a.price);\n  if (url.pathname === \"/items\") return send(res, 200, list);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "query-strings-school-club"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: query-strings-school-club.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-query-strings-school-club-6",
    "index": 346,
    "task": "You add a line to check if the user asks for a limit. This line cuts the list to only the first few items. The code below does this. Run the checker to see if it works.\n\nIn server.js:\n```\n  if (url.searchParams.has(\"limit\")) list = list.slice(0, Number(url.searchParams.get(\"limit\")));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "limit",
        "label": "GET /items?sort=price&limit=1 answers only the cheapest item",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items?sort=price&limit=1"
          }
        ],
        "bodyContains": "[{\"id\":2,\"name\":\"Pin\",\"price\":30}]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Check if the URL has a 'limit' part. If yes, take only the first few items."
      },
      {
        "level": 2,
        "text": "Add this line after the sort line in server.js.\n\nIn server.js:\n```\n  if (url.searchParams.has(\"limit\")) list = list.slice(0, Number(url.searchParams.get(\"limit\")));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n  if (url.searchParams.has(\"q\")) list = list.filter((item) => item.name.toLowerCase().includes(url.searchParams.get(\"q\").toLowerCase()));\n  if (url.searchParams.get(\"sort\") === \"price\") list = [...list].sort((a, b) => a.price - b.price);\n  if (url.searchParams.get(\"sort\") === \"-price\") list = [...list].sort((a, b) => b.price - a.price);\n  if (url.searchParams.has(\"limit\")) list = list.slice(0, Number(url.searchParams.get(\"limit\")));\n  if (url.pathname === \"/items\") return send(res, 200, list);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "query-strings-school-club"
  },
  {
    "id": "api-query-strings-school-club-7",
    "index": 347,
    "task": "You add a line to show only names when the user asks for 'fields=name'. This line changes the list to only names. The code below does this. Run the checker to see if it works.\n\nIn server.js:\n```\n  if (url.searchParams.get(\"fields\") === \"name\") list = list.map((item) => ({ name: item.name }));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "fields",
        "label": "GET /items?fields=name answers names only",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items?fields=name"
          }
        ],
        "bodyContains": "[{\"name\":\"Shirt\"},{\"name\":\"Pin\"},{\"name\":\"Badge\"}]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "If the user asks for 'fields=name', change each item to only its name."
      },
      {
        "level": 2,
        "text": "Add this line after the limit line in server.js.\n\nIn server.js:\n```\n  if (url.searchParams.get(\"fields\") === \"name\") list = list.map((item) => ({ name: item.name }));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n  if (url.searchParams.has(\"q\")) list = list.filter((item) => item.name.toLowerCase().includes(url.searchParams.get(\"q\").toLowerCase()));\n  if (url.searchParams.get(\"sort\") === \"price\") list = [...list].sort((a, b) => a.price - b.price);\n  if (url.searchParams.get(\"sort\") === \"-price\") list = [...list].sort((a, b) => b.price - a.price);\n  if (url.searchParams.has(\"limit\")) list = list.slice(0, Number(url.searchParams.get(\"limit\")));\n  if (url.searchParams.get(\"fields\") === \"name\") list = list.map((item) => ({ name: item.name }));\n  if (url.pathname === \"/items\") return send(res, 200, list);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "query-strings-school-club"
  },
  {
    "id": "api-query-strings-school-club-8",
    "index": 348,
    "task": "You add a line to send 400 if 'max' is not a number. This line checks if 'max' is not a number and sends an error. The code below does this. Run the checker to see if it works.\n\nIn server.js:\n```\n  if (url.searchParams.has(\"max\") && Number.isNaN(Number(url.searchParams.get(\"max\")))) return send(res, 400, { error: \"max must be a number\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "bad",
        "label": "GET /items?max=abc answers 400",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items?max=abc"
          }
        ],
        "status": 400
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Check if 'max' is not a number. If yes, send 400 with an error message."
      },
      {
        "level": 2,
        "text": "Add this line right after the url line in server.js.\n\nIn server.js:\n```\n  if (url.searchParams.has(\"max\") && Number.isNaN(Number(url.searchParams.get(\"max\")))) return send(res, 400, { error: \"max must be a number\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.searchParams.has(\"max\") && Number.isNaN(Number(url.searchParams.get(\"max\")))) return send(res, 400, { error: \"max must be a number\" });\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n  if (url.searchParams.has(\"q\")) list = list.filter((item) => item.name.toLowerCase().includes(url.searchParams.get(\"q\").toLowerCase()));\n  if (url.searchParams.get(\"sort\") === \"price\") list = [...list].sort((a, b) => a.price - b.price);\n  if (url.searchParams.get(\"sort\") === \"-price\") list = [...list].sort((a, b) => b.price - a.price);\n  if (url.searchParams.has(\"limit\")) list = list.slice(0, Number(url.searchParams.get(\"limit\")));\n  if (url.searchParams.get(\"fields\") === \"name\") list = list.map((item) => ({ name: item.name }));\n  if (url.pathname === \"/items\") return send(res, 200, list);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "query-strings-school-club"
  },
  {
    "id": "api-query-strings-school-club-9",
    "index": 349,
    "task": "You change the /items line to send a header with how many items match. This line adds the header and sends the list. The code below does this. Run the checker to see if it works.\n\nIn server.js:\n```\n  if (url.pathname === \"/items\") { res.setHeader(\"X-Total-Count\", String(list.length)); return send(res, 200, list); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "count",
        "label": "GET /items?max=45 sends X-Total-Count: 2",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items?max=45"
          }
        ],
        "header": {
          "name": "x-total-count",
          "value": "2"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Change the /items line to send X-Total-Count with the list's length."
      },
      {
        "level": 2,
        "text": "Replace the /items line in server.js with the code below.\n\nIn server.js:\n```\n  if (url.pathname === \"/items\") { res.setHeader(\"X-Total-Count\", String(list.length)); return send(res, 200, list); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.searchParams.has(\"max\") && Number.isNaN(Number(url.searchParams.get(\"max\")))) return send(res, 400, { error: \"max must be a number\" });\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n  if (url.searchParams.has(\"q\")) list = list.filter((item) => item.name.toLowerCase().includes(url.searchParams.get(\"q\").toLowerCase()));\n  if (url.searchParams.get(\"sort\") === \"price\") list = [...list].sort((a, b) => a.price - b.price);\n  if (url.searchParams.get(\"sort\") === \"-price\") list = [...list].sort((a, b) => b.price - a.price);\n  if (url.searchParams.has(\"limit\")) list = list.slice(0, Number(url.searchParams.get(\"limit\")));\n  if (url.searchParams.get(\"fields\") === \"name\") list = list.map((item) => ({ name: item.name }));\n  if (url.pathname === \"/items\") { res.setHeader(\"X-Total-Count\", String(list.length)); return send(res, 200, list); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "query-strings-school-club"
  },
  {
    "id": "api-query-strings-school-club-10",
    "index": 350,
    "task": "You add a line to show what filters are supported at /items/options. This line sends a JSON list of filters. The code below does this. Run the checker to see if it works.\n\nIn server.js:\n```\n  if (url.pathname === \"/items/options\") return send(res, 200, { filters: [\"max\", \"min\", \"q\"], sort: [\"price\", \"-price\"], other: [\"limit\", \"fields\"] });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "options",
        "label": "GET /items/options lists the filters",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/options"
          }
        ],
        "bodyContains": "\"filters\":[\"max\",\"min\",\"q\"]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Add a line to send the supported filters at /items/options."
      },
      {
        "level": 2,
        "text": "Add this line after the 400 line in server.js.\n\nIn server.js:\n```\n  if (url.pathname === \"/items/options\") return send(res, 200, { filters: [\"max\", \"min\", \"q\"], sort: [\"price\", \"-price\"], other: [\"limit\", \"fields\"] });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.searchParams.has(\"max\") && Number.isNaN(Number(url.searchParams.get(\"max\")))) return send(res, 400, { error: \"max must be a number\" });\n  if (url.pathname === \"/items/options\") return send(res, 200, { filters: [\"max\", \"min\", \"q\"], sort: [\"price\", \"-price\"], other: [\"limit\", \"fields\"] });\n  let list = items;\n  if (url.searchParams.has(\"max\")) list = list.filter((item) => item.price <= Number(url.searchParams.get(\"max\")));\n  if (url.searchParams.has(\"min\")) list = list.filter((item) => item.price >= Number(url.searchParams.get(\"min\")));\n  if (url.searchParams.has(\"q\")) list = list.filter((item) => item.name.toLowerCase().includes(url.searchParams.get(\"q\").toLowerCase()));\n  if (url.searchParams.get(\"sort\") === \"price\") list = [...list].sort((a, b) => a.price - b.price);\n  if (url.searchParams.get(\"sort\") === \"-price\") list = [...list].sort((a, b) => b.price - a.price);\n  if (url.searchParams.has(\"limit\")) list = list.slice(0, Number(url.searchParams.get(\"limit\")));\n  if (url.searchParams.get(\"fields\") === \"name\") list = list.map((item) => ({ name: item.name }));\n  if (url.pathname === \"/items\") { res.setHeader(\"X-Total-Count\", String(list.length)); return send(res, 200, list); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "query-strings-school-club"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: middleware-school-club.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-middleware-school-club-1",
    "index": 351,
    "task": "You add a counter and a function to set a header. This header tells the client which request it is. You call this function at the top of the handler. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\n  addRequestId(req, res);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "id",
        "label": "The first answer sends X-Request-Id: 1",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "header": {
          "name": "x-request-id",
          "value": "1"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The counter starts at 1 and increases each time. The header name is X-Request-Id."
      },
      {
        "level": 2,
        "text": "Put the code above the server part in server.js.\n\nIn server.js:\n```\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\n  addRequestId(req, res);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  addRequestId(req, res);\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "middleware-school-club"
  },
  {
    "id": "api-middleware-school-club-2",
    "index": 352,
    "task": "You add another function to set a header. This one says where the request came from. You call it after the first function. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"school-club\"); }\n  addPlace(req, res);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "place",
        "label": "Answers send X-Place: school-club",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "header": {
          "name": "x-place",
          "value": "school-club"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The header name is X-Place. The value is always 'school-club'."
      },
      {
        "level": 2,
        "text": "Put the code after the first function in server.js.\n\nIn server.js:\n```\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"school-club\"); }\n  addPlace(req, res);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"school-club\"); }\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  addRequestId(req, res);\n  addPlace(req, res);\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "middleware-school-club"
  },
  {
    "id": "api-middleware-school-club-3",
    "index": 353,
    "task": "You make a list of all the functions you want to run. You replace the two calls with a loop that runs each function. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\nconst middleware = [addRequestId, addPlace];\n  for (const step of middleware) step(req, res);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "list",
        "label": "server.js keeps its middleware in one list",
        "kind": "local-file-contains",
        "path": "server.js",
        "value": "const middleware = [addRequestId, addPlace];"
      },
      {
        "id": "still",
        "label": "Answers still send X-Place",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "header": {
          "name": "x-place",
          "value": "school-club"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The list is called middleware. Use a for loop to run each function."
      },
      {
        "level": 2,
        "text": "Put the list and loop code where the two calls were in server.js.\n\nIn server.js:\n```\nconst middleware = [addRequestId, addPlace];\n  for (const step of middleware) step(req, res);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"school-club\"); }\nconst middleware = [addRequestId, addPlace];\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  for (const step of middleware) step(req, res);\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "middleware-school-club"
  },
  {
    "id": "api-middleware-school-club-4",
    "index": 354,
    "task": "You add a new function that sets another header. This one says what built-in tools you used. You add it to the list. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\nfunction addPoweredBy(req, res) { res.setHeader(\"X-Powered-By\", \"Node built-ins\"); }\nconst middleware = [addRequestId, addPlace, addPoweredBy];\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "powered",
        "label": "Answers send X-Powered-By: Node built-ins",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "header": {
          "name": "x-powered-by",
          "value": "Node built-ins"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The header name is X-Powered-By. The value is 'Node built-ins'."
      },
      {
        "level": 2,
        "text": "Add the function and put it in the list in server.js.\n\nIn server.js:\n```\nfunction addPoweredBy(req, res) { res.setHeader(\"X-Powered-By\", \"Node built-ins\"); }\nconst middleware = [addRequestId, addPlace, addPoweredBy];\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"school-club\"); }\nfunction addPoweredBy(req, res) { res.setHeader(\"X-Powered-By\", \"Node built-ins\"); }\nconst middleware = [addRequestId, addPlace, addPoweredBy];\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  for (const step of middleware) step(req, res);\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "middleware-school-club"
  },
  {
    "id": "api-middleware-school-club-5",
    "index": 355,
    "task": "You add a function to let web pages from other sites call your API. This header lets them do that. You add it to the list. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\nfunction allowBrowsers(req, res) { res.setHeader(\"Access-Control-Allow-Origin\", \"*\"); }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers];\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "cors",
        "label": "Answers send Access-Control-Allow-Origin: *",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "header": {
          "name": "access-control-allow-origin",
          "value": "*"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The header is Access-Control-Allow-Origin. The value is '*'."
      },
      {
        "level": 2,
        "text": "Add the function and put it in the list in server.js.\n\nIn server.js:\n```\nfunction allowBrowsers(req, res) { res.setHeader(\"Access-Control-Allow-Origin\", \"*\"); }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers];\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"school-club\"); }\nfunction addPoweredBy(req, res) { res.setHeader(\"X-Powered-By\", \"Node built-ins\"); }\nfunction allowBrowsers(req, res) { res.setHeader(\"Access-Control-Allow-Origin\", \"*\"); }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers];\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  for (const step of middleware) step(req, res);\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "middleware-school-club"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: middleware-school-club.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-middleware-school-club-6",
    "index": 356,
    "task": "You add a function called requireKey. It checks if the request has the right API key. If not, it sends a 401 error. Then you add this function to the middleware list. The loop will stop early if this function returns true. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\nfunction requireKey(req, res) { if (req.headers[\"x-api-key\"] !== \"secret123\") { send(res, 401, { error: \"Missing or wrong API key\" }); return true; } }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers, requireKey];\n  for (const step of middleware) if (step(req, res)) return;\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "nokey",
        "label": "GET /items without a key answers 401",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "status": 401
      },
      {
        "id": "key",
        "label": "GET /items with the key answers 200",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items",
            "headers": {
              "x-api-key": "secret123"
            }
          }
        ],
        "status": 200
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The function must check the header named 'x-api-key' and return true if it's wrong."
      },
      {
        "level": 2,
        "text": "Put the function in the middleware list right before requireKey.\n\nIn server.js:\n```\nfunction requireKey(req, res) { if (req.headers[\"x-api-key\"] !== \"secret123\") { send(res, 401, { error: \"Missing or wrong API key\" }); return true; } }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers, requireKey];\n  for (const step of middleware) if (step(req, res)) return;\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"school-club\"); }\nfunction addPoweredBy(req, res) { res.setHeader(\"X-Powered-By\", \"Node built-ins\"); }\nfunction allowBrowsers(req, res) { res.setHeader(\"Access-Control-Allow-Origin\", \"*\"); }\nfunction requireKey(req, res) { if (req.headers[\"x-api-key\"] !== \"secret123\") { send(res, 401, { error: \"Missing or wrong API key\" }); return true; } }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers, requireKey];\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  for (const step of middleware) if (step(req, res)) return;\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "middleware-school-club"
  },
  {
    "id": "api-middleware-school-club-7",
    "index": 357,
    "task": "You add a line at the top of the handler. This line checks if the request is for /health. If yes, it sends a 200 OK answer and stops the loop. This lets monitors check the server without a key. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (req.url === \"/health\") return send(res, 200, { ok: true });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "health",
        "label": "GET /health answers without a key",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/health"
          }
        ],
        "status": 200,
        "bodyContains": "\"ok\":true"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The check must be before the middleware loop. It must return early if the URL is /health."
      },
      {
        "level": 2,
        "text": "Put this line at the top of the handler, right after the req and res variables.\n\nIn server.js:\n```\n  if (req.url === \"/health\") return send(res, 200, { ok: true });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"school-club\"); }\nfunction addPoweredBy(req, res) { res.setHeader(\"X-Powered-By\", \"Node built-ins\"); }\nfunction allowBrowsers(req, res) { res.setHeader(\"Access-Control-Allow-Origin\", \"*\"); }\nfunction requireKey(req, res) { if (req.headers[\"x-api-key\"] !== \"secret123\") { send(res, 401, { error: \"Missing or wrong API key\" }); return true; } }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers, requireKey];\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/health\") return send(res, 200, { ok: true });\n  for (const step of middleware) if (step(req, res)) return;\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "middleware-school-club"
  },
  {
    "id": "api-middleware-school-club-8",
    "index": 358,
    "task": "You change the allowBrowsers function. You add a check for the OPTIONS method. If it's OPTIONS, you set the status to 204 and end the response. This lets browsers send preflight requests without a key. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\nfunction allowBrowsers(req, res) { res.setHeader(\"Access-Control-Allow-Origin\", \"*\"); if (req.method === \"OPTIONS\") { res.statusCode = 204; res.end(); return true; } }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "preflight",
        "label": "OPTIONS /items answers 204 without a key",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "OPTIONS",
            "path": "/items"
          }
        ],
        "status": 204
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The function must check if the method is OPTIONS and return true if it is."
      },
      {
        "level": 2,
        "text": "Put the new code right after the res.setHeader line in the allowBrowsers function.\n\nIn server.js:\n```\nfunction allowBrowsers(req, res) { res.setHeader(\"Access-Control-Allow-Origin\", \"*\"); if (req.method === \"OPTIONS\") { res.statusCode = 204; res.end(); return true; } }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"school-club\"); }\nfunction addPoweredBy(req, res) { res.setHeader(\"X-Powered-By\", \"Node built-ins\"); }\nfunction allowBrowsers(req, res) { res.setHeader(\"Access-Control-Allow-Origin\", \"*\"); if (req.method === \"OPTIONS\") { res.statusCode = 204; res.end(); return true; } }\nfunction requireKey(req, res) { if (req.headers[\"x-api-key\"] !== \"secret123\") { send(res, 401, { error: \"Missing or wrong API key\" }); return true; } }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers, requireKey];\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/health\") return send(res, 200, { ok: true });\n  for (const step of middleware) if (step(req, res)) return;\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "middleware-school-club"
  },
  {
    "id": "api-middleware-school-club-9",
    "index": 359,
    "task": "You add a line after the loop. This line checks if the request is for /stats. If yes, it sends a 200 OK answer with the number of requests served. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (req.url === \"/stats\") return send(res, 200, { served: nextId - 1 });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "stats",
        "label": "After two requests, GET /stats answers served 3",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items",
            "headers": {
              "x-api-key": "secret123"
            }
          },
          {
            "method": "GET",
            "path": "/items",
            "headers": {
              "x-api-key": "secret123"
            }
          },
          {
            "method": "GET",
            "path": "/stats",
            "headers": {
              "x-api-key": "secret123"
            }
          }
        ],
        "bodyContains": "\"served\":3"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The number of requests is stored in nextId. Subtract 1 to get the count."
      },
      {
        "level": 2,
        "text": "Put this line after the loop, before any other code.\n\nIn server.js:\n```\n  if (req.url === \"/stats\") return send(res, 200, { served: nextId - 1 });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"school-club\"); }\nfunction addPoweredBy(req, res) { res.setHeader(\"X-Powered-By\", \"Node built-ins\"); }\nfunction allowBrowsers(req, res) { res.setHeader(\"Access-Control-Allow-Origin\", \"*\"); if (req.method === \"OPTIONS\") { res.statusCode = 204; res.end(); return true; } }\nfunction requireKey(req, res) { if (req.headers[\"x-api-key\"] !== \"secret123\") { send(res, 401, { error: \"Missing or wrong API key\" }); return true; } }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers, requireKey];\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/health\") return send(res, 200, { ok: true });\n  for (const step of middleware) if (step(req, res)) return;\n  if (req.url === \"/stats\") return send(res, 200, { served: nextId - 1 });\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "middleware-school-club"
  },
  {
    "id": "api-middleware-school-club-10",
    "index": 360,
    "task": "You add a function called secureHeaders. It adds a security header to every answer. Then you add this function to the middleware list. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\nfunction secureHeaders(req, res) { res.setHeader(\"X-Content-Type-Options\", \"nosniff\"); }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers, secureHeaders, requireKey];\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "nosniff",
        "label": "Answers send X-Content-Type-Options: nosniff",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items",
            "headers": {
              "x-api-key": "secret123"
            }
          }
        ],
        "header": {
          "name": "x-content-type-options",
          "value": "nosniff"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The function must set the header 'X-Content-Type-Options' to 'nosniff'."
      },
      {
        "level": 2,
        "text": "Put the function in the middleware list right after allowBrowsers and before requireKey.\n\nIn server.js:\n```\nfunction secureHeaders(req, res) { res.setHeader(\"X-Content-Type-Options\", \"nosniff\"); }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers, secureHeaders, requireKey];\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet nextId = 1;\nfunction addRequestId(req, res) { res.setHeader(\"X-Request-Id\", String(nextId++)); }\nfunction addPlace(req, res) { res.setHeader(\"X-Place\", \"school-club\"); }\nfunction addPoweredBy(req, res) { res.setHeader(\"X-Powered-By\", \"Node built-ins\"); }\nfunction allowBrowsers(req, res) { res.setHeader(\"Access-Control-Allow-Origin\", \"*\"); if (req.method === \"OPTIONS\") { res.statusCode = 204; res.end(); return true; } }\nfunction requireKey(req, res) { if (req.headers[\"x-api-key\"] !== \"secret123\") { send(res, 401, { error: \"Missing or wrong API key\" }); return true; } }\nfunction secureHeaders(req, res) { res.setHeader(\"X-Content-Type-Options\", \"nosniff\"); }\nconst middleware = [addRequestId, addPlace, addPoweredBy, allowBrowsers, secureHeaders, requireKey];\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/health\") return send(res, 200, { ok: true });\n  for (const step of middleware) if (step(req, res)) return;\n  if (req.url === \"/stats\") return send(res, 200, { served: nextId - 1 });\n  if (req.url === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "middleware-school-club"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: sqlite-storage-school-club.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-sqlite-storage-school-club-1",
    "index": 361,
    "task": "Open an in-memory SQLite database. Make a table called items. Answer GET /items with the data from that table. The code below does this. Run the checker to test it.\n\nIn server.js:\n```\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "empty",
        "label": "GET /items answers an empty list from the database",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "status": 200,
        "bodyContains": "[]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The database lives only in memory, so it disappears when the server stops."
      },
      {
        "level": 2,
        "text": "Add the code above createItem and before the 404 line.\n\nIn server.js:\n```\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "sqlite-storage-school-club"
  },
  {
    "id": "api-sqlite-storage-school-club-2",
    "index": 362,
    "task": "Prepare a statement to add items to the database. Add three items: Shirt, Pin, and Badge. The code below does this. Run the checker to test it.\n\nIn server.js:\n```\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Shirt\",250],[\"Pin\",30],[\"Badge\",45]]) insert.run(name, price);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "rows",
        "label": "GET /items includes Shirt from the database",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "bodyContains": "\"name\":\"Shirt\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The INSERT statement adds each item with its name and price."
      },
      {
        "level": 2,
        "text": "Add the code after the CREATE TABLE line.\n\nIn server.js:\n```\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Shirt\",250],[\"Pin\",30],[\"Badge\",45]]) insert.run(name, price);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Shirt\",250],[\"Pin\",30],[\"Badge\",45]]) insert.run(name, price);\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "sqlite-storage-school-club"
  },
  {
    "id": "api-sqlite-storage-school-club-3",
    "index": 363,
    "task": "Change the SQL to sort items by price from lowest to highest. The code below does this. Run the checker to test it.\n\nIn server.js:\n```\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items ORDER BY price\").all());\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "ordered",
        "label": "GET /items lists the items cheapest first, starting with Pin",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "bodyContains": "[{\"id\":2,\"name\":\"Pin\",\"price\":30},{\"id\":3,\"name\":\"Badge\",\"price\":45},{\"id\":1,\"name\":\"Shirt\",\"price\":250}]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use ORDER BY price to sort cheapest first."
      },
      {
        "level": 2,
        "text": "Change the SQL in the GET /items line.\n\nIn server.js:\n```\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items ORDER BY price\").all());\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Shirt\",250],[\"Pin\",30],[\"Badge\",45]]) insert.run(name, price);\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items ORDER BY price\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 2,
    "projectId": "sqlite-storage-school-club"
  },
  {
    "id": "api-sqlite-storage-school-club-4",
    "index": 364,
    "task": "Add a route to count the items in the database. The code below does this. Run the checker to test it.\n\nIn server.js:\n```\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "count",
        "label": "GET /items/count answers {\"count\":3}",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/count"
          }
        ],
        "bodyContains": "{\"count\":3}"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The route is GET /items/count. It returns the count as a number."
      },
      {
        "level": 2,
        "text": "Add the code after the GET /items line.\n\nIn server.js:\n```\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Shirt\",250],[\"Pin\",30],[\"Badge\",45]]) insert.run(name, price);\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items ORDER BY price\").all());\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 2,
    "projectId": "sqlite-storage-school-club"
  },
  {
    "id": "api-sqlite-storage-school-club-5",
    "index": 365,
    "task": "Answer one item by its ID. The ID comes after /items/. The code below does this. Run the checker to test it.\n\nIn server.js:\n```\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match) return send(res, 200, db.prepare(\"SELECT * FROM items WHERE id = ?\").get(Number(match[1])));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "one",
        "label": "GET /items/2 answers Pin",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/2"
          }
        ],
        "bodyContains": "\"name\":\"Pin\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use a regular expression to get the ID from the URL."
      },
      {
        "level": 2,
        "text": "Add the code before the 404 line.\n\nIn server.js:\n```\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match) return send(res, 200, db.prepare(\"SELECT * FROM items WHERE id = ?\").get(Number(match[1])));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Shirt\",250],[\"Pin\",30],[\"Badge\",45]]) insert.run(name, price);\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items ORDER BY price\").all());\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match) return send(res, 200, db.prepare(\"SELECT * FROM items WHERE id = ?\").get(Number(match[1])));\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "sqlite-storage-school-club"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: sqlite-storage-school-club.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-sqlite-storage-school-club-6",
    "index": 366,
    "task": "You change one line in server.js. This line checks if an item exists. If not, it sends a 404 error. This matters because users must know when an item is missing. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\n  if (match) { const row = db.prepare(\"SELECT * FROM items WHERE id = ?\").get(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "none",
        "label": "GET /items/99 answers 404",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/99"
          }
        ],
        "status": 404
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "If the database has no row with that id, return 404. Don't send the row."
      },
      {
        "level": 2,
        "text": "Put the code in server.js, inside the if (match) block.\n\nIn server.js:\n```\n  if (match) { const row = db.prepare(\"SELECT * FROM items WHERE id = ?\").get(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Shirt\",250],[\"Pin\",30],[\"Badge\",45]]) insert.run(name, price);\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items ORDER BY price\").all());\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match) { const row = db.prepare(\"SELECT * FROM items WHERE id = ?\").get(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "sqlite-storage-school-club"
  },
  {
    "id": "api-sqlite-storage-school-club-7",
    "index": 367,
    "task": "You replace one line with three lines. These lines read the posted item, insert it into the database, and send back the id the database gave. This matters because users need to know the id of their new item. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\n  const data = JSON.parse(await readBody(req));\n  const result = insert.run(data.name, data.price);\n  return send(res, 201, { id: Number(result.lastInsertRowid), ...data });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "insert",
        "label": "POST /items answers 201 with id 4",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "{\"name\":\"Tea\",\"price\":12}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 201,
        "bodyContains": "\"id\":4"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Read the JSON, insert the data, then send back the new id."
      },
      {
        "level": 2,
        "text": "Put the code in server.js, inside the createItem function.\n\nIn server.js:\n```\n  const data = JSON.parse(await readBody(req));\n  const result = insert.run(data.name, data.price);\n  return send(res, 201, { id: Number(result.lastInsertRowid), ...data });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Shirt\",250],[\"Pin\",30],[\"Badge\",45]]) insert.run(name, price);\nasync function createItem(req, res) {\n  const data = JSON.parse(await readBody(req));\n  const result = insert.run(data.name, data.price);\n  return send(res, 201, { id: Number(result.lastInsertRowid), ...data });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items ORDER BY price\").all());\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match) { const row = db.prepare(\"SELECT * FROM items WHERE id = ?\").get(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "sqlite-storage-school-club"
  },
  {
    "id": "api-sqlite-storage-school-club-8",
    "index": 368,
    "task": "You add one route after the count route. This route adds up all prices in the database. This matters because users may want to know the total. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\n  if (req.url === \"/items/total\") return send(res, 200, db.prepare(\"SELECT SUM(price) AS total FROM items\").get());\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "sum",
        "label": "GET /items/total answers the sum of prices",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/total"
          }
        ],
        "bodyContains": "{\"total\":325}"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use SUM to add all prices. Return the result as a single row."
      },
      {
        "level": 2,
        "text": "Put the code in server.js, after the count route.\n\nIn server.js:\n```\n  if (req.url === \"/items/total\") return send(res, 200, db.prepare(\"SELECT SUM(price) AS total FROM items\").get());\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Shirt\",250],[\"Pin\",30],[\"Badge\",45]]) insert.run(name, price);\nasync function createItem(req, res) {\n  const data = JSON.parse(await readBody(req));\n  const result = insert.run(data.name, data.price);\n  return send(res, 201, { id: Number(result.lastInsertRowid), ...data });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items ORDER BY price\").all());\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n  if (req.url === \"/items/total\") return send(res, 200, db.prepare(\"SELECT SUM(price) AS total FROM items\").get());\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match) { const row = db.prepare(\"SELECT * FROM items WHERE id = ?\").get(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "sqlite-storage-school-club"
  },
  {
    "id": "api-sqlite-storage-school-club-9",
    "index": 369,
    "task": "You add two lines after the total route. These lines let users search for items by name. This matters because users may want to find items quickly. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items/search\") return send(res, 200, db.prepare(\"SELECT * FROM items WHERE name LIKE ?\").all(`%${url.searchParams.get(\"q\") ?? \"\"}%`));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "like",
        "label": "GET /items/search?q=adg finds Badge",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/search?q=adg"
          }
        ],
        "bodyContains": "\"name\":\"Badge\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use LIKE to match names. Get the search text from the URL."
      },
      {
        "level": 2,
        "text": "Put the code in server.js, after the total route.\n\nIn server.js:\n```\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items/search\") return send(res, 200, db.prepare(\"SELECT * FROM items WHERE name LIKE ?\").all(`%${url.searchParams.get(\"q\") ?? \"\"}%`));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Shirt\",250],[\"Pin\",30],[\"Badge\",45]]) insert.run(name, price);\nasync function createItem(req, res) {\n  const data = JSON.parse(await readBody(req));\n  const result = insert.run(data.name, data.price);\n  return send(res, 201, { id: Number(result.lastInsertRowid), ...data });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items ORDER BY price\").all());\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n  if (req.url === \"/items/total\") return send(res, 200, db.prepare(\"SELECT SUM(price) AS total FROM items\").get());\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items/search\") return send(res, 200, db.prepare(\"SELECT * FROM items WHERE name LIKE ?\").all(`%${url.searchParams.get(\"q\") ?? \"\"}%`));\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match) { const row = db.prepare(\"SELECT * FROM items WHERE id = ?\").get(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "sqlite-storage-school-club"
  },
  {
    "id": "api-sqlite-storage-school-club-10",
    "index": 370,
    "task": "You change three lines in server.js. These lines open the database file, create the table if it doesn't exist, and add starting rows if the table is empty. This matters because the data must be saved to a file. The code below does this. Run the checker to confirm.\n\nIn server.js:\n```\nconst db = new DatabaseSync(process.env.DB_FILE ?? \":memory:\");\ndb.exec(\"CREATE TABLE IF NOT EXISTS items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nif (db.prepare(\"SELECT COUNT(*) AS count FROM items\").get().count === 0) for (const [name, price] of [[\"Shirt\",250],[\"Pin\",30],[\"Badge\",45]]) insert.run(name, price);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "file",
        "label": "With DB_FILE=shop.db the server answers from a file",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "env": {
          "DB_FILE": "shop.db"
        },
        "status": 200,
        "bodyContains": "\"name\":\"Shirt\""
      },
      {
        "id": "saved",
        "label": "shop.db exists on disk",
        "kind": "local-file-exists",
        "path": "shop.db"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use DB_FILE to name the file. Create the table only if it doesn't exist."
      },
      {
        "level": 2,
        "text": "Put the code in server.js, at the top where the db variable is defined.\n\nIn server.js:\n```\nconst db = new DatabaseSync(process.env.DB_FILE ?? \":memory:\");\ndb.exec(\"CREATE TABLE IF NOT EXISTS items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nif (db.prepare(\"SELECT COUNT(*) AS count FROM items\").get().count === 0) for (const [name, price] of [[\"Shirt\",250],[\"Pin\",30],[\"Badge\",45]]) insert.run(name, price);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(process.env.DB_FILE ?? \":memory:\");\ndb.exec(\"CREATE TABLE IF NOT EXISTS items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nif (db.prepare(\"SELECT COUNT(*) AS count FROM items\").get().count === 0) for (const [name, price] of [[\"Shirt\",250],[\"Pin\",30],[\"Badge\",45]]) insert.run(name, price);\nasync function createItem(req, res) {\n  const data = JSON.parse(await readBody(req));\n  const result = insert.run(data.name, data.price);\n  return send(res, 201, { id: Number(result.lastInsertRowid), ...data });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items ORDER BY price\").all());\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n  if (req.url === \"/items/total\") return send(res, 200, db.prepare(\"SELECT SUM(price) AS total FROM items\").get());\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items/search\") return send(res, 200, db.prepare(\"SELECT * FROM items WHERE name LIKE ?\").all(`%${url.searchParams.get(\"q\") ?? \"\"}%`));\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match) { const row = db.prepare(\"SELECT * FROM items WHERE id = ?\").get(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 5,
    "projectId": "sqlite-storage-school-club"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: sqlite-changes-school-club.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-sqlite-changes-school-club-1",
    "index": 371,
    "task": "You will delete a row from the school club's database. Replace the line inside deleteItem with three lines. The code below deletes the row, sets the response to 204 No Content, and ends the response. Run the checker to confirm it works.\n\nIn server.js:\n```\n  db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  res.statusCode = 204;\n  return res.end();\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Shirt\",250],[\"Pin\",30],[\"Badge\",45]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "deleted",
        "label": "DELETE /items/1 answers 204",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "DELETE",
            "path": "/items/1"
          }
        ],
        "status": 204
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "You are deleting a row, so the server must say it succeeded without sending any data."
      },
      {
        "level": 2,
        "text": "Put the three lines inside the deleteItem function, right after the existing line.\n\nIn server.js:\n```\n  db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  res.statusCode = 204;\n  return res.end();\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Shirt\",250],[\"Pin\",30],[\"Badge\",45]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  res.statusCode = 204;\n  return res.end();\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "sqlite-changes-school-club"
  },
  {
    "id": "api-sqlite-changes-school-club-2",
    "index": 372,
    "task": "You will check if the row was found before deleting it. Change the DELETE line to store the result. Add a check after it. If no row was deleted, send 404. Run the checker to confirm it works.\n\nIn server.js:\n```\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Shirt\",250],[\"Pin\",30],[\"Badge\",45]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "none",
        "label": "DELETE /items/99 answers 404",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "DELETE",
            "path": "/items/99"
          }
        ],
        "status": 404
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "If the result says no rows were changed, the item does not exist. Send 404."
      },
      {
        "level": 2,
        "text": "Put the new lines after the DELETE line, inside the deleteItem function.\n\nIn server.js:\n```\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Shirt\",250],[\"Pin\",30],[\"Badge\",45]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  res.statusCode = 204;\n  return res.end();\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "sqlite-changes-school-club"
  },
  {
    "id": "api-sqlite-changes-school-club-3",
    "index": 373,
    "task": "You will update a row's price. Replace the line inside updateItem with three lines. The code below parses the price, updates the row, and sends back the updated row. Run the checker to confirm it works.\n\nIn server.js:\n```\n  const data = JSON.parse(await readBody(req));\n  db.prepare(\"UPDATE items SET price = ? WHERE id = ?\").run(data.price, id);\n  return send(res, 200, one(id));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Shirt\",250],[\"Pin\",30],[\"Badge\",45]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "updated",
        "label": "PUT /items/1 with price 99 answers the new price",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "PUT",
            "path": "/items/1",
            "body": "{\"price\":99}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "bodyContains": "\"price\":99"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "You are updating a row, so the server must send back the new data."
      },
      {
        "level": 2,
        "text": "Put the three lines inside the updateItem function, right after the existing line.\n\nIn server.js:\n```\n  const data = JSON.parse(await readBody(req));\n  db.prepare(\"UPDATE items SET price = ? WHERE id = ?\").run(data.price, id);\n  return send(res, 200, one(id));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Shirt\",250],[\"Pin\",30],[\"Badge\",45]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  res.statusCode = 204;\n  return res.end();\n}\nasync function updateItem(req, res, id) {\n  const data = JSON.parse(await readBody(req));\n  db.prepare(\"UPDATE items SET price = ? WHERE id = ?\").run(data.price, id);\n  return send(res, 200, one(id));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "sqlite-changes-school-club"
  },
  {
    "id": "api-sqlite-changes-school-club-4",
    "index": 374,
    "task": "You will check if the row was found before updating it. Change the UPDATE line to store the result. Add a check after it. If no row was updated, send 404. Run the checker to confirm it works.\n\nIn server.js:\n```\n  const result = db.prepare(\"UPDATE items SET price = ? WHERE id = ?\").run(data.price, id);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Shirt\",250],[\"Pin\",30],[\"Badge\",45]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "none",
        "label": "PUT /items/99 answers 404",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "PUT",
            "path": "/items/99",
            "body": "{\"price\":1}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 404
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "If the result says no rows were changed, the item does not exist. Send 404."
      },
      {
        "level": 2,
        "text": "Put the new lines after the UPDATE line, inside the updateItem function.\n\nIn server.js:\n```\n  const result = db.prepare(\"UPDATE items SET price = ? WHERE id = ?\").run(data.price, id);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Shirt\",250],[\"Pin\",30],[\"Badge\",45]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  res.statusCode = 204;\n  return res.end();\n}\nasync function updateItem(req, res, id) {\n  const data = JSON.parse(await readBody(req));\n  const result = db.prepare(\"UPDATE items SET price = ? WHERE id = ?\").run(data.price, id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  return send(res, 200, one(id));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "sqlite-changes-school-club"
  },
  {
    "id": "api-sqlite-changes-school-club-5",
    "index": 375,
    "task": "You will check if the price is a whole number 0 or more. Add one line after the JSON.parse line. If the price is not valid, send 422. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (!Number.isInteger(data.price) || data.price < 0) return send(res, 422, { error: \"price must be a whole number 0 or more\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Shirt\",250],[\"Pin\",30],[\"Badge\",45]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "bad",
        "label": "PUT /items/1 with price -5 answers 422",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "PUT",
            "path": "/items/1",
            "body": "{\"price\":-5}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 422
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "If the price is not a whole number or is negative, the update is invalid. Send 422."
      },
      {
        "level": 2,
        "text": "Put the new line after the JSON.parse line, inside the updateItem function.\n\nIn server.js:\n```\n  if (!Number.isInteger(data.price) || data.price < 0) return send(res, 422, { error: \"price must be a whole number 0 or more\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Shirt\",250],[\"Pin\",30],[\"Badge\",45]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  res.statusCode = 204;\n  return res.end();\n}\nasync function updateItem(req, res, id) {\n  const data = JSON.parse(await readBody(req));\n  if (!Number.isInteger(data.price) || data.price < 0) return send(res, 422, { error: \"price must be a whole number 0 or more\" });\n  const result = db.prepare(\"UPDATE items SET price = ? WHERE id = ?\").run(data.price, id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  return send(res, 200, one(id));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "sqlite-changes-school-club"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: sqlite-changes-school-club.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-sqlite-changes-school-club-6",
    "index": 376,
    "task": "You change the price check to let 0 or any whole number. This lets the user send 0 if they want. Then you change the UPDATE line to use COALESCE. This keeps the old value if the user doesn't send a new one. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (data.price !== undefined && (!Number.isInteger(data.price) || data.price < 0)) return send(res, 422, { error: \"price must be a whole number 0 or more\" });\n  const result = db.prepare(\"UPDATE items SET name = COALESCE(?, name), price = COALESCE(?, price) WHERE id = ?\").run(data.name ?? null, data.price ?? null, id);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Shirt\",250],[\"Pin\",30],[\"Badge\",45]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "name",
        "label": "PUT /items/1 with only a name keeps the price",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "PUT",
            "path": "/items/1",
            "body": "{\"name\":\"Tea\"}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "bodyContains": "\"name\":\"Tea\",\"price\":250"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The COALESCE function keeps the old value if you don't send a new one."
      },
      {
        "level": 2,
        "text": "Put the new code in the PUT handler, right after the price check.\n\nIn server.js:\n```\n  if (data.price !== undefined && (!Number.isInteger(data.price) || data.price < 0)) return send(res, 422, { error: \"price must be a whole number 0 or more\" });\n  const result = db.prepare(\"UPDATE items SET name = COALESCE(?, name), price = COALESCE(?, price) WHERE id = ?\").run(data.name ?? null, data.price ?? null, id);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Shirt\",250],[\"Pin\",30],[\"Badge\",45]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  res.statusCode = 204;\n  return res.end();\n}\nasync function updateItem(req, res, id) {\n  const data = JSON.parse(await readBody(req));\n  if (data.price !== undefined && (!Number.isInteger(data.price) || data.price < 0)) return send(res, 422, { error: \"price must be a whole number 0 or more\" });\n  const result = db.prepare(\"UPDATE items SET name = COALESCE(?, name), price = COALESCE(?, price) WHERE id = ?\").run(data.name ?? null, data.price ?? null, id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  return send(res, 200, one(id));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "sqlite-changes-school-club"
  },
  {
    "id": "api-sqlite-changes-school-club-7",
    "index": 377,
    "task": "You add a new route for raising prices. This route runs a transaction to update all prices by 1. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (req.url === \"/items/raise\" && req.method === \"POST\") { db.exec(\"BEGIN\"); db.prepare(\"UPDATE items SET price = price + 1\").run(); db.exec(\"COMMIT\"); return send(res, 200, db.prepare(\"SELECT * FROM items\").all()); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Shirt\",250],[\"Pin\",30],[\"Badge\",45]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "raise",
        "label": "POST /items/raise answers the raised prices",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items/raise",
            "body": "{}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "bodyContains": "\"price\":251"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "A transaction keeps all changes safe until you commit it."
      },
      {
        "level": 2,
        "text": "Put this route at the top of the handler, right after the PUT route.\n\nIn server.js:\n```\n  if (req.url === \"/items/raise\" && req.method === \"POST\") { db.exec(\"BEGIN\"); db.prepare(\"UPDATE items SET price = price + 1\").run(); db.exec(\"COMMIT\"); return send(res, 200, db.prepare(\"SELECT * FROM items\").all()); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Shirt\",250],[\"Pin\",30],[\"Badge\",45]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  res.statusCode = 204;\n  return res.end();\n}\nasync function updateItem(req, res, id) {\n  const data = JSON.parse(await readBody(req));\n  if (data.price !== undefined && (!Number.isInteger(data.price) || data.price < 0)) return send(res, 422, { error: \"price must be a whole number 0 or more\" });\n  const result = db.prepare(\"UPDATE items SET name = COALESCE(?, name), price = COALESCE(?, price) WHERE id = ?\").run(data.name ?? null, data.price ?? null, id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  return send(res, 200, one(id));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items/raise\" && req.method === \"POST\") { db.exec(\"BEGIN\"); db.prepare(\"UPDATE items SET price = price + 1\").run(); db.exec(\"COMMIT\"); return send(res, 200, db.prepare(\"SELECT * FROM items\").all()); }\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 5,
    "projectId": "sqlite-changes-school-club"
  },
  {
    "id": "api-sqlite-changes-school-club-8",
    "index": 378,
    "task": "You add a route to count the rows. This lets you see how many items are left after deleting. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Shirt\",250],[\"Pin\",30],[\"Badge\",45]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "count",
        "label": "After DELETE /items/1, GET /items/count answers 2",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "DELETE",
            "path": "/items/1"
          },
          {
            "method": "GET",
            "path": "/items/count"
          }
        ],
        "bodyContains": "{\"count\":2}"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The COUNT function counts all rows in the table."
      },
      {
        "level": 2,
        "text": "Put this route after the raise route, right before the DELETE route.\n\nIn server.js:\n```\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Shirt\",250],[\"Pin\",30],[\"Badge\",45]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  res.statusCode = 204;\n  return res.end();\n}\nasync function updateItem(req, res, id) {\n  const data = JSON.parse(await readBody(req));\n  if (data.price !== undefined && (!Number.isInteger(data.price) || data.price < 0)) return send(res, 422, { error: \"price must be a whole number 0 or more\" });\n  const result = db.prepare(\"UPDATE items SET name = COALESCE(?, name), price = COALESCE(?, price) WHERE id = ?\").run(data.name ?? null, data.price ?? null, id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  return send(res, 200, one(id));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items/raise\" && req.method === \"POST\") { db.exec(\"BEGIN\"); db.prepare(\"UPDATE items SET price = price + 1\").run(); db.exec(\"COMMIT\"); return send(res, 200, db.prepare(\"SELECT * FROM items\").all()); }\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "sqlite-changes-school-club"
  },
  {
    "id": "api-sqlite-changes-school-club-9",
    "index": 379,
    "task": "You change the GET /items route to send a header. This header says how many rows are there. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (req.url === \"/items\") { const rows = db.prepare(\"SELECT * FROM items\").all(); res.setHeader(\"X-Total-Count\", String(rows.length)); return send(res, 200, rows); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Shirt\",250],[\"Pin\",30],[\"Badge\",45]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "header",
        "label": "GET /items sends X-Total-Count: 3",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "header": {
          "name": "x-total-count",
          "value": "3"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The header X-Total-Count tells the client how many rows are there."
      },
      {
        "level": 2,
        "text": "Put this code right after the DELETE route, before the GET /items route.\n\nIn server.js:\n```\n  if (req.url === \"/items\") { const rows = db.prepare(\"SELECT * FROM items\").all(); res.setHeader(\"X-Total-Count\", String(rows.length)); return send(res, 200, rows); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Shirt\",250],[\"Pin\",30],[\"Badge\",45]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  res.statusCode = 204;\n  return res.end();\n}\nasync function updateItem(req, res, id) {\n  const data = JSON.parse(await readBody(req));\n  if (data.price !== undefined && (!Number.isInteger(data.price) || data.price < 0)) return send(res, 422, { error: \"price must be a whole number 0 or more\" });\n  const result = db.prepare(\"UPDATE items SET name = COALESCE(?, name), price = COALESCE(?, price) WHERE id = ?\").run(data.name ?? null, data.price ?? null, id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  return send(res, 200, one(id));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items/raise\" && req.method === \"POST\") { db.exec(\"BEGIN\"); db.prepare(\"UPDATE items SET price = price + 1\").run(); db.exec(\"COMMIT\"); return send(res, 200, db.prepare(\"SELECT * FROM items\").all()); }\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") { const rows = db.prepare(\"SELECT * FROM items\").all(); res.setHeader(\"X-Total-Count\", String(rows.length)); return send(res, 200, rows); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "sqlite-changes-school-club"
  },
  {
    "id": "api-sqlite-changes-school-club-10",
    "index": 380,
    "task": "You add two lines to search by exact name. This stops bad text from changing the SQL. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items/search\") return send(res, 200, db.prepare(\"SELECT * FROM items WHERE name = ?\").all(url.searchParams.get(\"name\") ?? \"\"));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Shirt\",250],[\"Pin\",30],[\"Badge\",45]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nasync function updateItem(req, res, id) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") return send(res, 200, db.prepare(\"SELECT * FROM items\").all());\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "found",
        "label": "GET /items/search?name=Pin finds it",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/search?name=Pin"
          }
        ],
        "bodyContains": "\"name\":\"Pin\""
      },
      {
        "id": "safe",
        "label": "A search for ' OR '1'='1 finds nothing",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/search?name=%27%20OR%20%271%27%3D%271"
          }
        ],
        "status": 200,
        "bodyContains": "[]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The URL object helps you get the search parameter safely."
      },
      {
        "level": 2,
        "text": "Put these lines after the count route, right before the GET /items route.\n\nIn server.js:\n```\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items/search\") return send(res, 200, db.prepare(\"SELECT * FROM items WHERE name = ?\").all(url.searchParams.get(\"name\") ?? \"\"));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nimport { DatabaseSync } from \"node:sqlite\";\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nconst db = new DatabaseSync(\":memory:\");\ndb.exec(\"CREATE TABLE items (id INTEGER PRIMARY KEY, name TEXT NOT NULL, price INTEGER NOT NULL)\");\nconst insert = db.prepare(\"INSERT INTO items (name, price) VALUES (?, ?)\");\nfor (const [name, price] of [[\"Shirt\",250],[\"Pin\",30],[\"Badge\",45]]) insert.run(name, price);\nconst one = (id) => db.prepare(\"SELECT * FROM items WHERE id = ?\").get(id);\nfunction deleteItem(res, id) {\n  const result = db.prepare(\"DELETE FROM items WHERE id = ?\").run(id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  res.statusCode = 204;\n  return res.end();\n}\nasync function updateItem(req, res, id) {\n  const data = JSON.parse(await readBody(req));\n  if (data.price !== undefined && (!Number.isInteger(data.price) || data.price < 0)) return send(res, 422, { error: \"price must be a whole number 0 or more\" });\n  const result = db.prepare(\"UPDATE items SET name = COALESCE(?, name), price = COALESCE(?, price) WHERE id = ?\").run(data.name ?? null, data.price ?? null, id);\n  if (result.changes === 0) return send(res, 404, { error: \"Item not found\" });\n  return send(res, 200, one(id));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items/raise\" && req.method === \"POST\") { db.exec(\"BEGIN\"); db.prepare(\"UPDATE items SET price = price + 1\").run(); db.exec(\"COMMIT\"); return send(res, 200, db.prepare(\"SELECT * FROM items\").all()); }\n  if (req.url === \"/items/count\") return send(res, 200, db.prepare(\"SELECT COUNT(*) AS count FROM items\").get());\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items/search\") return send(res, 200, db.prepare(\"SELECT * FROM items WHERE name = ?\").all(url.searchParams.get(\"name\") ?? \"\"));\n  const match = req.url.match(/^\\/items\\/(\\d+)$/);\n  if (match && req.method === \"GET\") { const row = one(Number(match[1])); return row ? send(res, 200, row) : send(res, 404, { error: \"Item not found\" }); }\n  if (match && req.method === \"DELETE\") return deleteItem(res, Number(match[1]));\n  if (match && req.method === \"PUT\") return updateItem(req, res, Number(match[1]));\n  if (req.url === \"/items\") { const rows = db.prepare(\"SELECT * FROM items\").all(); res.setHeader(\"X-Total-Count\", String(rows.length)); return send(res, 200, rows); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 5,
    "projectId": "sqlite-changes-school-club"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: errors-school-club.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-errors-school-club-1",
    "index": 381,
    "task": "Add a /boom route. This route will crash the server if you don't catch it. Change the handle call to catch any error. This way, the server does not crash. The code below does this. Run the checker to test.\n\nIn server.js:\n```\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  handle(req, res).catch(() => send(res, 500, { error: \"Something went wrong\" }));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res);\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "500",
        "label": "GET /boom answers 500",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/boom"
          }
        ],
        "status": 500,
        "bodyContains": "Something went wrong"
      },
      {
        "id": "alive",
        "label": "The server keeps answering after an error",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/boom"
          },
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "status": 200
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The /boom route must throw an error to test the catch."
      },
      {
        "level": 2,
        "text": "Put the new code at the top of handle, then change the bottom catch.\n\nIn server.js:\n```\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  handle(req, res).catch(() => send(res, 500, { error: \"Something went wrong\" }));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res).catch(() => send(res, 500, { error: \"Something went wrong\" }));\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "errors-school-club"
  },
  {
    "id": "api-errors-school-club-2",
    "index": 382,
    "task": "Add a counter to track errors. Change the catch to include this counter. This helps you know how many errors happened. The code below does this. Run the checker to test.\n\nIn server.js:\n```\nlet errorCount = 0;\n  handle(req, res).catch(() => send(res, 500, { error: \"Something went wrong\", id: ++errorCount }));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res);\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "id",
        "label": "The first error answers id 1",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/boom"
          }
        ],
        "bodyContains": "\"id\":1"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The counter starts at 0 and increases each time an error happens."
      },
      {
        "level": 2,
        "text": "Put the counter above handle, then change the catch to use it.\n\nIn server.js:\n```\nlet errorCount = 0;\n  handle(req, res).catch(() => send(res, 500, { error: \"Something went wrong\", id: ++errorCount }));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet errorCount = 0;\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res).catch(() => send(res, 500, { error: \"Something went wrong\", id: ++errorCount }));\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "errors-school-club"
  },
  {
    "id": "api-errors-school-club-3",
    "index": 383,
    "task": "Add an HttpError class. This class holds error messages and status codes. Change the catch to use it. This way, errors show their own status. The code below does this. Run the checker to test.\n\nIn server.js:\n```\nclass HttpError extends Error { constructor(status, message) { super(message); this.status = status; } }\n  if (req.url === \"/teapot\") throw new HttpError(418, \"I am a teapot\");\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", id: ++errorCount }));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res);\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "teapot",
        "label": "GET /teapot answers 418",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/teapot"
          }
        ],
        "status": 418,
        "bodyContains": "I am a teapot"
      },
      {
        "id": "hidden",
        "label": "Unexpected errors still hide their details",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/boom"
          }
        ],
        "status": 500,
        "bodyContains": "Something went wrong"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The HttpError class lets you send a status code with the error message."
      },
      {
        "level": 2,
        "text": "Put the class and /teapot route before the handle call, then change the catch.\n\nIn server.js:\n```\nclass HttpError extends Error { constructor(status, message) { super(message); this.status = status; } }\n  if (req.url === \"/teapot\") throw new HttpError(418, \"I am a teapot\");\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", id: ++errorCount }));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet errorCount = 0;\nclass HttpError extends Error { constructor(status, message) { super(message); this.status = status; } }\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  if (req.url === \"/teapot\") throw new HttpError(418, \"I am a teapot\");\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", id: ++errorCount }));\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "errors-school-club"
  },
  {
    "id": "api-errors-school-club-4",
    "index": 384,
    "task": "Add a readJson helper. This helper reads JSON from the body. Use it in createItem to check if the body is valid. If not, throw an HttpError with status 400. The code below does this. Run the checker to test.\n\nIn server.js:\n```\nasync function readJson(req) { const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\n  const item = await readJson(req);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res);\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "400",
        "label": "POST /items with broken JSON answers 400",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "not json",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 400
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The readJson helper must parse the body as JSON. If it fails, throw an error."
      },
      {
        "level": 2,
        "text": "Put the helper above createItem, then use it in createItem.\n\nIn server.js:\n```\nasync function readJson(req) { const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\n  const item = await readJson(req);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet errorCount = 0;\nclass HttpError extends Error { constructor(status, message) { super(message); this.status = status; } }\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function readJson(req) { const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\nasync function createItem(req, res) {\n  const item = await readJson(req);\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  if (req.url === \"/teapot\") throw new HttpError(418, \"I am a teapot\");\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", id: ++errorCount }));\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "errors-school-club"
  },
  {
    "id": "api-errors-school-club-5",
    "index": 385,
    "task": "Add a route before GET /items. This route checks if the method is allowed. If not, send a 405 error with an Allow header. The code below does this. Run the checker to test.\n\nIn server.js:\n```\n  if (req.url === \"/items\" && ![\"GET\", \"POST\"].includes(req.method)) { res.setHeader(\"Allow\", \"GET, POST\"); throw new HttpError(405, \"Method not allowed\"); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res);\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "405",
        "label": "DELETE /items answers 405 with Allow: GET, POST",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "DELETE",
            "path": "/items"
          }
        ],
        "status": 405,
        "header": {
          "name": "allow",
          "value": "GET, POST"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The route must check if the method is GET or POST. If not, send a 405 error."
      },
      {
        "level": 2,
        "text": "Put the route before the GET /items line, then change the handle call.\n\nIn server.js:\n```\n  if (req.url === \"/items\" && ![\"GET\", \"POST\"].includes(req.method)) { res.setHeader(\"Allow\", \"GET, POST\"); throw new HttpError(405, \"Method not allowed\"); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet errorCount = 0;\nclass HttpError extends Error { constructor(status, message) { super(message); this.status = status; } }\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function readJson(req) { const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\nasync function createItem(req, res) {\n  const item = await readJson(req);\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  if (req.url === \"/teapot\") throw new HttpError(418, \"I am a teapot\");\n  if (req.url === \"/items\" && ![\"GET\", \"POST\"].includes(req.method)) { res.setHeader(\"Allow\", \"GET, POST\"); throw new HttpError(405, \"Method not allowed\"); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", id: ++errorCount }));\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "errors-school-club"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: errors-school-club.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-errors-school-club-6",
    "index": 386,
    "task": "You change the readBody helper. It checks if the body is too big. If it is, it throws an error with status 413. This stops big uploads. The code below does this. Run the checker to test it.\n\nIn server.js:\n```\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) { text += chunk; if (text.length > 1000) throw new HttpError(413, \"Body too large\"); } return text; }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res);\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "413",
        "label": "A 2,000-character body answers 413",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "{\"name\":\"\",\"price\":1}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 413
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "If the body is over 1,000 characters, stop it with 413."
      },
      {
        "level": 2,
        "text": "Put the code inside the readBody function in server.js.\n\nIn server.js:\n```\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) { text += chunk; if (text.length > 1000) throw new HttpError(413, \"Body too large\"); } return text; }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet errorCount = 0;\nclass HttpError extends Error { constructor(status, message) { super(message); this.status = status; } }\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) { text += chunk; if (text.length > 1000) throw new HttpError(413, \"Body too large\"); } return text; }\nasync function readJson(req) { const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\nasync function createItem(req, res) {\n  const item = await readJson(req);\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  if (req.url === \"/teapot\") throw new HttpError(418, \"I am a teapot\");\n  if (req.url === \"/items\" && ![\"GET\", \"POST\"].includes(req.method)) { res.setHeader(\"Allow\", \"GET, POST\"); throw new HttpError(405, \"Method not allowed\"); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", id: ++errorCount }));\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "errors-school-club"
  },
  {
    "id": "api-errors-school-club-7",
    "index": 387,
    "task": "You change the readJson helper. It checks if the body is marked as JSON. If not, it throws 415. This stops wrong formats. The code below does this. Run the checker to test it.\n\nIn server.js:\n```\nasync function readJson(req) { if (!req.headers[\"content-type\"]?.includes(\"application/json\")) throw new HttpError(415, \"Send JSON\"); const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res);\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "415",
        "label": "POST /items with text/plain answers 415",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "name=Tea",
            "headers": {
              "Content-Type": "text/plain"
            }
          }
        ],
        "status": 415
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "If the header says 'text/plain', answer 415."
      },
      {
        "level": 2,
        "text": "Put the code inside the readJson function in server.js.\n\nIn server.js:\n```\nasync function readJson(req) { if (!req.headers[\"content-type\"]?.includes(\"application/json\")) throw new HttpError(415, \"Send JSON\"); const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet errorCount = 0;\nclass HttpError extends Error { constructor(status, message) { super(message); this.status = status; } }\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) { text += chunk; if (text.length > 1000) throw new HttpError(413, \"Body too large\"); } return text; }\nasync function readJson(req) { if (!req.headers[\"content-type\"]?.includes(\"application/json\")) throw new HttpError(415, \"Send JSON\"); const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\nasync function createItem(req, res) {\n  const item = await readJson(req);\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  if (req.url === \"/teapot\") throw new HttpError(418, \"I am a teapot\");\n  if (req.url === \"/items\" && ![\"GET\", \"POST\"].includes(req.method)) { res.setHeader(\"Allow\", \"GET, POST\"); throw new HttpError(405, \"Method not allowed\"); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", id: ++errorCount }));\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "errors-school-club"
  },
  {
    "id": "api-errors-school-club-8",
    "index": 388,
    "task": "You add a line in createItem. It checks if an item with that name already exists. If it does, it throws 409. This stops duplicates. The code below does this. Run the checker to test it.\n\nIn server.js:\n```\n  if (items.some((existing) => existing.name === item.name)) throw new HttpError(409, \"Name already exists\");\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res);\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "409",
        "label": "POST /items with Shirt again answers 409",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "{\"name\":\"Shirt\",\"price\":1}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 409
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Check if any item has the same name before adding."
      },
      {
        "level": 2,
        "text": "Put the code after reading the item in createItem in server.js.\n\nIn server.js:\n```\n  if (items.some((existing) => existing.name === item.name)) throw new HttpError(409, \"Name already exists\");\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet errorCount = 0;\nclass HttpError extends Error { constructor(status, message) { super(message); this.status = status; } }\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) { text += chunk; if (text.length > 1000) throw new HttpError(413, \"Body too large\"); } return text; }\nasync function readJson(req) { if (!req.headers[\"content-type\"]?.includes(\"application/json\")) throw new HttpError(415, \"Send JSON\"); const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\nasync function createItem(req, res) {\n  const item = await readJson(req);\n  if (items.some((existing) => existing.name === item.name)) throw new HttpError(409, \"Name already exists\");\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  if (req.url === \"/teapot\") throw new HttpError(418, \"I am a teapot\");\n  if (req.url === \"/items\" && ![\"GET\", \"POST\"].includes(req.method)) { res.setHeader(\"Allow\", \"GET, POST\"); throw new HttpError(405, \"Method not allowed\"); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", id: ++errorCount }));\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "errors-school-club"
  },
  {
    "id": "api-errors-school-club-9",
    "index": 389,
    "task": "You change the catch at the bottom. It adds the status code to every error body. This helps users know what went wrong. The code below does this. Run the checker to test it.\n\nIn server.js:\n```\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", status: error.status ?? 500, id: ++errorCount }));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res);\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "status",
        "label": "The /teapot error body includes status 418",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/teapot"
          }
        ],
        "bodyContains": "\"status\":418"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Add the status code to every error message."
      },
      {
        "level": 2,
        "text": "Put the code in the catch block at the bottom of server.js.\n\nIn server.js:\n```\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", status: error.status ?? 500, id: ++errorCount }));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet errorCount = 0;\nclass HttpError extends Error { constructor(status, message) { super(message); this.status = status; } }\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) { text += chunk; if (text.length > 1000) throw new HttpError(413, \"Body too large\"); } return text; }\nasync function readJson(req) { if (!req.headers[\"content-type\"]?.includes(\"application/json\")) throw new HttpError(415, \"Send JSON\"); const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\nasync function createItem(req, res) {\n  const item = await readJson(req);\n  if (items.some((existing) => existing.name === item.name)) throw new HttpError(409, \"Name already exists\");\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  if (req.url === \"/teapot\") throw new HttpError(418, \"I am a teapot\");\n  if (req.url === \"/items\" && ![\"GET\", \"POST\"].includes(req.method)) { res.setHeader(\"Allow\", \"GET, POST\"); throw new HttpError(405, \"Method not allowed\"); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", status: error.status ?? 500, id: ++errorCount }));\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "errors-school-club"
  },
  {
    "id": "api-errors-school-club-10",
    "index": 390,
    "task": "You replace the last line of handle. It throws a 404 error with the method and path. This tells users the route doesn't exist. The code below does this. Run the checker to test it.\n\nIn server.js:\n```\n  throw new HttpError(404, `No route for ${req.method} ${req.url}`);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const item = JSON.parse(await readBody(req));\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res);\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "route",
        "label": "GET /nope answers No route for GET /nope",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/nope"
          }
        ],
        "status": 404,
        "bodyContains": "No route for GET /nope"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use the method and URL to make the error message clear."
      },
      {
        "level": 2,
        "text": "Put the code at the end of the handle function in server.js.\n\nIn server.js:\n```\n  throw new HttpError(404, `No route for ${req.method} ${req.url}`);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Shirt\", price: 250 }, { id: 2, name: \"Pin\", price: 30 }, { id: 3, name: \"Badge\", price: 45 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nlet errorCount = 0;\nclass HttpError extends Error { constructor(status, message) { super(message); this.status = status; } }\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) { text += chunk; if (text.length > 1000) throw new HttpError(413, \"Body too large\"); } return text; }\nasync function readJson(req) { if (!req.headers[\"content-type\"]?.includes(\"application/json\")) throw new HttpError(415, \"Send JSON\"); const text = await readBody(req); try { return JSON.parse(text); } catch { throw new HttpError(400, \"Body must be JSON\"); } }\nasync function createItem(req, res) {\n  const item = await readJson(req);\n  if (items.some((existing) => existing.name === item.name)) throw new HttpError(409, \"Name already exists\");\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nasync function handle(req, res) {\n  if (req.url === \"/boom\") throw new Error(\"Database is down\");\n  if (req.url === \"/teapot\") throw new HttpError(418, \"I am a teapot\");\n  if (req.url === \"/items\" && ![\"GET\", \"POST\"].includes(req.method)) { res.setHeader(\"Allow\", \"GET, POST\"); throw new HttpError(405, \"Method not allowed\"); }\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  throw new HttpError(404, `No route for ${req.method} ${req.url}`);\n}\nconst server = http.createServer((req, res) => {\n  handle(req, res).catch((error) => send(res, error.status ?? 500, { error: error.status ? error.message : \"Something went wrong\", status: error.status ?? 500, id: ++errorCount }));\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "errors-school-club"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: api-design-school-club.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-api-design-school-club-1",
    "index": 391,
    "task": "Add this code after the url line in server.js. This route lets the API return a list of items. It helps future clients use the API without breaking. The code below does that. Run the checker to test it.\n\nIn server.js:\n```\n  if (url.pathname === \"/v1/items\") return send(res, 200, items);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst names = [\"Shirt\",\"Pin\",\"Badge\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "v1",
        "label": "GET /v1/items answers the list",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/v1/items"
          }
        ],
        "status": 200,
        "bodyContains": "\"name\":\"Shirt 1\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The route checks if the URL is /v1/items, then sends the list."
      },
      {
        "level": 2,
        "text": "Add the code after the url line in server.js.\n\nIn server.js:\n```\n  if (url.pathname === \"/v1/items\") return send(res, 200, items);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst names = [\"Shirt\",\"Pin\",\"Badge\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/v1/items\") return send(res, 200, items);\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "api-design-school-club"
  },
  {
    "id": "api-api-design-school-club-2",
    "index": 392,
    "task": "Add a page line to get the page number from the URL. Change the /v1/items route to show only five items per page. The code below does that. Run the checker to test it.\n\nIn server.js:\n```\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  if (url.pathname === \"/v1/items\") return send(res, 200, items.slice((page - 1) * 5, page * 5));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst names = [\"Shirt\",\"Pin\",\"Badge\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "page2",
        "label": "GET /v1/items?page=2 answers items 6 to 10",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/v1/items?page=2"
          }
        ],
        "bodyContains": "[{\"id\":6,\"name\":\"Badge 6\",\"price\":30},{\"id\":7,\"name\":\"Shirt 7\",\"price\":35},{\"id\":8,\"name\":\"Pin 8\",\"price\":40},{\"id\":9,\"name\":\"Badge 9\",\"price\":45},{\"id\":10,\"name\":\"Shirt 10\",\"price\":50}]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use the searchParams to get the page number from the URL."
      },
      {
        "level": 2,
        "text": "Add the code after the url line in server.js.\n\nIn server.js:\n```\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  if (url.pathname === \"/v1/items\") return send(res, 200, items.slice((page - 1) * 5, page * 5));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst names = [\"Shirt\",\"Pin\",\"Badge\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  if (url.pathname === \"/v1/items\") return send(res, 200, items.slice((page - 1) * 5, page * 5));\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "api-design-school-club"
  },
  {
    "id": "api-api-design-school-club-3",
    "index": 393,
    "task": "Add a perPage line to let the client choose how many items to show. Change the route to use that number. The code below does that. Run the checker to test it.\n\nIn server.js:\n```\n  const perPage = Number(url.searchParams.get(\"perPage\") ?? 5);\n  if (url.pathname === \"/v1/items\") return send(res, 200, items.slice((page - 1) * perPage, page * perPage));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst names = [\"Shirt\",\"Pin\",\"Badge\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "per",
        "label": "GET /v1/items?perPage=3 answers three items",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/v1/items?perPage=3"
          }
        ],
        "bodyContains": "[{\"id\":1,\"name\":\"Shirt 1\",\"price\":5},{\"id\":2,\"name\":\"Pin 2\",\"price\":10},{\"id\":3,\"name\":\"Badge 3\",\"price\":15}]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use the searchParams to get the perPage value from the URL."
      },
      {
        "level": 2,
        "text": "Add the code after the url line in server.js.\n\nIn server.js:\n```\n  const perPage = Number(url.searchParams.get(\"perPage\") ?? 5);\n  if (url.pathname === \"/v1/items\") return send(res, 200, items.slice((page - 1) * perPage, page * perPage));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst names = [\"Shirt\",\"Pin\",\"Badge\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  const perPage = Number(url.searchParams.get(\"perPage\") ?? 5);\n  if (url.pathname === \"/v1/items\") return send(res, 200, items.slice((page - 1) * perPage, page * perPage));\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "api-design-school-club"
  },
  {
    "id": "api-api-design-school-club-4",
    "index": 394,
    "task": "Add a data line to store the sliced items. Change the route to send the page, perPage, and data. The code below does that. Run the checker to test it.\n\nIn server.js:\n```\n  const data = items.slice((page - 1) * perPage, page * perPage);\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst names = [\"Shirt\",\"Pin\",\"Badge\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "envelope",
        "label": "The answer says page 2 and perPage 5",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/v1/items?page=2"
          }
        ],
        "bodyContains": "\"page\":2,\"perPage\":5"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Store the sliced items in a data variable."
      },
      {
        "level": 2,
        "text": "Add the code after the url line in server.js.\n\nIn server.js:\n```\n  const data = items.slice((page - 1) * perPage, page * perPage);\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst names = [\"Shirt\",\"Pin\",\"Badge\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  const perPage = Number(url.searchParams.get(\"perPage\") ?? 5);\n  const data = items.slice((page - 1) * perPage, page * perPage);\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage });\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "api-design-school-club"
  },
  {
    "id": "api-api-design-school-club-5",
    "index": 395,
    "task": "Change the route to add a total number to the answer. The code below does that. Run the checker to test it.\n\nIn server.js:\n```\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst names = [\"Shirt\",\"Pin\",\"Badge\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "total",
        "label": "The answer says total 12",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/v1/items"
          }
        ],
        "bodyContains": "\"total\":12"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Add total: items.length to the answer object."
      },
      {
        "level": 2,
        "text": "Change the route in server.js.\n\nIn server.js:\n```\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst names = [\"Shirt\",\"Pin\",\"Badge\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  const perPage = Number(url.searchParams.get(\"perPage\") ?? 5);\n  const data = items.slice((page - 1) * perPage, page * perPage);\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length });\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "api-design-school-club"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: api-design-school-club.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-api-design-school-club-6",
    "index": 396,
    "task": "Add a line for the next page link. This helps users go to the next page. The code below does this. Run the checker to confirm it works. The checker will test if page 1 links to page 2 and if the last page has no next link.\n\nIn server.js:\n```\n  const next = page * perPage < items.length ? `/v1/items?page=${page + 1}&perPage=${perPage}` : null;\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length, next });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst names = [\"Shirt\",\"Pin\",\"Badge\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "next",
        "label": "Page 1 links to page 2",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/v1/items"
          }
        ],
        "bodyContains": "\"next\":\"/v1/items?page=2&perPage=5\""
      },
      {
        "id": "last",
        "label": "The last page has no next link",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/v1/items?page=3"
          }
        ],
        "bodyContains": "\"next\":null"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Think of next as the URL to the next page. It should be null if there is no next page."
      },
      {
        "level": 2,
        "text": "Add this line after the data line in server.js, before the if block.\n\nIn server.js:\n```\n  const next = page * perPage < items.length ? `/v1/items?page=${page + 1}&perPage=${perPage}` : null;\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length, next });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst names = [\"Shirt\",\"Pin\",\"Badge\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  const perPage = Number(url.searchParams.get(\"perPage\") ?? 5);\n  const data = items.slice((page - 1) * perPage, page * perPage);\n  const next = page * perPage < items.length ? `/v1/items?page=${page + 1}&perPage=${perPage}` : null;\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length, next });\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "api-design-school-club"
  },
  {
    "id": "api-api-design-school-club-7",
    "index": 397,
    "task": "Add a line for the previous page link. This helps users go back. The code below does this. Run the checker to confirm it works. The checker will test if page 2 links back to page 1.\n\nIn server.js:\n```\n  const previous = page > 1 ? `/v1/items?page=${page - 1}&perPage=${perPage}` : null;\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length, next, previous });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst names = [\"Shirt\",\"Pin\",\"Badge\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "previous",
        "label": "Page 2 links back to page 1",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/v1/items?page=2"
          }
        ],
        "bodyContains": "\"previous\":\"/v1/items?page=1&perPage=5\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Think of previous as the URL to the previous page. It should be null if there is no previous page."
      },
      {
        "level": 2,
        "text": "Add this line after the next line in server.js, before the if block.\n\nIn server.js:\n```\n  const previous = page > 1 ? `/v1/items?page=${page - 1}&perPage=${perPage}` : null;\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length, next, previous });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst names = [\"Shirt\",\"Pin\",\"Badge\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  const perPage = Number(url.searchParams.get(\"perPage\") ?? 5);\n  const data = items.slice((page - 1) * perPage, page * perPage);\n  const next = page * perPage < items.length ? `/v1/items?page=${page + 1}&perPage=${perPage}` : null;\n  const previous = page > 1 ? `/v1/items?page=${page - 1}&perPage=${perPage}` : null;\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length, next, previous });\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "api-design-school-club"
  },
  {
    "id": "api-api-design-school-club-8",
    "index": 398,
    "task": "Add a line to check if the page number is a whole number and at least 1. If not, send a 400 error. The code below does this. Run the checker to confirm it works. The checker will test if asking for page 0 returns a 400 error.\n\nIn server.js:\n```\n  if (!Number.isInteger(page) || page < 1) return send(res, 400, { error: \"page must be a whole number 1 or more\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst names = [\"Shirt\",\"Pin\",\"Badge\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "bad",
        "label": "GET /v1/items?page=0 answers 400",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/v1/items?page=0"
          }
        ],
        "status": 400
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "If the page number is not a whole number or less than 1, it's invalid. The server must say so with a 400 error."
      },
      {
        "level": 2,
        "text": "Add this line after the page line in server.js, before the if block.\n\nIn server.js:\n```\n  if (!Number.isInteger(page) || page < 1) return send(res, 400, { error: \"page must be a whole number 1 or more\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst names = [\"Shirt\",\"Pin\",\"Badge\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  if (!Number.isInteger(page) || page < 1) return send(res, 400, { error: \"page must be a whole number 1 or more\" });\n  const perPage = Number(url.searchParams.get(\"perPage\") ?? 5);\n  const data = items.slice((page - 1) * perPage, page * perPage);\n  const next = page * perPage < items.length ? `/v1/items?page=${page + 1}&perPage=${perPage}` : null;\n  const previous = page > 1 ? `/v1/items?page=${page - 1}&perPage=${perPage}` : null;\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length, next, previous });\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "api-design-school-club"
  },
  {
    "id": "api-api-design-school-club-9",
    "index": 399,
    "task": "Change the perPage line to limit it to 10. This stops clients from asking for too many items at once. The code below does this. Run the checker to confirm it works. The checker will test if asking for 100 items per page returns 10.\n\nIn server.js:\n```\n  const perPage = Math.min(Number(url.searchParams.get(\"perPage\") ?? 5), 10);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst names = [\"Shirt\",\"Pin\",\"Badge\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "cap",
        "label": "GET /v1/items?perPage=100 answers perPage 10",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/v1/items?perPage=100"
          }
        ],
        "bodyContains": "\"perPage\":10,\"total\":12"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The server must never send more than 10 items per page, no matter what the client asks."
      },
      {
        "level": 2,
        "text": "Change the perPage line in server.js to use Math.min with 10 as the max.\n\nIn server.js:\n```\n  const perPage = Math.min(Number(url.searchParams.get(\"perPage\") ?? 5), 10);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst names = [\"Shirt\",\"Pin\",\"Badge\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  if (!Number.isInteger(page) || page < 1) return send(res, 400, { error: \"page must be a whole number 1 or more\" });\n  const perPage = Math.min(Number(url.searchParams.get(\"perPage\") ?? 5), 10);\n  const data = items.slice((page - 1) * perPage, page * perPage);\n  const next = page * perPage < items.length ? `/v1/items?page=${page + 1}&perPage=${perPage}` : null;\n  const previous = page > 1 ? `/v1/items?page=${page - 1}&perPage=${perPage}` : null;\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length, next, previous });\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "api-design-school-club"
  },
  {
    "id": "api-api-design-school-club-10",
    "index": 400,
    "task": "Change the old /items route to add a Deprecation header. This tells users the old route is no longer recommended. The code below does this. Run the checker to confirm it works. The checker will test if asking for /items sends a Deprecation: true header.\n\nIn server.js:\n```\n  if (url.pathname === \"/items\") { res.setHeader(\"Deprecation\", \"true\"); return send(res, 200, items); }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "School Club API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst names = [\"Shirt\",\"Pin\",\"Badge\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/items\") return send(res, 200, items);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "deprecated",
        "label": "GET /items sends Deprecation: true",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "header": {
          "name": "deprecation",
          "value": "true"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Add a header called Deprecation with the value true to tell users this route is old."
      },
      {
        "level": 2,
        "text": "Change the old /items route in server.js to set the header before sending the response.\n\nIn server.js:\n```\n  if (url.pathname === \"/items\") { res.setHeader(\"Deprecation\", \"true\"); return send(res, 200, items); }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst names = [\"Shirt\",\"Pin\",\"Badge\"];\nconst items = Array.from({ length: 12 }, (_, n) => ({ id: n + 1, name: `${names[n % 3]} ${n + 1}`, price: (n + 1) * 5 }));\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  const url = new URL(req.url, \"http://localhost\");\n  const page = Number(url.searchParams.get(\"page\") ?? 1);\n  if (!Number.isInteger(page) || page < 1) return send(res, 400, { error: \"page must be a whole number 1 or more\" });\n  const perPage = Math.min(Number(url.searchParams.get(\"perPage\") ?? 5), 10);\n  const data = items.slice((page - 1) * perPage, page * perPage);\n  const next = page * perPage < items.length ? `/v1/items?page=${page + 1}&perPage=${perPage}` : null;\n  const previous = page > 1 ? `/v1/items?page=${page - 1}&perPage=${perPage}` : null;\n  if (url.pathname === \"/v1/items\") return send(res, 200, { data, page, perPage, total: items.length, next, previous });\n  if (url.pathname === \"/items\") { res.setHeader(\"Deprecation\", \"true\"); return send(res, 200, items); }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "api-design-school-club"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: first-server-tricycle.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-first-server-tricycle-1",
    "index": 401,
    "task": "You will change the answer your server sends. The code below says the server answers every request with 'Tricycle Terminal API'. This is the first message your server sends. You must change the res.end line in server.js. This makes your server say what it is.\n\nIn server.js:\n```\n  res.end(\"Tricycle Terminal API\");\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Tricycle Terminal API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "root",
        "label": "GET / answers Tricycle Terminal API",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/"
          }
        ],
        "status": 200,
        "bodyContains": "Tricycle Terminal API"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The server must answer every request with the same message. Think of it like a greeting."
      },
      {
        "level": 2,
        "text": "Go to server.js and change the res.end line. The code is shown below.\n\nIn server.js:\n```\n  res.end(\"Tricycle Terminal API\");\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Tricycle Terminal API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "first-server-tricycle"
  },
  {
    "id": "api-first-server-tricycle-2",
    "index": 402,
    "task": "You will add a header to tell the browser the answer is plain text. This helps the browser show the message correctly. Add one line above res.end. The code below sets the Content-Type header. This tells the browser how to read the answer.\n\nIn server.js:\n```\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Tricycle Terminal API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "type",
        "label": "GET / says its body is plain text",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/"
          }
        ],
        "header": {
          "name": "content-type",
          "value": "text/plain"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The browser needs to know the answer is plain text. Add a header before sending the answer."
      },
      {
        "level": 2,
        "text": "Go to server.js and add the line above res.end. The code is shown below.\n\nIn server.js:\n```\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.end(\"Tricycle Terminal API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "first-server-tricycle"
  },
  {
    "id": "api-first-server-tricycle-3",
    "index": 403,
    "task": "You will add a custom header to say where the server is. This helps other systems know the server's location. Add one line above res.end. The code below sets the X-Place header to 'tricycle'. This is your server's name.\n\nIn server.js:\n```\n  res.setHeader(\"X-Place\", \"tricycle\");\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Tricycle Terminal API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "custom",
        "label": "GET / sends X-Place: tricycle",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/"
          }
        ],
        "header": {
          "name": "x-place",
          "value": "tricycle"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Add a header to tell others where your server is. Think of it like a sign on the building."
      },
      {
        "level": 2,
        "text": "Go to server.js and add the line above res.end. The code is shown below.\n\nIn server.js:\n```\n  res.setHeader(\"X-Place\", \"tricycle\");\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.setHeader(\"X-Place\", \"tricycle\");\n  res.end(\"Tricycle Terminal API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "first-server-tricycle"
  },
  {
    "id": "api-first-server-tricycle-4",
    "index": 404,
    "task": "You will add a special path for health checks. This lets other systems know your server is working. Add one line at the top of the handler. The code below checks if the request is for /health. If so, it sends 'healthy'. This is for monitoring.\n\nIn server.js:\n```\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Tricycle Terminal API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "health",
        "label": "GET /health answers healthy",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/health"
          }
        ],
        "status": 200,
        "bodyContains": "healthy"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Add a path that answers 'healthy' for health checks. Think of it like a heartbeat."
      },
      {
        "level": 2,
        "text": "Go to server.js and add the line at the top of the handler. The code is shown below.\n\nIn server.js:\n```\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.setHeader(\"X-Place\", \"tricycle\");\n  res.end(\"Tricycle Terminal API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "first-server-tricycle"
  },
  {
    "id": "api-first-server-tricycle-5",
    "index": 405,
    "task": "You will add a rule for any other path. This tells the server to say 'Not found' if the path is not / or /health. Add one line right after the /health line. The code below sets the status to 404 and sends 'Not found'. This stops errors for unknown paths.\n\nIn server.js:\n```\n  if (req.url !== \"/\") { res.statusCode = 404; res.end(\"Not found\"); return; }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Tricycle Terminal API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "missing",
        "label": "GET /missing answers 404",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/missing"
          }
        ],
        "status": 404
      },
      {
        "id": "root",
        "label": "GET / still answers 200",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/"
          }
        ],
        "status": 200
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Add a rule for any path that is not / or /health. Think of it like a guard at the door."
      },
      {
        "level": 2,
        "text": "Go to server.js and add the line right after the /health line. The code is shown below.\n\nIn server.js:\n```\n  if (req.url !== \"/\") { res.statusCode = 404; res.end(\"Not found\"); return; }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n  if (req.url !== \"/\") { res.statusCode = 404; res.end(\"Not found\"); return; }\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.setHeader(\"X-Place\", \"tricycle\");\n  res.end(\"Tricycle Terminal API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "first-server-tricycle"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: first-server-tricycle.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-first-server-tricycle-6",
    "index": 406,
    "task": "You must refuse any method other than GET. Add this line at the very top of the handler. It stops other methods from working. The checker will test this by sending a POST request. Run the checker and paste its report.\n\nIn server.js:\n```\n  if (req.method !== \"GET\") { res.statusCode = 405; res.end(\"Method not allowed\"); return; }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Tricycle Terminal API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "post",
        "label": "POST / answers 405",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/",
            "body": "{}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 405
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "You stop unwanted methods by checking the request method first."
      },
      {
        "level": 2,
        "text": "Add the line at the very top of the handler, before any other code.\n\nIn server.js:\n```\n  if (req.method !== \"GET\") { res.statusCode = 405; res.end(\"Method not allowed\"); return; }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.method !== \"GET\") { res.statusCode = 405; res.end(\"Method not allowed\"); return; }\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n  if (req.url !== \"/\") { res.statusCode = 404; res.end(\"Not found\"); return; }\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.setHeader(\"X-Place\", \"tricycle\");\n  res.end(\"Tricycle Terminal API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "first-server-tricycle"
  },
  {
    "id": "api-first-server-tricycle-7",
    "index": 407,
    "task": "You must answer /info with JSON describing the place. Add this line right after the /health line. It sends a JSON object with the name of the terminal. The checker will test this by sending a GET request. Run the checker and paste its report.\n\nIn server.js:\n```\n  if (req.url === \"/info\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify({ name: \"Tricycle Terminal\" })); return; }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Tricycle Terminal API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "info",
        "label": "GET /info answers JSON with the name",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/info"
          }
        ],
        "bodyContains": "\"name\":\"Tricycle Terminal\"",
        "header": {
          "name": "content-type",
          "value": "application/json"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "You send JSON by setting the Content-Type header and using JSON.stringify."
      },
      {
        "level": 2,
        "text": "Add the line right after the /health route, before any other route.\n\nIn server.js:\n```\n  if (req.url === \"/info\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify({ name: \"Tricycle Terminal\" })); return; }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.method !== \"GET\") { res.statusCode = 405; res.end(\"Method not allowed\"); return; }\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n  if (req.url === \"/info\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify({ name: \"Tricycle Terminal\" })); return; }\n  if (req.url !== \"/\") { res.statusCode = 404; res.end(\"Not found\"); return; }\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.setHeader(\"X-Place\", \"tricycle\");\n  res.end(\"Tricycle Terminal API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "first-server-tricycle"
  },
  {
    "id": "api-first-server-tricycle-8",
    "index": 408,
    "task": "You must count requests and report the count at /visits. Add a counter above the server, count each request, and add a /visits route before the 404 line. The checker will test this by sending two requests and checking the count. Run the checker and paste its report.\n\nIn server.js:\n```\nlet visits = 0;\n  visits += 1;\n  if (req.url === \"/visits\") { res.end(String(visits)); return; }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Tricycle Terminal API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "visits",
        "label": "After two requests, GET /visits answers 3",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/"
          },
          {
            "method": "GET",
            "path": "/"
          },
          {
            "method": "GET",
            "path": "/visits"
          }
        ],
        "bodyContains": "3"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "You count requests by adding 1 to a variable each time a request comes in."
      },
      {
        "level": 2,
        "text": "Add the counter above the server, then add the /visits route before the 404 line.\n\nIn server.js:\n```\nlet visits = 0;\n  visits += 1;\n  if (req.url === \"/visits\") { res.end(String(visits)); return; }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nlet visits = 0;\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.method !== \"GET\") { res.statusCode = 405; res.end(\"Method not allowed\"); return; }\n  visits += 1;\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n  if (req.url === \"/info\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify({ name: \"Tricycle Terminal\" })); return; }\n  if (req.url === \"/visits\") { res.end(String(visits)); return; }\n  if (req.url !== \"/\") { res.statusCode = 404; res.end(\"Not found\"); return; }\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.setHeader(\"X-Place\", \"tricycle\");\n  res.end(\"Tricycle Terminal API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 5,
    "projectId": "first-server-tricycle"
  },
  {
    "id": "api-first-server-tricycle-9",
    "index": 409,
    "task": "You must tell browsers not to store any answer with Cache-Control. Add this line at the very top of the handler. It tells browsers to forget the answer. The checker will test this by sending a GET request and checking the header. Run the checker and paste its report.\n\nIn server.js:\n```\n  res.setHeader(\"Cache-Control\", \"no-store\");\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Tricycle Terminal API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "cache",
        "label": "GET / sends Cache-Control: no-store",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/"
          }
        ],
        "header": {
          "name": "cache-control",
          "value": "no-store"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "You control caching by setting the Cache-Control header to no-store."
      },
      {
        "level": 2,
        "text": "Add the line at the very top of the handler, before any other code.\n\nIn server.js:\n```\n  res.setHeader(\"Cache-Control\", \"no-store\");\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nlet visits = 0;\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.setHeader(\"Cache-Control\", \"no-store\");\n  if (req.method !== \"GET\") { res.statusCode = 405; res.end(\"Method not allowed\"); return; }\n  visits += 1;\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n  if (req.url === \"/info\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify({ name: \"Tricycle Terminal\" })); return; }\n  if (req.url === \"/visits\") { res.end(String(visits)); return; }\n  if (req.url !== \"/\") { res.statusCode = 404; res.end(\"Not found\"); return; }\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.setHeader(\"X-Place\", \"tricycle\");\n  res.end(\"Tricycle Terminal API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "first-server-tricycle"
  },
  {
    "id": "api-first-server-tricycle-10",
    "index": 410,
    "task": "You must read a name from the query string at /hello, with friend as the default. Add two lines right before the 404 line. The first line creates a URL object. The second line reads the name or uses 'friend' if none is given. The checker will test this by sending two requests. Run the checker and paste its report.\n\nIn server.js:\n```\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/hello\") { res.end(`Hello, ${url.searchParams.get(\"name\") ?? \"friend\"}`); return; }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Tricycle Terminal API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Hello\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "hello",
        "label": "GET /hello?name=Ana answers Hello, Ana",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/hello?name=Ana"
          }
        ],
        "bodyContains": "Hello, Ana"
      },
      {
        "id": "default",
        "label": "GET /hello answers Hello, friend",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/hello"
          }
        ],
        "bodyContains": "Hello, friend"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "You read query strings by using the URL class and its searchParams property."
      },
      {
        "level": 2,
        "text": "Add the two lines right before the 404 line, after the /hello route.\n\nIn server.js:\n```\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/hello\") { res.end(`Hello, ${url.searchParams.get(\"name\") ?? \"friend\"}`); return; }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nlet visits = 0;\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.setHeader(\"Cache-Control\", \"no-store\");\n  if (req.method !== \"GET\") { res.statusCode = 405; res.end(\"Method not allowed\"); return; }\n  visits += 1;\n  if (req.url === \"/health\") { res.end(\"healthy\"); return; }\n  if (req.url === \"/info\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify({ name: \"Tricycle Terminal\" })); return; }\n  if (req.url === \"/visits\") { res.end(String(visits)); return; }\n  const url = new URL(req.url, \"http://localhost\");\n  if (url.pathname === \"/hello\") { res.end(`Hello, ${url.searchParams.get(\"name\") ?? \"friend\"}`); return; }\n  if (req.url !== \"/\") { res.statusCode = 404; res.end(\"Not found\"); return; }\n  res.setHeader(\"Content-Type\", \"text/plain; charset=utf-8\");\n  res.setHeader(\"X-Place\", \"tricycle\");\n  res.end(\"Tricycle Terminal API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 5,
    "projectId": "first-server-tricycle"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: json-routes-tricycle.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-json-routes-tricycle-1",
    "index": 411,
    "task": "You will add code to handle the /items route. This route returns all items as JSON. The code goes at the top of the handler. The send helper sends the JSON response. The checker will test this route.\n\nIn server.js:\n```\n  if (req.url === \"/items\") return send(res, 200, items);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Tricycle Terminal API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Market\", price: 20 }, { id: 2, name: \"School\", price: 30 }, { id: 3, name: \"Clinic\", price: 25 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "list",
        "label": "GET /items answers the list as JSON",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "bodyContains": "\"name\":\"Market\"",
        "header": {
          "name": "content-type",
          "value": "application/json"
        }
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The code checks if the URL is /items, then sends the items array as JSON."
      },
      {
        "level": 2,
        "text": "Add this line right after the first if statement in server.js.\n\nIn server.js:\n```\n  if (req.url === \"/items\") return send(res, 200, items);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Market\", price: 20 }, { id: 2, name: \"School\", price: 30 }, { id: 3, name: \"Clinic\", price: 25 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "json-routes-tricycle"
  },
  {
    "id": "api-json-routes-tricycle-2",
    "index": 412,
    "task": "You will add code to handle the /items/count route. This route returns how many items there are. The code goes after the /items line. The checker will test this route.\n\nIn server.js:\n```\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Tricycle Terminal API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Market\", price: 20 }, { id: 2, name: \"School\", price: 30 }, { id: 3, name: \"Clinic\", price: 25 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "count",
        "label": "GET /items/count answers {\"count\":3}",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/count"
          }
        ],
        "bodyContains": "{\"count\":3}"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The code checks if the URL is /items/count, then sends an object with the count property."
      },
      {
        "level": 2,
        "text": "Add this line after the /items line in server.js.\n\nIn server.js:\n```\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Market\", price: 20 }, { id: 2, name: \"School\", price: 30 }, { id: 3, name: \"Clinic\", price: 25 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "json-routes-tricycle"
  },
  {
    "id": "api-json-routes-tricycle-3",
    "index": 413,
    "task": "You will add code to handle the /items/first route. This route returns only the first item. The code goes after the /items/count line. The checker will test this route.\n\nIn server.js:\n```\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Tricycle Terminal API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Market\", price: 20 }, { id: 2, name: \"School\", price: 30 }, { id: 3, name: \"Clinic\", price: 25 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "first",
        "label": "GET /items/first answers Market",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/first"
          }
        ],
        "bodyContains": "{\"id\":1,\"name\":\"Market\",\"price\":20}"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The code checks if the URL is /items/first, then sends the first item from the array."
      },
      {
        "level": 2,
        "text": "Add this line after the /items/count line in server.js.\n\nIn server.js:\n```\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Market\", price: 20 }, { id: 2, name: \"School\", price: 30 }, { id: 3, name: \"Clinic\", price: 25 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "json-routes-tricycle"
  },
  {
    "id": "api-json-routes-tricycle-4",
    "index": 414,
    "task": "You will add code to handle the /items/names route. This route returns only the names of the items. The code goes after the /items/first line. The checker will test this route.\n\nIn server.js:\n```\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Tricycle Terminal API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Market\", price: 20 }, { id: 2, name: \"School\", price: 30 }, { id: 3, name: \"Clinic\", price: 25 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "names",
        "label": "GET /items/names answers the three names",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/names"
          }
        ],
        "bodyContains": "[\"Market\",\"School\",\"Clinic\"]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The code checks if the URL is /items/names, then sends an array of names using map."
      },
      {
        "level": 2,
        "text": "Add this line after the /items/first line in server.js.\n\nIn server.js:\n```\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Market\", price: 20 }, { id: 2, name: \"School\", price: 30 }, { id: 3, name: \"Clinic\", price: 25 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "json-routes-tricycle"
  },
  {
    "id": "api-json-routes-tricycle-5",
    "index": 415,
    "task": "You will add code to handle the /items/total route. This route returns the sum of all prices. The code goes after the /items/names line. The checker will test this route.\n\nIn server.js:\n```\n  if (req.url === \"/items/total\") return send(res, 200, { total: items.reduce((sum, item) => sum + item.price, 0) });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Tricycle Terminal API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Market\", price: 20 }, { id: 2, name: \"School\", price: 30 }, { id: 3, name: \"Clinic\", price: 25 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "total",
        "label": "GET /items/total answers 75",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/total"
          }
        ],
        "bodyContains": "{\"total\":75}"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The code checks if the URL is /items/total, then sends an object with the total property using reduce."
      },
      {
        "level": 2,
        "text": "Add this line after the /items/names line in server.js.\n\nIn server.js:\n```\n  if (req.url === \"/items/total\") return send(res, 200, { total: items.reduce((sum, item) => sum + item.price, 0) });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Market\", price: 20 }, { id: 2, name: \"School\", price: 30 }, { id: 3, name: \"Clinic\", price: 25 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n  if (req.url === \"/items/total\") return send(res, 200, { total: items.reduce((sum, item) => sum + item.price, 0) });\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "json-routes-tricycle"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: json-routes-tricycle.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-json-routes-tricycle-6",
    "index": 416,
    "task": "You will handle unknown paths. When someone asks for a path that does not exist, the server must say 404. This is important because users should know when a route is broken. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  send(res, 404, { error: \"Not found\" });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Tricycle Terminal API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Market\", price: 20 }, { id: 2, name: \"School\", price: 30 }, { id: 3, name: \"Clinic\", price: 25 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "missing",
        "label": "GET /nope answers 404 with a JSON error",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/nope"
          }
        ],
        "status": 404,
        "bodyContains": "{\"error\":\"Not found\"}"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "If the URL is not known, send a 404 status with a JSON error message."
      },
      {
        "level": 2,
        "text": "Put this code at the end of the handler, right before the 404 line.\n\nIn server.js:\n```\n  send(res, 404, { error: \"Not found\" });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Market\", price: 20 }, { id: 2, name: \"School\", price: 30 }, { id: 3, name: \"Clinic\", price: 25 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n  if (req.url === \"/items/total\") return send(res, 200, { total: items.reduce((sum, item) => sum + item.price, 0) });\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "json-routes-tricycle"
  },
  {
    "id": "api-json-routes-tricycle-7",
    "index": 417,
    "task": "You will add a route for the home page. When someone asks for /, the server must return the API's name and its routes. This helps users know what the API offers. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (req.url === \"/\") return send(res, 200, { name: \"Tricycle Terminal\", routes: [\"/items\"] });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Tricycle Terminal API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Market\", price: 20 }, { id: 2, name: \"School\", price: 30 }, { id: 3, name: \"Clinic\", price: 25 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "root",
        "label": "GET / lists the routes",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/"
          }
        ],
        "status": 200,
        "bodyContains": "\"routes\":[\"/items\"]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Check if the URL is exactly \"/\". If yes, return a 200 status with the API's name and routes."
      },
      {
        "level": 2,
        "text": "Put this code at the top of the handler, before any other checks.\n\nIn server.js:\n```\n  if (req.url === \"/\") return send(res, 200, { name: \"Tricycle Terminal\", routes: [\"/items\"] });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Market\", price: 20 }, { id: 2, name: \"School\", price: 30 }, { id: 3, name: \"Clinic\", price: 25 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/\") return send(res, 200, { name: \"Tricycle Terminal\", routes: [\"/items\"] });\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n  if (req.url === \"/items/total\") return send(res, 200, { total: items.reduce((sum, item) => sum + item.price, 0) });\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "json-routes-tricycle"
  },
  {
    "id": "api-json-routes-tricycle-8",
    "index": 418,
    "task": "You will add a route to find the cheapest item. When someone asks for /items/cheapest, the server must return the item with the lowest price. This helps users find the best deal. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (req.url === \"/items/cheapest\") return send(res, 200, [...items].sort((a, b) => a.price - b.price)[0]);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Tricycle Terminal API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Market\", price: 20 }, { id: 2, name: \"School\", price: 30 }, { id: 3, name: \"Clinic\", price: 25 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "cheapest",
        "label": "GET /items/cheapest answers Market",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/cheapest"
          }
        ],
        "bodyContains": "\"name\":\"Market\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Sort the items by price and pick the first one. Use the array's sort method."
      },
      {
        "level": 2,
        "text": "Put this code before the 404 line, after the home page code.\n\nIn server.js:\n```\n  if (req.url === \"/items/cheapest\") return send(res, 200, [...items].sort((a, b) => a.price - b.price)[0]);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Market\", price: 20 }, { id: 2, name: \"School\", price: 30 }, { id: 3, name: \"Clinic\", price: 25 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/\") return send(res, 200, { name: \"Tricycle Terminal\", routes: [\"/items\"] });\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n  if (req.url === \"/items/total\") return send(res, 200, { total: items.reduce((sum, item) => sum + item.price, 0) });\n  if (req.url === \"/items/cheapest\") return send(res, 200, [...items].sort((a, b) => a.price - b.price)[0]);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 5,
    "projectId": "json-routes-tricycle"
  },
  {
    "id": "api-json-routes-tricycle-9",
    "index": 419,
    "task": "You will add a route to show items in a readable format. When someone asks for /items/pretty, the server must return the items as indented JSON. This helps people read the data easily. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (req.url === \"/items/pretty\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify(items, null, 2)); return; }\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Tricycle Terminal API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Market\", price: 20 }, { id: 2, name: \"School\", price: 30 }, { id: 3, name: \"Clinic\", price: 25 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "pretty",
        "label": "GET /items/pretty answers indented JSON",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/pretty"
          }
        ],
        "bodyContains": "[\n  {\n    \"id\": 1"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Set the Content-Type header to application/json. Then use JSON.stringify with 2 spaces for indentation."
      },
      {
        "level": 2,
        "text": "Put this code before the 404 line, after the cheapest item code.\n\nIn server.js:\n```\n  if (req.url === \"/items/pretty\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify(items, null, 2)); return; }\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Market\", price: 20 }, { id: 2, name: \"School\", price: 30 }, { id: 3, name: \"Clinic\", price: 25 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/\") return send(res, 200, { name: \"Tricycle Terminal\", routes: [\"/items\"] });\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n  if (req.url === \"/items/total\") return send(res, 200, { total: items.reduce((sum, item) => sum + item.price, 0) });\n  if (req.url === \"/items/cheapest\") return send(res, 200, [...items].sort((a, b) => a.price - b.price)[0]);\n  if (req.url === \"/items/pretty\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify(items, null, 2)); return; }\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 5,
    "projectId": "json-routes-tricycle"
  },
  {
    "id": "api-json-routes-tricycle-10",
    "index": 420,
    "task": "You will add a route to find expensive items. When someone asks for /items/expensive, the server must return items priced over 40. This helps users find high-cost items. The code below does this. Run the checker to confirm it works.\n\nIn server.js:\n```\n  if (req.url === \"/items/expensive\") return send(res, 200, items.filter((item) => item.price > 40));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Tricycle Terminal API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Market\", price: 20 }, { id: 2, name: \"School\", price: 30 }, { id: 3, name: \"Clinic\", price: 25 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  res.end(\"Items API\");\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "expensive",
        "label": "GET /items/expensive answers an empty list",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "GET",
            "path": "/items/expensive"
          }
        ],
        "bodyContains": "[]"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Use the filter method to keep only items with price greater than 40."
      },
      {
        "level": 2,
        "text": "Put this code before the 404 line, after the pretty JSON code.\n\nIn server.js:\n```\n  if (req.url === \"/items/expensive\") return send(res, 200, items.filter((item) => item.price > 40));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Market\", price: 20 }, { id: 2, name: \"School\", price: 30 }, { id: 3, name: \"Clinic\", price: 25 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/\") return send(res, 200, { name: \"Tricycle Terminal\", routes: [\"/items\"] });\n  if (req.url === \"/items\") return send(res, 200, items);\n  if (req.url === \"/items/count\") return send(res, 200, { count: items.length });\n  if (req.url === \"/items/first\") return send(res, 200, items[0]);\n  if (req.url === \"/items/names\") return send(res, 200, items.map((item) => item.name));\n  if (req.url === \"/items/total\") return send(res, 200, { total: items.reduce((sum, item) => sum + item.price, 0) });\n  if (req.url === \"/items/cheapest\") return send(res, 200, [...items].sort((a, b) => a.price - b.price)[0]);\n  if (req.url === \"/items/pretty\") { res.setHeader(\"Content-Type\", \"application/json\"); res.end(JSON.stringify(items, null, 2)); return; }\n  if (req.url === \"/items/expensive\") return send(res, 200, items.filter((item) => item.price > 40));\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "json-routes-tricycle"
  }
] satisfies typeof apiBasicsCourse.steps));

// Validated local authoring batch: request-bodies-tricycle.
apiBasicsCourse.steps.push(...([
  {
    "id": "api-request-bodies-tricycle-1",
    "index": 421,
    "task": "You will read what the client sends. The code below does that. Add it above the server. Then add a route for /echo. This route will send back what it received. The checker will test this.\n\nIn server.js:\n```\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Tricycle Terminal API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Market\", price: 20 }, { id: 2, name: \"School\", price: 30 }, { id: 3, name: \"Clinic\", price: 25 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "echo",
        "label": "POST /echo with hello answers it back",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/echo",
            "body": "hello",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "bodyContains": "\"received\":\"hello\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The readBody function reads chunks of data until it gets all of it."
      },
      {
        "level": 2,
        "text": "Add the code above the server and put the /echo route at the top of the handler.\n\nIn server.js:\n```\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Market\", price: 20 }, { id: 2, name: \"School\", price: 30 }, { id: 3, name: \"Clinic\", price: 25 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "request-bodies-tricycle"
  },
  {
    "id": "api-request-bodies-tricycle-2",
    "index": 422,
    "task": "You will now turn text into an object. Add one line after the /echo route. This line uses JSON.parse to turn the text into an object. The checker will test this.\n\nIn server.js:\n```\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Tricycle Terminal API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Market\", price: 20 }, { id: 2, name: \"School\", price: 30 }, { id: 3, name: \"Clinic\", price: 25 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "json",
        "label": "POST /echo-json answers the object it received",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/echo-json",
            "body": "{\"name\":\"Ana\"}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "bodyContains": "\"name\":\"Ana\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "JSON.parse turns text into an object. You must wait for the body to finish before parsing."
      },
      {
        "level": 2,
        "text": "Add the line right after the /echo route, inside the handler.\n\nIn server.js:\n```\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Market\", price: 20 }, { id: 2, name: \"School\", price: 30 }, { id: 3, name: \"Clinic\", price: 25 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "request-bodies-tricycle"
  },
  {
    "id": "api-request-bodies-tricycle-3",
    "index": 423,
    "task": "You will answer POST /items with status 201 and the new item. Replace the line inside createItem with two lines. The first line parses the body. The second sends the item back. The checker will test this.\n\nIn server.js:\n```\n  const data = JSON.parse(await readBody(req));\n  return send(res, 201, data);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Tricycle Terminal API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Market\", price: 20 }, { id: 2, name: \"School\", price: 30 }, { id: 3, name: \"Clinic\", price: 25 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "created",
        "label": "POST /items answers 201 with the item",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "{\"name\":\"Tea\",\"price\":12}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "status": 201,
        "bodyContains": "\"name\":\"Tea\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "You must parse the body first, then send the item back with status 201."
      },
      {
        "level": 2,
        "text": "Replace the current line with two lines: one to parse, one to send.\n\nIn server.js:\n```\n  const data = JSON.parse(await readBody(req));\n  return send(res, 201, data);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Market\", price: 20 }, { id: 2, name: \"School\", price: 30 }, { id: 3, name: \"Clinic\", price: 25 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const data = JSON.parse(await readBody(req));\n  return send(res, 201, data);\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 4,
    "projectId": "request-bodies-tricycle"
  },
  {
    "id": "api-request-bodies-tricycle-4",
    "index": 424,
    "task": "You will store the new item so it shows up in the list. Add one line inside createItem before the return. This line adds the item to the items array. The checker will test this.\n\nIn server.js:\n```\n  items.push(data);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Tricycle Terminal API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Market\", price: 20 }, { id: 2, name: \"School\", price: 30 }, { id: 3, name: \"Clinic\", price: 25 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "stored",
        "label": "After POST /items, GET /items includes Tea",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "{\"name\":\"Tea\",\"price\":12}",
            "headers": {
              "Content-Type": "application/json"
            }
          },
          {
            "method": "GET",
            "path": "/items"
          }
        ],
        "bodyContains": "\"name\":\"Tea\""
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "Add the item to the items array before sending it back."
      },
      {
        "level": 2,
        "text": "Put this line right before the return statement in createItem.\n\nIn server.js:\n```\n  items.push(data);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Market\", price: 20 }, { id: 2, name: \"School\", price: 30 }, { id: 3, name: \"Clinic\", price: 25 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const data = JSON.parse(await readBody(req));\n  items.push(data);\n  return send(res, 201, data);\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 3,
    "projectId": "request-bodies-tricycle"
  },
  {
    "id": "api-request-bodies-tricycle-5",
    "index": 425,
    "task": "You will give each new item an id one higher than the last. Change createItem so each new item gets the next id. The new item will have an id, and you will push it to the array. The checker will test this.\n\nIn server.js:\n```\n  const item = { id: items.length + 1, ...data };\n  items.push(item);\n  return send(res, 201, item);\n```",
    "kind": "local",
    "inputMode": "free",
    "files": {
      "report.txt": ""
    },
    "activeFile": "report.txt",
    "localSeed": {
      "package.json": "{\n  \"type\": \"module\"\n}\n",
      "README.txt": "Tricycle Terminal API project.\nStart the server with node server.js, then follow the CodeDaddy steps.\n",
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Market\", price: 20 }, { id: 2, name: \"School\", price: 30 }, { id: 3, name: \"Clinic\", price: 25 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function createItem(req, res) {\n  return send(res, 501, { error: \"Not built yet\" });\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "tests": [
      {
        "id": "id",
        "label": "The new item answers with id 4",
        "kind": "local-http",
        "file": "server.js",
        "requests": [
          {
            "method": "POST",
            "path": "/items",
            "body": "{\"name\":\"Tea\",\"price\":12}",
            "headers": {
              "Content-Type": "application/json"
            }
          }
        ],
        "bodyContains": "\"id\":4"
      }
    ],
    "hints": [
      {
        "level": 1,
        "text": "The new item's id is the length of items plus one. Use this to set the id."
      },
      {
        "level": 2,
        "text": "Replace the createItem code with the new version that includes the id.\n\nIn server.js:\n```\n  const item = { id: items.length + 1, ...data };\n  items.push(item);\n  return send(res, 201, item);\n```"
      }
    ],
    "xp": 10,
    "solution": {
      "commands.txt": ""
    },
    "localFiles": {
      "server.js": "import http from \"node:http\";\nconst items = [{ id: 1, name: \"Market\", price: 20 }, { id: 2, name: \"School\", price: 30 }, { id: 3, name: \"Clinic\", price: 25 }];\nfunction send(res, status, data) {\n  res.statusCode = status;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.end(JSON.stringify(data));\n}\nasync function readBody(req) { let text = \"\"; for await (const chunk of req) text += chunk; return text; }\nasync function createItem(req, res) {\n  const data = JSON.parse(await readBody(req));\n  const item = { id: items.length + 1, ...data };\n  items.push(item);\n  return send(res, 201, item);\n}\nconst port = Number(process.env.PORT ?? 3000);\nconst server = http.createServer(async (req, res) => {\n  if (req.url === \"/echo\" && req.method === \"POST\") return send(res, 200, { received: await readBody(req) });\n  if (req.url === \"/echo-json\" && req.method === \"POST\") return send(res, 200, JSON.parse(await readBody(req)));\n  if (req.url === \"/items\" && req.method === \"GET\") return send(res, 200, items);\n  if (req.url === \"/items\" && req.method === \"POST\") return createItem(req, res);\n  send(res, 404, { error: \"Not found\" });\n});\nserver.listen(port);\n"
    },
    "estimatedMinutes": 5,
    "projectId": "request-bodies-tricycle"
  }
] satisfies typeof apiBasicsCourse.steps));
